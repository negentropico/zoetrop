---
phase: 04-static-to-db-data-layer-migration
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - remix-app/scripts/seed-data.ts
  - remix-app/scripts/capture-fixtures.ts
  - remix-app/package.json
  - remix-app/tests/db/data-seed.test.ts
autonomous: true
requirements: [DATA-02, DATA-01]
must_haves:
  truths:
    - "Running db:seed-data inserts the owner's M0 data into Neon under the owner's tenantId/subjectId"
    - "The seed script resolves owner IDs by OWNER_EMAIL lookup (never hardcoded) and is idempotent (re-run exits early)"
    - "correlations.supplementId is resolved via a name→DB-id lookup built after supplements are seeded (not from static ids)"
    - "tests/db/data-seed.test.ts row counts are green against live Neon after seeding"
    - "Parity fixtures are captured from the STATIC modules into gitignored tests/fixtures/*.json BEFORE loaders are rewired (D-09 order)"
  artifacts:
    - path: "remix-app/scripts/seed-data.ts"
      provides: "idempotent M0 seed orchestrator (per-table helpers, FK dependency order)"
      contains: "OWNER_EMAIL"
      min_lines: 120
    - path: "remix-app/scripts/capture-fixtures.ts"
      provides: "one-shot static-module → gitignored JSON fixture writer"
      contains: "tests/fixtures"
    - path: "remix-app/package.json"
      provides: "db:seed-data + capture-fixtures npm scripts"
      contains: "db:seed-data"
  key_links:
    - from: "remix-app/scripts/seed-data.ts"
      to: "remix-app/app/lib/real-data.ts + protocol-data.ts + seed-data.ts (lib)"
      via: "consume static PHI arrays then delete in Plan 05"
      pattern: "realBloodWork|seedGeneticVariants"
    - from: "remix-app/scripts/seed-data.ts#seedCorrelations"
      to: "supplements.name → supplements.id map"
      via: "post-insert name lookup (Pitfall 4)"
      pattern: "supplementId"
    - from: "remix-app/scripts/capture-fixtures.ts"
      to: "remix-app/tests/fixtures/*.json (gitignored)"
      via: "fs.writeFileSync"
      pattern: "writeFileSync"
---

<objective>
Seed the owner's M0 data into live Neon (DATA-02), prove it with the row-count test, and capture the parity fixtures from the static modules — the cut-over baseline that gates deletion (D-09: seed → capture fixtures → [Plan 04] wire loaders → parity green → [Plan 05] spot-check → delete).

Purpose: This plan is the only writer of the owner's M0 health data into Neon, and the only capture of the pre-migration loader output. Both MUST happen before loaders are rewired (Plan 04): the seed populates the tables the rewired loaders read, and the fixtures freeze the static-module output so Plan 04's parity test can assert the DB-backed loaders produce the same shape. Fixtures are PHI → gitignored (guard landed in Plan 01) → never committed → deleted in Plan 05.

Output: `scripts/seed-data.ts` (orchestrator + per-table helpers), `scripts/capture-fixtures.ts` (fixture writer), `package.json` scripts, live Neon seeded, `tests/db/data-seed.test.ts` green, `tests/fixtures/*.json` written locally (gitignored).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-static-to-db-data-layer-migration/04-CONTEXT.md
@.planning/phases/04-static-to-db-data-layer-migration/04-RESEARCH.md
@.planning/phases/04-static-to-db-data-layer-migration/04-PATTERNS.md

# Prior plans — live table shape (01) + the data.server read functions whose output fixtures must match (02)
@.planning/phases/04-static-to-db-data-layer-migration/04-01-SUMMARY.md
@.planning/phases/04-static-to-db-data-layer-migration/04-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. -->

From remix-app/scripts/seed-owner.ts (EXACT pattern to follow):
- env validation: `const OWNER_EMAIL = process.env.OWNER_EMAIL; if (!OWNER_EMAIL) throw new Error(...)`.
- idempotency: select by email, early-`process.exit(0)` if exists. For data seed: check limit-1 on the first table (protocolVersions) WHERE tenantId = ownerTenantId, exit early if rows present.
- ID resolution: `const [ownerUser] = await db.select().from(user).where(eq(user.email, OWNER_EMAIL)).limit(1);` then `const tenantId = ownerUser.tenantId!;` then `const [subject] = await db.select().from(subjects).where(eq(subjects.tenantId, tenantId)).limit(1);` then `const subjectId = subject.id;`. NEVER hardcode IDs.
- imports: `import { getDb } from "../app/lib/db.server"; import { eq } from "drizzle-orm"; import { user, tenants, subjects, ... } from "../db/schema";`
- logging: `console.log("[seed-data] <table> seeded: N rows")` + final tenantId/subjectId print.
- run via tsx: `npm run db:seed-owner` = `tsx --env-file-if-exists=.env scripts/seed-owner.ts`.

Static source arrays to consume (paths + exports), all under remix-app/app/lib/:
- real-data.ts: `realBloodWorkM1: Metric[]`, `realBloodWorkM2: Metric[]`, `realBodyComposition: Metric[]`, `realAutonomicData: Metric[]` (~54 metric rows total), plus `getRealMetrics()`/`getLatestRealMetrics()` (combine helpers), `MILESTONES` (date constant), `METRIC_TARGETS`, `getMetricTargets`, `getProjections`.
- protocol-data.ts: `realProtocolVersions` (7, ids 1–7), `realProtocolChanges` (24, FK versionId → version id 1–7), `realSupplements` (17, isActive boolean), `realMilestones` (8), `realCessationLog` (1). Survivor engine fns: `getCessationDay(now)`, `getCurrentCessationPhase(day)`, `CESSATION_START_DATE`, `CESSATION_PHASES`, `dailySchedule`, `avoidList`.
- seed-data.ts (lib): `seedGeneticVariants` (16 — gene/genotype/rsid → subject_genotypes PHI plane), `seedCorrelations` (`SupplementCorrelation[]`, 10+, keyed by `supplementName` + `metricName`). Note: dashboard/protocol render the `real*` arrays; insights/correlations + insights/genetics render the seed-data lib arrays. Seed the set each loader currently renders so parity holds.

From remix-app/db/schema.ts (insert targets, post Plan 01):
- `metrics` PK is `varchar id` → provide a string id (reuse the static Metric.id). protocolVersions/changes/milestones/supplements/correlations/cessationLog/subjectGenotypes use `integer generatedAlwaysAsIdentity` PK → do NOT insert `id`; Postgres assigns.
- metrics insert: flatten `referenceRange.{min,max}` → referenceMin/referenceMax, `optimalRange.{min,max}` → optimalMin/optimalMax; `timestamp` from the ISO string → Date.
- correlations needs `supplementId` (FK → supplements.id), `metricName`, `correlation`, `lagDays`, `sampleSize`, `pValue`.
- protocolChanges.versionId is integer FK → protocolVersions.id. Static `versionId` (1–7) are array indices NOT DB ids → after inserting versions, build a `version-string ("P0".."P6") → db id` map and remap each protocolChanges row's versionId (Pitfall 4 analog).

From remix-app/app/lib/data.server.ts (Plan 02 — fixture target shape):
- the rewired loaders (Plan 04) call these; the fixtures captured here are the STATIC-module loader outputs the DB-backed outputs must match via `toMatchObject`.

OWNER_EMAIL / OWNER_PASSWORD available locally (per seed-owner.ts). seed-owner must have run first (owner tenant+subject already exist — Phase 3 seeded them).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed orchestrator (scripts/seed-data.ts) + npm script</name>
  <files>remix-app/scripts/seed-data.ts, remix-app/package.json</files>
  <read_first>
    - remix-app/scripts/seed-owner.ts (full — exact env/idempotency/ID-resolution/logging pattern)
    - remix-app/app/lib/protocol-data.ts (full — realProtocolVersions ids 1–7, realProtocolChanges versionId mapping, realSupplements, realMilestones, realCessationLog)
    - remix-app/app/lib/real-data.ts (offsets 115/582/897/1086 — the 4 metric arrays; 1322 — getRealMetrics combine)
    - remix-app/app/lib/seed-data.ts (offset 280 seedGeneticVariants; 471 seedCorrelations)
    - remix-app/db/schema.ts (insert column shapes; identity vs varchar PKs)
    - .planning/phases/04-static-to-db-data-layer-migration/04-PATTERNS.md (seed-data.ts section — FK order, idempotency, ID resolution, Pitfall 4 supplement name→id map)
  </read_first>
  <action>
    Create `scripts/seed-data.ts` modeled exactly on `seed-owner.ts`. Validate `OWNER_EMAIL`. Resolve owner ids: lookup user by OWNER_EMAIL → `tenantId = ownerUser.tenantId`, then subject by tenantId → `subjectId`. If owner user or subject missing, throw an instructive error ("run db:seed-owner first"). Idempotency: select limit-1 from `protocolVersions` WHERE tenantId = ownerTenantId; if a row exists, log "[seed-data] Already seeded. Nothing to do." and `process.exit(0)`. Then run per-table helper functions in FK dependency order: seedProtocolVersions → seedSupplements → seedProtocolChanges (remap static versionId→db id via a version-string map built from the inserted versions) → seedMilestones → seedMetrics (insert the combined `getRealMetrics()` set — reuse the static Metric.id strings as the varchar PK; flatten referenceRange/optimalRange to referenceMin/Max + optimalMin/Max; Date from the ISO timestamp) → seedCessationLog → seedCorrelations (after supplements: build a `supplement.name → db id` map by re-selecting seeded supplements, resolve each static `supplementName` to its db `supplementId`, throw if a name has no match — Pitfall 4) → seedSubjectGenotypes (insert gene/rsid/genotype/assaySource from seedGeneticVariants — PHI plane only; assaySource from the variant's source note or null). Stamp tenantId+subjectId on EVERY inserted row. Let Postgres assign identity PKs (omit `id` for identity tables). Log per-table row counts and the final tenantId/subjectId. Add `"db:seed-data": "tsx --env-file-if-exists=.env scripts/seed-data.ts"` to package.json scripts.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/seed-data.ts` contains `OWNER_EMAIL`, an idempotency early-exit (`process.exit(0)`), and no hardcoded tenant/subject UUID literal (ids come from the OWNER_EMAIL lookup).
    - Contains a supplement `name`→id resolution for correlations (grep `supplementId` + a name-keyed map build).
    - Contains a static-versionId→db-id remap for protocolChanges (grep `versionId`).
    - `package.json` contains `"db:seed-data"`.
    - The seed script imports the static arrays from `../app/lib/real-data`, `../app/lib/protocol-data`, `../app/lib/seed-data` (deleted in Plan 05 after the arrays are removed; one-shot script).
    - `cd remix-app && npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Idempotent seed orchestrator with per-table helpers in FK order, owner-id resolution by email, supplement name→id + version remap, tenant/subject stamping on every row, db:seed-data registered, typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Run the seed against live Neon + DATA-02 row-count test green</name>
  <files>remix-app/tests/db/data-seed.test.ts</files>
  <read_first>
    - remix-app/scripts/seed-data.ts (the script being run)
    - remix-app/tests/db/data-seed.test.ts (the scaffold from Plan 01 — SEEDED_TABLES, possibly `.skip`-guarded)
    - .planning/phases/04-static-to-db-data-layer-migration/04-01-SUMMARY.md (whether the DATA-02 block was `.skip`-guarded pending seed)
  </read_first>
  <action>
    Run `cd remix-app && npm run db:seed-data` to insert the owner's M0 data into live Neon. Capture the printed per-table row counts and tenantId/subjectId. If Plan 01 marked the DATA-02 `describe` block `.skip` pending seed, change it to `describe.skipIf(!connectionString)` so it runs now. Adjust `minRows` thresholds in SEEDED_TABLES only if the actual seeded counts are legitimately lower than the scaffold's estimate (document the true counts in the SUMMARY; never lower a threshold below the real count to force green). Re-run `npm run db:seed-data` once to confirm the idempotency guard exits early (no duplicate rows). Then run `npm test` so the DATA-02 row-count block executes against live Neon and is GREEN.
  </action>
  <verify>
    <automated>cd remix-app && npm test -- tests/db/data-seed.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - First `npm run db:seed-data` inserts rows and logs per-table counts; second run logs "Already seeded. Nothing to do." and exits 0 (idempotent — no duplicates).
    - `cd remix-app && npm test -- tests/db/data-seed.test.ts` exits 0 with the DATA-02 block RUNNING (not `.skip`): every SEEDED_TABLES count assertion passes against live Neon.
    - The DATA-02 block uses `describe.skipIf(!connectionString)` (runs whenever DATABASE_URL is present), not `describe.skip`.
  </acceptance_criteria>
  <done>Owner's M0 data seeded into live Neon, seed idempotent, DATA-02 row-count test green against the live DB.</done>
</task>

<task type="auto">
  <name>Task 3: Capture parity fixtures from the STATIC modules (gitignored, pre-rewire)</name>
  <files>remix-app/scripts/capture-fixtures.ts, remix-app/package.json</files>
  <read_first>
    - remix-app/scripts/seed-owner.ts (structural template — env validation, console logging, run-once shape)
    - remix-app/app/routes/_app/protocol/cessation.tsx (current static loader — shape to freeze)
    - remix-app/app/routes/_app/protocol/supplements.tsx (current static loader — byTier/stats shape)
    - remix-app/app/routes/_app/insights/genetics.tsx (current static loader — variants/byCategory/stats shape)
    - remix-app/app/routes/_app/dashboard.tsx (current static loader — the most composite shape)
    - remix-app/.gitignore (confirm tests/fixtures/ is ignored — Plan 01)
    - .planning/phases/04-static-to-db-data-layer-migration/04-PATTERNS.md (Pattern 6 + capture-fixtures.ts "No Analog Found" guidance)
  </read_first>
  <action>
    Create `scripts/capture-fixtures.ts`: a one-shot tsx script that reproduces each data-returning loader's CURRENT static return value (using the static modules + survivor engine fns with a PINNED `now = new Date("2026-06-10T00:00:00.000Z")` matching the parity test's FIXED_NOW) and writes one JSON file per loader to `tests/fixtures/<route-key>.json` via `fs.writeFileSync(path, JSON.stringify(value, null, 2))`. Cover the 13 data-returning loaders: dashboard, metrics/index, metrics/category (representative category, e.g. metabolic), metrics/detail (representative metricId), protocol/index, protocol/versions, protocol/version-detail (representative version), protocol/supplements, protocol/cessation, protocol/compare, insights/index, insights/correlations, insights/genetics. For param loaders (`category`, `metricId`, `version`), capture fixtures for the SAME representative params Plan 04's parity test will use, and record those param choices in the SUMMARY. `mkdir` `tests/fixtures/` if absent — it is gitignored (Plan 01), so the PHI-bearing fixtures stay local-only. Add `"capture-fixtures": "tsx --env-file-if-exists=.env scripts/capture-fixtures.ts"` to package.json. Run once: `npm run capture-fixtures`. NEVER git-add the fixtures (Pitfall 1 — PHI; the gitignore is the net but do not stage them explicitly).
  </action>
  <verify>
    <automated>cd remix-app && npm run capture-fixtures && test "$(ls tests/fixtures/*.json | wc -l | tr -d ' ')" -ge 13 && test -z "$(git status --porcelain tests/fixtures/)" && echo "FIXTURES CAPTURED AND UNTRACKED"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run capture-fixtures` writes >= 13 JSON files into `remix-app/tests/fixtures/`.
    - `git status --porcelain remix-app/tests/fixtures/` is EMPTY (no fixture is tracked/staged — PHI never enters git, T-04-PHI-FIX).
    - `git check-ignore remix-app/tests/fixtures/dashboard.json` succeeds (path ignored).
    - `scripts/capture-fixtures.ts` uses the pinned `now = new Date("2026-06-10T00:00:00.000Z")` (matches FIXED_NOW in the parity scaffold) and `fs.writeFileSync`.
    - `package.json` contains `"capture-fixtures"`.
  </acceptance_criteria>
  <done>13+ parity fixtures captured from the static modules into gitignored tests/fixtures/, none tracked by git, pinned-now matches the parity test, capture-fixtures script registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git working tree → committed history | PHI fixtures + static PHI arrays must not cross; fixtures gitignored, never staged |
| seed script → live Neon | the only authorized writer of the owner's M0 PHI rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-PHI-FIX | Information Disclosure | tests/fixtures/*.json | mitigate | Gitignore guard from Plan 01; Task 3 verify asserts `git status` for fixtures is empty; never `git add` them |
| T-04-SEED-LOG | Information Disclosure | seed-data.ts console output | mitigate | Log only row COUNTS and tenant/subject IDs — never log PHI field values (per security_requirement: seed scripts must not log PHI) |
| T-04-HARDID | Tampering | seed-data.ts owner IDs | mitigate | Owner tenant/subject resolved via OWNER_EMAIL lookup; no hardcoded UUIDs (verify asserts absence) |
| T-04-FKMAP | Tampering | correlations.supplementId / protocolChanges.versionId | mitigate | Post-insert name→id and version-string→id maps; throw on unmatched name (Pitfall 4); never trust static ids |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan (RESEARCH Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `cd remix-app && npm run typecheck` exits 0.
- `cd remix-app && npm test -- tests/db/data-seed.test.ts` exits 0 (DATA-02 green against live Neon).
- `npm run capture-fixtures` produces >= 13 gitignored fixtures; `git status` shows none tracked.
- Seed is idempotent (second run exits early).
</verification>

<success_criteria>
- DATA-02 satisfied: owner's M0 metrics/protocol versions/changes/milestones/supplements/correlations/cessation log/genotypes present as rows under the owner's tenant/subject.
- Parity baseline frozen: static-module loader outputs captured as gitignored fixtures, pinned-now aligned with Plan 04's parity test.
- D-09 ordering honored: seed + fixture capture both complete BEFORE loaders are rewired (Plan 04).
</success_criteria>

<output>
Create `.planning/phases/04-static-to-db-data-layer-migration/04-03-SUMMARY.md` when done. Record: actual seeded row counts per table, the final tenantId/subjectId, the representative params chosen for the param loaders' fixtures (category/metricId/version), and any `minRows` threshold adjustments with their justification.
</output>
