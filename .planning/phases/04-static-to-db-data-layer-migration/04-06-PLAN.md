---
phase: 04-static-to-db-data-layer-migration
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - remix-app/app/lib/protocol-data.ts
  - remix-app/app/lib/protocol-data.test.ts
  - remix-app/app/routes/_app/protocol/cessation.tsx
  - remix-app/app/routes/_app/dashboard.tsx
  - remix-app/app/routes/_app/protocol/index.tsx
autonomous: true
gap_closure: true
requirements:
  - DATA-01
user_setup: []

must_haves:
  truths:
    - "The cessation day count is computed from the DB cessation_log.startDate, not from the CESSATION_START_DATE module constant"
    - "For a subject whose DB startDate differs from 2025-12-23, the day count, phase badge, progress ring, and PhaseBar reflect the DB startDate"
    - "The owner's existing day count is unchanged (DB startDate equals the old constant, so currentDay stays 169 at FIXED_NOW=2026-06-10)"
    - "getCessationDay no longer reads CESSATION_START_DATE; the constant survives only as seed documentation / empty-cessation default"
  artifacts:
    - path: "remix-app/app/lib/protocol-data.ts"
      provides: "getCessationDay(startDateIso, now) signature reading the passed start date"
      contains: "startDateIso"
    - path: "remix-app/app/lib/protocol-data.test.ts"
      provides: "Updated unit tests passing CESSATION_START_DATE as the first argument"
      contains: "getCessationDay(CESSATION_START_DATE"
  key_links:
    - from: "remix-app/app/routes/_app/protocol/cessation.tsx"
      to: "getCessationDay"
      via: "cessation.startDate passed as startDateIso"
      pattern: "getCessationDay\\(cessation\\.startDate"
    - from: "remix-app/app/routes/_app/dashboard.tsx"
      to: "getCessationDay"
      via: "DB cessation row startDate passed as startDateIso"
      pattern: "getCessationDay\\("
    - from: "remix-app/app/routes/_app/protocol/index.tsx"
      to: "getCessationDay"
      via: "DB cessation row startDate passed as startDateIso"
      pattern: "getCessationDay\\("
---

<objective>
Close VERIFICATION.md BLOCKER 2 (CR-02, DATA-01 / SC-1): the cessation day count is computed from the hardcoded module constant `CESSATION_START_DATE` instead of the DB `cessation_log.startDate`. All three loaders fetch the DB cessation row but never feed its `startDate` into `getCessationDay`, so the most prominent derived number still reads static data — a direct contradiction of the phase goal ("all route loaders read live data from Neon").

Purpose: Make the DB the true source for the cessation day count, so the day count, phase badge, progress ring, and PhaseBar are correct for any subject whose start date differs from the legacy constant. The owner's current value (Day 169 at FIXED_NOW) must not change.

Output: `getCessationDay` takes the start date as a parameter; the three loaders pass the DB `startDate`; the constant survives only as seed documentation and the empty-cessation default; the existing unit test and loader-parity test stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-static-to-db-data-layer-migration/04-VERIFICATION.md
@.planning/phases/04-static-to-db-data-layer-migration/04-CONTEXT.md

<interfaces>
<!-- Extracted from codebase. Executor uses these directly — no exploration needed. -->

From remix-app/app/lib/protocol-data.ts (CURRENT — to be changed):
  export const CESSATION_START_DATE = "2025-12-23T00:00:00.000Z";
  export function getCessationDay(now: Date = new Date()): number   // reads CESSATION_START_DATE via parseISO — THE DEFECT
  export function getCurrentCessationPhase(day: number): typeof CESSATION_PHASES[0]   // takes a NUMBER, does NOT read the constant — already correct, leave it

From remix-app/app/lib/cessation.ts (survivor re-export — routes import from here, NOT protocol-data):
  export { getCessationDay, getCurrentCessationPhase, CESSATION_START_DATE, dailySchedule, avoidList } from "./protocol-data";
  export { CESSATION_PHASES } from "~/types/protocol";

From remix-app/db/schema.ts (cessationLog table):
  startDate: timestamp('start_date').notNull()   // a Date when read raw; ISO string after a route normalizes it
  notes: text('notes')

From remix-app/app/lib/data.server.ts:
  export async function getCessationLog(tenantId: string, subjectId: string)   // returns raw rows; startDate is a Date

Call-site shapes (IMPORTANT — startDate type differs per route):
  - cessation.tsx loader: builds `cessation` with startDate NORMALIZED to an ISO string (lines 29-42). currentDay = getCessationDay(now) at line 67.
  - dashboard.tsx loader: `cessation = cessationRows[0] ?? null` (line 168) — RAW row, startDate is a Date. cessationDay = getCessationDay(now) at line 169 (guarded by `cessation ?`).
  - protocol/index.tsx loader: `cessation = cessationRows[0] ?? null` (line 51) — RAW row, startDate is a Date. cessationDay = getCessationDay(new Date()) at line 65 (guarded by `cessation ?`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-signature getCessationDay to take the start date as a parameter, and update its unit test</name>
  <files>remix-app/app/lib/protocol-data.ts, remix-app/app/lib/protocol-data.test.ts</files>
  <read_first>
    - remix-app/app/lib/protocol-data.ts (current getCessationDay reads CESSATION_START_DATE at lines 29-31; CESSATION_START_DATE defined line 24)
    - remix-app/app/lib/protocol-data.test.ts (existing assertions call getCessationDay(day(n)) with a Date as the ONLY arg — these break under the new signature)
    - remix-app/app/lib/cessation.ts (the re-export surface routes consume; must stay coherent)
  </read_first>
  <behavior>
    - getCessationDay("2025-12-23T00:00:00.000Z", start + 169 days) returns 169 (matches FIXED_NOW=2026-06-10 owner value)
    - getCessationDay(CESSATION_START_DATE, START) returns 0 (start date itself)
    - getCessationDay(CESSATION_START_DATE, start + n days) returns n for n in [1,21,22,60,61,120,121,150,151]
    - getCurrentCessationPhase(day) is UNCHANGED — it takes a number and is already correct (cases [-5→acute, 0→acute, 22→stabilization, 61→clearing, 121→optimization, 151→optimization] still pass)
  </behavior>
  <action>
    Change the signature of `getCessationDay` in protocol-data.ts to `export function getCessationDay(startDateIso: string, now: Date = new Date()): number` and compute `differenceInDays(now, parseISO(startDateIso))`. The function MUST NOT reference `CESSATION_START_DATE` anymore.

    Keep `CESSATION_START_DATE` exported (it is still re-exported by cessation.ts and used as the empty-cessation default in Task 2). Update its JSDoc to state it is seed-data documentation / the empty-cessation default only, NOT a runtime input to the day calculation.

    Do NOT touch `getCurrentCessationPhase` — it already takes a numeric `day` and reads no constant. Per VERIFICATION.md the note "check getCurrentCessationPhase consistently" resolves to: confirm it takes a number (it does) and leave it.

    Do NOT change the preserved off-by-one behavior: keep `differenceInDays` returning 0 at the start date (do NOT add `+ 1`). IN-06 in the review proposed a +1 change; it is OUT OF SCOPE for this gap and would break the parity fixtures (which expect 169, not 170). Leave the zero-based behavior as-is.

    cessation.ts re-exports `getCessationDay` by name — no edit needed there as long as the export name is unchanged.

    In protocol-data.test.ts: update every `getCessationDay(day(n))` call to `getCessationDay(CESSATION_START_DATE, day(n))`, and `getCessationDay(START)` to `getCessationDay(CESSATION_START_DATE, START)`. Import `CESSATION_START_DATE` from "~/lib/protocol-data" alongside the existing imports. The asserted return values (0, 1, 21, ...) stay identical because START is parseISO(CESSATION_START_DATE). Leave the getCurrentCessationPhase describe block unchanged.
  </action>
  <verify>
    <automated>cd remix-app && npx vitest run app/lib/protocol-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && grep -nE "getCessationDay\(" app/lib/protocol-data.ts` shows the definition signature `getCessationDay(startDateIso: string, now: Date = new Date())`
    - `cd remix-app && grep -c "CESSATION_START_DATE" app/lib/protocol-data.ts` is >= 1 (constant still defined/exported) AND the `getCessationDay` function body contains `parseISO(startDateIso)` (not `parseISO(CESSATION_START_DATE)`)
    - `cd remix-app && grep -c "getCessationDay(CESSATION_START_DATE" app/lib/protocol-data.test.ts` equals the number of getCessationDay call sites in the test (no bare `getCessationDay(day(` or `getCessationDay(START)` remain): `grep -cE "getCessationDay\((day\(|START\))" app/lib/protocol-data.test.ts` returns 0
    - `cd remix-app && npx vitest run app/lib/protocol-data.test.ts` exits 0 with all cases green
  </acceptance_criteria>
  <done>getCessationDay reads its startDateIso parameter (never the constant); the unit test passes the constant explicitly and stays green; getCurrentCessationPhase untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Pass DB cessation.startDate into getCessationDay at all three loader call sites</name>
  <files>remix-app/app/routes/_app/protocol/cessation.tsx, remix-app/app/routes/_app/dashboard.tsx, remix-app/app/routes/_app/protocol/index.tsx</files>
  <read_first>
    - remix-app/app/routes/_app/protocol/cessation.tsx (loader lines 24-126: `cessation` is built with startDate normalized to an ISO string at lines 29-42; getCessationDay(now) at line 67; the !active branch returns a default object at lines 44-65)
    - remix-app/app/routes/_app/dashboard.tsx (loader: `cessation = cessationRows[0] ?? null` at line 168 — RAW row; getCessationDay(now) at line 169)
    - remix-app/app/routes/_app/protocol/index.tsx (loader: `cessation = cessationRows[0] ?? null` at line 51 — RAW row; getCessationDay(new Date()) at line 65)
    - remix-app/tests/parity/loader-parity.test.ts (asserts result.currentDay===169 for cessation, result.cessationDay===169 for dashboard and protocol-index, all at FIXED_NOW=2026-06-10; the cessation loader is invoked with FIXED_NOW as the 2nd loader arg)
  </read_first>
  <action>
    Update all three call sites to pass the DB cessation row's startDate as the first argument to getCessationDay, keeping `now` as the second argument exactly as today.

    cessation.tsx line 67: change `getCessationDay(now)` to `getCessationDay(cessation.startDate, now)`. Here `cessation.startDate` is ALREADY an ISO string (normalized at lines 29-42), so it can be passed directly. This call is inside the `if (!cessation)` early-return guard, so `cessation` is non-null at line 67.

    dashboard.tsx line 169: `cessation` is the RAW row (startDate is a Date). Change `cessation ? getCessationDay(now) : 0` to pass the start date as an ISO string. Convert the Date to ISO at the call: pass `cessation.startDate instanceof Date ? cessation.startDate.toISOString() : (cessation.startDate as unknown as string)` as the first arg, keeping `now` as the second arg and the `cessation ? ... : 0` guard. Match the same Date→ISO normalization idiom already used in cessation.tsx lines 32-34 so the conversion is consistent across the codebase.

    protocol/index.tsx line 65: `cessation` is the RAW row (startDate is a Date). Change `cessation ? getCessationDay(new Date()) : 0` to pass the start date as an ISO string the same way as dashboard.tsx (Date→toISOString fallback), keeping the `new Date()` argument as the `now` second argument and the `cessation ? ... : 0` guard.

    Do NOT change the empty-cessation branches: cessation.tsx's `!active` branch (lines 44-65) returns `currentDay: 0` and does not call getCessationDay — leave it. dashboard.tsx and protocol/index.tsx already short-circuit to 0 when `cessation` is null — keep those guards.

    Do NOT change markup, the projectedCompletion math (already derived from DB startDate), or any other loader return field. The only behavioral change is the SOURCE of the day count.

    For the owner the DB startDate equals the old constant (2025-12-23T00:00:00.000Z), so currentDay/cessationDay stays 169 at FIXED_NOW=2026-06-10 — the loader-parity fixtures remain valid and must stay green.
  </action>
  <verify>
    <automated>cd remix-app && npx react-router typegen && npx tsc --noEmit && DATABASE_URL_UNPOOLED="$DATABASE_URL_UNPOOLED" DATABASE_URL="$DATABASE_URL" npx vitest run tests/parity/loader-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && grep -c "getCessationDay(cessation.startDate" app/routes/_app/protocol/cessation.tsx` returns 1
    - `cd remix-app && grep -c "getCessationDay(now)" app/routes/_app/protocol/cessation.tsx` returns 0 (no bare-now call remains)
    - `cd remix-app && grep -c "getCessationDay(new Date())" app/routes/_app/protocol/index.tsx` returns 0 (start date now passed as first arg)
    - In dashboard.tsx and protocol/index.tsx, the getCessationDay call passes a start-date string as the first argument (verify by reading: each call has the form `getCessationDay(<startDate-ISO-expr>, <now>)`)
    - `cd remix-app && npx tsc --noEmit` exits 0 (signature change type-checks at all three sites)
    - The loader-parity test stays green: cessation `result.currentDay === 169`, dashboard `result.cessationDay === 169`, protocol-index `result.cessationDay === 169` (run against live Neon with DATABASE_URL set; if the env is unavailable in the executor, the suite skips via skipIf — in that case the executor MUST run it once with DATABASE_URL set to confirm green before marking done)
  </acceptance_criteria>
  <done>All three loaders feed the DB startDate into getCessationDay; tsc passes; the loader-parity test confirms the owner's day count is unchanged (169) while the source is now the DB row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → loader → client | Cessation start date crosses from Neon into the loader-computed day count rendered to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering / Integrity | getCessationDay reading a stale constant instead of DB | mitigate | Re-signature getCessationDay to read the passed DB startDate; verify via parity test that the owner value is unchanged and via tsc that all call sites pass the DB value |
| T-04-06-02 | Information Disclosure | startDate value in loader output | accept | startDate is already returned by the loader today and is a single date (low sensitivity vs the PHI handled in 04-07); no new exposure introduced by this change |
| T-04-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no new dependencies. date-fns (parseISO/differenceInDays) is already a declared, used dependency. |
</threat_model>

<verification>
After both tasks:
- `cd remix-app && grep -n "parseISO(CESSATION_START_DATE)" app/lib/protocol-data.ts` returns nothing (the constant is no longer the day-calc input)
- `cd remix-app && npx react-router typegen && npx tsc --noEmit` exits 0
- `cd remix-app && npx vitest run app/lib/protocol-data.test.ts` passes
- Loader-parity test green against live Neon (owner currentDay/cessationDay still 169 at FIXED_NOW)
</verification>

<success_criteria>
- getCessationDay computes from the startDateIso parameter, never CESSATION_START_DATE
- All three loaders pass the DB cessation.startDate
- CESSATION_START_DATE retained only as seed documentation + empty-cessation default
- Unit test and loader-parity test green; owner day count unchanged (169)
- DATA-01 / SC-1 "cessation day computed from the DB startDate" is now satisfied
</success_criteria>

<output>
Create `.planning/phases/04-static-to-db-data-layer-migration/04-06-SUMMARY.md` when done.
Stage only the five files in files_modified (and the SUMMARY) explicitly — do NOT use `git add -A` (parallel sessions may have unrelated changes in the tree).
</output>
