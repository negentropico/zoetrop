---
phase: 04-static-to-db-data-layer-migration
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - remix-app/app/lib/real-data.ts
  - remix-app/app/lib/protocol-data.ts
  - remix-app/app/lib/seed-data.ts
  - remix-app/app/lib/metric-targets.ts
  - remix-app/scripts/seed-data.ts
  - remix-app/scripts/capture-fixtures.ts
  - remix-app/package.json
autonomous: false
requirements: [DATA-04]
must_haves:
  truths:
    - "real-data.ts and seed-data.ts no longer exist (or contain zero PHI arrays); protocol-data.ts retains only survivor engine fns + non-PHI constants"
    - "No _app route or component imports a deleted PHI module"
    - "Non-PHI survivors (CESSATION_PHASES, getCessationDay/getCurrentCessationPhase, METRIC_TARGETS goals, dailySchedule, avoidList) are relocated/kept and still resolve"
    - "The Vercel/react-router build client bundle contains no PHI strings (DATA-04)"
    - "tsc passes with zero errors and the parity + seed + schema tests stay green"
  artifacts:
    - path: "remix-app/app/lib/metric-targets.ts"
      provides: "relocated non-PHI target/goal constants (if METRIC_TARGETS survives)"
      contains: "METRIC_TARGETS"
  key_links:
    - from: "remix-app build client bundle"
      to: "PHI grep gate"
      via: "grep -r against build/client"
      pattern: "realBloodWork|metabolic-glucose|Cessation Attempt"
    - from: "remix-app/app/lib/protocol-data.ts"
      to: "survivor engine fns"
      via: "getCessationDay/getCurrentCessationPhase retained"
      pattern: "getCessationDay"
---

<objective>
Complete the cut-over (D-09 final steps, gated on Plan 04's owner approval): delete the PHI data arrays from `real-data.ts` / `protocol-data.ts` / `seed-data.ts`, relocate the non-PHI survivors (D-06), retire the now-consumed seed/capture scripts' static imports, and prove DATA-04 — no PHI in the Vercel build client bundle. After this plan, Neon is the single source of truth and no PHI lives in committed TypeScript source.

Purpose: DATA-04 requires no PHI in the client bundle or static source, verified against the build output (D-10: "Vercel build output", not "Netlify function bundle"). Deletion is the last step of D-09's order (seed → wire → parity green → spot-check → delete) and is gated on the owner approval recorded in Plan 04. The non-PHI engine fns and display constants (D-06) survive — only the owner's measured values are removed. The build PHI grep is the DATA-04 acceptance gate.

Output: PHI arrays deleted; survivors relocated/kept; seed + capture scripts retired; build clean of PHI; final test/typecheck/build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-static-to-db-data-layer-migration/04-CONTEXT.md
@.planning/phases/04-static-to-db-data-layer-migration/04-RESEARCH.md
@.planning/phases/04-static-to-db-data-layer-migration/04-PATTERNS.md

# Prior plan — the owner approval + survivor-helper homes resolved
@.planning/phases/04-static-to-db-data-layer-migration/04-04-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase + prior plans. -->

PHI arrays to DELETE (D-05):
- real-data.ts: `realBloodWorkM1`, `realBloodWorkM2`, `realBodyComposition`, `realAutonomicData` (the 4 metric arrays), `getRealMetrics`, `getLatestRealMetrics`, `MILESTONES` (M1–M4 appointment dates = PHI), `MILESTONE_INFO`. These are now seeded into Neon (Plan 03) and read via data.server.ts (Plan 04).
- protocol-data.ts: `realCessationLog`, `realProtocolVersions`, `realProtocolChanges`, `realSupplements`, `realMilestones`. Seeded + DB-backed now.
- seed-data.ts (lib): `seedGeneticVariants` (genotypes → subject_genotypes), `seedCorrelations` (→ correlations table), plus the synthetic `generateSeedMetrics`/`seedProtocol*`/`seedSupplements`/`seedCessationLog`/`seedMilestones` arrays if PHI-adjacent.

NON-PHI SURVIVORS to KEEP/RELOCATE (D-06 — nothing with measured values; these are goals/constants/engine fns):
- protocol-data.ts: `getCessationDay(now)`, `getCurrentCessationPhase(day)`, `CESSATION_START_DATE`, `dailySchedule`, `avoidList`. KEEP in protocol-data.ts (delete only the real* arrays from this file) UNLESS Plan 04 Task 3 relocated the cessation engine fns to a non-lint-blocked module (e.g. `app/lib/cessation.ts`) — check 04-04-SUMMARY and honor whatever home Plan 04 chose; do not re-relocate.
- types/protocol.ts: `CESSATION_PHASES`, `SUPPLEMENT_TIERS` — already in types, unchanged.
- real-data.ts: `METRIC_TARGETS`, `MetricTarget`, `getMetricTargets`, `getProjections` — goals/targets (non-PHI per RESEARCH Open Q1). RELOCATE to a new `app/lib/metric-targets.ts` (or the home Plan 04 chose — check 04-04-SUMMARY). `getProjections` may reference M5/M6 future target dates (non-PHI) — keep those; strip any M1–M4 measured-snapshot references.
- seed-data.ts (lib): `calculatePearsonCorrelation`, `getCorrelationSignificance`, `getCorrelationColor`, `getMetricsCountByCategory` — pure engine/presentation helpers (non-PHI). RELOCATE survivors that routes still import (per 04-04-SUMMARY) to a non-PHI home (`app/lib/metrics.ts` or a new `app/lib/correlations.ts`), then delete the PHI arrays.

After deletion, `scripts/seed-data.ts` and `scripts/capture-fixtures.ts` have dangling imports of the deleted arrays. They are ONE-SHOT scripts that already ran (Plan 03). Preferred disposition: DELETE both scripts (the seed is not re-runnable once the arrays are gone, and re-seeding is unnecessary — Neon is authoritative) and remove the `db:seed-data` + `capture-fixtures` npm scripts from package.json. Record the decision in the SUMMARY.

DATA-04 build grep (D-10 — Vercel build output): after deletion, run `npm run build`, then grep the CLIENT bundle for known PHI markers. Expect ZERO matches:
  `grep -rE "realBloodWork|metabolic-glucose|Cessation Attempt|A1298C|rs324420|153\.13|166\.36|98th percentile" remix-app/build/client/`
(markers from actual PHI: metric ids, genotype strings, DEXA lean-mass numbers, milestone descriptions.) `build/` is gitignored.

Tests that must stay green (regression gate): `tests/db/schema-columns.test.ts` (DATA-05), `tests/db/data-seed.test.ts` (DATA-02), `tests/parity/loader-parity.test.ts` (DATA-01). The parity test reads the gitignored JSON fixtures (Plan 03), NOT the static modules — deleting the arrays does NOT break it. Confirm no static-module import remains in the parity test file before deleting.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate non-PHI survivors (D-06)</name>
  <files>remix-app/app/lib/metric-targets.ts, remix-app/app/lib/protocol-data.ts</files>
  <read_first>
    - remix-app/app/lib/real-data.ts (offsets 7–113 — MILESTONES/METRIC_TARGETS/getMetricTargets/getProjections; identify PHI vs goal)
    - remix-app/app/lib/seed-data.ts (offsets 605–645 — pure helpers calculatePearsonCorrelation/getCorrelationSignificance/getCorrelationColor)
    - remix-app/app/lib/protocol-data.ts (survivor engine fns to keep)
    - .planning/phases/04-static-to-db-data-layer-migration/04-04-SUMMARY.md (the survivor homes Plan 04 resolved + which helpers routes still import + the cessation engine-fn relocation decision)
    - .planning/phases/04-static-to-db-data-layer-migration/04-CONTEXT.md (D-06; RESEARCH Open Q1/Q2)
  </read_first>
  <action>
    Create `app/lib/metric-targets.ts` and move the non-PHI target/goal exports out of real-data.ts: `MetricTarget` interface, `METRIC_TARGETS` (goals — LDL targets etc, NOT measurements), `getMetricTargets`, and `getProjections` (keep its M5/M6 future-target-date logic; strip any reference to M1–M4 measured snapshots — those come from DB milestones). Update the import path in every route Plan 04 left pointing at real-data for these helpers (per 04-04-SUMMARY) to `~/lib/metric-targets`. Move any pure correlation helpers routes still import (`getCorrelationSignificance`, `getCorrelationColor`, `calculatePearsonCorrelation`) into a non-PHI home (`app/lib/metrics.ts` or a new `app/lib/correlations.ts` — match 04-04-SUMMARY) and update those imports. Do NOT move anything containing the owner's measured values. Confirm the cessation survivor engine fns (`getCessationDay`, `getCurrentCessationPhase`, `CESSATION_START_DATE`, `dailySchedule`, `avoidList`) are in the home Plan 04 chose (protocol-data.ts OR a relocated module) — leave them there; only the real* PHI arrays leave protocol-data.ts (Task 2).
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/metric-targets.ts` exists and contains `METRIC_TARGETS` and `getMetricTargets`.
    - `grep -cE "realBloodWork|153\.13|166\.36|98th percentile" remix-app/app/lib/metric-targets.ts` returns 0 (no PHI relocated).
    - No route imports the survivor correlation helpers from `seed-data` (they resolve from their new non-PHI home).
    - The cessation survivor engine fns still resolve from their home (typecheck would fail otherwise).
    - `cd remix-app && npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Non-PHI goals/constants/helpers relocated to non-PHI homes; routes repointed; no measured values relocated; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete PHI arrays + retire one-shot scripts (gated on Plan 04 approval)</name>
  <files>remix-app/app/lib/real-data.ts, remix-app/app/lib/protocol-data.ts, remix-app/app/lib/seed-data.ts, remix-app/scripts/seed-data.ts, remix-app/scripts/capture-fixtures.ts, remix-app/package.json</files>
  <read_first>
    - remix-app/app/lib/real-data.ts (full — what remains after Task 1 relocation; delete the 4 metric arrays + MILESTONES + getRealMetrics/getLatestRealMetrics)
    - remix-app/app/lib/protocol-data.ts (full — delete real* arrays; keep survivors)
    - remix-app/app/lib/seed-data.ts (full — delete seedGeneticVariants/seedCorrelations + synthetic PHI arrays; helpers relocated in Task 1)
    - remix-app/scripts/seed-data.ts + capture-fixtures.ts (one-shot; their static imports become dangling)
    - remix-app/package.json (db:seed-data / capture-fixtures scripts)
    - .planning/phases/04-static-to-db-data-layer-migration/04-04-SUMMARY.md (owner approval recorded — REQUIRED before this task runs)
  </read_first>
  <action>
    GATED: only proceed if Plan 04's owner visual verification was approved (check 04-04-SUMMARY; if not approved, STOP and report). Delete the PHI arrays: from real-data.ts remove `realBloodWorkM1/M2`, `realBodyComposition`, `realAutonomicData`, `MILESTONES`, `MILESTONE_INFO`, `getRealMetrics`, `getLatestRealMetrics` — if nothing non-PHI remains in real-data.ts after Task 1's relocation, DELETE the file entirely and remove its references. From protocol-data.ts remove `realCessationLog`, `realProtocolVersions`, `realProtocolChanges`, `realSupplements`, `realMilestones` (keep the survivor engine fns + dailySchedule/avoidList/CESSATION_START_DATE — the file stays). From seed-data.ts (lib) remove `seedGeneticVariants`, `seedCorrelations`, `generateSeedMetrics`, and the synthetic `seedProtocol*`/`seedSupplements`/`seedCessationLog`/`seedMilestones` PHI-adjacent arrays — if no non-PHI export remains (helpers relocated in Task 1), DELETE the file. Retire the one-shot scripts: DELETE `scripts/seed-data.ts` and `scripts/capture-fixtures.ts` and remove the `db:seed-data` + `capture-fixtures` entries from package.json (record the decision in the SUMMARY). Fix any remaining import of a deleted symbol anywhere in app/ (typecheck surfaces them). Do NOT touch the gitignored `tests/fixtures/` (local-only, leave them). Run typecheck + the full test suite to confirm parity/seed/schema tests still pass (they read DB + JSON fixtures, not the deleted arrays).
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "realBloodWork|realProtocolVersions|realSupplements|realCessationLog|realMilestones|seedGeneticVariants|seedCorrelations|getLatestRealMetrics|getRealMetrics" remix-app/app/` returns NO matches (PHI arrays + accessors gone from app source).
    - `grep -rnE "import.*['\"][^'\"]*real-data|import.*['\"][^'\"]*seed-data" remix-app/app/` returns NO matches; `protocol-data` imports appear only for survivor engine fns.
    - protocol-data.ts still exports `getCessationDay`/`getCurrentCessationPhase`/`dailySchedule`/`avoidList` (unless Plan 04 relocated them — then those exports live in the chosen module and typecheck passes).
    - `scripts/seed-data.ts` and `scripts/capture-fixtures.ts` are deleted; `package.json` no longer contains `db:seed-data` or `capture-fixtures`.
    - `cd remix-app && npm run typecheck` exits 0; `cd remix-app && npm test` exits 0 (DATA-01/02/05 tests all green).
  </acceptance_criteria>
  <done>All PHI arrays deleted from source, survivors intact, one-shot scripts retired, no dangling imports, typecheck + full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: DATA-04 build PHI grep — no PHI in the Vercel build output</name>
  <files>(none — verification gate; the build is generated, not committed — build/ is gitignored)</files>
  <what-built>The PHI arrays are deleted from TypeScript source; routes read DB-backed data; the production build (`react-router build`, the Vercel build output per D-10) should now contain zero PHI strings in the client bundle. This task runs the build and the PHI grep — the DATA-04 acceptance gate.</what-built>
  <how-to-verify>
    1. Run `cd remix-app && npm run build` (produces `build/client` + `build/server`).
    2. Grep the CLIENT bundle for known PHI markers — expect ZERO matches:
       `grep -rE "realBloodWork|metabolic-glucose|Cessation Attempt|A1298C|rs324420|153\.13|166\.36|98th percentile" remix-app/build/client/`
    3. Also grep the route source one final time — expect ZERO matches:
       `grep -rnE "real-data|seed-data" remix-app/app/routes/`
    4. Confirm the dev app still renders (sign in, spot-check `/dashboard`, `/insights/genetics`, `/metrics/metabolic`) — DB-backed, identical to Plan 04's approved state.
    5. Confirm `build/` is gitignored (it is) so no build artifact is committed.
  </how-to-verify>
  <action>CHECKPOINT — run the build and the PHI grep, then pause for the owner. Execute `npm run build`, run the client-bundle PHI grep and the route-source grep from how-to-verify, and report the match counts (expected: zero). If any match is found, trace it (likely a module-level import outside a loader, Pitfall 6) and fix before requesting approval. Wait for the owner to confirm the grep is clean before marking the phase complete. Note on the SUMMARY that the Phase 7 pre-client repo-squash (D-07) remains the chosen PHI-history remedy and is on the Phase 7 gate checklist (no history rewrite here).</action>
  <verify><human-check>Owner confirms the client-bundle PHI grep returns zero matches, the route-source grep returns zero matches, and the app renders DB-backed.</human-check></verify>
  <resume-signal>Type "approved" if the client-bundle PHI grep returns zero matches and the app renders, or paste any grep match so the leak can be traced.</resume-signal>
  <done>npm run build succeeds; the client-bundle PHI grep returns zero matches; route-source grep returns zero matches; owner approved; D-07 Phase-7 repo-squash noted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TypeScript source → committed repo | post-deletion, no PHI may remain in committed source |
| server loader → client bundle | a module-level static import (outside a loader) would tree-shake PHI into the client bundle (Pitfall 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-BUNDLE | Information Disclosure | build/client bundle | mitigate | Task 3 greps the client bundle for PHI markers after deletion (DATA-04 gate, Pitfall 6); zero matches required |
| T-04-SRC-PHI | Information Disclosure | app/lib/*-data.ts | mitigate | Task 2 deletes all PHI arrays; grep asserts no `real*`/`seed*` PHI accessors remain in app/ |
| T-04-SURV | Tampering | survivor relocation | mitigate | Task 1 verifies relocated files carry no measured values (grep for DEXA numbers/genotype strings) |
| T-04-HIST | Information Disclosure | git history (pre-deletion commits) | accept | D-07: no destructive history rewrite now (private n=1 repo); pre-pilot SQUASHED-REPO cutover is on the Phase 7 pre-client gate checklist — out of Phase 4 scope |
| T-04-SC | Tampering | npm installs | accept | No new packages this plan |
</threat_model>

<verification>
- `cd remix-app && npm run typecheck` exits 0.
- `cd remix-app && npm test` exits 0 (DATA-01 parity, DATA-02 seed counts, DATA-05 schema — all green post-deletion).
- `cd remix-app && npm run lint` exits 0.
- `cd remix-app && npm run build` succeeds; `grep -rE "<PHI markers>" build/client/` returns zero matches (DATA-04).
- `grep -rnE "real-data|seed-data" remix-app/app/routes/` returns no matches.
- Owner approval recorded (Task 3).
</verification>

<success_criteria>
- DATA-04 satisfied: no PHI in the Vercel build client bundle (verified by grep against the build artifact, D-10 wording) and none in static source.
- D-05 complete: seed-then-delete done; Neon is the single source of truth; no gitignored JSON sidecars in source (the parity fixtures are local-only test artifacts, gitignored).
- D-06 honored: engine fns + non-PHI constants survive in relocated/kept homes.
- D-07 recorded: git-history squash deferred to Phase 7 gate (not done here).
- Phase 4 closes DATA-01, DATA-02, DATA-04, DATA-05.
</success_criteria>

<output>
Create `.planning/phases/04-static-to-db-data-layer-migration/04-05-SUMMARY.md` when done. Record: which files were deleted vs trimmed, the final survivor homes, the build PHI-grep result (zero matches), the disposition of the one-shot scripts, and confirmation that the Phase 7 pre-client repo-squash (D-07) is noted on the Phase 7 gate.
</output>
