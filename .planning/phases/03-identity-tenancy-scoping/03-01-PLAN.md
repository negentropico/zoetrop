---
phase: 03-identity-tenancy-scoping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - remix-app/package.json
  - remix-app/vite.config.ts
  - remix-app/tests/auth/session.test.ts
  - remix-app/tests/auth/role.test.ts
  - remix-app/tests/auth/invite.test.ts
  - remix-app/tests/routes/auth-layout.test.ts
  - remix-app/tests/db/schema-columns.test.ts
  - remix-app/tests/db/constraints.test.ts
autonomous: false
requirements: [AUTH-01, AUTH-02, TEN-01, TEN-04]
user_setup:
  - service: vercel
    why: "Better-Auth invite gate + seed script need OWNER_INVITE_TOKEN; the seed needs owner credentials. Set locally for Plan 04 seed run; set in Vercel for deployed signup."
    env_vars:
      - name: OWNER_INVITE_TOKEN
        source: "Generate a random secret (e.g. `openssl rand -hex 32`); add to remix-app/.env and Vercel project env (Production + Preview)"
      - name: OWNER_EMAIL
        source: "Owner's login email — remix-app/.env (local seed only)"
      - name: OWNER_PASSWORD
        source: "Owner's login password — remix-app/.env (local seed only)"

must_haves:
  truths:
    - "The Vitest harness discovers and runs test files under remix-app/tests/"
    - "All 6 Wave-0 test files exist and run (red, since the code under test does not exist yet)"
    - "@better-auth/drizzle-adapter and tsx are installed and registry-verified before use"
  artifacts:
    - path: "remix-app/tests/auth/session.test.ts"
      provides: "AUTH-01 getSession contract stub"
    - path: "remix-app/tests/auth/role.test.ts"
      provides: "AUTH-02 role-field contract stub"
    - path: "remix-app/tests/auth/invite.test.ts"
      provides: "D-01 invite-gate contract stub"
    - path: "remix-app/tests/routes/auth-layout.test.ts"
      provides: "AUTH-02 layout-redirect contract stub"
    - path: "remix-app/tests/db/schema-columns.test.ts"
      provides: "TEN-01 column/index introspection stub"
    - path: "remix-app/tests/db/constraints.test.ts"
      provides: "TEN-04 constraint-swap introspection stub"
  key_links:
    - from: "remix-app/vite.config.ts"
      to: "remix-app/tests/**/*.test.ts"
      via: "test.include glob"
      pattern: "tests/\\*\\*"
---

<objective>
Lay the Wave-0 verification foundation for Phase 3: install the two new dependencies (gated behind a legitimacy checkpoint), extend the Vitest `include` glob so the new `tests/` directory is discovered, and create the 6 test files from 03-VALIDATION.md as the failing contracts that every later plan satisfies.

Purpose: Every Phase 3 requirement (AUTH-01, AUTH-02, TEN-01, TEN-04) and D-01/D-05 binds to an automated test. Those tests must exist and run before the implementing plans, so executors verify against a fixed contract instead of inventing acceptance criteria. This is the Nyquist Wave-0 step required by 03-VALIDATION.md.

Output: Updated `package.json` (deps + db:seed-owner script reserved for Plan 04), updated `vite.config.ts` test glob, and 6 red test files under `remix-app/tests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-identity-tenancy-scoping/03-VALIDATION.md
@.planning/phases/03-identity-tenancy-scoping/03-RESEARCH.md

<interfaces>
<!-- Existing Vitest config (remix-app/vite.config.ts) — the include glob currently
     only covers app/**, so tests under tests/** are NOT discovered until extended. -->

test: {
  environment: "node",
  setupFiles: ["./app/test-setup.ts"],
  include: ["app/**/*.test.ts", "app/**/*.test.tsx"],   // ← MUST add "tests/**/*.test.ts"
  passWithNoTests: true,
}

<!-- Existing test idiom (app/lib/metrics.test.ts) — import from vitest, describe/it/expect: -->
import { describe, it, expect } from "vitest";

<!-- @neondatabase/serverless Pool is the live-DB client used by introspection tests.
     Reuse the same connection-string resolution as db.server.ts:
     process.env.DATABASE_URL_UNPOOLED || process.env.DATABASE_URL -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy, then install @better-auth/drizzle-adapter + tsx</name>
  <read_first>
    - .planning/phases/03-identity-tenancy-scoping/03-RESEARCH.md (§ Package Legitimacy Audit — both packages are [ASSUMED], slopcheck unavailable at research time; § Standard Stack — versions)
    - remix-app/package.json (current deps — better-auth@^1.6.14 already present; @better-auth/drizzle-adapter and tsx are NOT)
  </read_first>
  <what-built>
    Two new dev dependencies are required for Phase 3 and are tagged [ASSUMED] in 03-RESEARCH.md (slopcheck was unavailable at research time), so per the package-legitimacy gate they must be human-verified BEFORE install:
    - `@better-auth/drizzle-adapter` (~1.6.15) — connects Better-Auth to the existing Drizzle/Neon Pool (same monorepo as better-auth)
    - `tsx` (latest) — TypeScript executor for running scripts/seed-owner.ts in Plan 04 (currently absent from node_modules)
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@better-auth/drizzle-adapter — confirm repo is github.com/better-auth/better-auth (same org as the already-trusted better-auth), recent publish, non-trivial download count.
    2. Open https://www.npmjs.com/package/tsx — confirm repo github.com/privatenumber/tsx, millions of weekly downloads, actively maintained.
    3. If both look legitimate, type "approved" and the executor runs:
       `cd remix-app && npm install -D @better-auth/drizzle-adapter tsx`
    4. After install, confirm both appear in remix-app/package.json devDependencies and `npm test` still exits 0.
  </how-to-verify>
  <action>
    Do NOT install before approval. After "approved": run `cd remix-app && npm install -D @better-auth/drizzle-adapter tsx`. Confirm `@better-auth/drizzle-adapter` and `tsx` are listed in package.json devDependencies. Also add the script `"db:seed-owner": "tsx scripts/seed-owner.ts"` to the package.json `scripts` block (the script file itself is created in Plan 04). Run `npm test` to confirm the harness still boots green.
  </action>
  <acceptance_criteria>
    - `grep -c "@better-auth/drizzle-adapter" remix-app/package.json` returns ≥ 1
    - `grep -c '"tsx"' remix-app/package.json` returns ≥ 1
    - `grep -c "db:seed-owner" remix-app/package.json` returns 1
    - `cd remix-app && npm test` exits 0
  </acceptance_criteria>
  <resume-signal>Type "approved" to install, or describe a concern with either package</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Extend the Vitest include glob to discover tests/</name>
  <read_first>
    - remix-app/vite.config.ts (the `test.include` array — currently ["app/**/*.test.ts", "app/**/*.test.tsx"])
    - remix-app/app/test-setup.ts (existing setup file, referenced by setupFiles — leave unchanged)
    - .planning/phases/03-identity-tenancy-scoping/03-VALIDATION.md (test files live under remix-app/tests/, e.g. tests/db/schema-columns.test.ts — these must be discoverable)
  </read_first>
  <action>
    Add `"tests/**/*.test.ts"` (and `"tests/**/*.test.tsx"`) to the `test.include` array in remix-app/vite.config.ts so the new `remix-app/tests/` directory is discovered alongside the existing `app/**` engine tests. Do NOT change `environment`, `setupFiles`, or `passWithNoTests`. Keep the existing `app/**` globs — Phase 1 engine tests live there and must keep running.
  </action>
  <verify>
    <automated>cd remix-app && grep -c "tests/\*\*" vite.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tests/\*\*/\*.test.ts" remix-app/vite.config.ts` returns ≥ 1
    - `grep -c "app/\*\*/\*.test.ts" remix-app/vite.config.ts` returns ≥ 1 (existing globs preserved)
    - `cd remix-app && npm test` exits 0 (no test files in tests/ yet → passWithNoTests still green)
  </acceptance_criteria>
  <done>vite.config.ts test.include covers both app/** and tests/**; npm test green</done>
</task>

<task type="auto">
  <name>Task 3: Create the 6 Wave-0 contract test files (red)</name>
  <read_first>
    - .planning/phases/03-identity-tenancy-scoping/03-VALIDATION.md (§ Per-Task Verification Map + § Wave 0 Requirements — the exact 6 files and what each asserts)
    - .planning/phases/03-identity-tenancy-scoping/03-RESEARCH.md (§ Code Examples — getSession/role/invite-hook shapes; § Migration Sequence — column/index/constraint names to assert)
    - remix-app/app/lib/metrics.test.ts (existing test idiom: `import { describe, it, expect } from "vitest"`, describe/it blocks)
    - remix-app/app/lib/db.server.ts (connection-string resolution to copy into the DB introspection tests via @neondatabase/serverless Pool)
  </read_first>
  <action>
    Create these 6 test files. Each is intentionally RED now (the code/migration it asserts does not exist yet — implemented in Plans 02–05). Write real assertions, not `it.todo`:

    1. `remix-app/tests/auth/session.test.ts` (AUTH-01) — import `auth` from `~/lib/auth.server`; assert `auth.api.getSession({ headers })` returns null for an empty/no-cookie Headers, and that the `auth` object exposes `.api.getSession` and `.handler`. (Red until Plan 03 creates auth.server.ts.)
    2. `remix-app/tests/auth/role.test.ts` (AUTH-02) — assert the Better-Auth config exposes a `role` additional field whose allowed values are exactly `owner|practitioner|client` and that it is `input:false`. Assert by importing `auth` and checking `auth.options.user.additionalFields.role` (input === false; type array contains the three roles). (Red until Plan 03.)
    3. `remix-app/tests/auth/invite.test.ts` (D-01) — assert that calling the sign-up path without a valid `inviteToken` is rejected. Drive it through `auth.api.signUpEmail` with a bogus token and assert it throws/returns a 403-shaped error (`APIError` FORBIDDEN). Use `OWNER_INVITE_TOKEN` indirection: a wrong token is rejected. (Red until Plan 03.)
    4. `remix-app/tests/routes/auth-layout.test.ts` (AUTH-02) — import the `loader` from `~/routes/_app/layout`; call it with a Request carrying no session cookie; assert it throws a redirect Response to `/login` (status 302, Location starts with `/login`). (Red until Plan 05.)
    5. `remix-app/tests/db/schema-columns.test.ts` (TEN-01) — open a `@neondatabase/serverless` Pool using `process.env.DATABASE_URL_UNPOOLED || process.env.DATABASE_URL`; for each of the 8 tables (`metrics`, `protocol_versions`, `protocol_changes`, `milestones`, `supplements`, `supplement_log`, `correlations`, `cessation_log`) query `information_schema.columns` to assert `tenant_id` and `subject_id` exist and `is_nullable = 'NO'`; query `pg_indexes` to assert a composite index on `(tenant_id, subject_id)` exists per table. Skip the suite (not fail) when no DATABASE_URL is set, so local CI without DB credentials stays green, but assert hard when the URL is present. (Red until Plans 02+04 migrate the DB.)
    6. `remix-app/tests/db/constraints.test.ts` (TEN-04) — using the same Pool, query `pg_constraint`/`information_schema.table_constraints` to assert a UNIQUE constraint on `(tenant_id, subject_id, version)` exists on `protocol_versions` AND the old global `protocol_versions_version_unique` is absent. Same DATABASE_URL skip-guard. (Red until Plan 04.)

    For the DB tests, gate the live-DB assertions behind a presence check of the connection string and call `it.skip` / `describe.skipIf(!url)` so they are green-skipped without DB env and hard-assert with it — never silently pass. No `any`: type query rows as `Record<string, unknown>` or a narrow row interface.
  </action>
  <verify>
    <automated>cd remix-app && npm test 2>&1 | grep -E "session|role|invite|auth-layout|schema-columns|constraints"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist at the paths in 03-VALIDATION.md § Wave 0 Requirements
    - `cd remix-app && npm test` discovers all 6 (they appear in the run output)
    - The auth/route tests are RED (fail to import the not-yet-existent modules) — this is expected and proves the contract is live
    - The DB tests are green-skipped when no DATABASE_URL is set (no false-positive pass)
    - No `any` token in any of the 6 files: `grep -rn ": any\b\|<any>\|as any" remix-app/tests/` returns empty
  </acceptance_criteria>
  <done>6 contract test files exist, are discovered by Vitest, and are red/skip-guarded exactly as 03-VALIDATION.md specifies</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Untrusted package code crosses into the build; [ASSUMED]-tagged packages must be human-verified (slopcheck was unavailable) |
| developer shell → .env / Vercel env | OWNER_INVITE_TOKEN / OWNER_PASSWORD are secrets entered here; never commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm install of @better-auth/drizzle-adapter + tsx | mitigate | Blocking human checkpoint (Task 1) verifies both on npmjs.com before install; both [ASSUMED] per 03-RESEARCH.md |
| T-03-secret | Information disclosure | OWNER_INVITE_TOKEN / OWNER_PASSWORD in .env | mitigate | Documented as user_setup env vars only; .env is gitignored; never echoed into test files or committed |
</threat_model>

<verification>
- `cd remix-app && npm test` runs and discovers all 6 new test files plus the existing Phase 1 engine tests
- Auth/route contract tests are red (modules not yet built); DB introspection tests skip-guard without DATABASE_URL
- package.json contains @better-auth/drizzle-adapter, tsx, and the db:seed-owner script
- vite.config.ts test.include covers tests/**
</verification>

<success_criteria>
- The 6 Wave-0 test files from 03-VALIDATION.md exist and are discovered by Vitest
- The two new dependencies are installed only after human legitimacy verification
- The test harness exits 0 in the no-DB / pre-implementation state via skip-guards and red-import expectations being the only failures (which downstream plans turn green)
</success_criteria>

<output>
Create `.planning/phases/03-identity-tenancy-scoping/03-01-SUMMARY.md` when done
</output>
