---
phase: 07-phi-compliance-hardening-pre-client-gate-deferred-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - remix-app/app/lib/db.server.ts
  - remix-app/db/schema.ts
  - remix-app/tests/db/rls-isolation.test.ts
autonomous: true
requirements: [TEN-03, AUTH-03]
must_haves:
  truths:
    - "withTenantDb(ctx, fn) exists and wraps fn in a transaction issuing SET LOCAL GUCs + SET LOCAL ROLE app_user"
    - "TenantCtx type { userId, tenantId, subjectId } is exported and importable by service modules"
    - "practitioner_subject_assignments table is defined in schema.ts with tenant/practitioner/subject FKs and a unique active-assignment index"
    - "A RED rls-isolation.test.ts file exists, skip-guarded on DATABASE_URL, asserting cross-tenant zero-read + WITH CHECK rejection + pool non-leak"
  artifacts:
    - path: "remix-app/app/lib/db.server.ts"
      provides: "withTenantDb wrapper + TenantCtx type, exported beside getDb()"
      contains: "withTenantDb"
    - path: "remix-app/db/schema.ts"
      provides: "practitionerSubjectAssignments table definition"
      contains: "practitioner_subject_assignments"
    - path: "remix-app/tests/db/rls-isolation.test.ts"
      provides: "TEN-02/TEN-03 isolation + pool-leak test (RED until RLS migration applies)"
      contains: "describe.skipIf"
  key_links:
    - from: "remix-app/app/lib/db.server.ts"
      to: "drizzle transaction + set_config"
      via: "db.transaction with sql`SELECT set_config('app.tenant_id', ..., true)`"
      pattern: "set_config\\('app\\.tenant_id'"
    - from: "remix-app/db/schema.ts"
      to: "tenants / user / subjects FKs"
      via: "references(() => ...)"
      pattern: "practitionerSubjectAssignments"
---

<objective>
Lay the Phase 7 foundation with zero DB-apply risk: add the `withTenantDb(ctx, fn)` transaction wrapper and `TenantCtx` type beside `getDb()` in `db.server.ts`, define the `practitioner_subject_assignments` table in `schema.ts`, and commit a RED cross-tenant isolation + pool-leak test that all later plans turn green.

Purpose: Establish the host-portable GUC RLS mechanism (D-02) and the AUTH-03 assignment schema as the contract that Plans 02–04 implement against, before any live-DB change touches the existing Neon project.
Output: `withTenantDb` + `TenantCtx` exports, `practitionerSubjectAssignments` schema table, and `tests/db/rls-isolation.test.ts` (RED — skip-guarded, fails/skips until the RLS migration in Plan 02 applies).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-phi-compliance-hardening-pre-client-gate-deferred-hardening/07-CONTEXT.md
@.planning/phases/07-phi-compliance-hardening-pre-client-gate-deferred-hardening/07-PATTERNS.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-SPIKE-FINDINGS.md

<interfaces>
<!-- Contracts this plan creates. Plans 02/03/04 import these directly — no exploration. -->

From remix-app/app/lib/db.server.ts (current — DO NOT remove getDb; ADD beside it):
```typescript
export function getDb(): ReturnType<typeof drizzle<typeof schema>>;  // existing — unchanged
```

To ADD in db.server.ts (this plan):
```typescript
export interface TenantCtx { userId: string; tenantId: string; subjectId: string; }
export async function withTenantDb<T>(
  ctx: TenantCtx,
  fn: (tx: ReturnType<typeof getDb>) => Promise<T>
): Promise<T>;
```

Existing schema FK targets (remix-app/db/schema.ts) the new table references:
```typescript
export const tenants = pgTable('tenants', { id: text('id').primaryKey(), ... });
export const subjects = pgTable('subjects', { id: text('id').primaryKey(), tenantId: ..., ... });
export const user = ...; // from auth-schema barrel — text('id').primaryKey()
export const invites = pgTable('invites', { ... }, (t) => [ index('idx_invites_tenant').on(t.tenantId), ... ]);
```

GUC names (D-02, host-portable, NOT Supabase auth.jwt()):
- app.tenant_id, app.subject_id, app.user_id
- NOBYPASSRLS role name: app_user
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add withTenantDb wrapper + TenantCtx to db.server.ts</name>
  <read_first>
    - remix-app/app/lib/db.server.ts (the file being modified — getPool/getDb lazy-init pattern must be preserved exactly)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-SPIKE-FINDINGS.md (the proven SET LOCAL mechanism: NOBYPASSRLS role, SET LOCAL non-leak, empty-claims fail-closed)
    - .planning/phases/07-phi-compliance-hardening-pre-client-gate-deferred-hardening/07-PATTERNS.md §withTenantDb.server.ts (signature + set_config body shape)
  </read_first>
  <files>remix-app/app/lib/db.server.ts</files>
  <behavior>
    - withTenantDb returns the value of fn(tx) when the transaction commits
    - withTenantDb wraps fn inside db.transaction (Drizzle) — never a bare connection
    - Inside the transaction, before fn runs: set_config('app.tenant_id', ctx.tenantId, true), set_config('app.subject_id', ctx.subjectId, true), set_config('app.user_id', ctx.userId, true) are issued, then SET LOCAL ROLE app_user
    - On error thrown inside fn, the transaction rolls back (Drizzle default — no try/catch added in the wrapper)
    - TenantCtx is exported as an interface with exactly userId, tenantId, subjectId (all string)
  </behavior>
  <action>
    Keep `getDb`, `getPool`, the lazy `_db`/`_pool` singletons, the `db` named export, and `export { schema }` UNCHANGED. ADD beside `getDb()`: (1) `import { sql } from 'drizzle-orm';` at top; (2) `export interface TenantCtx { userId: string; tenantId: string; subjectId: string; }`; (3) `export async function withTenantDb<T>(ctx: TenantCtx, fn: (tx: ReturnType<typeof getDb>) => Promise<T>): Promise<T>`. The body calls `getDb().transaction(async (tx) => { ... })`. Inside the transaction, FIRST execute a single statement issuing the three GUCs transaction-scoped: `tx.execute(sql\`SELECT set_config('app.tenant_id', ${ctx.tenantId}, true), set_config('app.subject_id', ${ctx.subjectId}, true), set_config('app.user_id', ${ctx.userId}, true)\`)`, THEN `tx.execute(sql\`SET LOCAL ROLE app_user\`)`, THEN `return fn(tx)`. The third `set_config` arg `true` = transaction-local (non-leaking per 01-SPIKE-FINDINGS line 19); `SET LOCAL ROLE app_user` switches to the NOBYPASSRLS role created in Plan 02 (01-SPIKE-FINDINGS line 24 — neondb_owner silently bypasses RLS). Add a header comment: GUC names are plain Postgres (D-02 host-portable), driver stays @neondatabase/serverless for Phase 7 (D-01 — the postgres-js swap is Phase 8 if a host migration occurs). Do NOT add an empty-claims guard in the wrapper — that lives in the RLS policy predicate (Plan 02). TS strict: no `any` (D-11).
  </action>
  <verify>
    <automated>cd remix-app && npx tsc --noEmit 2>&1 | grep -E "db.server|withTenantDb|TenantCtx" ; test $? -eq 1 && echo "no type errors in db.server"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function withTenantDb" remix-app/app/lib/db.server.ts` returns 1
    - `grep -c "export interface TenantCtx" remix-app/app/lib/db.server.ts` returns 1
    - `grep -q "set_config('app.tenant_id'" remix-app/app/lib/db.server.ts` succeeds
    - `grep -q "SET LOCAL ROLE app_user" remix-app/app/lib/db.server.ts` succeeds
    - `grep -q "db.transaction\|\.transaction(async" remix-app/app/lib/db.server.ts` succeeds (wrapper uses a transaction)
    - `grep -c "export function getDb" remix-app/app/lib/db.server.ts` still returns 1 (getDb preserved)
    - `cd remix-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>withTenantDb + TenantCtx exported beside an unchanged getDb(); transaction wraps set_config GUCs + SET LOCAL ROLE app_user; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Define practitioner_subject_assignments table in schema.ts</name>
  <read_first>
    - remix-app/db/schema.ts (the file being modified — observe the `invites` table FK+index pattern lines 96-110 and `subjects`/`tenants`/`user` definitions; the barrel re-export convention)
    - .planning/phases/07-phi-compliance-hardening-pre-client-gate-deferred-hardening/07-PATTERNS.md §schema.ts (exact column + index shape for the new table)
  </read_first>
  <files>remix-app/db/schema.ts</files>
  <action>
    Add `export const practitionerSubjectAssignments = pgTable('practitioner_subject_assignments', {...})` near the other tenant-scoped tables (after `subjects`/`subjectGenotypes`). Columns: `id: text('id').primaryKey()`; `tenantId: text('tenant_id').notNull().references(() => tenants.id)`; `practitionerId: text('practitioner_id').notNull().references(() => user.id)`; `subjectId: text('subject_id').notNull().references(() => subjects.id)`; `assignedBy: text('assigned_by').notNull().references(() => user.id)`; `assignedAt: timestamp('assigned_at').notNull().defaultNow()`; `revokedAt: timestamp('revoked_at')` (nullable — null means active); `createdAt: timestamp('created_at').defaultNow()`. Index callback returns: `index('idx_psa_tenant').on(t.tenantId)`, `index('idx_psa_practitioner').on(t.practitionerId)`, `index('idx_psa_subject').on(t.subjectId)`, and `uniqueIndex('idx_psa_active_unique').on(t.tenantId, t.practitionerId, t.subjectId)`. Import `uniqueIndex` / `index` / `timestamp` / `text` / `pgTable` from the existing imports (they are already imported — verify, add only if missing). Confirm `user` is in scope (it is re-exported via the auth-schema barrel — `invites` already references `user.id`). Do NOT run `db:generate` here — the Drizzle-generated table-creation migration plus the hand-authored RLS DDL are both authored in Plan 02 as a single atomic migration (D-10). TS strict, no `any`.
  </action>
  <verify>
    <automated>cd remix-app && npx tsc --noEmit 2>&1 | grep -i "schema.ts\|practitioner" ; test $? -eq 1 && echo "schema typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "practitioner_subject_assignments" remix-app/db/schema.ts` returns at least 1
    - `grep -c "practitionerSubjectAssignments" remix-app/db/schema.ts` returns at least 1
    - `grep -q "idx_psa_active_unique" remix-app/db/schema.ts` succeeds (unique active-assignment index present)
    - `grep -q "references(() => user.id)" remix-app/db/schema.ts` succeeds (practitioner + assignedBy FKs to user)
    - `grep -q "revokedAt: timestamp" remix-app/db/schema.ts` succeeds (soft-revoke column)
    - `cd remix-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>practitionerSubjectAssignments table defined with tenant/practitioner/subject/assignedBy FKs, soft-revoke column, and 4 indexes incl. the unique active index; tsc clean; no migration generated yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Commit RED cross-tenant isolation + pool-leak test</name>
  <read_first>
    - remix-app/tests/db/constraints.test.ts (the analog — copy its skip-guard, describe.skipIf, pool setup, and afterAll cleanup exactly)
    - remix-app/app/lib/db.server.ts (the withTenantDb signature from Task 1 the test exercises)
    - .planning/phases/07-phi-compliance-hardening-pre-client-gate-deferred-hardening/07-PATTERNS.md §rls-isolation.test.ts (the three test bodies: cross-tenant read, WITH CHECK reject, pool non-leak)
  </read_first>
  <files>remix-app/tests/db/rls-isolation.test.ts</files>
  <behavior>
    - TEN-02 cross-tenant read: a row inserted under withTenantDb(tenantA ctx) is NOT returned to a withTenantDb(tenantB ctx) SELECT — result length 0
    - TEN-02 WITH CHECK reject: an INSERT under withTenantDb(tenantA ctx) carrying tenant_id = tenantB is rejected with a policy error (the promise rejects)
    - TEN-03 pool non-leak: after withTenantDb(tenantA) commits on a pooled connection, a subsequent withTenantDb(tenantB) on the same pool does NOT see tenantA's row (SET LOCAL cleared at COMMIT)
    - All three describe blocks are skip-guarded on connectionString and will SKIP (not error) in CI without DATABASE_URL
  </behavior>
  <action>
    Create `remix-app/tests/db/rls-isolation.test.ts`. Copy the skip-guard from constraints.test.ts: `const connectionString = process.env["DB_URL_STUBBED"] ? undefined : process.env["DATABASE_URL_UNPOOLED"] || process.env["DATABASE_URL"];`. Use `describe.skipIf(!connectionString)(...)` for three blocks: `"TEN-02: RLS cross-tenant isolation (live Neon)"`, `"TEN-02: WITH CHECK rejects mismatched-tenant INSERT"`, and `"TEN-03: withTenantDb context non-leak (live Neon)"`. Import `withTenantDb`, `TenantCtx` from `../../app/lib/db.server` and `metrics` from `../../db/schema`. In `beforeAll`, set up two disjoint test tenants + subjects (insert via the admin getDb() path: two `tenants` rows, two `subjects` rows with distinct ids, e.g. `t-rls-test-a`/`t-rls-test-b`); in `afterAll`, delete the test rows and `await pool.end()` if a pool was opened. Build two ctx objects `{ userId, tenantId, subjectId }` for A and B. Test bodies implement the TEN-02/TEN-03 assertions above using `withTenantDb`. This test is RED until Plan 02 applies the RLS migration AND creates the app_user role — note in a top comment: "RED until 0010 RLS migration + app_user role apply (Plan 02). The withTenantDb SET LOCAL ROLE app_user statement will fail with 'role app_user does not exist' until then." Mark the live-DB assertions so a missing app_user role surfaces as a failing assertion (the intended RED state), not a swallowed error. Do NOT use neondb_owner for the assertion path — withTenantDb already issues SET LOCAL ROLE app_user. TS strict, no `any`.
  </action>
  <verify>
    <automated>cd remix-app && DB_URL_STUBBED=1 npx vitest run tests/db/rls-isolation.test.ts 2>&1 | grep -E "skipped|passed|no test" </automated>
  </verify>
  <acceptance_criteria>
    - File `remix-app/tests/db/rls-isolation.test.ts` exists
    - `grep -c "describe.skipIf" remix-app/tests/db/rls-isolation.test.ts` returns 3
    - `grep -q "DATABASE_URL_UNPOOLED" remix-app/tests/db/rls-isolation.test.ts` succeeds (skip-guard present)
    - `grep -q "withTenantDb" remix-app/tests/db/rls-isolation.test.ts` succeeds (exercises the wrapper)
    - `grep -q "WITH CHECK\|mismatched" remix-app/tests/db/rls-isolation.test.ts` succeeds (write-rejection case present)
    - `cd remix-app && DB_URL_STUBBED=1 npx vitest run tests/db/rls-isolation.test.ts` exits 0 with the suite SKIPPED (no live DB) — proves the file parses and skip-guards correctly
  </acceptance_criteria>
  <done>rls-isolation.test.ts committed with 3 skip-guarded describe blocks (cross-tenant read, WITH CHECK reject, pool non-leak); skips cleanly with DB_URL_STUBBED; RED against live DB until Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remix loader/action → DB | Tenant/subject context crosses into the SQL session; the wrapper is the only sanctioned crossing |
| Pooled connection reuse → next request | Serverless pool reuses physical connections across tenants; a leaked GUC/ROLE would cross tenants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | withTenantDb GUC scoping | mitigate | set_config(..., true) is transaction-local (01-SPIKE-FINDINGS confirms non-leak); wrapper forces db.transaction — no bare-connection path. Pool non-leak proven by the RED TEN-03 test (green after Plan 02) |
| T-07-02 | Elevation of Privilege | SET LOCAL ROLE target | mitigate | Wrapper switches to app_user (NOBYPASSRLS, created in Plan 02); never queries as neondb_owner which silently bypasses RLS (01-SPIKE-FINDINGS line 24) |
| T-07-03 | Tampering | practitioner_subject_assignments rows | mitigate | FK constraints to tenants/user/subjects; unique active index prevents duplicate assignments; RLS policy (Plan 02) confines reads/writes to the owning tenant |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (D-01 keeps @neondatabase/serverless; drizzle-orm/sql already present) — supply-chain surface unchanged |
</threat_model>

<verification>
- `cd remix-app && npx tsc --noEmit` exits 0 (schema + db.server typecheck together)
- `cd remix-app && DB_URL_STUBBED=1 npx vitest run` exits 0 (full suite, new RLS test skips cleanly)
- No new dependencies added to package.json (`git diff --stat remix-app/package.json` shows no change)
</verification>

<success_criteria>
- withTenantDb(ctx, fn) + TenantCtx exported from db.server.ts; getDb() preserved
- practitionerSubjectAssignments table in schema.ts with FKs + unique active index
- rls-isolation.test.ts committed (RED/skip-guarded) covering TEN-02 read, TEN-02 WITH CHECK, TEN-03 pool non-leak
- tsc clean; full vitest suite green with the new test skipping in no-DB CI
</success_criteria>

<output>
Create `.planning/phases/07-phi-compliance-hardening-pre-client-gate-deferred-hardening/07-01-SUMMARY.md` when done
</output>
