# Phase 03.1: Account & Roles — UX + Authorization - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-09
**Phase:** 03.1-account-roles-ux-authorization
**Areas discussed:** Scope shape, Account menu + settings, Invite system, Authorization model

---

## Scope shape

| Option | Description | Selected |
|--------|-------------|----------|
| UX slice only (recommended) | Nav + logout + settings now; defer invites + authz enforcement | |
| UX + invite system | Add per-invite tokens; defer authz enforcement to Phase 7 | |
| All three now | Nav + settings, invites, AND owner/practitioner/client enforcement | ✓ |

**User's choice:** All three now.
**Notes:** Owner overrode the lean recommendation. Accepted tradeoff captured in CONTEXT D-01: authz partially front-runs Phase 7 RLS and is built before a non-owner user exists. Mitigation — design authz RLS-compatible.

---

## Account menu + settings

| Option | Description | Selected |
|--------|-------------|----------|
| Avatar/initials menu (recommended) | Top-right dropdown: Account/Settings, theme, Logout; mirrored to mobile BottomTab | ✓ |
| Dedicated 'Account' nav item | 6th nav item routing to /account | |
| Gear/user icon → dropdown | Minimal icon button → same dropdown | |

**User's choice:** Avatar/initials menu.

| Option (settings contents, multi-select) | Selected |
|--------|----------|
| Profile (name + email) | ✓ |
| Password change | ✓ |
| Invite panel | ✓ |
| Theme preference | ✓ |
| Other (free text): "integrations, export, privacy, units, etc." | ✓ |

**User's choice:** All four core sections + a request for forward-looking sections (integrations, export, privacy, units).

**Follow-up — Settings IA:**

| Option | Description | Selected |
|--------|-------------|----------|
| Scaffold placeholders (recommended) | Core 4 functional; integrations/export/privacy/units as visible "Coming soon" sections | ✓ |
| Functional 4 only, grow later | Clean page, no placeholders | |
| Add 'units/display' too | Make units functional now | |

**User's choice:** Scaffold placeholders.
**Notes:** Forward-looking sections gated to Phase 4/5/7 functionally; only their IA exists in 3.1.

---

## Invite system

| Option | Description | Selected |
|--------|-------------|----------|
| Hand-rolled invites table (recommended) | New `invites` table; rewrite beforeSignUp hook | ✓ |
| Better-Auth org plugin | Adopt organization plugin invitations | |

**User's choice:** Hand-rolled invites table.

| Option (delivery) | Description | Selected |
|--------|-------------|----------|
| Copy invite link (recommended) | Show copyable URL; no email infra | (✓ via pause) |
| Email the invite | Send via provider (Resend/SES) | |
| Both (link now, email later) | Build link, structure for email | |
| Other (free text): "perhaps a migration to supabase solves all this and more" | | ✓ (raised, then paused) |

**User's choice:** Initially floated a Supabase migration instead of choosing delivery; after the blast-radius + credential check, chose **Pause migration** → delivery resolves to **copy-link** (email-ready).
**Notes:** See "Supabase migration" under Deferred. Finding: no Supabase creds in `ngtops/.env` (it's Neon); migration would rewrite Phases 3–7. Owner paused.

| Option (policy) | Description | Selected |
|--------|-------------|----------|
| Owner+practitioner tiered (recommended) | Owner→practitioner+client; practitioner→client (own tenant); client none | ✓ |
| Owner-only | Only owner issues invites | |
| Owner issues all roles, flat | Owner invites both; no practitioner invites yet | |

**User's choice:** Owner+practitioner tiered.

---

## Authorization model

| Option (where) | Description | Selected |
|--------|-------------|----------|
| Reusable server helpers (recommended) | `requireUser`/`requireRole`/`assertSubjectAccess` in loaders/actions | ✓ |
| Per-route inline checks | Inline `session.user.role` checks | |
| Better-Auth admin/access-control plugin | Declare roles+permissions in the auth layer | |

**User's choice:** Reusable server helpers (`app/lib/authz.server.ts`).

| Option (depth) | Description | Selected |
|--------|-------------|----------|
| Matrix + real gates (recommended) | Define full matrix in code; enforce real gates (admin owner/practitioner-only, client read-only) | ✓ |
| Enforce full matrix everywhere | Wire all read/write rules into every loader | |
| Owner-vs-not only | Only gate admin surfaces to owner | |

**User's choice:** Matrix + real gates.

| Option (practitioner→client interim) | Description | Selected |
|--------|-------------|----------|
| Tenant-scoped interim (recommended) | Practitioner sees all tenant subjects; AUTH-03 + RLS → Phase 7 | ✓ |
| Build assignment now | Add practitioner↔subject assignment table now | |
| Owner-only for now | Don't model practitioner→client yet | |

**User's choice:** Tenant-scoped interim.

---

## Claude's Discretion

- Dropdown/menu component implementation; settings route path (`/account` vs `/settings`) + sub-route structure.
- Invite token format + expiry window; `invites` table file location (`db/schema.ts` vs `db/auth-schema.ts`); whether to keep `OWNER_INVITE_TOKEN` as a break-glass fallback.
- Role-aware nav rendering (UI hide is cosmetic; the loader gate is authoritative).

## Deferred Ideas

- **Supabase migration (full re-platform)** — project-level decision, **paused** by owner after blast-radius review. No Supabase creds provisioned in `ngtops` (DATABASE_URL there is Neon). Revisit deliberately before Phase 7, ideally via a `/gsd-spike`, only once a project is provisioned.
- **Email invite delivery** — later layer; copy-link ships in 3.1.
- **AUTH-03 / AUTH-04 / RLS + `SET LOCAL` isolation (TEN-02/03)** → Phase 7.
- **Settings sections** Integrations / Export / Privacy-Consent / Units → functional in Phases 4/5/7; placeholders only now.
- **Reviewed, not folded:** `delete-pilot-basic-auth-post-deploy.md` — Phase 3 deploy carry-forward, resolves at branch merge.
