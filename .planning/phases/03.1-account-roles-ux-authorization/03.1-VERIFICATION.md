---
phase: 03.1-account-roles-ux-authorization
verified: 2026-06-10T05:15:00Z
status: human_needed
score: 14/14
overrides_applied: 0
human_verification:
  - test: "Account dropdown opens/closes and is keyboard-accessible"
    expected: "Clicking the avatar pill opens the dropdown. Pressing Escape closes it and returns focus to the trigger. Outside-click closes. ArrowDown/ArrowUp move focus between items. aria-expanded reflects state. /logout POST works without JS."
    why_human: "DOM interaction, focus management, and real logout behavior cannot be verified by grep or static analysis."
  - test: "Mobile BottomTab shows Account tab (User icon → /settings, Import tab gone)"
    expected: "At viewport width ≤760px the bottom nav shows Dashboard / Metrics / Protocol / Insights / Account — 'Account' uses the User icon and navigates to /settings. Import tab no longer present."
    why_human: "Responsive layout is viewport-dependent and requires visual confirmation."
  - test: "Settings profile name saves and persists"
    expected: "Editing the display name and pressing 'Save changes' calls updateUser; reloading the page shows the new name."
    why_human: "Requires a live session and Better-Auth updateUser call to Neon; cannot verify without a running server."
  - test: "Password change requires the correct current password"
    expected: "Empty/short new password → 'Password must be at least 8 characters.' (on newPassword field). Mismatch → 'Passwords do not match.' Wrong current → 'Current password is incorrect.' Correct current + valid new → 'Password updated.'"
    why_human: "Password validation against a live Better-Auth session; error-field mapping requires runtime."
  - test: "Invite generation, list, and copy flow"
    expected: "An owner or practitioner generates a copy-link invite for the allowed role. The invite appears in ACTIVE INVITES with an ACTIVE badge. 'Copy link' transitions the icon from Copy to Check (periwinkle, 1.5s). The aria-live region announces 'Invite link copied to clipboard'."
    why_human: "clipboard.writeText and CSS transitions require a browser runtime."
  - test: "Invite redemption end-to-end (generated link → new account with encoded role+tenant)"
    expected: "Opening a generated link in a fresh session lands on /login in 'Create your account' mode (name/email/password fields visible). Completing the form creates a user with the role and tenantId encoded in the invite. The invite transitions to USED in the active-invites table."
    why_human: "Full flow requires live Neon, a real invite row, and the Better-Auth databaseHooks path to fire."
  - test: "Invite revoke inline confirmation and REVOKED badge persistence"
    expected: "Clicking 'Revoke invite' shows inline text 'Revoke this invite? This cannot be undone.' with [Keep invite] / [Revoke invite]. Confirming leaves the row visible with a REVOKED badge. Re-attempting the invite link returns FORBIDDEN."
    why_human: "useFetcher state and inline confirmation UI require browser interaction."
  - test: "Client cannot see or reach the Invites surface"
    expected: "A signed-in client loading /settings sees Profile / Security / Preferences / Coming-soon sections — INVITES section is absent. A hand-crafted POST to /settings with _intent=generate-invite returns a 403 response."
    why_human: "Requires a test client account and an HTTP client to confirm the server-side gate fires."
---

# Phase 03.1: Account Surface + Authorization Verification Report

**Phase Goal:** The authenticated app has a real account surface and authorization model on top of the Phase 3 identity layer — a nav affordance to log out and reach preferences/account settings, an invite function that issues per-invite single-use role-scoped tokens, and an enforced owner/practitioner/client permission model that agrees with tenant/subject scoping.

**Verified:** 2026-06-10T05:15:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

All three scope pillars — account navigation, invite function, role permissions — are observably implemented in source. All 14 must-have truths verified. Automated checks (tsc, vitest 117/117) green. Status is `human_needed` because several behaviors are inherently browser/runtime-dependent (dropdown UX, clipboard, live invite redemption, client gating at runtime).

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | user table carries tenantId; signed-in user is bound to one tenant | VERIFIED | `db/auth-schema.ts:26` — `tenantId: text('tenant_id')` (nullable, per expand-contract). `additionalFields.tenantId` declared in `auth.server.ts:73-78` with `input:false`. |
| 2 | invites table exists with tokenHash (only), role, tenant_id, created_by, expires_at, consumed_at, revoked_at | VERIFIED | `db/schema.ts:81-95` — `pgTable('invites'`, 9 columns including `tokenHash` UNIQUE, no `token`/`rawToken` column. `migrations/0005_fantastic_deadpool.sql` has the CREATE TABLE. |
| 3 | requireUser(request) returns session user or throws redirect to /login | VERIFIED | `app/lib/authz.server.ts:28-35` — calls `auth.api.getSession`; on null throws `redirect('/login?redirect=...')`. |
| 4 | requireRole(user, roles) throws 403 Response when role not in allowed list; fail-closed on missing role | VERIFIED | `app/lib/authz.server.ts:43-52`. 16 unit tests in `tests/auth/authz.test.ts` all passing (117/117 total). Null/undefined role → 403 proven by test. |
| 5 | assertSubjectAccess denies client and denies cross-tenant access | VERIFIED | `app/lib/authz.server.ts:61-76`. Tests cover client-role denial and tenant-mismatch denial (T-031-AZ-2). |
| 6 | authz helpers are RLS-compatible — key on tenantId for Phase 7 SET LOCAL mirror | VERIFIED | Header comment in `authz.server.ts:7-12` documents RLS-compatible design. `requireUser` returns `session.user` which includes `tenantId` (added via `additionalFields`). Phase 7 compatibility is architectural; the design choice is confirmed. |
| 7 | CAPABILITIES matrix + can() helper, fail-closed for unknown roles | VERIFIED | `app/lib/authz.server.ts:87-106`. owner→["invite:practitioner","invite:client","admin:settings"], practitioner→["invite:client","admin:settings"], client→[]. `?? "client"` fallback (T-031-AZ-3). Tested. |
| 8 | Avatar/initials dropdown in TopNav; sign-out is a POST Form to /logout; no-JS required | VERIFIED | `app/components/shell/AccountMenu.tsx:158-173` — `<Form method="post" action="/logout">`. `TopNav.tsx:8,123` imports and renders `<AccountMenu user={user} />`. |
| 9 | Dropdown closes on outside-click and Escape; keyboard navigable; aria-expanded | VERIFIED | `app/components/ui/DropdownMenu.tsx` — mousedown outside-click (line 37), Escape+focus-return (line 45), ArrowDown/Up roving focus (line 58), `aria-expanded={open}` (line 93), `role="menu"` on panel (line 110). No hardcoded hex (confirmed by grep). |
| 10 | Mobile BottomTab exposes Account tab → /settings; Import tab removed | VERIFIED | `app/components/shell/BottomTab.tsx:34-38` — `{ to: "/settings", label: "Account", icon: User, match: ... }`. No Import item present in NAV_ITEMS. |
| 11 | Per-invite single-use, role-scoped, 7-day-expiring tokens; hash-at-rest; tiered policy | VERIFIED | `app/lib/invites.server.ts` — `generateInvite` (policy via `can`, 7-day expiry, only hash persisted), `consumeInviteByToken` (guarded UPDATE WHERE consumedAt IS NULL, T-031-INV-3), `resolveInviteByToken` (CR-01 non-mutating validate). 9 unconditional + 4 DB-skipped tests passing. |
| 12 | beforeSignUp hook (CR-01): resolves per-invite hashed token non-mutably; burns in databaseHooks.user.create.before; assigns role+tenant from invite row; break-glass hardened (CR-02) | VERIFIED | `app/lib/auth.server.ts` — `resolveInviteByToken` called in hook (line 258), `consumeInviteByToken` called in `user.create.before` (line 131). `defineRequestState` replaces module Map. Break-glass requires `OWNER_TENANT_ID` + first-user-only check (lines 214-248). `pendingInviteRoles` Map is gone. |
| 13 | /settings route registered inside authenticated _app layout; 8 sections in correct order; Invites server-gated | VERIFIED | `app/routes.ts:41-42` — settings and invites revoke route registered inside `layout("routes/_app/layout.tsx", [...])`. `settings/index.tsx` — `requireUser` in loader, `requireRole(user, ["owner","practitioner"])` + `can(user, "invite:${role}")` in generate-invite action. canInviteClient/canInvitePractitioner computed server-side (no authz.server import in client component). |
| 14 | Revoke resource route: requireRole gate + revokeInvite scoped by user.tenantId; explicit 403 when tenantId missing; 404 when param missing | VERIFIED | `app/routes/_app/settings/invites.ts:21-53` — `requireRole`, explicit `if (!params.inviteId)` 404, explicit `if (!user.tenantId)` 403, `revokeInvite({ id: params.inviteId, tenantId: user.tenantId })`. `revokeInvite` returns `{ revoked: boolean }` and reports no-op as false (WR-02 fix). |

**Score:** 14/14 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `remix-app/db/schema.ts` | invites table + invitesRelations + tokenHash only | VERIFIED | Line 81: `pgTable('invites'`. tokenHash unique, no raw-token column. invitesRelations at line 278. |
| `remix-app/db/auth-schema.ts` | user.tenantId nullable text | VERIFIED | Line 26: `tenantId: text('tenant_id')` — nullable, no `.notNull()`. |
| `remix-app/app/lib/authz.server.ts` | requireUser, requireRole, assertSubjectAccess, CAPABILITIES, can | VERIFIED | All 5 exported. 107 lines. |
| `remix-app/tests/auth/authz.test.ts` | Unit tests, ≥40 lines, all passing | VERIFIED | 148 lines, 16 tests, all green. |
| `remix-app/scripts/seed-owner.ts` | Sets tenantId on owner user | VERIFIED | Line 125: `.set({ role: "owner", tenantId })`. |
| `remix-app/migrations/0005_fantastic_deadpool.sql` | Adds user.tenant_id (nullable) + CREATE TABLE invites | VERIFIED | File exists. ALTER TABLE adds nullable text. CREATE TABLE invites with token_hash UNIQUE, no raw token column. |
| `remix-app/app/lib/invites.server.ts` | hashToken, newRawToken, generateInvite, listInvites, revokeInvite, consumeInviteByToken, resolveInviteByToken | VERIFIED | All 7 exported. resolveInviteByToken added for CR-01 split. |
| `remix-app/tests/auth/invites-server.test.ts` | Token lifecycle tests, ≥50 lines, passing | VERIFIED | 231 lines, 9 passing + 4 DB-skipped. |
| `remix-app/app/lib/auth.server.ts` | Rewritten beforeSignUp using resolveInviteByToken; databaseHooks with consumeInviteByToken; break-glass hardened | VERIFIED | Line 11: `resolveInviteByToken` import. Line 131: `consumeInviteByToken` in create.before. Lines 214-248: CR-02 hardened break-glass. |
| `remix-app/app/components/ui/DropdownMenu.tsx` | Accessible dropdown (open/close, outside-click, Escape, keyboard) | VERIFIED | 50+ lines. All accessibility contracts present. No hardcoded hex. |
| `remix-app/app/components/shell/AccountMenu.tsx` | Avatar trigger + Settings/Theme/Sign-out Form | VERIFIED | Form POST to /logout (line 158). aria-label "Open account menu" on trigger span. |
| `remix-app/app/components/shell/TopNav.tsx` | Renders AccountMenu + accepts user prop | VERIFIED | `AccountMenu user={user}` at line 123. TopNavProps includes user. |
| `remix-app/app/components/shell/BottomTab.tsx` | Account tab → /settings (Import replaced) | VERIFIED | NAV_ITEMS[4]: `{ to: "/settings", label: "Account", icon: User }`. No Import entry. |
| `remix-app/app/routes.ts` | settings route inside authenticated layout | VERIFIED | Lines 41-42: settings + invites revoke route inside layout block. |
| `remix-app/app/routes/_app/settings/index.tsx` | loader+action+default+meta; ≥120 lines; 8 sections; Invites server-gated | VERIFIED | 983 lines. loader calls requireUser + listInvites tenant-scoped. action dispatches update-profile/change-password/generate-invite. 8 sections in order. Raw token never rendered in table. |
| `remix-app/app/routes/_app/settings/invites.ts` | Action-only resource route; requireRole gate; revokeInvite tenant-scoped | VERIFIED | 57 lines. Exports `action` only, no default export. All gates present. |
| `remix-app/app/routes/auth/login.tsx` | Invite redemption surface (inviteToken → signUpEmail) | VERIFIED | Lines 38-94: signup branch reads inviteToken from formData, calls `auth.api.signUpEmail` with token. beforeSignUp hook fires and assigns role+tenant. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `authz.server.ts` | `auth.server.ts` | `auth.api.getSession` in requireUser | VERIFIED | Line 29: `auth.api.getSession({ headers: request.headers })` |
| `db/schema.ts user.tenantId` | `db/schema.ts tenants.id` | Logical FK (comment doc; no .references() to avoid circular import) | VERIFIED | auth-schema.ts:26 documents the logical FK |
| `auth.server.ts (beforeSignUp)` | `invites.server.ts (resolveInviteByToken)` | Non-mutating validate in sign-up gate (CR-01) | VERIFIED | auth.server.ts:11,258 imports and calls resolveInviteByToken |
| `auth.server.ts (databaseHooks.user.create.before)` | `invites.server.ts (consumeInviteByToken)` | Atomic burn adjacent to user-row write (CR-01) | VERIFIED | auth.server.ts:131 — consumeInviteByToken(pending.rawToken) |
| `invites.server.ts` | `db/schema.ts (invites)` | Drizzle insert/select/update on invites table | VERIFIED | invites.server.ts:22 imports invites from schema |
| `settings/index.tsx (action)` | `invites.server.ts (generateInvite)` | generate-invite intent | VERIFIED | settings/index.tsx:174 calls generateInvite({inviter: user, role}) |
| `settings/index.tsx (loader/action)` | `authz.server.ts (requireUser/requireRole)` | Auth gate at top of loader and action | VERIFIED | loader:36, action:57 call requireUser; action:139 calls requireRole |
| `app/routes.ts` | `settings/index.tsx` | route() inside authenticated layout() | VERIFIED | routes.ts:41 inside layout block |
| `_app/layout.tsx` | `AppShell user=...` | useLoaderData user passed to AppShell | VERIFIED | layout.tsx:36 `<AppShell user={user}>` |
| `AccountMenu.tsx` | `/logout` | Form method post action /logout | VERIFIED | AccountMenu.tsx:158 `<Form method="post" action="/logout">` |
| `TopNav.tsx` | `AccountMenu` | Rendered in desktop right cluster | VERIFIED | TopNav.tsx:123 `<AccountMenu user={user} />` |
| `settings/invites.ts (action)` | `invites.server.ts (revokeInvite)` | Tenant-scoped revoke call | VERIFIED | invites.ts:41 revokeInvite({ id: params.inviteId, tenantId: user.tenantId }) |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `settings/index.tsx` | `invites` (active invites table) | `listInvites(user.tenantId!)` in loader | SELECT from live Neon invites table WHERE tenantId matches | FLOWING |
| `settings/index.tsx` | `user` (Profile/Security) | `requireUser(request)` → `auth.api.getSession` | Live session from Neon via Better-Auth | FLOWING |
| `AccountMenu.tsx` | `user` prop (name/email/role) | `_app/layout.tsx` loader → `session.user` | Live session from Neon via Better-Auth | FLOWING |
| `settings/index.tsx` | `canInviteClient`, `canInvitePractitioner` | Computed from `can(user, ...)` in loader | Derived from session role (live) | FLOWING |

---

### Behavioral Spot-Checks (Static)

| Behavior | Evidence | Status |
|----------|----------|--------|
| tsc --noEmit exits 0 | Confirmed in this verification run — zero errors | PASS |
| vitest run 117 passing / 0 failing | Confirmed: 117 passed, 22 skipped (DB-dependent), 0 failed | PASS |
| npm run build exits 0 | Confirmed: client 2790+ modules, server 72 modules transformed, exit 0 | PASS |
| invites table has no raw-token column | `db/schema.ts:83` tokenHash only; grep found no `token\b` or `rawToken` column definition | PASS |
| requireRole throws 403 for null/undefined role | authz.test.ts lines 39-58 — null and undefined both produce 403 Response | PASS |
| consumeInviteByToken uses isNull(invites.consumedAt) guard | `invites.server.ts:255` — `isNull(invites.consumedAt)` in SELECT; line 284 same guard in UPDATE (race-safe) | PASS |
| auth.server.ts no longer uses module-global pendingInviteRoles Map | `defineRequestState` from `@better-auth/core/context` used at line 47. No `new Map()` in auth.server.ts | PASS |
| break-glass requires OWNER_TENANT_ID + first-user-only | auth.server.ts lines 214-248 — checks ownerTenantId present and DB user count === 0 | PASS |

---

### Probe Execution

Step 7c: SKIPPED — no `scripts/*/tests/probe-*.sh` files exist for this phase; phase does not declare probes.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| AUTH-01 | Plan 03 | User can sign in with email+password; stay signed in | SATISFIED | Phase 3 delivered; Plan 03 extends with account nav affordance (logout, settings link). TopNav logout POST Form. |
| AUTH-02 | Plans 01, 02, 04 | Each user has a role (owner/practitioner/client) that gates access | SATISFIED | CAPABILITIES matrix, requireRole, per-invite role injection via databaseHooks (input:false), invites section gated server-side. |
| TEN-01 | Plan 01 | Every health-data table scoped by tenantId + subjectId | SATISFIED (prior phase) | user.tenantId added; invites table tenant-scoped. Prior phase delivered TEN-01 on health tables. |

AUTH-03, AUTH-04, TEN-02, TEN-03 — explicitly deferred to Phase 7 per REQUIREMENTS.md and 03.1-CONTEXT.md. Not in scope.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `settings/index.tsx` | 650 | Stale planning comment: `{/* ── 3. INVITES (Task 2 fills this) ──────────────────────────────── */}` | Info | Harmless — section is fully implemented. Planning scaffold comment not removed (IN-05 from review). |
| `settings/index.tsx` | 667 | Stale comment: `{/* invite panel — Task 2 */}` | Info | Harmless — panel is implemented. Scaffolding artifact. |
| `settings/index.tsx` | 909 | Stale comment: `// ── InviteTableRowAction (used in Tasks 2/3 ...)` | Info | Harmless — component is implemented and wired. |
| `AppShell.tsx` | 52 | Brand typo: `zoetrope` (with trailing "e") instead of `zoetrop` | Info | Cosmetic/brand inconsistency. Not a blocker. |

No TBD, FIXME, or XXX markers found in any phase-modified file. No stub implementations. No hardcoded hex in DropdownMenu. No `any` usages in security-critical files. All four items above are Info-level leftovers from the code review's IN-04 and IN-05 findings; none block goal achievement.

---

### Human Verification Required

#### 1. Account dropdown UX and logout

**Test:** Run `cd remix-app && npm run dev`. Sign in as owner. Confirm the avatar pill appears top-right in TopNav with a ChevronDown. Click it — dropdown opens showing name, email, OWNER role badge, "Account settings" link, theme toggle, and "Sign out" in danger color. Click outside → closes. Open again → Escape → closes and focus returns to trigger. Click "Sign out" → redirected to /login with cookie cleared.

**Expected:** Dropdown fully interactive; logout works without JS; focus returns to trigger on Escape.

**Why human:** DOM focus management, CSS transitions, and real cookie-clearing logout require a browser runtime.

---

#### 2. Mobile BottomTab Account entry

**Test:** Resize to ≤760px. Confirm BottomTab shows Account (User icon) in the 5th position where Import was. Tap Account → navigates to /settings.

**Expected:** Import tab absent; Account tab present; navigates to /settings.

**Why human:** Responsive CSS breakpoint requires a real viewport.

---

#### 3. Profile name save

**Test:** On /settings, edit Display name → "Save changes" → "Changes saved." appears. Reload → new name persists.

**Expected:** updateUser writes to Neon; session reflects new name on reload.

**Why human:** Requires live Neon session and Better-Auth updateUser.

---

#### 4. Password change validation and enforcement

**Test:** Try new password < 8 chars → error on newPassword field. Mismatched confirm → error on confirmPassword field. Wrong current password → error on currentPassword field. Correct current + valid new → "Password updated." Sign out and back in with new password.

**Expected:** Three distinct inline errors; successful change persists.

**Why human:** Requires live Better-Auth changePassword call and session verification.

---

#### 5. Invite generate, copy, list, and redemption

**Test:** As owner, open /settings INVITES section. Generate a Practitioner invite → link card appears. Copy link → icon transitions to check (1.5s). Invite appears ACTIVE in list. Open link in private window → /login shows "Create your account" form. Complete signup → new account has practitioner role + owner's tenant. Invite transitions to USED.

**Expected:** Full end-to-end invite lifecycle works; role and tenant are encoded from the invite row (not from URL params).

**Why human:** Requires live Neon, clipboard API, and a second browser session to confirm role+tenant assignment.

---

#### 6. Invite revoke inline confirmation and REVOKED persistence

**Test:** Click "Revoke invite" on an ACTIVE row → inline "Revoke this invite? This cannot be undone." → [Keep invite] / [Revoke invite]. Confirm → row stays with REVOKED badge. Attempt the revoked link in a private window → FORBIDDEN error.

**Expected:** Row persists with REVOKED; revoked link cannot be redeemed.

**Why human:** useFetcher state, inline confirmation UI, and live revoke call require browser.

---

#### 7. Client role cannot see or reach Invites surface

**Test:** Sign in as a client user. Load /settings → INVITES section is absent. Send a crafted POST to /settings with `_intent=generate-invite` → 403 response.

**Expected:** UI hides invites for client (cosmetic). Server gate returns 403 on direct POST.

**Why human:** Requires a test client account; 403 verification needs an HTTP client.

---

## Gaps Summary

None. All 14 must-have truths are VERIFIED by source-code evidence. The code review's two BLOCKERs (CR-01 non-atomic consume, CR-02 tenant-less break-glass) were fully remediated per `03.1-REVIEW.md` and are confirmed in the live code:

- CR-01: `resolveInviteByToken` (non-mutating SELECT) in `beforeSignUp`; `consumeInviteByToken` moved into `databaseHooks.user.create.before` adjacent to the user-row write; module-global Map replaced with `defineRequestState` from `@better-auth/core/context`.
- CR-02: Break-glass now requires `OWNER_TENANT_ID` env var and fires only when zero users exist (first-user-only); stashes `role:"owner" + tenantId` so the account is usable.

All 7 warnings (WR-01 through WR-07) confirmed fixed. Remaining items are Info-level cosmetic stubs (stale planning comments, brand typo) that do not affect functionality or security.

The `status: human_needed` reflects that 7 behavioral flows require a browser/live-server to confirm — all automated checks pass cleanly.

---

_Verified: 2026-06-10T05:15:00Z_
_Verifier: Claude (gsd-verifier)_
