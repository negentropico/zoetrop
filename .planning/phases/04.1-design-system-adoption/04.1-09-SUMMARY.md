---
phase: 04.1-design-system-adoption
plan: "09"
subsystem: ui-layout
tags: [design-system, css-grid, responsive, overflow, regression-guard, gap-closure]
gap_closure: true
dependency_graph:
  requires: ["04.1-02", "04.1-03", "04.1-05", "04.1-06", "04.1-07"]
  provides:
    - "remix-app/app/app.css .zt-grid-* helpers hardened (minmax(0,1fr) + min-width:0)"
    - "remix-app/scripts/ds-audit.sh UI-01-n grid-track regression guard"
  affects: ["ROADMAP SC3 (in-brand archetype screens)", "ROADMAP SC5 (responsive nav/layout)"]
tech_stack:
  added: []
  patterns:
    - "CSS Grid blowout prevention: minmax(0, 1fr) tracks + min-width:0 on grid children (replaces bare 1fr == minmax(auto,1fr))"
key_files:
  created:
    - .planning/phases/04.1-design-system-adoption/04.1-09-SUMMARY.md
  modified:
    - remix-app/app/app.css
    - remix-app/app/routes/protocol/index.tsx
    - remix-app/app/routes/protocol/compare.tsx
    - remix-app/app/routes/protocol/cessation.tsx
    - remix-app/app/routes/insights/index.tsx
    - remix-app/app/routes/insights/genetics.tsx
    - remix-app/app/routes/insights/correlations.tsx
    - remix-app/app/routes/metrics/index.tsx
    - remix-app/app/routes/metrics/category.tsx
    - remix-app/app/routes/metrics/detail.tsx
    - remix-app/app/routes/metrics/layout.tsx
    - remix-app/app/routes/import/whoop.tsx
    - remix-app/app/routes/import/vault.tsx
    - remix-app/scripts/ds-audit.sh
decisions:
  - "Grid-blowout fixed at the source helper (minmax(0,1fr) + min-width:0 on children) so every .zt-grid-* consumer is fixed at once, not per-route"
  - "Inline multi-column grids (not using the .zt-grid helper) were also hardened to minmax(0,..) — the helper fix alone does not cover route-local inline grids"
  - "Dense-screen reflow left as in-card horizontal scroll (DataTable overflowX wrapper) / existing card-per-row (correlations) — both satisfy the no-page-overflow criterion; no new reflow pattern introduced"
  - "UI-01-n gate added to ds-audit.sh as a hard CI-style guard against reintroducing bare-1fr multi-column tracks in app.css"
metrics:
  duration: "6m"
  completed: "2026-06-08"
  tasks_completed: 3
  files_modified: 14
---

# Phase 04.1 Plan 09: Grid-Blowout Gap-Closure Summary

**One-liner:** Fixed the CSS-grid blowout regression at its source — `.zt-grid-2/3/4` helpers now use `minmax(0, 1fr)` tracks plus `min-width:0` on grid children — swept all 16 routes hardening every multi-column inline grid (and dense-row tracks) against overflow, gave the `/protocol` 4-up stat row equal-height tiles, and added a `UI-01-n` regression guard to `ds-audit.sh` so bare-`1fr` tracks cannot silently return.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Fix grid-blowout root cause in shared `.zt-grid-*` helpers | `dbdff5f` | remix-app/app/app.css |
| 2 | Audit + fix all 16 routes for overflow / column-placement / equal-height | `dab4395` | 12 route/component files (protocol×3, insights×3, metrics×4, import×2) |
| 3 | Add `UI-01-n` grid-track regression guard to `ds-audit.sh` | `b554e10` | remix-app/scripts/ds-audit.sh |

## What Was Done

### Task 1 — Source-level grid fix (`app/app.css`)
- Every bare `1fr` track in `.zt-grid-4` (`repeat(4, 1fr)`), `.zt-grid-3` (`repeat(3, 1fr)`), and `.zt-grid-2` (`1fr 1fr`) → `minmax(0, 1fr)`, including the `@media (max-width:980px)` and `@media (max-width:760px)` overrides (single-column collapses normalized to `minmax(0,1fr)` for consistency).
- Added `.zt-grid-2 > *, .zt-grid-3 > *, .zt-grid-4 > * { min-width: 0; }` so flex/grid descendants with `white-space: nowrap` / ellipsis can actually shrink instead of forcing a track wider than its `1fr` share.
- Root cause: in CSS Grid a bare `1fr` is `minmax(auto, 1fr)` whose `auto` floor is the column min-content; a non-shrinkable child grows the track past its share, the grid becomes wider than its container, and later columns (e.g. `/protocol`'s "Supplements by tier" right column) get clipped off-screen. No gap or breakpoint values were changed.

### Task 2 — Route sweep + equal-height stats
- **`protocol/index.tsx` (4-up stat row equal-height):** the `ProtoStat` `Card` now sets `height: 100%`, and its `<Link>` wrapper is `display: block; height: 100%`, so the grid cell's default `stretch` actually reaches the card — the 2-line "Cessation Attempt 2" tile now aligns with the row baseline instead of hanging below.
- **`protocol/compare.tsx`:** the inline `repeat(3, 1fr)` change-summary grid was replaced with the `.zt-grid-3` helper (now hardened + responsive).
- **Hardened every multi-column inline grid** (bare `1fr` → `minmax(0, 1fr)`), since the helper fix does not reach route-local inline grids: `protocol/cessation` (`1fr 1.1fr`), `insights/index` (`1fr 1fr`), `insights/genetics` (`1fr 1fr`), `insights/correlations` (interpretation guide `1fr 1fr`), `metrics/detail` (`1.3fr 1fr`), `metrics/layout` (sidebar `232px 1fr` content column), `import/whoop` (`1.4fr 1fr` + faux-skeleton `0.9fr 1.1fr`), `import/vault` (`1.4fr 1fr` + categories `1fr 1fr`).
- **Dense metric rows** (`metrics/index`, `metrics/category`): the flexible-track floors `minmax(120px, …)` (which summed past a 360px mobile viewport together with the fixed value columns) were dropped to `minmax(0, …)` so columns can shrink; the `metrics/category` row's name cell got `min-width:0` + ellipsis (`overflow:hidden; text-overflow:ellipsis; white-space:nowrap`) on both the name and timestamp lines so long content truncates instead of overflowing.
- **No data or loader changes** — all edits are markup/class/style only.
- Dense `DataTable` screens (`genetics`, `correlations`, `metrics/detail`) already scroll within their card via the `DataTable` `overflowX:auto` wrapper (or, for correlations, an existing `.corr-mobile` card-per-row at ≤760px) — both satisfy the "no page-level horizontal scroll" criterion, so no new reflow pattern was added.

### Task 3 — Regression guard (`scripts/ds-audit.sh`)
- Added gate `UI-01-n` that scans `app/app.css` `grid-template-columns` declarations and FAILS on any multi-column bare-`1fr` track (`repeat(N, 1fr)` or `1fr 1fr …`) that does not use `minmax(0`; single-column `1fr;` and `minmax(0,1fr)` forms PASS.
- Follows the existing `UI-01-b`/`UI-01-i` echo + exit-code convention and contributes to the overall script exit status. Verified bidirectionally: PASS (exit 0) on the fixed CSS, FAIL (exit 1, violation listed) on a temporarily reintroduced `repeat(4, 1fr)` track (then fully reverted — `app.css` is byte-identical to the Task-1 commit).

## Verification

Automated (executor) — full combined gate, run from `remix-app/`:

```
npm run typecheck   # => PASS (0 errors)
npm test            # => 75 passed (7 files)
bash scripts/ds-audit.sh   # => ALL GATES PASSED (UI-01-b, UI-01-i, UI-01-n)
npm run build       # => ✓ built (client + ssr)
```

Pattern checks for Task 1:
```
grep -Eq "minmax\(0" app/app.css                               # present
grep -Eq "\.zt-grid-(2|3|4) > \*|min-width: 0" app/app.css     # present
! grep -nE "grid-template-columns:\s*(repeat\([0-9]+,\s*1fr\)|1fr 1fr|1fr;)" app/app.css   # no bare multi-col 1fr
```

### Pending — orchestrator browser visual pass (REQUIRED, NOT done by this executor)

Per the plan's `<verification>` "Manual / orchestrator" section and this execution's scope boundary, the live-browser visual UAT is the orchestrator's responsibility and has **not** been run here (no dev server was started, no browser automation attempted). The orchestrator must still:

- Start `cd remix-app && npm run dev`; for EACH of the 16 routes, at 1280px AND 390px, in BOTH light and dark, assert `document.documentElement.scrollWidth <= window.innerWidth + 1` (no page-level horizontal scrollbar).
- Confirm `/protocol` (Overview): the "Supplements by tier" right column is fully visible (not clipped) and the 4-up stat tiles are equal-height.
- Spot-check dense screens (`/insights/correlations`, `/insights/genetics`, `/protocol/compare`, `/metrics/<cat>/<id>`) reflow cleanly at 390px (card-per-row or in-card scroll, no page overflow).
- Record results in `04.1-HUMAN-UAT.md` (or a gap-verification note).

Until that visual pass is recorded, the `must_haves.truths` "no route produces horizontal overflow … on every one of the 16 routes" is asserted structurally (source-level) but not yet confirmed at runtime.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 — Missing critical coverage] Hardened route-local inline grids, not just the `.zt-grid` helpers**
- **Found during:** Task 2 sweep.
- **Issue:** The plan's root-cause fix targets the `.zt-grid-*` helpers, but the audit surfaced numerous *inline* multi-column grids (`gridTemplateColumns: "1fr 1fr"`, `"1.3fr 1fr"`, `"1.4fr 1fr"`, etc., and `protocol/compare`'s `repeat(3, 1fr)`) that bypass the helper entirely and carry the identical bare-`1fr` blowout risk. The helper fix alone would leave these overflow-prone.
- **Fix:** Converted each inline multi-column `1fr` track to `minmax(0, 1fr)`; replaced `protocol/compare`'s inline `repeat(3,1fr)` with the now-hardened `.zt-grid-3` helper.
- **Files modified:** the 11 route files listed under Task 2.
- **Commit:** `dab4395`.

**2. [Rule 2 — Missing critical coverage] Dense metric-row truncation safety**
- **Found during:** Task 2 sweep of `metrics/category.tsx`.
- **Issue:** The 5-column metric row (`18px minmax(120px,1.4fr) 80px minmax(120px,1.6fr) 140px`) summed to ~542px minimum — wider than a 360px mobile viewport — with no responsive collapse, and its name cell had no ellipsis, so long metric names would overflow.
- **Fix:** Flexible floors `minmax(120px,…)` → `minmax(0,…)`; added `min-width:0` + ellipsis to the name/timestamp cell so it truncates within the shrunk column.
- **Files modified:** `metrics/category.tsx`, `metrics/index.tsx`.
- **Commit:** `dab4395`.

These are presentation-only correctness fixes (Rule 2) directly in scope of the Task 2 "sweep every route for overflow-prone patterns and fix any found" action — no user decision required.

## Known Stubs

None. This plan made no data-flow or loader changes; all edits are CSS/markup/style and a shell audit gate.

## Threat Flags

None. CSS/markup-only layout fixes + a shell audit gate (matches the plan's threat register T-04.1-16 "accept" — presentation-only, no trust boundary, no executable surface, no PII).

## Self-Check: PASSED

- [x] `remix-app/app/app.css` modified and committed (`dbdff5f`) — `minmax(0` + `min-width: 0` present, no bare multi-column `1fr`
- [x] 12 route/component files modified and committed (`dab4395`)
- [x] `remix-app/scripts/ds-audit.sh` modified and committed (`b554e10`) — `UI-01-n` gate present
- [x] Commits `dbdff5f`, `dab4395`, `b554e10` all exist in git log
- [x] Full combined gate green: typecheck 0 errors · 75 tests pass · ds-audit ALL GATES PASSED · build clean
- [x] `ds-audit.sh UI-01-n` verified to FAIL on a reintroduced bare-`1fr` track and PASS on the fixed CSS (then fully reverted)
- [x] Orchestrator browser visual pass documented as pending (NOT executed by this executor, per scope boundary)
