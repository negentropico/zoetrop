---
phase: 04.1-design-system-adoption
plan: 04
type: execute
wave: 3
depends_on: ["04.1-02"]
files_modified:
  - remix-app/app/components/ui/Button.tsx
  - remix-app/app/components/ui/IconButton.tsx
  - remix-app/app/components/ui/Badge.tsx
  - remix-app/app/components/ui/Avatar.tsx
  - remix-app/app/components/ui/Card.tsx
  - remix-app/app/components/ui/Input.tsx
  - remix-app/app/components/ui/Switch.tsx
  - remix-app/app/components/ui/SegmentedControl.tsx
  - remix-app/app/components/ui/ProgressBar.tsx
  - remix-app/app/components/ui/Stat.tsx
  - remix-app/app/components/ui/DataTable.tsx
  - remix-app/app/components/ui/Dropzone.tsx
  - remix-app/app/components/ui/PageHeader.tsx
  - remix-app/app/components/ui/Crumb.tsx
autonomous: true

requirements: [UI-01]
user_setup: []

must_haves:
  truths:
    - "Card renders a brand frame surface with elevation/accent/tone/padding/interactive variants"
    - "Button renders pill-shaped variants (primary/secondary/ghost/ink/danger) with periwinkle accent as the primary fill"
    - "DataTable renders a mono-header hairline table with tabular numeric columns"
    - "Dropzone is client-only: hands the selected File to onFile with no server-side processing"
  artifacts:
    - path: "remix-app/app/components/ui/Card.tsx"
      provides: "Frame surface primitive (the card shape used app-wide)"
      exports: ["Card"]
    - path: "remix-app/app/components/ui/Button.tsx"
      provides: "Pill action button with periwinkle primary (accent reserved)"
      exports: ["Button"]
    - path: "remix-app/app/components/ui/DataTable.tsx"
      provides: "Generic mono-header data table"
      exports: ["DataTable"]
    - path: "remix-app/app/components/ui/Dropzone.tsx"
      provides: "Client-only drag/drop file zone (Phase 5 ancestor)"
      exports: ["Dropzone"]
    - path: "remix-app/app/components/ui/PageHeader.tsx"
      provides: "Eyebrow + title + sub + right-slot page header"
      exports: ["PageHeader"]
  key_links:
    - from: "remix-app/app/components/ui/Card.tsx"
      to: "app.css brand utilities"
      via: "bg-surface rounded-xl border-border + shadow-* elevation map"
      pattern: "bg-surface|rounded-xl"
    - from: "remix-app/app/components/ui/Dropzone.tsx"
      to: "caller"
      via: "onFile(file) hands File to caller; no upload/storage in this phase"
      pattern: "onFile"
    - from: "remix-app/app/components/ui/Crumb.tsx"
      to: "react-router"
      via: "Link for breadcrumb navigation"
      pattern: "react-router|Link"
---

<objective>
Port the core/form/layout primitives and the new structural components to typed Tailwind TSX: Button, IconButton, Badge, Avatar, Card, Input, Switch, SegmentedControl, ProgressBar, Stat (DS primitives with `.d.ts` signatures), plus DataTable, Dropzone, PageHeader, Crumb (round1 source, no `.d.ts`). All colors reference brand tokens; all navigation uses react-router; Dropzone is strictly client-only (no server processing — that's Phase 5).

Purpose: Completes the typed signature component library (ROADMAP SC2: typed TSX, tsc 0 errors, no .jsx shipped) and supplies the surface/action/table/upload/header primitives the Wave 4 screen retrofits compose with. Disjoint file set from Plan 03 — runs in parallel.
Output: Fourteen `components/ui/` files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md
@.planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md

<interfaces>
DS primitives with type signatures (port JSX → typed TSX; signatures already exist):
- docs/design-system/components/core/Button.d.ts (+ Button.jsx) — variants primary/secondary/ghost/ink/danger, sizes sm/md/lg, fullWidth, iconLeft/iconRight
- docs/design-system/components/core/IconButton.d.ts — ghost/secondary, 40px default, ≥44px touch
- docs/design-system/components/core/Badge.d.ts — tone energy/vital/focus/neutral/success/danger, variant soft/solid/outline
- docs/design-system/components/core/Avatar.d.ts
- docs/design-system/components/core/Card.d.ts — RESEARCH § Card.tsx pattern has the COMPLETE implementation to copy verbatim
- docs/design-system/components/forms/Input.d.ts, Switch.d.ts, SegmentedControl.d.ts
- docs/design-system/components/data/ProgressBar.d.ts (+ ProgressBar.jsx), Stat.d.ts

Round1 source (no .d.ts — write interface from props):
- docs/design-system/_rounds/round1/app/lib.jsx: DataTable (302-335), PageHeader (86-97), Crumb (441-452)
- docs/design-system/_rounds/round1/app/screen-import.jsx: Dropzone (16-40)

Accent reservation (UI-SPEC § Color 60/30/10): periwinkle var(--accent)/var(--focus-300) is ONLY for primary CTA fill, active BottomTab, wordmark period, Dropzone drag-active, focus/target accents, action links, focus ring. Button variant="primary" uses accent fill; other variants do NOT.

Card complete implementation: RESEARCH.md § Card.tsx pattern (ELEVATION/PADDING/TONE_BG/ACCENT_COLOR maps) — copy as-is.
DataTable: generic <T>, Column{key,label,align,mono,render,wrap,width}, mono header (.zt-eyebrow), hairline rows (border-border), Space Mono for mono columns.
Dropzone: props accept/onFile/label; idle dashed border-strong + surface-2 + Upload icon; drag-active dashed accent + focus-50 + FileJson icon; click-to-browse; onFile(File) only — NO fetch/upload/storage (RESEARCH § Security Domain).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port core primitives — Button, IconButton, Badge, Avatar, Card</name>
  <read_first>
    - docs/design-system/components/core/Button.d.ts, IconButton.d.ts, Badge.d.ts, Avatar.d.ts, Card.d.ts
    - docs/design-system/_rounds/round1/app/lib.jsx (Button/Badge usages for realized styling)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ Card.tsx — complete impl)
    - .planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md (§ Card.tsx typed TSX pattern)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Color accent reservation, § Component Inventory)
  </read_first>
  <files>remix-app/app/components/ui/Button.tsx, remix-app/app/components/ui/IconButton.tsx, remix-app/app/components/ui/Badge.tsx, remix-app/app/components/ui/Avatar.tsx, remix-app/app/components/ui/Card.tsx</files>
  <action>
    Port the five core primitives to typed TSX. `Card.tsx`: copy the complete implementation from PATTERNS/RESEARCH § Card.tsx (ELEVATION/PADDING/TONE_BG/ACCENT_COLOR maps, bg-surface rounded-xl border-border, interactive lift). `Button.tsx`: from Button.d.ts — variants primary/secondary/ghost/ink/danger, sizes sm/md/lg, fullWidth, iconLeft/iconRight (ReactNode slots for lucide icons); rounded-pill; variant="primary" uses periwinkle accent fill (var(--accent)/bg-accent) per the reservation; press scale(0.97). `IconButton.tsx`: square/round icon-only, 40px default, ≥44px touch target, ghost/secondary; press scale(0.92). `Badge.tsx`: mono pill, tone energy/vital/focus/neutral/success/danger × variant soft/solid/outline using var(--token) families. `Avatar.tsx`: circle per Avatar.d.ts. All colors via brand utilities/var(--token) — no hardcoded hex, no dark: prefixes.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && test -z "$(grep -lE '#[0-9a-fA-F]{6}' app/components/ui/Button.tsx app/components/ui/Card.tsx app/components/ui/Badge.tsx app/components/ui/IconButton.tsx app/components/ui/Avatar.tsx 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0.
    - Button.tsx exports `Button` with a `variant` prop including `"primary"`; the primary path references `var(--accent)` or `bg-accent` (accent reservation).
    - Card.tsx exports `Card` and references `bg-surface` + `rounded-xl` + `border-border`.
    - No hardcoded 6-digit hex color literals in any of the five files.
  </acceptance_criteria>
  <done>Button/IconButton/Badge/Avatar/Card ported, typed, token-driven; primary button uses reserved periwinkle accent.</done>
</task>

<task type="auto">
  <name>Task 2: Port form primitives — Input, Switch, SegmentedControl, ProgressBar, Stat</name>
  <read_first>
    - docs/design-system/components/forms/Input.d.ts, Switch.d.ts, SegmentedControl.d.ts
    - docs/design-system/components/data/ProgressBar.d.ts (+ ProgressBar.jsx), Stat.d.ts
    - docs/design-system/_rounds/round1/app/lib.jsx (SegmentedControl/ProgressBar/Stat realized usages)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Component Inventory, § Typography for Stat readout, § Motion for focus ring)
  </read_first>
  <files>remix-app/app/components/ui/Input.tsx, remix-app/app/components/ui/Switch.tsx, remix-app/app/components/ui/SegmentedControl.tsx, remix-app/app/components/ui/ProgressBar.tsx, remix-app/app/components/ui/Stat.tsx</files>
  <action>
    Port the five form/data primitives. `Input.tsx`: brand text input with the warm focus ring (var(--shadow-ring) via focus-visible). `Switch.tsx`: toggle control. `SegmentedControl.tsx`: pill tab switch for views (e.g. Day/Week/Month) — controlled value + onChange, Ink-active segment. `ProgressBar.tsx`: horizontal fill bar with `tone` (energy/vital/focus → var(--token) fill) and height; solid/segmented (NOT gradient — UI-SPEC drift note). `Stat.tsx`: eyebrow label (.zt-eyebrow) + large display value (.zt-readout, tabular-nums) + unit + optional trend chip. All var(--token) colors; rounded per the radii tokens; no hardcoded hex, no dark: prefixes.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && test -z "$(grep -lE '#[0-9a-fA-F]{6}' app/components/ui/Input.tsx app/components/ui/Switch.tsx app/components/ui/SegmentedControl.tsx app/components/ui/ProgressBar.tsx app/components/ui/Stat.tsx 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0.
    - ProgressBar.tsx exports `ProgressBar` with a `tone` prop and uses a solid var(--token) fill (no `gradient` / `bg-gradient` class).
    - Stat.tsx exports `Stat` and applies `.zt-readout` (or var(--font-display) + tabular-nums) to the value.
    - SegmentedControl.tsx exports `SegmentedControl` with controlled value + onChange.
    - No hardcoded 6-digit hex color literals in any of the five files.
  </acceptance_criteria>
  <done>Input/Switch/SegmentedControl/ProgressBar/Stat ported, typed, token-driven; progress bar is solid (not gradient).</done>
</task>

<task type="auto">
  <name>Task 3: Port structural components — DataTable, Dropzone (client-only), PageHeader, Crumb</name>
  <read_first>
    - docs/design-system/_rounds/round1/app/lib.jsx (DataTable 302-335, PageHeader 86-97, Crumb 441-452)
    - docs/design-system/_rounds/round1/app/screen-import.jsx (Dropzone 16-40)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ DataTable, § Dropzone, § PageHeader, § Crumb analogs)
    - .planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md (§ Security Domain — Dropzone is UI-only in 04.1)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Dense table reflow, § Component Inventory)
  </read_first>
  <files>remix-app/app/components/ui/DataTable.tsx, remix-app/app/components/ui/Dropzone.tsx, remix-app/app/components/ui/PageHeader.tsx, remix-app/app/components/ui/Crumb.tsx</files>
  <action>
    Port the four structural components. `DataTable.tsx`: generic `<T extends Record<string, unknown>>` with `Column` interface (key/label/align/mono/render/wrap/width), `rows`, `rowKey`; mono `.zt-eyebrow` header, `border-border` hairline rows (`.zt-trow` hover), Space Mono + tabular-nums for `mono` columns; horizontally scrollable wrapper. `Dropzone.tsx`: from screen-import.jsx — props accept (default ".json,application/json"), onFile, label; drag state toggles dashed border-strong+surface-2 (idle) ↔ dashed accent+focus-50 (drag) with Upload↔FileJson lucide icons; click opens the hidden file input; on file selection/drop call `onFile(file)` ONLY — implement NO fetch, upload, or storage (Phase 5 scope; T-04.1-06). `PageHeader.tsx`: eyebrow + h1 title + sub paragraph + optional right slot. `Crumb.tsx`: breadcrumb in mono uppercase using react-router `Link` for linked items (import React for Fragment or use a keyed array). All var(--token) colors; no hardcoded hex; no dark: prefixes.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && bash scripts/ds-audit.sh && test -z "$(grep -nE 'fetch\(|upload|FormData|XMLHttpRequest' app/components/ui/Dropzone.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0.
    - Dropzone.tsx exports `Dropzone`, calls `onFile` with the selected File, and contains NO `fetch(`, `FormData`, `XMLHttpRequest`, or upload/storage call (T-04.1-06).
    - DataTable.tsx exports a generic `DataTable` with a `columns` + `rows` API and uses `.zt-eyebrow` for the header.
    - Crumb.tsx imports `Link` from react-router; PageHeader.tsx exports `PageHeader`.
    - `cd remix-app && bash scripts/ds-audit.sh` exits 0 (no .jsx + no banned hexes across app/components — UI-01-b, UI-01-i).
  </acceptance_criteria>
  <done>DataTable/PageHeader/Crumb ported; Dropzone is strictly client-only (no server processing).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client file picker → Dropzone | A user-selected File enters the component, but is handed to the caller only — no processing/upload in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-06 | Tampering / Elevation (premature file handling) | Dropzone.tsx | mitigate | Dropzone is UI-only in 04.1 — it implements no server upload, no fetch, no storage; it only calls onFile(File). Server-side validation/parsing is Phase 5 (LAB-01..03) with its own threat model |
| T-04.1-07 | Information disclosure | DataTable rendering loader data | accept | Renders existing static loader data already shipped in M0; no new data exposure, no untrusted input |
</threat_model>

<verification>
- `cd remix-app && npm run typecheck` exits 0.
- `cd remix-app && bash scripts/ds-audit.sh` exits 0 (no .jsx shipped, no banned hexes — UI-01-b, UI-01-i).
- `cd remix-app && npm run build` exits 0 (no dead imports).
</verification>

<success_criteria>
- SC2 (completes alongside Plan 03): the full signature component library is typed TSX under app/components/ui/, tsc passes with 0 errors, no .jsx / _ds_bundle.js shipped.
- Primary Button uses the reserved periwinkle accent; progress bars are solid (not gradient); Dropzone is client-only.
</success_criteria>

<output>
Create `.planning/phases/04.1-design-system-adoption/04.1-04-SUMMARY.md` when done.
</output>
