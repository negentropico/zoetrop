---
phase: 04.1-design-system-adoption
plan: 03
type: execute
wave: 3
depends_on: ["04.1-02"]
files_modified:
  - remix-app/app/types/metrics.ts
  - remix-app/app/components/ui/StatusBadge.tsx
  - remix-app/app/components/ui/StatusBadge.test.tsx
  - remix-app/app/components/ui/StatusDot.tsx
  - remix-app/app/components/ui/CatChip.tsx
  - remix-app/app/components/ui/CatChip.test.tsx
  - remix-app/app/components/ui/MetricRing.tsx
  - remix-app/app/components/ui/MetricRing.test.tsx
  - remix-app/app/components/ui/RangeBar.tsx
  - remix-app/app/components/ui/PhaseBar.tsx
  - remix-app/app/components/ui/Sparkline.tsx
  - remix-app/app/components/ui/TrendChart.tsx
autonomous: true

requirements: [UI-01]
user_setup: []

must_haves:
  truths:
    - "StatusBadge renders the correct color, glyph, and mono label for all four statuses (optimal/borderline/deficient/excess)"
    - "CatChip renders a neutral Ink chip for null-family categories and a family tint for vital/energy, inverting on active"
    - "MetricRing renders an SVG progress ring using the correct tone CSS variable"
    - "The 9 metric categories carry Lucide icon names (not emoji) and the correct family field"
    - "TrendChart renders with brand token colors (no hardcoded clinical hexes) while preserving the actual/target/optimal/reference chart semantics"
  artifacts:
    - path: "remix-app/app/components/ui/StatusBadge.tsx"
      provides: "4-status pill with glyph + mono label (Q2 status system, SC4)"
      exports: ["StatusBadge"]
    - path: "remix-app/app/components/ui/CatChip.tsx"
      provides: "Category icon chip, neutral or family-tinted, active inverts (Q1 category system, SC4)"
      exports: ["CatChip"]
    - path: "remix-app/app/components/ui/MetricRing.tsx"
      provides: "Signature animated tone ring"
      exports: ["MetricRing"]
    - path: "remix-app/app/components/ui/TrendChart.tsx"
      provides: "Brand-tokened Recharts chart (chart language for Phase 6)"
      exports: ["TrendChart"]
    - path: "remix-app/app/types/metrics.ts"
      provides: "CATEGORY_INFO with Lucide icon names + family fields"
      contains: "family"
  key_links:
    - from: "remix-app/app/components/ui/StatusBadge.tsx"
      to: "app.css status tokens"
      via: "var(--success)/var(--warning)/var(--danger)/var(--excess) + var(--*-bg)"
      pattern: "var\\(--excess\\)"
    - from: "remix-app/app/components/ui/CatChip.tsx"
      to: "lucide-react"
      via: "renders an injected LucideIcon component"
      pattern: "LucideIcon|Icon"
    - from: "remix-app/app/components/ui/TrendChart.tsx"
      to: "recharts"
      via: "ComposedChart/Line/ReferenceArea with var(--token) color strings"
      pattern: "var\\(--ink\\)|var\\(--vital"
---

<objective>
Port the data + status + category visualization components to typed Tailwind TSX and resolve the category/status systems in code. Build StatusBadge, StatusDot, CatChip, MetricRing, RangeBar, PhaseBar, Sparkline; restyle the existing `TrendChart.tsx` to brand tokens (preserving all data/projection logic); and migrate `types/metrics.ts` `CATEGORY_INFO` from emoji to Lucide icon names + family fields. Create the three component unit tests this plan owns (StatusBadge, CatChip, MetricRing).

Purpose: Implements ROADMAP SC4 (9-category icon system + 4-status palette resolved in code, D-04/D-05/D-06), part of SC2 (typed signature components, tsc 0 errors, no .jsx shipped), and supplies the visualization primitives the Wave 4 screens consume. TrendChart is the brand chart language Phases 5–6 build on.
Output: Eight `components/ui/` files + updated `types/metrics.ts` + three passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md
@.planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md
@.planning/phases/04.1-design-system-adoption/04.1-VALIDATION.md

<interfaces>
Source-of-truth ports (translate JSX → typed TSX; colors as var(--token) strings, NEVER hardcoded hex):
- docs/design-system/_rounds/round1/app/lib.jsx: StatusBadge (112-123), StatusDot (108-111), CatChip (138-152), RangeBar (170-194), PhaseBar (207-239), Sparkline (155-167)
- docs/design-system/components/data/MetricRing.jsx (full) + MetricRing.d.ts (type signature)
- remix-app/app/components/TrendChart.tsx (existing — restyle in place; preserve data/projection logic) + lib.jsx TrendChart (242-299) brand color refs
- docs/design-system/_rounds/round1/app/data.js: category family field (autonomic:'vital', bodyComposition:'energy', all others:null)

Status config (from UI-SPEC § Status System + RESEARCH excerpt):
- optimal: var(--success) on var(--vital-50), glyph ✓, label OPTIMAL
- borderline: var(--warning) on var(--energy-50), glyph ~, label BORDERLINE
- deficient: var(--danger) on var(--danger-bg), glyph ↓, label DEFICIENT
- excess: var(--excess) on var(--excess-bg), glyph ↑, label EXCESS

CatChip rule (UI-SPEC § Category System):
- bg = active ? var(--ink) : (family ? var(--{family}-50) : var(--surface-2))
- color = active ? var(--n-50) : (family ? var(--{family}) : var(--ink))
- icon size = round(chipSize * 0.5)

CATEGORY_INFO icon → Lucide mapping (UI-SPEC § Category table): vitamins pill | minerals gem | inflammatory flame | metabolic zap | hormones flask-conical | autonomic heart-pulse (family vital) | bodyComposition dumbbell (family energy) | lipids droplet | hematology dna. Remove the per-category color field.

TrendChart hardcoded hex → token swaps (PATTERNS § TrendChart): #22c55e→var(--vital-50); #16a34a→var(--vital-400); #eab308→var(--n-200) dashed ref; #6b7280→var(--text-muted); #e5e7eb→var(--border); #3b82f6→var(--ink) (actual line, sw 2.25); #a855f7→var(--energy-400) dashed (projection); sparkline status color→var(--ink) opacity 0.7.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port StatusBadge + StatusDot + CatChip + resolve category/status systems in metrics.ts (with StatusBadge + CatChip tests)</name>
  <behavior>
    - StatusBadge("optimal") renders glyph "✓" and text "OPTIMAL" with color var(--success).
    - StatusBadge renders distinct glyph (✓ ~ ↓ ↑) and label for each of the 4 statuses.
    - CatChip with family=null renders neutral (no family-50 bg); with family="vital" renders the vital tint; active=true inverts to Ink bg.
    - CATEGORY_INFO.autonomic.family === "vital" and .bodyComposition.family === "energy"; all others family === null.
  </behavior>
  <read_first>
    - docs/design-system/_rounds/round1/app/lib.jsx (StatusBadge 112-123, StatusDot 108-111, CatChip 138-152)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ StatusBadge, § StatusDot, § CatChip, § types/metrics.ts analogs)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Status System, § Category System)
    - remix-app/app/types/metrics.ts (current CATEGORY_INFO to edit)
    - .planning/phases/04.1-design-system-adoption/04.1-VALIDATION.md (UI-01-e, UI-01-f)
  </read_first>
  <files>remix-app/app/components/ui/StatusBadge.tsx, remix-app/app/components/ui/StatusBadge.test.tsx, remix-app/app/components/ui/StatusDot.tsx, remix-app/app/components/ui/CatChip.tsx, remix-app/app/components/ui/CatChip.test.tsx, remix-app/app/types/metrics.ts</files>
  <action>
    Create `ui/StatusBadge.tsx` from lib.jsx StatusBadge + the STATUS_CONFIG map (D-06: includes excess=var(--excess)/var(--excess-bg)); pill is font-mono text-2xs uppercase rounded-pill, glyph span is aria-hidden. Create `ui/StatusDot.tsx` (DOT_COLOR map, size prop default 9, var(--token) backgrounds). Create `ui/CatChip.tsx` accepting `icon: LucideIcon` plus `family`/`active`/`size`, applying the active/family/neutral rule from UI-SPEC; icon rendered with `color="currentColor"` and size round(size*0.5). Update `types/metrics.ts` `CATEGORY_INFO`: replace each emoji `icon` with the Lucide icon NAME string per the mapping (D-05), add/set the `family` field (autonomic 'vital', bodyComposition 'energy', else null — D-04), and remove the per-category Tailwind `color` field (status-driven now). If `CATEGORY_INFO` is consumed elsewhere with the old `color` field, update the type and call sites so typecheck stays green. Write `StatusBadge.test.tsx` (jsdom pragma) asserting each status's glyph + label; write `CatChip.test.tsx` (jsdom pragma) asserting neutral vs family-tint vs active rendering using a stub Lucide icon.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && npm test -- run app/components/ui/StatusBadge.test.tsx app/components/ui/CatChip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0.
    - `StatusBadge.test.tsx` and `CatChip.test.tsx` pass (UI-01-e, UI-01-f).
    - `types/metrics.ts` CATEGORY_INFO contains `'pill'`, `'heart-pulse'`, `'dna'` (Lucide names) and a `family` field; contains no emoji icon strings and no per-category `text-*-500` color class.
    - `grep -nE "#22c55e|#3b82f6|#a855f7|#eab308|#ef4444|#f97316" remix-app/app/components/ui/StatusBadge.tsx remix-app/app/components/ui/StatusDot.tsx remix-app/app/components/ui/CatChip.tsx` returns empty.
  </acceptance_criteria>
  <done>Status + category systems resolved in code (SC4); StatusBadge/StatusDot/CatChip ported and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port MetricRing (with test) + RangeBar + PhaseBar + Sparkline</name>
  <behavior>
    - MetricRing(tone="vital") renders an <svg> whose progress <circle> stroke is var(--vital).
    - MetricRing(tone="energy") uses var(--energy); default/unknown tone falls back to var(--focus).
    - RangeBar positions the value tick within [min,max] and tints by status.
    - PhaseBar renders one segment per phase; current phase carries the Ink border treatment.
  </behavior>
  <read_first>
    - docs/design-system/components/data/MetricRing.jsx + MetricRing.d.ts
    - docs/design-system/_rounds/round1/app/lib.jsx (RangeBar 170-194, PhaseBar 207-239, Sparkline 155-167)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ MetricRing, § RangeBar, § PhaseBar, § Sparkline analogs)
    - .planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md (§ SVG components — CSS var strings; § MetricRing animation)
    - .planning/phases/04.1-design-system-adoption/04.1-VALIDATION.md (UI-01-h)
  </read_first>
  <files>remix-app/app/components/ui/MetricRing.tsx, remix-app/app/components/ui/MetricRing.test.tsx, remix-app/app/components/ui/RangeBar.tsx, remix-app/app/components/ui/PhaseBar.tsx, remix-app/app/components/ui/Sparkline.tsx</files>
  <action>
    Port `ui/MetricRing.tsx` from MetricRing.jsx (typed props per the PATTERNS interface; TONE_COLOR map → var(--energy)/var(--vital)/var(--focus); SVG stroke uses the CSS var string, NOT a Tailwind class, so dark cascade works; keep the requestAnimationFrame sweep + stroke-dashoffset transition). Port `ui/RangeBar.tsx` (MetricWithRange interface, frac() positioning, reference/optimal bands + status-colored value tick, var(--token) backgrounds). Port `ui/PhaseBar.tsx` (Phase interface with state completed/current/upcoming; completed→var(--vital), current→var(--focus-50)+2px var(--ink) border, upcoming→var(--n-150); optional label row; height/showLabels/compact props). Port `ui/Sparkline.tsx` (data:number[], default color var(--ink), opacity 0.7). Write `MetricRing.test.tsx` (jsdom pragma) asserting the rendered progress circle's stroke equals var(--vital) for tone="vital".
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && npm test -- run app/components/ui/MetricRing.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0.
    - `MetricRing.test.tsx` passes: tone="vital" yields a circle with stroke `var(--vital)` (UI-01-h).
    - `grep -nE "#22c55e|#3b82f6|#a855f7|#eab308|#ef4444|#f97316" remix-app/app/components/ui/MetricRing.tsx remix-app/app/components/ui/RangeBar.tsx remix-app/app/components/ui/PhaseBar.tsx remix-app/app/components/ui/Sparkline.tsx` returns empty.
    - RangeBar.tsx exports a `RangeBar` accepting an `m` prop with status; PhaseBar.tsx exports `PhaseBar` accepting a `phases` array.
  </acceptance_criteria>
  <done>MetricRing/RangeBar/PhaseBar/Sparkline ported, token-driven; MetricRing tone verified.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle existing TrendChart.tsx to brand tokens (preserve chart semantics)</name>
  <read_first>
    - remix-app/app/components/TrendChart.tsx (existing — 325 lines; preserve all data + projection logic and TrendChartProps; this file will be RELOCATED to app/components/ui/TrendChart.tsx)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ TrendChart analog — exact hex→token swap list; § Brand-Tokened Recharts Props)
    - .planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md (§ Recharts theming via CSS variable strings; § TrendChart zt-draw; Pitfall 7 ResponsiveContainer)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Component Inventory — TrendChart responsibility)
  </read_first>
  <files>remix-app/app/components/ui/TrendChart.tsx</files>
  <action>
    Relocate the existing `app/components/TrendChart.tsx` to `app/components/ui/TrendChart.tsx` (Claude's discretion D — consolidate under ui/; preserve both `TrendChart` and `TrendSparkline` exports so existing importers can be repointed in Wave 4). Replace every hardcoded clinical hex with the brand CSS var string per the PATTERNS swap list: optimal band fill var(--vital-50); reference outline var(--n-200) dashed; axis ticks var(--text-muted), axis line var(--border); actual data line var(--ink) strokeWidth 2.25 with surface-fill/ink-stroke dots; projection line var(--energy-400) dashed 4 4; TrendSparkline always var(--ink) opacity ~0.7 (not status-colored). Keep ResponsiveContainer with a concrete numeric height (Pitfall 7). Do NOT change the data shape, projection math, axis domains, or reference-band meaning — only colors and stroke treatments. Optionally wrap the chart in a `.zt-draw` container for the on-mount line sweep (keyframe already in app.css); fidelity is not a hard requirement (RESEARCH Open Question 3). Update the old path `app/components/TrendChart.tsx` to be removed (git mv) — confirm no remaining importer references the old path after this task (Wave 4 repoints route imports; if any route currently imports `~/components/TrendChart`, leave a note for Wave 4).
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && grep -RnE "#22c55e|#3b82f6|#a855f7|#eab308|#6b7280" app/components/ui/TrendChart.tsx; test -z "$(grep -RnE '#22c55e|#3b82f6|#a855f7|#eab308|#6b7280' app/components/ui/TrendChart.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0.
    - `app/components/ui/TrendChart.tsx` exists and exports `TrendChart` (and `TrendSparkline`); the old `app/components/TrendChart.tsx` no longer contains hardcoded clinical hexes (file moved/removed).
    - `grep -nE "#22c55e|#3b82f6|#a855f7|#eab308|#6b7280" remix-app/app/components/ui/TrendChart.tsx` returns empty (UI-01-i); chart colors are all `var(--...)`.
    - TrendChart still references `var(--ink)` (actual line) and `var(--vital-50)` (optimal band) — semantics preserved with brand colors.
  </acceptance_criteria>
  <done>TrendChart is brand-tokened (no clinical hexes), chart semantics intact, consolidated under components/ui/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | UI reskin only — no new auth, no server data writes, no new external calls (RESEARCH § Security Domain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-04 | Tampering | lucide-react icon imports in CatChip | accept | Dependency already legitimacy-gated + installed in Plan 01 (T-04.1-01); named imports only, tree-shaken |
| T-04.1-05 | n/a | data-only visualization components | accept | No untrusted input crosses a boundary; components render static loader data as colored SVG/markup |
</threat_model>

<verification>
- `cd remix-app && npm run typecheck` exits 0.
- `cd remix-app && npm test` passes (StatusBadge, CatChip, MetricRing tests green).
- `cd remix-app && bash scripts/ds-audit.sh` exits 0 (no .jsx + no banned hexes in components — UI-01-b, UI-01-i).
</verification>

<success_criteria>
- SC4: category system (Lucide icons + family in CATEGORY_INFO via CatChip) and 4-status palette (StatusBadge/StatusDot) resolved in code.
- SC2 (partial): these signature components are typed TSX, tsc passes, no .jsx shipped.
- TrendChart brand-tokened as the Phase 6 chart language; chart semantics preserved.
</success_criteria>

<output>
Create `.planning/phases/04.1-design-system-adoption/04.1-03-SUMMARY.md` when done.
</output>
