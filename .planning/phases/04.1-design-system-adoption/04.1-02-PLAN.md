---
phase: 04.1-design-system-adoption
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - remix-app/app/app.css
  - remix-app/app/root.tsx
  - remix-app/app/components/ui/SpiralMark.tsx
  - remix-app/app/components/ui/Wordmark.tsx
  - remix-app/app/components/ui/ThemeToggle.tsx
  - remix-app/app/components/ui/ThemeToggle.test.tsx
  - remix-app/app/components/shell/TopNav.tsx
  - remix-app/app/components/shell/BottomTab.tsx
  - remix-app/app/components/shell/AppShell.tsx
autonomous: true

requirements: [UI-01]
user_setup: []

must_haves:
  truths:
    - "Brand utilities (bg-paper, text-ink, font-display, rounded-xl, shadow-sm) generate and resolve to brand values in the built CSS"
    - "Toggling html[data-theme] flips warm-dark surfaces/text with no dark: prefixes in component JSX and no flash on reload"
    - "The app renders inside an AppShell: sticky TopNav on desktop, fixed BottomTab on mobile, both wrapping the route Outlet"
    - "ThemeToggle persists the chosen theme to localStorage key zt-theme and sets data-theme"
    - "Decisions covered: D-01 (DS tokens bridged into Tailwind @theme so bg-paper/text-ink/font-display/text-vital/rounded-frame/shadow-warm-md resolve to brand values), D-02 (signature components ported to typed TSX under app/components/ui/ + shell/, no .jsx/_ds_bundle runtime shipped), D-03 (no shadcn — Tailwind @theme + hand-ported TSX), D-12 (round1 realized values win, e.g. reduced --radius-xl:14px not the readme's 28px)"
  artifacts:
    - path: "remix-app/app/app.css"
      provides: "Three-layer token bridge (@theme inline), @custom-variant dark, dark remap block, base helpers, grid/row helpers, scoped transitions"
      contains: "@theme inline"
    - path: "remix-app/app/root.tsx"
      provides: "No-flash theme script + suppressHydrationWarning + brand font links + AppShell wiring"
      contains: "suppressHydrationWarning"
    - path: "remix-app/app/components/shell/AppShell.tsx"
      provides: "Root layout wrapper (TopNav + main + footer + BottomTab)"
      exports: ["AppShell"]
    - path: "remix-app/app/components/shell/TopNav.tsx"
      provides: "Sticky desktop nav with Wordmark + NavLinks + ThemeToggle"
      exports: ["TopNav"]
    - path: "remix-app/app/components/shell/BottomTab.tsx"
      provides: "Fixed mobile bottom bar, 5 icon+label tabs, >=44px targets"
      exports: ["BottomTab"]
    - path: "remix-app/app/components/ui/ThemeToggle.tsx"
      provides: "40px theme toggle, sun/moon, localStorage zt-theme, accessible label"
      exports: ["ThemeToggle"]
  key_links:
    - from: "remix-app/app/root.tsx"
      to: "remix-app/app/components/shell/AppShell.tsx"
      via: "App default export renders <AppShell><Outlet/></AppShell>"
      pattern: "AppShell"
    - from: "remix-app/app/app.css"
      to: "docs/design-system/tokens"
      via: "@import of colors/typography/spacing token files (or inlined fallback)"
      pattern: "tokens/colors|--paper"
    - from: "remix-app/app/components/shell/TopNav.tsx"
      to: "remix-app/app/components/ui/ThemeToggle.tsx"
      via: "TopNav renders ThemeToggle"
      pattern: "ThemeToggle"
---

<objective>
Land the design-system foundation that unblocks every component and screen retrofit: rewrite `app/app.css` as the three-layer Tailwind 4 token bridge (DS token import → `:root` additions → `@theme inline` mapping) with the `@custom-variant dark`, the warm-dark remap block, the ported `base.css` helpers, and scoped (not global) theme transitions; rewire `root.tsx` (brand fonts, SSR no-flash theme script, `suppressHydrationWarning`, AppShell); and build the shell + theming primitives (SpiralMark, Wordmark, ThemeToggle, TopNav, BottomTab, AppShell). Resolve research open questions A1 (self-referential `@theme inline --font-*`) and A7 (`@import` path resolution from app.css) during the build, applying the documented fallbacks if they fail.

Purpose: Implements ROADMAP SC1 (token bridge + fonts + base helpers app-wide), SC5 (responsive nav replacing the overflowing header, ≥44px targets), and the dark-mode decision D-09. Every Wave 3 component and Wave 4 screen consumes these utilities and this shell.
Output: Brand-tokened `app.css`, rewired `root.tsx`, and `components/shell/` + the three theming-coupled `components/ui/` primitives, with a passing ThemeToggle jsdom test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md
@.planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md

<interfaces>
DS token files to bridge (verify @import path A7; fallback = inline their contents into app.css):
- docs/design-system/tokens/colors.css  (--paper, --surface, --ink, --energy/--vital/--focus ramps, --success/--warning/--danger, neutral ramp --n-0..--n-950)
- docs/design-system/tokens/typography.css (--text-* scale, --leading-*, --tracking-*, --fw-*)
- docs/design-system/tokens/spacing.css (--gap-*, --space-*, --radius-* readme values, --shadow-*, --dur-*, --ease-*)
- docs/design-system/tokens/base.css (.zt-eyebrow, .zt-readout, focus ring, reduced motion — port helpers, do NOT re-import fonts.css; fonts load via root.tsx links())

Round1 source-of-truth for shell components (port to typed TSX, swap hash Link → react-router):
- docs/design-system/_rounds/round1/app/lib.jsx: SpiralMark (71-80), Wordmark (346-355), ThemeToggle (368-378), TopNav (380-405), BottomTab (406-420), AppShell (421-438)
- docs/design-system/_rounds/round1/Zoetrope Redesign.html: :root excess/radii overrides, html[data-theme="dark"] block, no-flash script

Current files to transform (replace, preserve ErrorBoundary):
- remix-app/app/app.css (current 24 lines — REPLACE entirely per PATTERNS analog)
- remix-app/app/root.tsx (preserve ErrorBoundary export; replace links()/Layout/App; remove Header)

NAV constant (5 items, from UI-SPEC § NAV items):
- Dashboard /, exact match path === "/"
- Metrics /metrics, startsWith /metrics
- Protocol /protocol, startsWith /protocol
- Insights /insights/correlations, startsWith /insights
- Import /import/whoop, startsWith /import
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite app.css as the three-layer token bridge + dark remap + base helpers</name>
  <read_first>
    - remix-app/app/app.css (current contents to replace)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ app.css analog — complete replacement structure, lines 96-278)
    - .planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md (§ Token Bridge Core Mechanics, Pitfalls 1-4, Open Questions 1-2 / A1 / A7)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Token → Tailwind @theme Bridge, § Shape radii, § Shadows, § Dark Theme remap table)
    - docs/design-system/tokens/colors.css, typography.css, spacing.css, base.css
    - docs/design-system/_rounds/round1/Zoetrope Redesign.html (:root + html[data-theme="dark"])
  </read_first>
  <files>remix-app/app/app.css</files>
  <action>
    Replace `app/app.css` entirely with the bridge from the PATTERNS § app.css analog. Layers in order: (1) `@import "tailwindcss"` then `@import` the three DS token files (colors, typography, spacing) — resolve A7: if the relative path from `remix-app/app/app.css` fails the build, inline the token files' contents directly (Open Question 2 fallback). (2) `:root { ... }` additions NOT in DS files: `--excess: #c98910; --excess-bg: #f7edd8;` (D-06), the round1 reduced radii (`--radius-xs:2px` … `--radius-2xl:18px`, `--radius-pill:999px` — D-12, NOT the readme 28px), and semantic surface/border aliases (`--surface`, `--surface-2`, `--surface-sunken`, `--border`, `--border-strong`). (3) `@custom-variant dark (&:where([data-theme=dark], [data-theme=dark] *))`. (4) `@theme inline { ... }` mapping ALL color/font/text/radius/shadow/ease tokens to Tailwind namespaces via `var(--token)` so the dark remap cascades (Pitfall 1 — `inline` is mandatory). For fonts, resolve A1/Pitfall 2 by inlining the font STRINGS directly (`--font-display: 'Space Grotesk', ui-sans-serif, system-ui, sans-serif;` etc.) — do NOT write self-referential `--font-display: var(--font-display)`. Include the `--color-excess`/`--color-excess-bg` and full status tokens. (5) `html[data-theme="dark"] { ... }` warm-dark remap from the UI-SPEC Dark Theme table (neutral ramp + ink + family washes + excess + shadows). (6) Scoped transitions only on `html, body, .zt-topnav, .zt-bottomtab, .zt-theme-toggle` — REMOVE the global `* { @apply transition-colors }` rule (Pitfall 4). (7) Base element defaults (body font-family var(--font-text), h1-h4 var(--font-display)) and brand helper classes `.zt-eyebrow`, `.zt-readout`, `.zt-tnum`, the `.zt-draw` keyframe, `.zt-grid-2/3/4` + responsive reflow, `.zt-trow`/`.zt-mrow` hover, reduced-motion rule, focus-visible ring, `::selection`. Do NOT import tokens/fonts.css (fonts load via root.tsx — avoids double font load).
  </action>
  <verify>
    <automated>cd remix-app && npm run build && grep -RE "var\(--paper\)|bg-paper" build/client/assets/*.css >/dev/null && echo "bg-paper-present"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run build` exits 0 (token @import resolves or inlined fallback applied; no CSS parse error).
    - Built CSS under `remix-app/build/client/assets/*.css` contains a `bg-paper` rule referencing `var(--paper)` (proves @theme inline generated the utility — UI-01-a, A1 resolved).
    - `app.css` contains `@theme inline`, `@custom-variant dark`, an `html[data-theme="dark"]` block, `.zt-eyebrow`, `.zt-readout`, and `--radius-xl: 14px` (round1 value, D-12).
    - `app.css` does NOT contain a global `* { @apply transition-colors` rule.
    - `app.css` does NOT contain `--font-display: var(--font-display)` (A1 fallback: inlined font string).
    - `grep -nE "#22c55e|#eab308|#ef4444|#f97316" remix-app/app/app.css` returns empty (old status hexes removed; replaced by brand tokens).
  </acceptance_criteria>
  <done>app.css is the brand bridge: utilities generate, dark remap present, base helpers available, global transition anti-pattern removed.</done>
</task>

<task type="auto">
  <name>Task 2: Build shell + theming primitives (SpiralMark, Wordmark, ThemeToggle, TopNav, BottomTab, AppShell) + ThemeToggle test</name>
  <read_first>
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ SpiralMark, § Wordmark, § shell/TopNav, § shell/BottomTab, § shell/AppShell — full ports with line refs)
    - .planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md (§ ThemeToggle component pattern — complete TSX, § lucide-react icon mapping/props)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Responsive / Nav Contract, § NAV items, § Dark Theme ThemeToggle accessible-label rule)
    - docs/design-system/_rounds/round1/app/lib.jsx (SpiralMark, Wordmark, ThemeToggle, TopNav, BottomTab, AppShell — copy SVG path data verbatim)
    - .planning/phases/04.1-design-system-adoption/04.1-VALIDATION.md (UI-01-g — ThemeToggle test contract)
  </read_first>
  <files>remix-app/app/components/ui/SpiralMark.tsx, remix-app/app/components/ui/Wordmark.tsx, remix-app/app/components/ui/ThemeToggle.tsx, remix-app/app/components/ui/ThemeToggle.test.tsx, remix-app/app/components/shell/TopNav.tsx, remix-app/app/components/shell/BottomTab.tsx, remix-app/app/components/shell/AppShell.tsx</files>
  <action>
    Port the six shell/theming components to typed TSX (D-02), translating round1 hash `Link`/`navigate` → react-router `Link`/`NavLink`/`useLocation` (D-07). Files: `ui/SpiralMark.tsx` (copy the full `<path d="...">` golden-spiral data verbatim from lib.jsx SpiralMark; props size/color, default color "var(--ink)"); `ui/Wordmark.tsx` (react-router `Link to="/"`, SpiralMark + "zoetrope" Space Grotesk 600 + periwinkle "." via var(--accent) + small "codename Zoetrop" tag per D-08); `ui/ThemeToggle.tsx` (RESEARCH complete pattern — 40px button, sun in dark / moon in light from lucide-react, writes localStorage key `zt-theme`, sets data-theme on document.documentElement, initial state read from document.documentElement so no client flash; aria-label "Switch to dark theme" in light / "Switch to light theme" in dark, mirror in title); `shell/TopNav.tsx` (sticky, the 5-item NAV constant with active-underline NavLink pattern, color-mix backdrop WITH solid `background: var(--paper)` fallback before color-mix per Pitfall 8/A6, renders Wordmark + ThemeToggle, desktop-only via `hidden md:flex` on the link cluster); `shell/BottomTab.tsx` (fixed, `md:hidden`, 5 Link items with lucide icons LayoutGrid/Activity/ListChecks/GitCompare/Download, `min-height: 48px` per item ≥44px target UI-01-l, active icon strokeWidth 2.3 + label var(--accent), inactive 1.9 + var(--text-muted), Space Mono 10px uppercase labels); `shell/AppShell.tsx` (TopNav + `<main>` max-width 1180 default / 1280 wide prop + footer with SpiralMark + BottomTab + a `md:hidden` 80px bottom-pad spacer). All SVG/icon colors use `var(--token)` strings or `currentColor` — NO hardcoded hex, NO `dark:` prefixes (cascade handles dark mode). Write `ui/ThemeToggle.test.tsx` with `// @vitest-environment jsdom` at the top: render ThemeToggle, assert initial aria-label, fire a click via @testing-library/user-event, assert `document.documentElement.getAttribute('data-theme')` flipped and `localStorage.getItem('zt-theme')` matches.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && npm test -- run app/components/ui/ThemeToggle.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0 (all six components typed, no `.+types` errors).
    - `app/components/ui/ThemeToggle.test.tsx` passes under jsdom: clicking the toggle sets `data-theme` on the documentElement and writes `localStorage` key `zt-theme` (UI-01-g).
    - `grep -REn "#[0-9a-fA-F]{6}" remix-app/app/components/shell remix-app/app/components/ui/SpiralMark.tsx remix-app/app/components/ui/Wordmark.tsx remix-app/app/components/ui/ThemeToggle.tsx | grep -vE "viewBox|path d=|d=\"M"` returns empty (no hardcoded color hexes outside SVG path geometry).
    - `app/components/shell/BottomTab.tsx` contains `min-height` of at least 48 (UI-01-l) and `md:hidden`; `TopNav.tsx` contains `md:flex` (responsive nav, UI-01-k).
    - TopNav backdrop has a solid `var(--paper)` fallback declared before `color-mix(` (A6).
  </acceptance_criteria>
  <done>Shell + theming primitives ported, typed, color-token-driven; ThemeToggle behavior proven in jsdom.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire root.tsx — brand fonts, no-flash theme script, suppressHydrationWarning, AppShell</name>
  <read_first>
    - remix-app/app/root.tsx (current — preserve ErrorBoundary; replace links/Layout/App; delete Header)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ root.tsx analog — links(), Layout, App replacements)
    - .planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md (§ Dark Theme SSR-Safe — Layout pattern + no-flash script, § Font Loading)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Dark Theme implementation — no-flash script literal)
  </read_first>
  <files>remix-app/app/root.tsx</files>
  <action>
    Edit `root.tsx`: (1) Replace `links()` — swap the Inter `<link>` for the three brand fonts via the exact Google Fonts css2 URL from PATTERNS/RESEARCH (Space Grotesk 400;500;600;700 + Hanken Grotesk 300..800 + Space Mono 400;700, `display=swap`), keeping the two preconnect links (D-11). (2) Replace `Layout` — add `suppressHydrationWarning` on `<html>`, inject the no-flash theme `<script>` via `dangerouslySetInnerHTML` placed BEFORE `<Meta />`/`<Links />`; the script body MUST be the static literal from UI-SPEC § Dark Theme (reads localStorage `zt-theme`, falls back to prefers-color-scheme, sets data-theme) with NO interpolation of any variable/runtime value (security threat T-04.1-02 — static literal prevents XSS); set body className to `bg-paper text-ink min-h-screen font-text` (remove the gray-50/gray-950/dark: classes). (3) Replace the default `App` export to render `<AppShell><Outlet/></AppShell>` and DELETE the inline `Header` function (superseded by TopNav, D-07). (4) Remove the now-unused `NavLink` import. (5) Leave `ErrorBoundary` intact. (6) Remove the old `@media (prefers-color-scheme: dark)` reliance — theme is now data-attribute driven.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && npm run build && grep -q "suppressHydrationWarning" app/root.tsx && grep -q "Space+Grotesk" app/root.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0 and `npm run build` exits 0 (SSR renders script + AppShell).
    - `app/root.tsx` contains `suppressHydrationWarning`, a `dangerouslySetInnerHTML` script reading `localStorage` `zt-theme`, and the Google Fonts URL with `Space+Grotesk`, `Hanken+Grotesk`, `Space+Mono`.
    - `app/root.tsx` no longer contains `function Header` nor the Inter font URL nor `bg-gray-50`.
    - The no-flash script contains no template-literal interpolation of variables (static string only — T-04.1-02).
    - The default export renders `<AppShell>` wrapping `<Outlet />`.
  </acceptance_criteria>
  <done>root.tsx loads brand fonts, applies the no-flash theme before paint, suppresses the hydration warning, and renders all routes inside AppShell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR HTML stream → browser | The inline no-flash `<script>` is injected into the document head and runs before paint |
| localStorage → DOM | Theme preference read from client storage sets a DOM attribute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-02 | Injection (XSS via dangerouslySetInnerHTML) | root.tsx no-flash theme `<script>` | mitigate | Script body is a static, non-interpolated string literal (UI-SPEC § Dark Theme) — no user/runtime input is ever concatenated into it; reads only its own localStorage/matchMedia and writes only data-theme |
| T-04.1-03 | Information disclosure | localStorage `zt-theme` value | accept | Stores only "dark"/"light"; no PII, no security value; reading is wrapped in try/catch |
</threat_model>

<verification>
- `cd remix-app && npm run typecheck` exits 0.
- `cd remix-app && npm run build` exits 0; built CSS contains brand utilities (`bg-paper` → `var(--paper)`).
- `cd remix-app && npm test -- run app/components/ui/ThemeToggle.test.tsx` passes.
- Manual (deferred to phase gate): toggle data-theme — surfaces/text flip with no `dark:` prefixes, no reload flash (UI-01-m); at 375px BottomTab shows / TopNav links hidden, at 1280px reversed (UI-01-k).
</verification>

<success_criteria>
- SC1: brand tokens bridged into `@theme`; Inter replaced by the three brand fonts; base helpers (`.zt-eyebrow`, `.zt-readout`, tabular nums, focus ring) available app-wide.
- SC5: responsive nav (TopNav ⇄ BottomTab) replaces the non-collapsing header; BottomTab targets ≥44px.
- D-09: dark mode wired (data-theme, no-flash script, warm-dark block, ThemeToggle).
- The app shell wraps all 16 routes via root.tsx without touching loaders or the route table.
</success_criteria>

<output>
Create `.planning/phases/04.1-design-system-adoption/04.1-02-SUMMARY.md` when done.
</output>
