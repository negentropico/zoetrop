---
phase: 04.1-design-system-adoption
plan: 06
type: execute
wave: 4
depends_on: ["04.1-03", "04.1-04"]
files_modified:
  - remix-app/app/routes/protocol/layout.tsx
  - remix-app/app/routes/protocol/index.tsx
  - remix-app/app/routes/protocol/cessation.tsx
  - remix-app/app/routes/protocol/versions.tsx
  - remix-app/app/routes/protocol/version-detail.tsx
  - remix-app/app/routes/protocol/supplements.tsx
  - remix-app/app/routes/protocol/compare.tsx
autonomous: true

requirements: [UI-01]
user_setup: []

must_haves:
  truths:
    - "The protocol section renders in-brand: underline tab strip sub-nav, segmented PhaseBar (no gradient), Card surfaces, Badge for version status"
    - "The cessation tracker renders a MetricRing + PhaseBar with family-tinted phase cards and the calm 'you' voice ('Cessation tracker')"
    - "Version history, version detail, supplements, and compare render in-brand via DataTable / Card / Badge"
    - "Every loader, route, and data value is preserved — only markup/classes/components change"
    - "Decisions covered: D-10 — all 7 protocol-section routes retrofit in-brand (part of all-16-routes, no half-branded app)"
  artifacts:
    - path: "remix-app/app/routes/protocol/cessation.tsx"
      provides: "In-brand cessation tracker (archetype 06)"
      contains: "PhaseBar"
    - path: "remix-app/app/routes/protocol/layout.tsx"
      provides: "Underline tab strip sub-nav for the protocol section"
      contains: "NavLink"
  key_links:
    - from: "remix-app/app/routes/protocol/cessation.tsx"
      to: "remix-app/app/components/ui/MetricRing.tsx"
      via: "cessation progress rendered as MetricRing + PhaseBar (not a gradient bar)"
      pattern: "MetricRing"
    - from: "remix-app/app/routes/protocol/versions.tsx"
      to: "remix-app/app/components/ui/DataTable.tsx"
      via: "version history rendered as DataTable / Card"
      pattern: "DataTable|Card"
---

<objective>
Retrofit the entire protocol section (7 routes: layout, index, cessation, versions, version-detail, supplements, compare) to the Zoetrope brand using the ported components. Replace the sub-nav with an underline tab strip; replace gradient cessation bars with MetricRing + segmented PhaseBar; use Card/Badge/DataTable/PageHeader throughout. Apply the calm "you" voice and "Cessation tracker" copy. Preserve every loader, route, and data value.

Purpose: Delivers ROADMAP SC3 for archetype 05 (Protocol overview) and 06 (Cessation), plus the four non-archetype protocol routes (D-10: all 16 routes in-brand, no half-branded app) following the same ported components.
Output: 7 retrofit route files; typecheck + build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md

<interfaces>
Components available (Plans 03/04): Card, Button, Badge, StatusBadge, StatusDot, MetricRing, PhaseBar, ProgressBar, DataTable, PageHeader, Crumb, Stat, SegmentedControl, CatChip.

Retrofit map (UI-SPEC § Per-Screen Retrofit Map rows 05-06 + D-10 for the other 4):
- protocol/layout.tsx: section sub-nav → underline tab strip (PATTERNS § Underline Tab Strip) — NavLink Overview/Versions/Supplements/Cessation/Compare, active weight 600 + 2px Ink border-bottom, inactive weight 500 text-muted; preserve existing route table + useLocation.
- protocol/index.tsx (ProtocolScreen): gradient cessation bar → <PhaseBar>; protocol stat tiles → ProtoStat (Card + .zt-eyebrow + .zt-readout); current version → <Badge tone="success">Current</Badge>.
- protocol/cessation.tsx (CessationScreen): gradient progress → <MetricRing tone="vital" size={150}/> + <PhaseBar height={16}/>; phase state chips → PhaseCard with Vital/Focus/Energy family tints (Card tone prop); per-phase progress → <ProgressBar tone={familyTone}/>; copy "Cessation Protocol" → "Cessation tracker", calm "you" voice; phase labels Acute · Stabilization · Clearing · Optimization (sentence case).
- protocol/versions.tsx: version list → DataTable or Card grid, Badge for current/status (role-match, follows Card + DataTable pattern).
- protocol/version-detail.tsx: Card + PageHeader + Crumb; supplement/change lists styled per brand.
- protocol/supplements.tsx: DataTable + Badge for supplement tiers (SUPPLEMENTS BY TIER eyebrow).
- protocol/compare.tsx: Card grid comparison; "Compare versions" CTA copy.

Universal class swaps: PATTERNS § Route retrofit pattern (gray→brand surfaces/text, gradient→PhaseBar/ProgressBar, h2/h3→.zt-eyebrow, blue→accent, no emoji).
Copy (UI-SPEC § Copywriting): sub-nav labels stay Overview/Versions/Supplements/Cessation/Compare; "Cessation tracker"; "Compare versions" CTA; section eyebrows VERSION HISTORY, SUPPLEMENTS BY TIER, FAAH CESSATION TIMELINE, PHASES.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit protocol layout + overview + cessation (layout.tsx, index.tsx, cessation.tsx)</name>
  <read_first>
    - remix-app/app/routes/protocol/layout.tsx, index.tsx, cessation.tsx (current — preserve loaders, routes, data)
    - docs/design-system/_rounds/round1/app/screen-protocol.jsx (ProtocolScreen, CessationScreen)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ protocol/cessation.tsx analog, § Underline Tab Strip, § Eyebrow Section Labels)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Per-Screen Retrofit Map rows 05-06, § Copywriting Contract)
  </read_first>
  <files>remix-app/app/routes/protocol/layout.tsx, remix-app/app/routes/protocol/index.tsx, remix-app/app/routes/protocol/cessation.tsx</files>
  <action>
    Retrofit `protocol/layout.tsx`: replace the sub-nav with the underline tab strip (NavLink Overview→/protocol end, Versions→/protocol/versions, Supplements→/protocol/supplements, Cessation→/protocol/cessation, Compare→/protocol/compare; active weight 600 + 2px Ink border-bottom, inactive weight 500 text-muted; horizontal scroll on overflow); preserve the route table. Retrofit `protocol/index.tsx`: gradient cessation bar → <PhaseBar>; stat tiles → Card + .zt-eyebrow + .zt-readout; current version → <Badge tone="success">Current</Badge>; eyebrow section labels; sentence case. Retrofit `protocol/cessation.tsx`: gradient progress → <MetricRing tone="vital" size={150} /> + <PhaseBar height={16} />; phase state cards → Card with family tone (vital/focus/energy); per-phase progress → <ProgressBar tone={family} />; copy "Cessation Protocol" → "Cessation tracker", calm "you" voice, phase labels Acute/Stabilization/Clearing/Optimization. Preserve all loaders + cessation math/data values.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && grep -q "MetricRing" app/routes/protocol/cessation.tsx && grep -q "PhaseBar" app/routes/protocol/index.tsx && grep -q "Cessation tracker" app/routes/protocol/cessation.tsx && test -z "$(grep -nE 'bg-gradient|text-gray-|bg-gray-' app/routes/protocol/layout.tsx app/routes/protocol/index.tsx app/routes/protocol/cessation.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0.
    - cessation.tsx renders MetricRing + PhaseBar and contains the copy "Cessation tracker"; index.tsx renders PhaseBar; layout.tsx renders an underline-tab NavLink strip.
    - The three files contain no `bg-gradient`, `text-gray-`, or `bg-gray-` classes.
    - Loaders and cessation data/math values are unchanged.
  </acceptance_criteria>
  <done>Protocol overview + cessation in-brand (MetricRing/PhaseBar, family-tinted phase cards, underline sub-nav, "you" voice); data intact.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit version history + version detail (versions.tsx, version-detail.tsx)</name>
  <read_first>
    - remix-app/app/routes/protocol/versions.tsx, version-detail.tsx (current — preserve loaders, routes, data)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ Route retrofit pattern, § Eyebrow Section Labels — versions/version-detail follow Card + DataTable + PageHeader)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Component Inventory, § Copywriting Contract — VERSION HISTORY eyebrow)
  </read_first>
  <files>remix-app/app/routes/protocol/versions.tsx, remix-app/app/routes/protocol/version-detail.tsx</files>
  <action>
    Retrofit `protocol/versions.tsx`: version list → <DataTable> (or Card grid) with Badge for current/status, eyebrow "VERSION HISTORY", Card surfaces, links via react-router Link to version-detail; universal gray→brand swaps. Retrofit `protocol/version-detail.tsx`: <PageHeader> + <Crumb items=[Protocol→/protocol, Versions→/protocol/versions, version]>; supplement/change lists in Card + brand styling + Badge tiers; eyebrow section labels. Preserve loaders + data.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && test -z "$(grep -nE 'text-gray-|bg-gray-|bg-gradient|💊|🧪' app/routes/protocol/versions.tsx app/routes/protocol/version-detail.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0.
    - versions.tsx renders DataTable or Card + Badge; version-detail.tsx renders PageHeader + Crumb.
    - Both files contain no `text-gray-`/`bg-gray-`/`bg-gradient` classes and no emoji.
    - Loaders and data values unchanged.
  </acceptance_criteria>
  <done>Version history + detail in-brand via DataTable/Card/Badge/PageHeader/Crumb; data intact.</done>
</task>

<task type="auto">
  <name>Task 3: Retrofit supplements + compare (supplements.tsx, compare.tsx)</name>
  <read_first>
    - remix-app/app/routes/protocol/supplements.tsx, compare.tsx (current — preserve loaders, routes, data)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ Route retrofit pattern, § Eyebrow Section Labels)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Per-Screen Retrofit Map, § Copywriting — SUPPLEMENTS BY TIER, "Compare versions")
  </read_first>
  <files>remix-app/app/routes/protocol/supplements.tsx, remix-app/app/routes/protocol/compare.tsx</files>
  <action>
    Retrofit `protocol/supplements.tsx`: supplement-by-tier list → <DataTable> + <Badge> tier chips, eyebrow "SUPPLEMENTS BY TIER", Card grouping; universal gray→brand swaps. Retrofit `protocol/compare.tsx`: version comparison → Card grid (per UI-SPEC), brand surfaces + Badge for diffs, "Compare versions" CTA copy where applicable, eyebrow labels. Preserve loaders + data.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && npm run build && test -z "$(grep -nE 'text-gray-|bg-gray-|bg-gradient|💊|🧪' app/routes/protocol/supplements.tsx app/routes/protocol/compare.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0 and `npm run build` exits 0.
    - supplements.tsx renders DataTable + Badge; compare.tsx renders a Card-based comparison.
    - Both files contain no `text-gray-`/`bg-gray-`/`bg-gradient` classes and no emoji.
    - Loaders and data values unchanged.
  </acceptance_criteria>
  <done>Supplements + compare in-brand; whole protocol section branded (D-10); data intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | UI reskin only — loaders, routes, data sources unchanged (RESEARCH § Security Domain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-10 | Tampering | route loader integrity during retrofit | mitigate | Retrofit changes markup/classes/components only; loaders and data values preserved (verified by unchanged loader exports + typecheck/build) |
| T-04.1-11 | n/a | static-data rendering | accept | Same M0 static loader data; no new untrusted input or external call |
</threat_model>

<verification>
- `cd remix-app && npm run typecheck` exits 0.
- `cd remix-app && npm run build` exits 0.
- `cd remix-app && bash scripts/ds-audit.sh` exits 0.
- Manual (phase gate): protocol overview + cessation match their round1 screens (no gradient, MetricRing/PhaseBar, periwinkle accents); all 7 protocol routes in-brand (D-10).
</verification>

<success_criteria>
- SC3 (archetypes 05-06): protocol overview + cessation render in-brand.
- D-10: all 7 protocol routes branded (no half-branded section).
- Loaders, routes, and data values preserved across all 7 files.
</success_criteria>

<output>
Create `.planning/phases/04.1-design-system-adoption/04.1-06-SUMMARY.md` when done.
</output>
