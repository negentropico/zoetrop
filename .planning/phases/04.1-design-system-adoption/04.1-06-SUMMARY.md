---
phase: 04.1-design-system-adoption
plan: "06"
subsystem: protocol-routes
tags: [retrofit, brand, protocol, cessation, phaseBar, metricRing, dataTable, card, badge]

dependency_graph:
  requires: ["04.1-03", "04.1-04"]
  provides: ["D-10 protocol section fully in-brand", "SC3 archetypes 05-06 (protocol overview + cessation)"]
  affects: []

tech_stack:
  added: []
  patterns:
    - "Underline tab strip sub-nav (NavLink + border-bottom 2px Ink, weight 600 active / 500 muted inactive)"
    - "ProtoStat (Card + .zt-eyebrow + .zt-readout) for protocol stat tiles"
    - "MetricRing(tone:vital, size:150) + PhaseBar(height:16) for cessation hero"
    - "Family-tinted PhaseCard (Card with tone prop + ProgressBar per family) for cessation phases"
    - "ChangeTypeBadge routing to brand tones (vital/danger/focus/energy/neutral)"
    - "TierBadge using Badge tones (vital/focus/energy/neutral) replacing hardcoded Tailwind color classes"

key_files:
  created: []
  modified:
    - remix-app/app/routes/protocol/layout.tsx
    - remix-app/app/routes/protocol/index.tsx
    - remix-app/app/routes/protocol/cessation.tsx
    - remix-app/app/routes/protocol/versions.tsx
    - remix-app/app/routes/protocol/version-detail.tsx
    - remix-app/app/routes/protocol/supplements.tsx
    - remix-app/app/routes/protocol/compare.tsx

decisions:
  - "D-10 delivered: all 7 protocol routes fully in-brand (no half-branded app)"
  - "SC3 complete: protocol overview (archetype 05) and cessation (archetype 06) both render in-brand"
  - "Underline tab strip implemented in layout.tsx via NavLink inline style (not className callback) — avoids TW purge edge cases with dynamic isActive"
  - "Cessation phase family mapping: acute=energy, stabilization=vital, clearing=vital, optimization=focus — matches screen-protocol.jsx FAMILY_COLOR pattern"

metrics:
  duration_seconds: 338
  completed_date: "2026-06-08"
  tasks_completed: 3
  tasks_total: 3
  files_modified: 7
---

# Phase 04.1 Plan 06: Protocol Section Retrofit Summary

All 7 protocol routes retrofitted to the Zoetrop brand design system. MetricRing + segmented PhaseBar for cessation, underline tab strip sub-nav, Card/Badge/DataTable throughout. Loaders and data values untouched.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Layout + overview + cessation | 3a6318f | layout.tsx, index.tsx, cessation.tsx |
| 2 | Version history + version detail | 680d19a | versions.tsx, version-detail.tsx |
| 3 | Supplements + compare | 9887c25 | supplements.tsx, compare.tsx |

## What Was Built

**Task 1 — Layout, overview, cessation:**

- `layout.tsx`: Replaced gray-border/dark: NavLink tab strip with brand underline tab strip (NavLink inline style, 2px Ink border-bottom on active, weight 600 active / 500 muted inactive, overflow-x auto).
- `index.tsx`: Gradient cessation bar replaced with `<PhaseBar>` inside a Card; stat tiles rebuilt as `ProtoStat` (Card + `.zt-eyebrow` + `.zt-readout`); current version uses `<Badge tone="success">Current</Badge>`; VERSION HISTORY and SUPPLEMENTS BY TIER eyebrow section labels; accent link for "View tracker →".
- `cessation.tsx`: Main gradient progress bar replaced with `<MetricRing tone="vital" size={150} thickness={15}>` + `<PhaseBar height={16}>`; phase cards rebuilt as `PhaseCard` with family tints (energy/vital/focus) via Card `tone` prop and `<ProgressBar tone={family}>`; copy changed from "Cessation Protocol" to "Cessation tracker"; calm "you" voice; PHASES eyebrow; Check/Play/Circle Lucide icons for phase status; Why 150 days card uses Card `tone="focus"` panel.

**Task 2 — Versions + version detail:**

- `versions.tsx`: Timeline view replaced with Card grid; `PageHeader` with VERSION HISTORY eyebrow; "Compare versions" CTA Button; `ChangeTypeBadge` maps change types to brand Badge tones (vital/danger/focus/energy/neutral); `Badge tone="success"` for Current version.
- `version-detail.tsx`: `Crumb` breadcrumb (Protocol / Versions / version); `PageHeader` with PROTOCOL VERSION eyebrow; changes list with brand `ChangeTypeBadge`; supplements preview in Card grid with `TierBadge`; milestones in Card; all `bg-gray-`, `text-gray-`, `dark:` prefixes removed.

**Task 3 — Supplements + compare:**

- `supplements.tsx`: Stats in 4-up Card grid (`.zt-eyebrow` + `.zt-readout`); filter pills use Ink-active pill style (not gray); supplement cards use `Card + TierBadge`; tier definitions in Card with TIER DEFINITIONS eyebrow; `PageHeader` with SUPPLEMENTS BY TIER eyebrow.
- `compare.tsx`: Version selectors in Card with pill-styled `<select>`; change summary as 3 Card tiles (vital/danger/focus tones); change lists in Card with brand Badge counts; version notes in Card grid; `PageHeader` with VERSION COMPARISON eyebrow; "Compare versions" title per copywriting contract.

## Verification Gates

- `npm run typecheck` exits 0 (all 3 tasks verified)
- `npm run build` exits 0 (Task 3 final)
- `bash scripts/ds-audit.sh` exits 0 (UI-01-b: no .jsx; UI-01-i: no banned hexes)
- All 7 files contain no `bg-gradient`, `text-gray-`, `bg-gray-`, `dark:` prefix classes
- All loaders and data math preserved unchanged

## Deviations from Plan

None — plan executed exactly as written. All substitutions followed the specified component mapping and copy contract.

## Known Stubs

None. All data is wired to real protocol data loaders.

## Threat Flags

None. This is a UI-only reskin. No new network endpoints, auth paths, file access patterns, or schema changes were introduced. Loader integrity preserved (T-04.1-10 mitigated).

## Self-Check: PASSED

Files exist:
- remix-app/app/routes/protocol/layout.tsx: FOUND
- remix-app/app/routes/protocol/index.tsx: FOUND
- remix-app/app/routes/protocol/cessation.tsx: FOUND
- remix-app/app/routes/protocol/versions.tsx: FOUND
- remix-app/app/routes/protocol/version-detail.tsx: FOUND
- remix-app/app/routes/protocol/supplements.tsx: FOUND
- remix-app/app/routes/protocol/compare.tsx: FOUND

Commits exist:
- 3a6318f: Task 1 — FOUND
- 680d19a: Task 2 — FOUND
- 9887c25: Task 3 — FOUND
