---
phase: 06-engine-promotion-confidence-graded-reports
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - remix-app/scripts/seed-corpus.ts
  - remix-app/app/lib/corpus.server.ts
  - remix-app/app/routes/_app/insights/genetics.tsx
  - remix-app/app/routes/_app/insights/index.tsx
  - remix-app/app/routes/_app/dashboard.tsx
  - remix-app/app/lib/genetics-knowledge.server.ts
autonomous: false
requirements: [ENG-02, ENG-03]

must_haves:
  truths:
    - "The owner-complete corpus (variant + metric rules) is authored, evidence-tiered (K1–K4), hedged, and seeded into Neon"
    - "Every seeded corpus row carries a non-null evidence_tier; SELECT COUNT(*) WHERE evidence_tier IS NULL = 0 on variant_protocol_map and metric_protocol_map (this content assertion is owned by THIS plan, after the seed — 06-02 ships empty tables with only the structural NOT-NULL column)"
    - "No corpus recommendationText contains owner PHI (specific lab values, rsids, owner name)"
    - "genetics-knowledge.server.ts (GENETIC_KNOWLEDGE) is retired and its 3 consumers read from the corpus instead"
    - "Corpus recommendationText is non-imperative (corpus-lint test green over real content)"
  artifacts:
    - path: "remix-app/scripts/seed-corpus.ts"
      provides: "Owner-complete corpus content + idempotent seed"
      contains: "variantRules"
    - path: "remix-app/app/lib/corpus.server.ts"
      provides: "Genetics-knowledge read helper replacing GENETIC_KNOWLEDGE"
      contains: "getVariantMaps"
  key_links:
    - from: "remix-app/app/routes/_app/insights/genetics.tsx"
      to: "remix-app/app/lib/corpus.server.ts"
      via: "loader reads corpus instead of GENETIC_KNOWLEDGE"
      pattern: "corpus.server"
    - from: "remix-app/scripts/seed-corpus.ts"
      to: "Neon variant_protocol_map / metric_protocol_map"
      via: "db:seed-corpus insert"
      pattern: "variantProtocolMap"
---

<objective>
Author the owner-complete evidence-tiered knowledge corpus (ENG-02, D-10/11/12/19) and seed it into the Neon tables built in 06-02, then retire `genetics-knowledge.server.ts` (D-06) by re-pointing its three consumers to the corpus read layer. Corpus content is LLM-assisted-extracted from the owner's PDFs at authoring time (D-11), human-reviewed for evidence tiering + PHI scrubbing + non-imperative phrasing (D-19), then committed as the `corpusSeedData` arrays in `scripts/seed-corpus.ts`. This makes the corpus-lint test green over real content and proves ENG-03 (the engine derives graded recommendations from corpus + subject data). This plan owns the non-null-K CONTENT assertion (`COUNT(*) WHERE evidence_tier IS NULL = 0` after seed); 06-02 ships only the structural NOT-NULL column on empty tables.

Purpose: The corpus is the heart of the phase. Until it holds reviewed content, no report can be generated and the non-null K content assertions cannot pass. Retiring the 16-entry interim module (D-06) and re-pointing the genetics/insights/dashboard loaders completes the schema promotion.
Output: filled `corpusSeedData`, seeded Neon corpus, retired interim module, 3 re-pointed loaders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-engine-promotion-confidence-graded-reports/06-CONTEXT.md
@.planning/phases/06-engine-promotion-confidence-graded-reports/06-RESEARCH.md
@.planning/phases/06-engine-promotion-confidence-graded-reports/06-PATTERNS.md
@.planning/phases/06-engine-promotion-confidence-graded-reports/06-VALIDATION.md
@.planning/phases/06-engine-promotion-confidence-graded-reports/06-02-SUMMARY.md

<interfaces>
<!-- The corpus content source + the consumers to retire. -->

Corpus source PDFs (PHI — in vault, NEVER committed; D-11):
  /Users/mac/vaults/#Bwell/pureinsight-report-mb.pdf (PureInsights)
  /Users/mac/vaults/#Bwell/_archive/Well/Plan4/401/Reports/{allergy,brain,fattyliver,fitfunc,ketones,mito,nutrient_factors,nutrition}.pdf (SelfDecode, 8)
  /Users/mac/vaults/#Bwell/_archive/Well/Tests/labs.pdf (bloodwork)

From app/lib/genetics-knowledge.server.ts (the module to retire — 16 gene entries):
  export const GENETIC_KNOWLEDGE: Record<string, GeneticKnowledgeEntry>
  // header already says "retired by Phase 6 engine"

Three GENETIC_KNOWLEDGE consumers to re-point (grep-confirmed against the live tree):
  app/routes/_app/insights/genetics.tsx:6 — import { GENETIC_KNOWLEDGE }; loader joins DB genotypes + knowledge by gene (line 56-58)
  app/routes/_app/insights/index.tsx:10 — import { GENETIC_KNOWLEDGE }; loader join at line 67-69
  app/routes/_app/dashboard.tsx:16 — import { GENETIC_KNOWLEDGE }; loader join at line 140-143
  (All three paths are canonical and confirmed by `grep -rln GENETIC_KNOWLEDGE app/` — they match files_modified exactly.)

From app/lib/corpus.server.ts (06-02): CORPUS_VERSION, getVariantMaps(), getMetricRules()

K1–K4 rubric (06-RESEARCH §Evidence-Tier Rubric, Oxford CEBM mapping):
  K1 Established (SR/RCT) · K2 Probable (single RCT/cohort/mechanistic-human) · K3 Emerging (preliminary/animal/expert-consensus) · K4 Speculative (expert opinion/vendor-score/theory)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Author + human-review the owner-complete corpus, fill corpusSeedData</name>
  <files>remix-app/scripts/seed-corpus.ts</files>
  <read_first>
    - .planning/phases/06-engine-promotion-confidence-graded-reports/06-RESEARCH.md §"Evidence-Tier Rubric" (lines 276-305 — the K1–K4 rubric + example authoring decisions), §"Corpus Build-Time Extraction" (lines 424-465 — workflow + owner-complete scope), Pitfall 4 (PHI in corpus tables)
    - .planning/phases/06-engine-promotion-confidence-graded-reports/06-CONTEXT.md D-07/D-08/D-10/D-11/D-12/D-19 (evidence-tier semantics, owner-complete, LLM-assisted + human review)
    - remix-app/app/lib/genetics-knowledge.server.ts (the 16 gene entries — the starting finding set to promote, genotype-specific + evidence-tiered)
    - remix-app/app/types/genetics.ts VARIANT_CATEGORIES (the category values corpus rows must use)
    - remix-app/scripts/seed-corpus.ts (the scaffold from 06-02 — fill corpusSeedData.variantRules and .metricRules)
  </read_first>
  <action>
    LLM-assisted-extract corpus findings from the owner's vault PDFs (Read tool — PDFs stay in the vault, never copied to the repo) using the K1-K4 rubric; draft hedged non-imperative recommendation text; fill corpusSeedData.variantRules + .metricRules in scripts/seed-corpus.ts (owner-complete scope per D-10, each row stamped CORPUS_VERSION). Then human-review (D-19): confirm evidence tiering is defensible and NO recommendationText contains owner PHI (Pitfall 4). This is a blocking review gate.
  </action>
  <what-built>
    The corpus is LLM-assisted-extracted from the owner's PDFs (PureInsights + 8 SelfDecode topic reports + labs) at authoring time, then human-reviewed. Claude reads the vault PDFs (Read tool — they stay in the vault, never copied into the repo), extracts findings using the K1–K4 rubric, drafts hedged non-imperative recommendation text, and writes the reviewed entries into `corpusSeedData.variantRules` (variantProtocolMap edges, each with a parent geneticVariants entry: gene, genotypePattern, category, impact, clinicalImplication, knowledgeSource) and `corpusSeedData.metricRules` (metricProtocolMap edges: metricName matching metrics.name, conditionStatus 'deficient'|'excess'|'borderline'|'any_non_optimal', category metricCategoryEnum, evidenceTier, recommendationText, evidenceCitation). Owner-complete scope (D-10): cover the 16 genes + the owner's non-optimal committed metrics (~30–50 variant edges + ~20–30 metric edges per RESEARCH). Each row carries CORPUS_VERSION.
    HUMAN REVIEW GATE (D-19): the owner/reviewer must confirm evidence tiering is defensible and NO recommendationText contains owner PHI (specific lab numbers, rsids, owner name) — Pitfall 4. This is the manual-only verification from 06-VALIDATION.md.
  </what-built>
  <how-to-verify>
    1. Review the drafted `corpusSeedData` entries in scripts/seed-corpus.ts against the source PDFs: each finding→action evidenceTier matches the K1–K4 rubric (e.g. MTHFR C677T het → methylfolate = K1; COMT Val/Met → limit methyl donors = K2; FAAH → 120-day cessation = K3; SelfDecode wellness-score-only = K4).
    2. Confirm NO recommendationText contains a specific owner lab value, an rsid string, or the owner's name (Pitfall 4) — corpus text must read naturally for ANY subject with that finding.
    3. Confirm every entry has a non-null evidenceTier and a category from VARIANT_CATEGORIES / metricCategoryEnum.
    4. Run `cd remix-app && npx vitest run tests/lib/corpus-lint.test.ts` — the imperative-pattern lint must be GREEN over the real content (no "you should/must/need to").
    Reply "approved" once the corpus content is reviewed (tiering defensible, PHI-free, lint-clean), or list entries to fix.
  </how-to-verify>
  <verify>
    <automated>cd remix-app && npx vitest run tests/lib/corpus-lint.test.ts</automated>
  </verify>
  <resume-signal>Type "approved" (corpus reviewed: tiering defensible + PHI-free + lint-clean) or list entries to revise</resume-signal>
  <acceptance_criteria>
    - corpusSeedData.variantRules has >= 30 entries and corpusSeedData.metricRules has >= 20 entries (owner-complete scope per RESEARCH)
    - every entry's evidenceTier is one of 'k1'|'k2'|'k3'|'k4' (non-null)
    - `cd remix-app && npx vitest run tests/lib/corpus-lint.test.ts` imperative-pattern assertions exit 0 over the real content
    - no recommendationText matches a numeric-PHI pattern from the owner's labs (reviewer-confirmed, Pitfall 4)
    - every row references CORPUS_VERSION from corpus.server.ts
  </acceptance_criteria>
  <done>corpusSeedData filled with owner-complete, evidence-tiered, hedged, PHI-free content; corpus-lint green; human-reviewed.</done>
</task>

<task type="auto">
  <name>Task 2: Seed the corpus to Neon + assert non-null K</name>
  <files>remix-app/scripts/seed-corpus.ts</files>
  <read_first>
    - remix-app/scripts/seed-analyte-dictionary.ts (the idempotent-seed run analog)
    - .planning/phases/06-engine-promotion-confidence-graded-reports/06-VALIDATION.md §"Per-Task Verification Map" (the COUNT(*) WHERE evidence_tier IS NULL = 0 assertions — owned by this plan)
    - remix-app/tests/db/corpus-schema.test.ts (the DB-gated assertions; this plan adds the COUNT content assertions that must now go fully green)
  </read_first>
  <action>
    Run `cd remix-app && npm run db:seed-corpus` against live Neon (export DATABASE_URL_UNPOOLED first; do not `source .env` — the string contains `&`). Confirm idempotency by running it twice — the second run inserts zero new rows (ON CONFLICT DO NOTHING / check-before-insert). The seed populates genetic_variants, variant_protocol_map, metric_protocol_map with the reviewed corpusSeedData, each row stamped CORPUS_VERSION. Then add the content-level `COUNT(*) WHERE evidence_tier IS NULL = 0` assertions to tests/db/corpus-schema.test.ts (this plan owns them — 06-02 left only the structural NOT-NULL column) and run the DB-gated corpus-schema tests to prove non-null K over real seeded rows.
  </action>
  <verify>
    <automated>cd remix-app && npx vitest run tests/db/corpus-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run db:seed-corpus` exits 0; second run reports 0 new inserts (idempotent)
    - `SELECT COUNT(*) FROM variant_protocol_map WHERE evidence_tier IS NULL` returns 0 (DB-gated content test green — owned by this plan)
    - `SELECT COUNT(*) FROM metric_protocol_map WHERE evidence_tier IS NULL` returns 0
    - `SELECT COUNT(*) FROM genetic_variants WHERE confidence IS NULL` (or the evidence/impact non-null equivalent per ROADMAP SC1) returns 0
    - `cd remix-app && npx vitest run tests/db/corpus-schema.test.ts` exits 0
  </acceptance_criteria>
  <done>Corpus seeded idempotently to Neon; the content-level non-null K DB assertions (COUNT = 0) added here and green.</done>
</task>

<task type="auto">
  <name>Task 3: Retire genetics-knowledge.server.ts + re-point its 3 consumers to the corpus</name>
  <files>remix-app/app/lib/corpus.server.ts, remix-app/app/routes/_app/insights/genetics.tsx, remix-app/app/routes/_app/insights/index.tsx, remix-app/app/routes/_app/dashboard.tsx, remix-app/app/lib/genetics-knowledge.server.ts</files>
  <read_first>
    - .planning/phases/06-engine-promotion-confidence-graded-reports/06-RESEARCH.md §Open Questions #2 (genetics-knowledge retirement timing — retire within Phase 6)
    - remix-app/app/routes/_app/insights/genetics.tsx (loader lines 48-106 — the GENETIC_KNOWLEDGE join to replace)
    - remix-app/app/routes/_app/insights/index.tsx (loader line 67-69 join)
    - remix-app/app/routes/_app/dashboard.tsx (loader line 140-143 join)
    - remix-app/app/lib/corpus.server.ts (add a gene-keyed read helper that returns the corpus knowledge shape the 3 loaders expect)
  </read_first>
  <action>
    Add to corpus.server.ts a read helper `getGeneticKnowledgeByGene()` (or equivalent) that returns the geneticVariants corpus rows keyed/joinable by gene — the replacement for the `GENETIC_KNOWLEDGE[row.gene]` lookup the three loaders do. In genetics.tsx, index.tsx, and dashboard.tsx: replace `import { GENETIC_KNOWLEDGE } from "~/lib/genetics-knowledge.server"` with the corpus read (called in the loader, joined to subject genotypes by gene the same way). Preserve the existing rendered output shape (the Shape-C join: DB genotype rows + knowledge by gene) so the genetics/insights/dashboard pages render the same information — now sourced from the corpus table instead of the interim constant. Delete app/lib/genetics-knowledge.server.ts once no import of GENETIC_KNOWLEDGE remains. The three consumer paths are canonical and already correct in this plan's files_modified: `app/routes/_app/insights/genetics.tsx`, `app/routes/_app/insights/index.tsx`, `app/routes/_app/dashboard.tsx`.
  </action>
  <verify>
    <automated>cd remix-app && grep -rn "genetics-knowledge\|GENETIC_KNOWLEDGE" app/ ; test -z "$(grep -rn 'GENETIC_KNOWLEDGE' app/)" && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "GENETIC_KNOWLEDGE" remix-app/app/` returns nothing (constant fully retired)
    - `ls remix-app/app/lib/genetics-knowledge.server.ts` fails (file deleted)
    - genetics.tsx, insights/index.tsx, dashboard.tsx each import from `~/lib/corpus.server` and join corpus rows by gene in their loaders
    - `cd remix-app && npx tsc --noEmit` exits 0
    - `cd remix-app && npx vitest run` full suite exits 0
  </acceptance_criteria>
  <done>genetics-knowledge.server.ts deleted; all 3 consumers read genetics knowledge from the corpus; typecheck + full suite green.</done>
</task>

</tasks>

<verification>
- `cd remix-app && npx vitest run` — corpus-lint green over real content; DB-gated non-null K content assertions (COUNT = 0) green; full suite green after loader re-point
- `grep -rn "GENETIC_KNOWLEDGE" remix-app/app/` — empty (interim module retired, D-06)
- `npm run db:seed-corpus` idempotent against Neon
- corpus content is human-reviewed: evidence tiering defensible (D-07), PHI-free (Pitfall 4/D-06), non-imperative (RPT-03)
</verification>

<success_criteria>
ENG-02 content + ENG-03: the owner-complete evidence-tiered corpus is seeded into Neon with non-null K1–K4 on every rule (COUNT = 0 content assertions owned here); `genetics-knowledge.server.ts` is retired and its 3 consumers read from the corpus; the engine's `mapVariantToProtocol` + metric-rule evaluation now have real corpus data to grade against. Corpus is hedged + PHI-free (human-reviewed).
</success_criteria>

<output>
Create `.planning/phases/06-engine-promotion-confidence-graded-reports/06-03-SUMMARY.md` when done
</output>
