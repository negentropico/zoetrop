---
phase: 06-engine-promotion-confidence-graded-reports
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - remix-app/app/components/ui/KGradeBadge.tsx
  - remix-app/app/components/ui/DisclaimerCallout.tsx
  - remix-app/app/components/ui/RecommendationBlock.tsx
  - remix-app/app/components/shell/nav-tree.ts
  - remix-app/app/components/shell/Sidebar.tsx
  - remix-app/app/components/shell/MobileTopBar.tsx
autonomous: true
requirements: [RPT-02, RPT-03]

must_haves:
  truths:
    - "KGradeBadge renders K1–K4 as an evidence-tier pill using CSS vars only (visually distinct from StatusBadge)"
    - "DisclaimerCallout hard-codes the exact locked K4 disclaimer string with zero props"
    - "RecommendationBlock assembles the locked 'K{N} ({label}): {text}' inline body and renders DisclaimerCallout only for K4"
    - "Reports nav entry (file-text → /reports) is added; the BottomTab 6-item overflow is resolved"
  artifacts:
    - path: "remix-app/app/components/ui/KGradeBadge.tsx"
      provides: "Evidence-tier K1–K4 badge (chip + inline variants)"
      contains: "K_CONFIG"
    - path: "remix-app/app/components/ui/DisclaimerCallout.tsx"
      provides: "Locked K4 disclaimer callout"
      contains: "This recommendation is speculative (limited evidence). Discuss with a licensed practitioner before acting."
    - path: "remix-app/app/components/ui/RecommendationBlock.tsx"
      provides: "Per-finding recommendation card with inline K body"
      contains: "RecommendationBlock"
  key_links:
    - from: "remix-app/app/components/ui/RecommendationBlock.tsx"
      to: "remix-app/app/components/ui/DisclaimerCallout.tsx"
      via: "renders DisclaimerCallout when kLevel === 'K4'"
      pattern: "DisclaimerCallout"
    - from: "remix-app/app/components/shell/nav-tree.ts"
      to: "/reports"
      via: "NAV_TREE Reports group"
      pattern: "reports"
---

<objective>
Build the three net-new Phase 6 UI components (RPT-02/RPT-03, UI-SPEC Patterns 1–3) and extend the nav: `KGradeBadge` (evidence-tier pill, chip + inline, CSS-var colors distinct from StatusBadge), `DisclaimerCallout` (zero-props, hard-coded locked K4 disclaimer string), and `RecommendationBlock` (per-finding card assembling the locked `"K{N} ({label}): {text}"` inline body and rendering DisclaimerCallout for K4 only). Add the Reports nav entry (`file-text` → `/reports`) and resolve the BottomTab 6-item overflow by dropping "Import" from the mobile BottomTab (kept in the sidebar rail) per RESEARCH Open-Question #3.

Purpose: These are leaf UI primitives consumed by the report routes in 06-05 (interface-first ordering — components exist before the routes that import them). Building them in their own plan keeps the route-backend plan focused and lets this run in parallel with the corpus plan (disjoint files). The DisclaimerCallout string is what the corpus-lint source-assertion test (06-02) checks for.
Output: 3 UI components + nav extension (nav-tree + shell components).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-engine-promotion-confidence-graded-reports/06-CONTEXT.md
@.planning/phases/06-engine-promotion-confidence-graded-reports/06-PATTERNS.md
@.planning/phases/06-engine-promotion-confidence-graded-reports/06-UI-SPEC.md

<interfaces>
<!-- Component analogs + the locked specs. Extracted from codebase + UI-SPEC. -->

UI-SPEC Pattern 1 (KGradeBadge) props: { level: 'K1'|'K2'|'K3'|'K4'; variant?: 'chip'|'inline' }  default 'chip'
  colors (CSS vars only): K1 var(--ink)/var(--n-100) · K2 var(--focus-500)/var(--focus-50) · K3 var(--n-600)/var(--surface-sunken) · K4 var(--n-500)/var(--n-100)
  font: var(--font-mono) 11px(var(--text-2xs)) weight 400, letter-spacing 0.06em, uppercase, radius var(--radius-pill)
  chip: "K{N}" pill + " ({label})" muted mono after (NOT inside badge); inline: "K{N}" only
  aria-label="Evidence tier: K{N} — {label}"

UI-SPEC Pattern 2 (RecommendationBlock) props: { kLevel, recommendationText, source:'metric'|'variant', metricName?, metricStatus?, geneName?, genotype?, detectionConfidence?:'verified'|'inferred' }
  wrap in Card elevation="xs" padding="md"; K4 adds tone="mist"
  header row (flex gap-2 wrap mb-2): KGradeBadge chip · source name (metric name OR "{gene} ({genotype})", --text-sm w500) · StatusBadge (metric only) · SubBadge row (variant only)
  body LOCKED: "K{level} ({label}): {recommendationText}" — KGradeBadge variant="inline" + " ({label})" mono muted + ": " + text(Hanken 16px var(--ink) leading-1.5)
  SubBadge (Pattern 4): existing Badge — verified: tone="vital" variant="soft" + shield-check icon + "VERIFIED"; inferred: tone="neutral" variant="outline" + "INFERRED"; render only if detectionConfidence defined

UI-SPEC Pattern 3 (DisclaimerCallout) — NO props; bg var(--excess-bg); left-border 3px solid var(--energy); radius 0 right-rounded; padding 12px 16px; mt 12px; alert-triangle lucide 16px var(--energy-600) flex-shrink:0; text --text-sm var(--text-secondary) leading-1.5; role="note" aria-label="K4 speculative recommendation notice"
  LOCKED string (verbatim): "This recommendation is speculative (limited evidence). Discuss with a licensed practitioner before acting."

Analogs (read these files):
  app/components/ui/StatusBadge.tsx (lines 1-29 — CONFIG-table + fn pattern)
  app/components/ui/Badge.tsx (lines 23-63 — CSS-var TONES config + inline style object; SubBadge reuses Badge)
  app/components/ui/Card.tsx (lines 1-91 — elevation/padding/tone props)

Nav (UI-SPEC §Nav Extension): Reports | file-text | /reports | path.startsWith('/reports'). Becomes nav item 6.
  app/components/shell/nav-tree.ts NAV_TREE array (add Reports group); Sidebar.tsx + MobileTopBar.tsx consume it.
  RESEARCH Open-Q #3: drop "Import" from BottomTab (mobile), keep in sidebar rail.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KGradeBadge + DisclaimerCallout (CSS-var components)</name>
  <files>remix-app/app/components/ui/KGradeBadge.tsx, remix-app/app/components/ui/DisclaimerCallout.tsx</files>
  <read_first>
    - .planning/phases/06-engine-promotion-confidence-graded-reports/06-UI-SPEC.md §"Pattern 1: KGradeBadge" (props, visual spec, color table, rendering, accessibility) and §"Pattern 3: DisclaimerCallout" (visual spec, locked string)
    - .planning/phases/06-engine-promotion-confidence-graded-reports/06-PATTERNS.md §"app/components/ui/KGradeBadge.tsx" (K_CONFIG table) and §"app/components/ui/DisclaimerCallout.tsx" (the full zero-props implementation)
    - remix-app/app/components/ui/StatusBadge.tsx and remix-app/app/components/ui/Badge.tsx (the CSS-var config-table pattern to copy)
  </read_first>
  <action>
    Create KGradeBadge.tsx: props `{ level: 'K1'|'K2'|'K3'|'K4'; variant?: 'chip'|'inline' }` (default 'chip'). K_CONFIG record maps each level to { color, bg, label } using CSS vars ONLY (K1 var(--ink)/var(--n-100) Established; K2 var(--focus-500)/var(--focus-50) Probable; K3 var(--n-600)/var(--surface-sunken) Emerging; K4 var(--n-500)/var(--n-100) Speculative) — never Tailwind color classes (dark remap via cascade). Inline style: font var(--font-mono), var(--text-2xs), weight 400, letter-spacing 0.06em, text-transform uppercase, border-radius var(--radius-pill), line-height 1, padding 4px 8px (chip) / 2px 4px (inline). chip variant: render "K{N}" pill + a sibling muted mono "({label})" span AFTER the pill (not inside). inline variant: "K{N}" only. aria-label="Evidence tier: K{N} — {label}" on the chip span.
    Create DisclaimerCallout.tsx: NO props. Define `const K4_DISCLAIMER = "This recommendation is speculative (limited evidence). Discuss with a licensed practitioner before acting.";` (verbatim — corpus-lint asserts presence). Render a div role="note" aria-label="K4 speculative recommendation notice" with bg var(--excess-bg), left-border 3px solid var(--energy), radius 0 right-rounded (var(--radius-md) on the right), padding 12px 16px, marginTop 12, flex gap 8 align-items flex-start; alert-triangle from lucide-react at 16px color var(--energy-600) flex-shrink:0; text span var(--text-sm) var(--text-secondary) line-height 1.5 containing K4_DISCLAIMER.
  </action>
  <verify>
    <automated>cd remix-app && npx tsc --noEmit 2>&1 | grep -E "KGradeBadge|DisclaimerCallout" || echo "COMPONENTS_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "This recommendation is speculative (limited evidence). Discuss with a licensed practitioner before acting." remix-app/app/components/ui/DisclaimerCallout.tsx` matches verbatim
    - `grep -E "className=\"text-(blue|amber|red|green)" remix-app/app/components/ui/KGradeBadge.tsx` returns nothing (CSS vars only, no Tailwind color classes)
    - `grep -E "var\(--focus-500\)|var\(--ink\)" remix-app/app/components/ui/KGradeBadge.tsx` matches (token-based color)
    - `grep "aria-label" remix-app/app/components/ui/KGradeBadge.tsx` and `grep 'role="note"' remix-app/app/components/ui/DisclaimerCallout.tsx` both match
    - DisclaimerCallout takes no props (its component signature has an empty/`{}` param)
    - `cd remix-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>KGradeBadge (chip+inline, CSS-var, distinct from StatusBadge) and DisclaimerCallout (zero-props, locked string verbatim) compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: RecommendationBlock (inline K body assembly + K4 callout)</name>
  <files>remix-app/app/components/ui/RecommendationBlock.tsx</files>
  <read_first>
    - .planning/phases/06-engine-promotion-confidence-graded-reports/06-UI-SPEC.md §"Pattern 2: RecommendationBlock" (props, visual layout, header row, body text assembly, card wrapping) and §"Pattern 4: SubBadge"
    - .planning/phases/06-engine-promotion-confidence-graded-reports/06-PATTERNS.md §"app/components/ui/RecommendationBlock.tsx"
    - remix-app/app/components/ui/Card.tsx (elevation/padding/tone), remix-app/app/components/ui/StatusBadge.tsx, remix-app/app/components/ui/Badge.tsx (SubBadge reuse)
    - remix-app/app/components/ui/KGradeBadge.tsx, remix-app/app/components/ui/DisclaimerCallout.tsx (from Task 1)
  </read_first>
  <action>
    Create RecommendationBlock.tsx with the UI-SPEC Pattern 2 props exactly: `{ kLevel, recommendationText, source:'metric'|'variant', metricName?, metricStatus?, geneName?, genotype?, detectionConfidence? }`. Wrap in Card elevation="xs" padding="md"; if kLevel === 'K4' pass tone="mist". Header row (flex, align-items center, gap 8, flex-wrap, margin-bottom 8): KGradeBadge variant="chip" + source name (metric → metricName --text-sm weight 500; variant → "{geneName} ({genotype})" --text-sm weight 500) + StatusBadge for metricStatus (metric source only). Variant-only second row: SubBadge (the D-09 detection-confidence secondary annotation — verified vs inferred, subordinate to the headline K) via existing Badge — detectionConfidence 'verified' → Badge tone="vital" variant="soft" with shield-check icon (12px) + "VERIFIED"; 'inferred' → Badge tone="neutral" variant="outline" + "INFERRED"; render only if detectionConfidence is defined. Body (inline flow): KGradeBadge variant="inline" + " ({label})" Space Mono 11px var(--text-muted) + ": " var(--text-muted) + recommendationText Hanken 16px var(--ink) weight 400 line-height 1.5 — this is the LOCKED "K{N} ({label}): {text}" template; the K{N} prefix + (label) are component-rendered, NOT stored in recommendationText. If kLevel === 'K4', render <DisclaimerCallout /> below the body. Pull the label from CONFIDENCE_LEVELS in ~/types/genetics (the relabeled evidence-tier labels from 06-01).
  </action>
  <verify>
    <automated>cd remix-app && npx tsc --noEmit 2>&1 | grep -E "RecommendationBlock" || echo "BLOCK_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DisclaimerCallout" remix-app/app/components/ui/RecommendationBlock.tsx` matches and is gated on `kLevel === 'K4'`
    - `grep -E "KGradeBadge" remix-app/app/components/ui/RecommendationBlock.tsx` matches (used in header chip AND inline body)
    - the body assembles `({label}): ` around recommendationText (inline flow), with the K prefix component-rendered not concatenated into the corpus text
    - SubBadge render is guarded by `detectionConfidence` being defined (metric-source omits it)
    - `cd remix-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>RecommendationBlock renders the locked inline K body, the header (badge+source+status+subbadge), and the K4 DisclaimerCallout; compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add Reports nav entry + resolve BottomTab overflow</name>
  <files>remix-app/app/components/shell/nav-tree.ts, remix-app/app/components/shell/Sidebar.tsx, remix-app/app/components/shell/MobileTopBar.tsx</files>
  <read_first>
    - .planning/phases/06-engine-promotion-confidence-graded-reports/06-UI-SPEC.md §"Nav Extension" (Reports | file-text | /reports) and RESEARCH Open-Q #3 (drop Import from BottomTab)
    - remix-app/app/components/shell/nav-tree.ts (NAV_TREE array + the lucide icon import block + groupOfPath)
    - remix-app/app/components/shell/Sidebar.tsx and remix-app/app/components/shell/MobileTopBar.tsx (the BottomTab/mobile nav consumers — find where the 5-item bottom set is rendered)
  </read_first>
  <action>
    In nav-tree.ts: import `FileText` from lucide-react; add a Reports NavGroup to NAV_TREE — { id: "reports", label: "Reports", icon: FileText, base: "/reports", children: [{ label: "All reports", to: "/reports", end: true }, { label: "Generate", to: "/reports/generate" }] } (or exact:true single-link if a child list is not wanted — match the existing group shape). The full sidebar rail shows all groups including Reports. For the mobile BottomTab (Sidebar.tsx / MobileTopBar.tsx — whichever renders the bottom tab set), resolve the 6-item overflow by dropping "Import" from the BottomTab item set (Import stays reachable via the sidebar rail) per RESEARCH Open-Q #3 — so the BottomTab shows Dashboard, Metrics, Protocol, Insights, Reports. Do not remove Import from NAV_TREE (sidebar keeps it). Confirm the path-matching (`groupOfPath`/`isChildActive`) highlights Reports on /reports and deeper.
  </action>
  <verify>
    <automated>cd remix-app && grep -n "reports\|FileText" app/components/shell/nav-tree.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "id: \"reports\"|base: \"/reports\"" remix-app/app/components/shell/nav-tree.ts` matches
    - `grep "FileText" remix-app/app/components/shell/nav-tree.ts` matches (icon imported + used)
    - the mobile BottomTab item set no longer includes Import but the sidebar NAV_TREE still has the import group (Import reachable via rail)
    - `cd remix-app && npx tsc --noEmit` exits 0
    - `cd remix-app && npm run lint` exits 0
  </acceptance_criteria>
  <done>Reports group added to NAV_TREE (file-text → /reports), highlighted on /reports*; BottomTab overflow resolved by dropping Import from the mobile bottom tab (kept in sidebar).</done>
</task>

</tasks>

<verification>
- `cd remix-app && npx tsc --noEmit` — zero errors across the 3 components + nav
- `cd remix-app && npm run lint` — clean
- DisclaimerCallout contains the verbatim locked string (satisfies the 06-02 corpus-lint source assertion)
- KGradeBadge uses CSS-var colors only (distinct from StatusBadge teal/amber/red); RecommendationBlock gates DisclaimerCallout on K4
</verification>

<success_criteria>
RPT-02 / RPT-03 UI layer: KGradeBadge surfaces K1–K4 inline (not tooltip), RecommendationBlock assembles the locked "K{N} ({label}): {text}" body, DisclaimerCallout carries the exact K4 disclaimer with zero props; Reports nav entry added and the mobile BottomTab overflow resolved. Components are ready for the report routes in 06-05.
</success_criteria>

<output>
Create `.planning/phases/06-engine-promotion-confidence-graded-reports/06-04-SUMMARY.md` when done
</output>
