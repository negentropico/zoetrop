---
context: phase
phase: 06-engine-promotion-confidence-graded-reports
workflow: plan-phase
position: revision-loop iteration 1/3 (post first plan-checker)
status: in_progress
last_updated: 2026-06-11T22:43:45Z
---

# Resume: /gsd-plan-phase 6 — paused mid revision-loop

We are inside `/gsd-plan-phase 6`. Research → Validation → Patterns → 5 Plans were written and committed; the plan-checker ran once and returned **5 blockers + 3 warnings**. The revision pass was dispatched but **never applied** (the planner subagent had already terminated, and `SendMessage` to it failed). On resume, apply the (already-decided) revision below, re-check, then finish.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| SendMessage to a finished Agent | After a subagent returns its final result it is no longer addressable; `SendMessage({to: 'phase6-planner'})` failed with "No agent named … is currently addressable". | advisory | To revise plans, spawn a FRESH `gsd-planner` (model: opus) in revision mode and embed the FULL context (files_to_read + verbatim revision instructions) — a fresh agent starts with zero context. |
| Researcher deviated from a locked decision | RESEARCH.md recommended `engine.ts`; CONTEXT D-01 + ROADMAP SC2 lock `engine.server.ts`. The planner followed research and broke the locked decision. | advisory | Resolution decided: honor D-01 → `engine.server.ts`. See B1 below. |

<current_state>
`/gsd-plan-phase 6`, revision loop iteration 1 of 3. Last completed action: `gsd-plan-checker` returned `## ISSUES FOUND` (5 blockers, 3 warnings). The revision instructions were composed and the resolutions DECIDED, but not yet applied to the plan files. `prev_issue_count = Infinity → 8` (no stall). The 5 PLAN.md files on disk are the UNREVISED versions (commit 77665d2).
</current_state>

<completed_work>
- Research → `06-RESEARCH.md` (gsd-phase-researcher, HIGH confidence) — commit 73a8d67. Oxford CEBM → K1–K4; engine extraction is re-export; 2 separate corpus tables; deterministic report path; Validation Architecture section included.
- Validation → `06-VALIDATION.md` — commit 56dd8b2. (`nyquist_compliant: false` — flip to true once revised plans confirm Wave-0 coverage.)
- Patterns → `06-PATTERNS.md` (gsd-pattern-mapper) — commit 7b4b463. 20 files classified, 19 analogs; auth 4-step sequence, Drizzle table style, CSS-var UI pattern, enum-collision warning.
- Plans → 5 × PLAN.md in 3 waves (gsd-planner, opus) — commit 77665d2.
- Plan check → gsd-plan-checker → ISSUES FOUND (5 blockers, 3 warnings).

**Plan map:** 06-01 engine extraction + report types (ENG-01) · 06-02 schema + corpus read layer + Wave-0 tests + [BLOCKING] migration (ENG-02) · 06-03 corpus authoring/seed + retire genetics-knowledge (ENG-02/03) · 06-04 report UI components + nav (RPT-02/03) · 06-05 generateReport + routes + threat model (RPT-01/02/03, ENG-03). Waves: W1=06-01,06-02 · W2=06-03,06-04 · W3=06-05.
</completed_work>

<remaining_work>
1. **Apply revision pass 1** (resolutions below — all decided, do not re-derive).
2. Re-run `gsd-plan-checker` (workflow step 10/11). If it passes → continue; if new issues, iterate (max 3, currently on iteration 1).
3. Update `06-VALIDATION.md` frontmatter `nyquist_compliant: true` once the revised plans confirm all four Wave-0 files are planned (leave `wave_0_complete: false` — that flips during execution).
4. **Requirements coverage gate** (step 13) + **Decision coverage gate** (step 13a, BLOCKING): `gsd-sdk query check.decision-coverage-plan "<phase_dir>" "<context_path>"`.
5. `gsd-sdk query state.planned-phase --phase 06 --name engine-promotion-confidence-graded-reports --plans 5` (step 13b).
6. `gsd-sdk query roadmap.annotate-dependencies 06` (step 13c).
7. Commit (step 13d, commit_docs=true): `gsd-sdk query commit "docs(06): revise phase plan per plan-checker" --files <phase_dir>/*-PLAN.md .planning/STATE.md .planning/ROADMAP.md` (explicit --files; never `git add -A`).
8. Post-planning gap analysis (step 13e) + present `<offer_next>` (step 14). **No auto-advance** (`--auto`/`--chain` absent, `auto_advance=false`) → manual offer to run `/gsd:execute-phase 6`.
</remaining_work>

## Revision Instructions (verbatim — embed these in the fresh gsd-planner spawn)

Make TARGETED edits to the 5 existing PLAN.md files; do NOT replan from scratch.

**B1 — Engine module name → `app/lib/engine.server.ts` (NOT `engine.ts`). DECIDED.**
D-01 + ROADMAP SC2 both lock `engine.server.ts`; D-01 says the suffix is bundle-hygiene only and the module must stay import-pure regardless (suffix is a Vite client-bundle convention; imports fine from vitest/Node so SC2 holds). Rename `engine.ts` → `engine.server.ts` EVERYWHERE in all 5 plans (`files_modified`, `<action>`, `<read_first>`, `<acceptance_criteria>`, must_haves, every cross-plan import ref). In 06-01, REPLACE the criterion asserting `engine.server.ts` does NOT exist with: `ls remix-app/app/lib/engine.server.ts` succeeds AND `grep -E "from ['\"](drizzle|@react-router|~/db|~/lib/db)" remix-app/app/lib/engine.server.ts` returns nothing (import-purity). Drop any task step that would edit ROADMAP SC2 — none is needed.

**B2 — Split 06-02 Task 1 (currently 6 files / breaks RED-first).** First task creates Wave-0 test stubs RED-first: `tests/lib/corpus-lint.test.ts` + `tests/db/corpus-schema.test.ts` (confirm `app/types/report.ts` exists / depend on 06-01 if it owns it). Second task: schema enum+tables + `corpus.server.ts` + `data.server.ts` extension + `seed-corpus.ts` scaffold. ≤5 files/task; production task depends on the test-stub task.

**B3 — Add ENG-03 metric-rule eval unit test.** Assert: a synthetic deficient metric matching a `metricProtocolMap` rule → `GradedRecommendation` with correct `evidenceTier`, `source: 'metric'`, `sourceContext.metricName`. Place where metric-rule eval lives (engine.server.ts test, else 06-05 report-generator test). Add matching `<behavior>` + `<acceptance_criteria>`.

**B4 — DisclaimerCallout assertion → `test.skip` (or `test.todo`).** In 06-02 `corpus-lint.test.ts`, the DisclaimerCallout source assertion depends on 06-04; make it `test.skip` so `vitest run` exits 0 at 06-02 (consistent with "tests pass"). Imperative-pattern lint over corpus arrays runs normally and must pass.

**W1 — Move content truths out of 06-02.** Remove `SELECT COUNT(*) … WHERE evidence_tier IS NULL = 0` from 06-02 `must_haves.truths` (tables empty at 06-02). Keep structural truths (enum exists, tables exist, column NOT NULL, corpus tables carry no tenant/subject FK). Ensure 06-03 owns the non-null-K content assertions.

**W2 — Add reports sub-layout (convention).** Add `remix-app/app/routes/_app/reports/layout.tsx` to 06-05 `files_modified`; register the 3 routes under `layout("routes/_app/reports/layout.tsx", [ route("reports", …), route("reports/generate", …), route("reports/:reportId", …) ])`.

**W3** — folds into B3 (06-05 generator test K4 example must exercise the metric-rule path, not only variant).

**Also** — fix 06-03 Task 3 `files_modified`: make the retired `genetics-knowledge.server.ts` consumer filename canonical/correct (the "typo" parenthetical in the action is confusing); re-point consumers to the corpus read layer.

Have the planner return `## REVISION COMPLETE` confirming all 6 req IDs (ENG-01/02/03, RPT-01/02/03) still appear in plan `requirements`.

<decisions_made>
- **DB target = Neon** (not Supabase) for Phase 6 — explicit in ROADMAP SC1/SC3 + D-03/D-05. Resolves the standing memory note to confirm DB target before Phase 6.
- **Engine = `engine.server.ts`** honoring locked D-01 + ROADMAP SC2 (overrides the researcher's `engine.ts` recommendation).
- All gates cleared: closed-phase (Pending) · AI-SPEC skipped (deterministic engine, no runtime LLM; build-time corpus LLM is human-reviewed) · UI (06-UI-SPEC.md approved) · schema-push (Drizzle → [BLOCKING] db:generate→db:migrate in 06-02) · nyquist (06-VALIDATION.md) · security (ASVS L1, block-on high → threat_model on report-path plans).
</decisions_made>

<blockers>
- SendMessage to finished planner failed → on resume spawn a FRESH gsd-planner (opus) with full context embedded. No human action pending.
</blockers>

## Required Reading (in order)
1. `.planning/phases/06-engine-promotion-confidence-graded-reports/06-CONTEXT.md` — locked D-01..D-19 (authoritative)
2. This file's "Revision Instructions" — the decided fixes to apply
3. The 5 `06-0X-PLAN.md` files — the artifacts to revise
4. `.planning/phases/06-engine-promotion-confidence-graded-reports/06-RESEARCH.md` + `06-PATTERNS.md` + `06-VALIDATION.md` + `06-UI-SPEC.md` — supporting contracts
5. `$HOME/.claude/get-shit-done/workflows/plan-phase.md` steps 10–15 — the remaining workflow

## Infrastructure State
- No running servers/watchers. Nothing to restart.
- Git: on branch `003-remix-foundation`. Plans committed at 77665d2. Only `.planning/STATE.md` is dirty (pre-existing, not this session). Untracked design-system round2 files exist (unrelated — do not touch).

<context>
Config is "yolo" but `auto_advance=false` and no `--auto`/`--chain`, so finish with the manual `<offer_next>` block (offer `/gsd:execute-phase 6`), not auto-advance. Models: researcher/checker=sonnet, planner=opus. The revision is low-risk and fully specified; a fresh agent can apply it directly. Re-run the checker after — expect it to pass or surface only minor items; we are on iteration 1 of 3.
</context>

<next_action>
Spawn a fresh `gsd-planner` (model: opus, mode: revision) with: the files_to_read list (5 PLANs + CONTEXT + RESEARCH + PATTERNS + UI-SPEC + VALIDATION + ROADMAP + REQUIREMENTS + CLAUDE.md) and the verbatim "Revision Instructions" above. Then re-run gsd-plan-checker (sonnet) and proceed through plan-phase steps 13–14.
</next_action>
