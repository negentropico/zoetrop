---
phase: 01-schema-baseline-engine-tests-auth-spike
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - remix-app/migrations/0000_baseline.sql
  - remix-app/migrations/meta/_journal.json
  - remix-app/migrations/meta/0000_snapshot.json
autonomous: false
requirements: [DATA-03]

must_haves:
  truths:
    - "remix-app/migrations/ directory exists and is committed to git"
    - "The generated baseline SQL contains CREATE TABLE for all 8 tables (metrics, protocol_versions, protocol_changes, milestones, supplements, supplement_log, correlations, cessation_log)"
    - "The generated baseline SQL contains CREATE TYPE for all 7 enums (metric_category, metric_status, data_source, sync_status, supplement_tier, protocol_change_type, cessation_phase)"
    - "`drizzle-kit migrate --dry-run` completes without error against the Neon project"
    - "The baseline is a pure as-is snapshot — no schema drift cleanup (syncStatus/syncVersion, isActive int, as any) is applied here"
  artifacts:
    - path: "remix-app/migrations/0000_baseline.sql"
      provides: "Full CREATE TYPE + CREATE TABLE DDL snapshot of the 8 deployed tables and 7 enums"
      contains: "CREATE TABLE"
    - path: "remix-app/migrations/meta/_journal.json"
      provides: "Drizzle migration journal (hash needed to mark the baseline applied on prod)"
      contains: "\"entries\""
    - path: "remix-app/migrations/meta/0000_snapshot.json"
      provides: "Schema snapshot for future drizzle-kit diff tracking"
  key_links:
    - from: "remix-app/db/schema.ts"
      to: "remix-app/migrations/0000_baseline.sql"
      via: "drizzle-kit generate reads schema, emits DDL into out: './migrations'"
      pattern: "CREATE TABLE"
    - from: "remix-app/migrations/0000_baseline.sql"
      to: "Neon project"
      via: "drizzle-kit migrate --dry-run validates without applying"
      pattern: "migrate"
---

<objective>
Create the committed Drizzle migrations baseline (DATA-03): a pure as-is snapshot of the schema already deployed to Neon (8 tables, 7 enums), generated via `drizzle-kit generate`, verified with `drizzle-kit migrate --dry-run`. This is the FIRST committed migration; from here, all future schema change goes through migrations.

This phase introduces NO schema change. The baseline must faithfully record "this is what exists" — including the vestigial `syncStatus`/`syncVersion` columns and the `isActive: integer` relic. All schema drift cleanup is deferred to Phase 4 (D-09). There is NO `drizzle-kit push` anywhere in this plan.

Purpose: Establish the migrations source of truth that Phases 3–6 depend on for every schema change.
Output: `remix-app/migrations/0000_baseline.sql` + `migrations/meta/` (journal + snapshot), committed; a verified `migrate --dry-run`; the production `__drizzle_migrations` tracking record marked applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-CONTEXT.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-RESEARCH.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-PATTERNS.md
@CLAUDE.md
@remix-app/drizzle.config.ts
@remix-app/db/schema.ts

<interfaces>
<!-- drizzle.config.ts is already fully configured. The migrations dir just does not exist yet. -->
From remix-app/drizzle.config.ts:
- dialect: 'postgresql'
- schema: './db/schema.ts'
- out: './migrations'   (relative to remix-app/ → final path remix-app/migrations/)
- dbCredentials.url: process.env.NETLIFY_DATABASE_URL || process.env.DATABASE_URL

Existing package.json scripts: db:generate (drizzle-kit generate), db:migrate (drizzle-kit migrate). drizzle-kit installed: 0.31.8.

Expected DDL targets (verify after generate):
- 8 tables: metrics, protocol_versions, protocol_changes, milestones, supplements, supplement_log, correlations, cessation_log
- 7 enums: metric_category, metric_status, data_source, sync_status, supplement_tier, protocol_change_type, cessation_phase
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate baseline migration and verify it covers all 8 tables + 7 enums</name>
  <files>remix-app/migrations/0000_baseline.sql, remix-app/migrations/meta/_journal.json, remix-app/migrations/meta/0000_snapshot.json</files>
  <read_first>
    - remix-app/db/schema.ts (the 8 tables + 7 enums being snapshotted)
    - remix-app/drizzle.config.ts (out: './migrations' already set)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-RESEARCH.md (§Stream 3 command sequence, §What Gets Generated, Pitfall 1, Assumption A2)
  </read_first>
  <action>From `remix-app/`, run `npm run db:generate` (which runs `drizzle-kit generate`). This reads `db/schema.ts`, diffs against an empty prior state, and emits the full CREATE TYPE + CREATE TABLE DDL, creating the `migrations/` directory automatically. The generated SQL file may be named `0000_<random-tag>.sql` by drizzle-kit — leave whatever name it generates; do NOT hand-rename to `0000_baseline.sql` if drizzle-kit chose a different tag (the journal references the actual filename). Inspect the generated `.sql` and confirm it contains CREATE TABLE for all 8 tables (metrics, protocol_versions, protocol_changes, milestones, supplements, supplement_log, correlations, cessation_log) and CREATE TYPE for all 7 enums (metric_category, metric_status, data_source, sync_status, supplement_tier, protocol_change_type, cessation_phase). Per D-08/D-09 this is a pure as-is snapshot: the vestigial `sync_status` enum, `syncStatus`/`syncVersion` columns, and `isActive: integer` MUST appear in the SQL exactly as generated — do NOT edit the SQL to remove them. Do NOT run `drizzle-kit push`.</action>
  <verify>
    <automated>cd remix-app && test -d migrations && SQL=$(ls migrations/*.sql | head -1) && for t in metrics protocol_versions protocol_changes milestones supplements supplement_log correlations cessation_log; do grep -q "CREATE TABLE.*\"$t\"\|CREATE TABLE IF NOT EXISTS \"$t\"\|CREATE TABLE \"$t\"" "$SQL" || { echo "MISSING TABLE $t"; exit 1; }; done && for e in metric_category metric_status data_source sync_status supplement_tier protocol_change_type cessation_phase; do grep -q "$e" "$SQL" || { echo "MISSING ENUM $e"; exit 1; }; done && test -f migrations/meta/_journal.json && echo "OK 8 tables 7 enums journal"</automated>
  </verify>
  <done>`remix-app/migrations/` exists with one `.sql` file covering all 8 tables and 7 enums, plus `meta/_journal.json` and `meta/0000_snapshot.json`; no schema cleanup applied; no push run.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the baseline with drizzle-kit migrate --dry-run</name>
  <files>remix-app/migrations/0000_baseline.sql</files>
  <read_first>
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-RESEARCH.md (§Stream 3 Step 3 validation, Pitfall 1 "already exists")
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-VALIDATION.md (§Per-Task Verification Map — DATA-03 dry-run is the success bar)
  </read_first>
  <action>From `remix-app/`, run `npx drizzle-kit migrate --dry-run` (requires `NETLIFY_DATABASE_URL` or `DATABASE_URL` to be set in the environment). The dry-run validates the migration plan against the Neon project WITHOUT applying any DDL — it must complete with exit 0 and emit no error. This is the ROADMAP Phase 1 success bar for DATA-03. Do NOT run a real (non-dry-run) `drizzle-kit migrate` against production in this task — production already has the tables, so a real migrate would fail with "relation already exists" (Pitfall 1); marking it applied on prod is the separate Task 3. If the dry-run reports a problem with the generated SQL, the SQL itself is wrong — return to Task 1 to fix before proceeding.</action>
  <verify>
    <automated>cd remix-app && npx drizzle-kit migrate --dry-run; echo "dry-run-exit:$?"</automated>
  </verify>
  <done>`drizzle-kit migrate --dry-run` exits 0 against the Neon project with no error.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Mark the baseline applied on production Neon (touches live DB)</name>
  <read_first>
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-RESEARCH.md (§Stream 3 Step 4 + Pitfall 1 — production apply protocol)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-PATTERNS.md (§migrations — production apply pattern, the __drizzle_migrations INSERT)
    - remix-app/migrations/meta/_journal.json (source of the migration hash)
  </read_first>
  <what-built>
    The baseline migration is generated and dry-run-verified (Tasks 1–2). Production Neon already contains all 8 tables (they were created outside the migrations workflow), so running a real `drizzle-kit migrate` against prod would fail with "relation already exists" (Pitfall 1). The migration must instead be marked as ALREADY APPLIED by inserting its tracking record into `__drizzle_migrations` — a write against the LIVE production database, which is why this is a human-gated checkpoint, not an autonomous task.
  </what-built>
  <how-to-verify>
    1. Read the migration hash from `remix-app/migrations/meta/_journal.json` (the `entries[0]` tag/hash for migration index 0).
    2. Connect to the PRODUCTION Neon database (Drizzle Studio: `npm run db:studio`, or psql with the production connection string). Confirm you are on production, NOT a branch.
    3. Insert the tracking record so drizzle-kit treats the baseline as applied:
       `INSERT INTO __drizzle_migrations (hash, created_at) VALUES ('<hash from _journal.json>', extract(epoch from now()) * 1000);`
       (If `__drizzle_migrations` does not yet exist on prod, create it per drizzle-kit's schema, or run `drizzle-kit migrate` against a throwaway clean Neon branch to let drizzle materialize the table shape, then replicate the single INSERT on prod.)
    4. Re-run `cd remix-app && npx drizzle-kit migrate --dry-run` and confirm it reports NO pending migrations (the baseline is now recorded as applied).
    5. Do NOT run a real `drizzle-kit migrate` and do NOT run `drizzle-kit push` against production at any point.
  </how-to-verify>
  <verify>
    <human-check>The `__drizzle_migrations` row exists on production with the baseline hash; `drizzle-kit migrate --dry-run` now reports zero pending migrations; no DDL was executed against prod.</human-check>
  </verify>
  <resume-signal>Type "applied" once the tracking record is inserted and the dry-run shows no pending migrations, or describe any blocker.</resume-signal>
  <done>The baseline is recorded as applied in production `__drizzle_migrations`; future `drizzle-kit migrate` will skip it; no destructive DDL ran against prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → production Neon database | Migration tooling holds production DB credentials and can execute DDL against live data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | drizzle-kit migrate against production | mitigate | Task 2 uses `--dry-run` only (no DDL applied); the real production touch (Task 3) is a blocking-human checkpoint that inserts a tracking row, never runs CREATE TABLE against prod; `drizzle-kit push` is explicitly forbidden everywhere in this plan (Pitfall 1) |
| T-01-03 | Information Disclosure | production DB connection string in env | accept | Credentials are read from `NETLIFY_DATABASE_URL`/`DATABASE_URL` already present in the operator environment; this plan adds no secret to source and commits no connection string |
| T-01-04 | Tampering | wrong hash inserted into __drizzle_migrations | mitigate | Task 3 reads the hash directly from `migrations/meta/_journal.json` and confirms via a post-insert `--dry-run` showing zero pending migrations |
</threat_model>

<verification>
- `remix-app/migrations/` exists with the generated `.sql` (8 tables, 7 enums) + `meta/_journal.json` + `meta/0000_snapshot.json`.
- `drizzle-kit migrate --dry-run` exits 0 with no error (DATA-03 success bar).
- Production `__drizzle_migrations` has the baseline tracking row; post-insert dry-run reports no pending migrations.
- No schema cleanup was applied; no `drizzle-kit push` was run.
</verification>

<success_criteria>
- DATA-03 satisfied: a committed `migrations/` baseline exists; `drizzle-kit migrate --dry-run` is clean against Neon.
- The baseline is a faithful as-is snapshot (vestiges intact, deferred to Phase 4 per D-09).
- Production is reconciled to the migrations workflow via the tracking record, without destructive DDL.
</success_criteria>

<output>
Create `.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-02-SUMMARY.md` when done.
</output>
