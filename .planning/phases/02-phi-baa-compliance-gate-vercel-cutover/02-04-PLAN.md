---
phase: 02-phi-baa-compliance-gate-vercel-cutover
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - docs/COMPLIANCE-RUNBOOK.md
autonomous: false
requirements: []
track: B
re_scoped: "2026-06-08 — pilot-first. Original Track B (production deploy + pgAudit sample via Neon Support + full gate review) had its pgAudit/HIPAA half DEFERRED to Phase 7. This plan now proves the pilot deploy baseline only."
user_setup:
  - service: vercel
    why: "Trigger + confirm the production deploy on Vercel (pilot baseline)"
    dashboard_config:
      - task: "Trigger a production deploy (git push to the production branch or `vercel --prod`) and confirm it succeeds"
        location: "Vercel Dashboard -> Project -> Deployments (or `vercel --prod`)"

must_haves:
  truths:
    - "The app is deployed and reachable on Vercel (https://zoetrop.vercel.app returns 200) — Netlify is retired"
    - "DB connectivity from the deployed app to the existing standard-tier Neon project is confirmed"
    - "The Phase-7 hardening deferral (Neon HIPAA + BAAs + pgAudit verification + RLS) is recorded in docs/COMPLIANCE-RUNBOOK.md with its trigger (before first external client's PHI)"
    - "The automatable Phase-2 baseline re-checks pass (netlify.toml gone, vercelPreset present, vercel.app in docs, npm test + npm run build green)"
  artifacts:
    - path: "docs/COMPLIANCE-RUNBOOK.md"
      provides: "Production-deploy confirmation (date/URL/200/DB-connectivity) + recorded Phase-7 deferral"
      contains: "Production deploy"
---

<objective>
Finalize the Phase 2 pilot deploy baseline: trigger the production Vercel deploy and confirm the app + DB connectivity are live, confirm the Phase-7 hardening deferral is recorded, and run the automatable baseline re-checks. The pgAudit sample / Neon Support ticket and all BAA/HIPAA work are DEFERRED to Phase 7 (pre-client gate) per the 2026-06-08 re-scope — they are NOT done here.

Purpose: closes the Phase 2 deploy baseline (a live single-user production deploy on standard-tier infra) so the engine-first build can continue. Phase 2 is then complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-CONTEXT.md
@docs/COMPLIANCE-RUNBOOK.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Trigger production Vercel deploy + confirm app and DB connectivity</name>
  <read_first>
    - docs/COMPLIANCE-RUNBOOK.md (Pilot Deploy Baseline section — env vars set in 02-03)
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md (## Standard Stack A — Neon Pool on Vercel gotcha + Pitfall 3 cold-start)
  </read_first>
  <action>
    User-action verify (Claude can build locally but cannot click Vercel's deploy UI). Trigger the production deploy (push to the production branch or `vercel --prod` from `remix-app/`); confirm the Vercel build SUCCEEDED with the `@vercel/react-router` preset; confirm `curl -I https://zoetrop.vercel.app` returns 200; confirm DB connectivity to the existing Neon project (route exercise or a `drizzle-kit` connectivity check against `DATABASE_URL_UNPOOLED`). Record the deploy date, URL, 200-OK, and DB-connectivity confirmation in docs/COMPLIANCE-RUNBOOK.md (Pilot Deploy Baseline section) — NO secrets. Note any cold-start/pool issue for the Phase 4 refactor (DATA-01) without fixing it here.
  </action>
  <verify>
    <human-check>curl to https://zoetrop.vercel.app returns 200 from a successful preset build; DB connectivity to the existing Neon project confirmed; deploy date/URL/200/connectivity recorded in the runbook.</human-check>
  </verify>
  <acceptance_criteria>
    `https://zoetrop.vercel.app` returns HTTP 200 from a successful Vercel production deploy built with the `@vercel/react-router` preset; DB connectivity to the existing standard-tier Neon project is confirmed; Netlify retired. The runbook Pilot Deploy Baseline records the deploy date, URL, 200-OK, and DB-connectivity — NO secrets.
  </acceptance_criteria>
  <resume-signal>Type "approved — zoetrop.vercel.app returns 200, DB connectivity confirmed, recorded in runbook" or describe the failure.</resume-signal>
  <done>Live Vercel production deploy (200 OK) + confirmed DB connectivity, recorded; Netlify retired.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the Phase-7 deferral is recorded + run baseline re-checks</name>
  <read_first>
    - docs/COMPLIANCE-RUNBOOK.md (Deferral Decision banner + Pilot Deploy Baseline section)
    - .planning/ROADMAP.md (Phase 2 success criteria; Phase 7 gate)
  </read_first>
  <action>
    Final baseline review (Claude runs the automatable checks; the human confirms). Confirm docs/COMPLIANCE-RUNBOOK.md carries the Deferral Decision banner pointing the BAA/HIPAA/pgAudit/RLS hardening at Phase 7 (trigger: before the first external client's PHI), and that the Pilot Deploy Baseline section is filled in. Run the automatable Phase-2 baseline re-checks below. Declare Phase 2 done when all pass and the deferral is recorded.
  </action>
  <verify>
    <automated>cd remix-app && npm test && npm run build</automated>
    <human-check>From repo root all true: test ! -f netlify.toml; grep -q vercelPreset remix-app/react-router.config.ts; grep -q vercel.app CLAUDE.md; grep -q vercel.app docs/PLATFORM.md; grep -q "Deferral Decision" docs/COMPLIANCE-RUNBOOK.md. Human confirms the Pilot Deploy Baseline records the deploy + env-vars-set.</human-check>
  </verify>
  <acceptance_criteria>
    The runbook records the Phase-7 hardening deferral (banner) and the completed Pilot Deploy Baseline; the automatable re-checks (`test ! -f netlify.toml`, `vercelPreset` present, `vercel.app` in both docs, `npm test` + `npm run build` green) all pass. Phase 2 (Vercel Cutover + Pilot Deploy Baseline) is complete; all PHI/BAA hardening is tracked in Phase 7.
  </acceptance_criteria>
  <resume-signal>Type "approved — baseline complete, hardening deferral recorded, Phase 2 done" or list what's open.</resume-signal>
  <done>Phase 2 deploy baseline proven; Phase-7 deferral recorded; automatable re-checks green.</done>
</task>

</tasks>

<verification>
- `curl -I https://zoetrop.vercel.app` returns 200; Vercel Deployments shows a successful production build with the preset.
- DB connectivity to the existing standard-tier Neon project confirmed post-deploy.
- docs/COMPLIANCE-RUNBOOK.md: Deferral Decision banner present (Phase 7 gate) + Pilot Deploy Baseline filled in.
- Automatable baseline re-checks pass: `test ! -f netlify.toml`; `grep -q vercelPreset remix-app/react-router.config.ts`; `grep -q vercel.app CLAUDE.md docs/PLATFORM.md`; `cd remix-app && npm test`; `cd remix-app && npm run build`.
</verification>

<success_criteria>
- Successful Vercel production deploy (200 OK) + confirmed DB connectivity on standard-tier infra; Netlify retired.
- Phase-7 hardening deferral recorded in the runbook with its trigger.
- Automatable baseline re-checks green. Phase 2 complete.
</success_criteria>

<output>
Create `.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-04-SUMMARY.md` when done.
</output>
