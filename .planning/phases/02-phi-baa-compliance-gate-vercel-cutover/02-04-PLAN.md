---
phase: 02-phi-baa-compliance-gate-vercel-cutover
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - docs/COMPLIANCE-RUNBOOK.md
autonomous: false
requirements: [COMP-02, COMP-03]
track: B
user_setup:
  - service: vercel
    why: "Trigger + confirm the production deploy on Vercel (SC#5)"
    dashboard_config:
      - task: "Trigger a production deploy (git push to the production branch or Vercel CLI) and confirm it succeeds"
        location: "Vercel Dashboard -> Project -> Deployments (or `vercel --prod`)"
  - service: neon
    why: "Retrieve a pgAudit log sample for SC#4 (audit logs are NOT self-serve)"
    dashboard_config:
      - task: "Open a Neon Support ticket requesting an audit-log sample for a test write on the metrics table"
        location: "Neon Console -> Support"

must_haves:
  truths:
    - "The app is deployed and reachable on Vercel (https://zoetrop.vercel.app returns 200) — Netlify is retired"
    - "DB connectivity from the deployed Vercel app is confirmed against the existing Neon project"
    - "A pgAudit log sample proves an entry records {user, table, operation, timestamp} and does NOT record bind parameters (log_parameter=off)"
    - "Every executed action + the pgAudit sample is recorded in docs/COMPLIANCE-RUNBOOK.md (no secrets); the phase gate is auditable"
    - "Locked decisions honored: D-05 (confirm a green Vercel production deploy + DB connectivity before the cutover is done), D-12 (pgAudit entry records {user, table, operation, timestamp} with log_parameter=off), D-13 (Phase 3 carry-forward for PHI read-access logging confirmed recorded)"
  artifacts:
    - path: "docs/COMPLIANCE-RUNBOOK.md"
      provides: "Production-deploy confirmation + pgAudit verification sample + Neon Support ticket ref"
      contains: "AUDIT: SESSION"
  key_links:
    - from: "deployed Vercel app"
      to: "the existing Neon project"
      via: "DATABASE_URL runtime connectivity (health check)"
      pattern: "DATABASE_URL"
    - from: "pgAudit log sample"
      to: "SC#4 fields {user, table, operation, timestamp} + log_parameter=off"
      via: "Neon Support ticket sample pasted into runbook"
      pattern: "log_parameter"
---

<objective>
Finalize and prove the Phase 2 gate: trigger the production Vercel deploy and confirm the app + DB connectivity are live (SC#5 deploy half), retrieve the pgAudit log sample via a Neon Support ticket to prove `{user, table, operation, timestamp}` are logged but bind parameters are NOT (SC#4 / COMP-03), and record everything in `docs/COMPLIANCE-RUNBOOK.md`. This Wave 3 plan depends on the cutover code (02-01), the runbook scaffold (02-02), and the env/HIPAA setup (02-03). It is Track B — the deliverable is the verified, recorded gate, not code.

Purpose: Closes SC#4 and the deploy/connectivity half of SC#5, and produces the final auditable proof-of-gate the verifier checks. After this plan, the compliance gate that blocks Phase 3 (RLS retrofit) and Phase 5 (lab ingest) is satisfied and recorded.

Output: A live Vercel production deploy with confirmed DB connectivity; a pgAudit verification sample in the runbook; the runbook complete (all BAA dates, pgAudit sample, Phase 3 carry-forward).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-CONTEXT.md
@.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md
@.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-VALIDATION.md
@docs/COMPLIANCE-RUNBOOK.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Trigger production Vercel deploy + confirm app and DB connectivity</name>
  <read_first>
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md (## Standard Stack A — Neon Pool on Vercel gotcha + Pitfall 3; ## Validation Architecture — post-deploy connectivity check; D-05 must confirm green deploy + DB connectivity)
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-VALIDATION.md (Wave 0 — post-deploy DB connectivity check)
  </read_first>
  <action>
    User-action verify (Claude can build locally but cannot click Vercel's deploy UI or guarantee the live runtime). Trigger the production deploy (push to the production branch or `vercel --prod` from `remix-app/`), confirm the Vercel build SUCCEEDED with the `@vercel/react-router` preset, confirm `curl -I https://zoetrop.vercel.app` returns 200, and confirm DB connectivity to the existing Neon project (route exercise or a drizzle-kit connectivity check against `DATABASE_URL_UNPOOLED` — do NOT assume the Pool singleton works without testing, per Pitfall 3). Record the deploy date, URL, 200-OK, and DB-connectivity confirmation in `docs/COMPLIANCE-RUNBOOK.md` (no secrets); note any cold-start/pool issue for the Phase 4 refactor without fixing it here. Full step-by-step is in `<how-to-verify>`.
  </action>
  <what-built>
    The cutover code is already in the repo (02-01: Vercel preset, `netlify.toml` removed, drizzle unpooled) and the Vercel env vars are set (02-03). This task triggers and verifies the actual production deploy — Claude can build locally but cannot click Vercel's deploy UI or guarantee the live runtime; the human triggers + confirms it.
  </what-built>
  <how-to-verify>
    1. Trigger the production deploy: push to the production branch (Vercel auto-deploys) or run `vercel --prod` from `remix-app/`. Confirm the Vercel Deployments view shows the build SUCCEEDED with the `@vercel/react-router` preset (Deployment Summary lists SSR functions).
    2. Confirm the app is reachable: `curl -I https://zoetrop.vercel.app` returns HTTP 200 (or load it in a browser — the M0 dashboard renders).
    3. Confirm DB connectivity (per Pitfall 3 — do NOT assume the Pool singleton works on Vercel without testing): exercise a path that hits Neon, OR run a one-off connectivity check from the deployed environment / a `drizzle-kit` operation. Since current loaders use static data, a documented `curl` to a route plus a manual `drizzle-kit` connectivity check (e.g. `db:studio` or a trivial SELECT) against the live `DATABASE_URL_UNPOOLED` is acceptable for Phase 2. Note any cold-start/pool issue for the Phase 4 refactor (DATA-01) but do not fix it here.
    4. Record in `docs/COMPLIANCE-RUNBOOK.md` (Vercel BAA / deploy section): production-deploy date, deploy URL `https://zoetrop.vercel.app`, "200 OK confirmed", and "DB connectivity confirmed against existing Neon project". NO secrets.
  </how-to-verify>
  <verify>
    <human-check>curl to https://zoetrop.vercel.app returns 200 from a successful preset build; DB connectivity to the existing Neon project confirmed; deploy date/URL/200/connectivity recorded in runbook.</human-check>
  </verify>
  <acceptance_criteria>
    `https://zoetrop.vercel.app` returns HTTP 200 from a successful Vercel production deploy built with the `@vercel/react-router` preset; DB connectivity to the existing Neon project is confirmed (route exercise or drizzle-kit connectivity check); Netlify is retired (no parallel deploy). The runbook records the deploy date, URL, 200-OK, and DB-connectivity confirmation — NO secrets. (Closes the deploy/connectivity half of SC#5.)
  </acceptance_criteria>
  <resume-signal>Type "approved — zoetrop.vercel.app returns 200, DB connectivity confirmed, recorded in runbook" or describe the failure.</resume-signal>
  <done>Successful Vercel production deploy (200 OK) + confirmed DB connectivity, recorded in the runbook; Netlify retired.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Retrieve + record the pgAudit verification sample (Neon Support ticket)</name>
  <read_first>
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md (## D — D2 auto-configured pgAudit settings; D3 test-query + Support-ticket verification procedure + expected AUDIT: SESSION format; Pitfall 5 logs not self-serve)
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-CONTEXT.md (D-12 pgAudit class; SC#4 log_parameter=off)
    - docs/COMPLIANCE-RUNBOOK.md (pgAudit Status section to fill in)
  </read_first>
  <action>
    User-action (audit logs on Neon are NOT self-serve — Pitfall 5; Claude cannot open the ticket or read the isolated logs). pgAudit was auto-configured when HIPAA was enabled in 02-03 (`pgaudit.log='all,-misc'`, `pgaudit.log_parameter='off'`). Execute a known test INSERT on the `metrics` table (owner-only data), open a Neon Support ticket requesting an audit-log sample, confirm the returned `AUDIT: SESSION` line shows CLASS=WRITE / COMMAND=INSERT / OBJECT_NAME=public.metrics / timestamp+user and PARAMETER=`<not logged>`, and paste the sample + ticket ref + date into `docs/COMPLIANCE-RUNBOOK.md` (pgAudit Status section). Full step-by-step is in `<how-to-verify>`.
  </action>
  <what-built>
    pgAudit was auto-configured by Neon when HIPAA mode was enabled in 02-03 (`pgaudit.log='all,-misc'`, `pgaudit.log_parameter='off'`) — no manual SQL. This task PROVES it: audit log access on Neon is NOT self-serve (Pitfall 5), so the sample must come via a Neon Support ticket. Claude cannot open the ticket or read the isolated audit logs.
  </what-built>
  <how-to-verify>
    1. Execute a known test write against the existing (now HIPAA-enabled) project (via Drizzle Studio or psql against `DATABASE_URL_UNPOOLED`), e.g. an INSERT on the `metrics` table with `ON CONFLICT DO NOTHING` (owner-only data; no client PHI exists yet).
    2. Open a Neon Support ticket: "Request pgAudit log sample for HIPAA verification — show a recent INSERT on the metrics table. Confirm the entry contains user, table name, operation type, timestamp. Confirm bind-parameter values are NOT present (log_parameter=off)." (Lead time ~1-3 business days — start early.)
    3. When the sample returns, confirm the `AUDIT: SESSION,...` line shows CLASS=WRITE, COMMAND=INSERT, OBJECT_NAME=public.metrics, a timestamp + user in the log prefix, and the PARAMETER field = `<not logged>` (proving log_parameter=off — no bind values).
    4. Record in `docs/COMPLIANCE-RUNBOOK.md` (pgAudit Status section): Enabled = auto-configured on HIPAA enablement, log class = all,-misc, log_parameter = off (verified [date] via Neon Support ticket #[number]), and paste the AUDIT: SESSION sample line (it contains no PHI / no bind params).
  </how-to-verify>
  <verify>
    <human-check>Runbook pgAudit section contains a real AUDIT: SESSION sample (WRITE/INSERT/public.metrics, PARAMETER not logged) with ticket # and date; log_parameter=off confirmed.</human-check>
  </verify>
  <acceptance_criteria>
    The runbook pgAudit Status section contains a real `AUDIT: SESSION,...` sample line (from the Neon Support ticket) showing CLASS=WRITE / COMMAND=INSERT / OBJECT_NAME=public.metrics / timestamp + user, with the PARAMETER field = `<not logged>`; it records the verification date and the Support ticket number, and states log class = all,-misc and log_parameter = off. This proves SC#4: entries record {user, table, operation, timestamp} and NOT bind parameters. (COMP-03 baseline satisfied; PHI read-access logging remains the Phase 3 carry-forward.)
  </acceptance_criteria>
  <resume-signal>Type "done — pgAudit sample retrieved via Neon Support ticket #[number], log_parameter=off confirmed, pasted into runbook" or describe a blocker.</resume-signal>
  <done>pgAudit sample (log_parameter=off proven) recorded in the runbook with the Neon Support ticket ref — SC#4 satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final gate review — runbook complete + all 5 SCs recorded</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 — the 5 success criteria, the verification bar)
    - docs/COMPLIANCE-RUNBOOK.md (all sections — must be fully populated)
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-VALIDATION.md (Manual-Only Verifications table; Validation Sign-Off)
  </read_first>
  <action>
    Final consolidation review (Claude assists the automatable checks; the human confirms the gate). Walk the 5 ROADMAP SCs against `docs/COMPLIANCE-RUNBOOK.md` + repo state, run the automatable SC#5 re-checks, and confirm the Phase 3 carry-forward (PHI read-access logging, D-13) is recorded. Declare the phase done only when SC#1/#2/#4/#5 are fully recorded; SC#3 (Anthropic) may be "initiated, signing pending — Phase 5 blocked" if vendor turnaround is incomplete. Full checklist is in `<how-to-verify>`.
  </action>
  <what-built>
    All Track A code/docs (02-01, 02-02) and Track B vendor actions (02-03, this plan's Tasks 1-2) are complete. This is the final consolidation: confirm the runbook is the complete auditable proof-of-gate before the phase is declared done.
  </what-built>
  <how-to-verify>
    Walk the 5 ROADMAP SCs against `docs/COMPLIANCE-RUNBOOK.md` + repo state:
    1. SC#1 — Neon Scale + HIPAA on the existing project + signed Neon BAA (date recorded)?
    2. SC#2 — app on Vercel (Netlify retired) + Vercel BAA on a BAA-eligible plan (date recorded)?
    3. SC#3 — Anthropic HIPAA-Ready API BAA covering the extraction tier (recorded; initiated-or-signed with the "Phase 5 blocked until signed" note)?
    4. SC#4 — pgAudit sample proving {user, table, operation, timestamp} and log_parameter=off (pasted)?
    5. SC#5 — RR7 Vercel preset, Netlify config removed, env vars set, successful production deploy (200 OK), CLAUDE.md + docs/PLATFORM.md updated to Vercel? Run the automated re-check below.
    Also confirm the Phase 3 Carry-Forward (PHI read-access logging, D-13) is recorded in the runbook so it is not lost.
  </how-to-verify>
  <verify>
    <automated>cd remix-app && npm test && npm run build</automated>
    <human-check>From repo root, all true: test ! -f netlify.toml; grep -q vercelPreset remix-app/react-router.config.ts; grep -q vercel.app CLAUDE.md; grep -q vercel.app docs/PLATFORM.md; grep -q "Phase 3 Carry-Forward" docs/COMPLIANCE-RUNBOOK.md. Human confirms SC#1/#2/#3/#4 recorded in the runbook.</human-check>
  </verify>
  <acceptance_criteria>
    All five ROADMAP SCs are satisfied and recorded in `docs/COMPLIANCE-RUNBOOK.md` (with the one allowed exception that the Anthropic BAA may be "initiated, signing pending — Phase 5 blocked" if vendor turnaround is incomplete; SC#1, SC#2, SC#4, SC#5 must be fully complete). The runbook contains no secrets and records the Phase 3 carry-forward. The automatable SC#5 checks (`test ! -f netlify.toml`, `vercelPreset` present, `vercel.app` in both docs, `npm test` + `npm run build` green) all pass.
  </acceptance_criteria>
  <resume-signal>Type "approved — gate complete, runbook is the auditable proof-of-gate, all SCs recorded" or list the SC still open.</resume-signal>
  <done>All 5 SCs recorded in the runbook (Anthropic may be initiated-pending); automatable SC#5 checks green; phase gate auditable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> deployed Vercel app | Untrusted traffic reaches SSR routes (no client PHI yet) |
| deployed app -> Neon | Runtime DB connectivity over the pooled `DATABASE_URL` |
| Neon audit logs -> user (via Support) | Isolated compliance logs cross a controlled retrieval boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Denial of Service | Neon Pool singleton fails on Vercel cold start | accept | Phase 2 is low-risk (no loaders call the DB — static data); Task 1 verifies connectivity post-deploy and notes the cold-start risk for the Phase 4 (DATA-01) refactor; not fixed here |
| T-02-12 | Information Disclosure | Connection string in the client bundle | mitigate | `db.server.ts` is server-only (`.server.ts` suffix); no client import; Vercel does not expose server env at build time — verified no `DATABASE_URL` in `build/client` |
| T-02-13 | Information Disclosure | PHI / bind params in pgAudit log | mitigate | `log_parameter=off` proven by the Task 2 sample (PARAMETER not logged); parameterized drizzle queries (no PHI interpolated into SQL) |
| T-02-14 | Repudiation | Incomplete/undated gate record | mitigate | Task 3 final review confirms every SC is recorded in the runbook before the phase closes |
</threat_model>

<verification>
- `curl -I https://zoetrop.vercel.app` returns 200; Vercel Deployments shows a successful production build with the preset.
- DB connectivity to the existing Neon project confirmed post-deploy.
- `docs/COMPLIANCE-RUNBOOK.md` pgAudit Status contains an `AUDIT: SESSION` sample with PARAMETER not logged + ticket ref + date.
- Automatable SC#5 re-checks pass: `test ! -f netlify.toml`; `grep -q vercelPreset remix-app/react-router.config.ts`; `grep -q vercel.app CLAUDE.md` and `docs/PLATFORM.md`; `cd remix-app && npm test`; `cd remix-app && npm run build`.
- Phase 3 carry-forward recorded in the runbook.
</verification>

<success_criteria>
- SC#4: pgAudit sample proves {user, table, operation, timestamp} logged + log_parameter=off — recorded.
- SC#5 (deploy half): successful Vercel production deploy (200 OK) + confirmed DB connectivity; Netlify retired.
- The runbook is the complete, secret-free, auditable proof-of-gate for all 5 SCs (Anthropic BAA may be initiated-pending per turnaround).
- Phase 2 gate satisfied — unblocks Phase 3 and (once the Anthropic BAA signs) Phase 5.
</success_criteria>

<output>
Create `.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-04-SUMMARY.md` when done.
</output>
