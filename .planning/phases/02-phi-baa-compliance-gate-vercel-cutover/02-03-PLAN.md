---
phase: 02-phi-baa-compliance-gate-vercel-cutover
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - docs/COMPLIANCE-RUNBOOK.md
autonomous: false
requirements: [COMP-02]
track: B
user_setup:
  - service: neon
    why: "Scale plan + HIPAA mode + signed BAA on the EXISTING project (COMP-02 / SC#1)"
    dashboard_config:
      - task: "Upgrade to Scale plan (if not already), accept org-level HIPAA BAA, enable HIPAA on the existing project"
        location: "Neon Console -> Organization -> Billing / Settings -> HIPAA support; then Project -> Settings -> HIPAA support"
  - service: vercel
    why: "Pro HIPAA add-on + signed Vercel BAA (COMP-02 / SC#2)"
    dashboard_config:
      - task: "Purchase the HIPAA add-on (self-serve, Pro plan) and accept the BAA"
        location: "Vercel Dashboard -> Settings -> Billing"
      - task: "Set DATABASE_URL (pooled), DATABASE_URL_UNPOOLED (direct), BETTER_AUTH_SECRET, BETTER_AUTH_URL in project env (Production + Preview)"
        location: "Vercel Dashboard -> Project -> Settings -> Environment Variables"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> Project -> Connect -> pooled connection string (.pooler.neon.tech)"
      - name: DATABASE_URL_UNPOOLED
        source: "Neon Console -> Project -> Connect -> direct connection string ([project-id].[region].neon.tech)"
      - name: BETTER_AUTH_SECRET
        source: "Generate 32+ random bytes (e.g. openssl rand -base64 32)"
      - name: BETTER_AUTH_URL
        source: "https://zoetrop.vercel.app"
  - service: anthropic
    why: "HIPAA-Ready API BAA covering the PHI-extraction tier (COMP-02 / SC#3) — long-pole, sales-gated"
    dashboard_config:
      - task: "Start the sales conversation for a HIPAA-Ready API org and sign the BAA (separate from the Enterprise chat product)"
        location: "https://claude.com/contact-sales"

must_haves:
  truths:
    - "Neon is on the Scale plan with HIPAA mode enabled on the EXISTING project (not a new empty DB); the Neon BAA is accepted"
    - "The Vercel Pro HIPAA add-on is purchased and the Vercel BAA is accepted; the required env vars are set in the Vercel project"
    - "The Anthropic HIPAA-Ready API BAA process is initiated (long-pole) and, when signed, recorded — covering the extraction tier"
    - "Every executed action is recorded in docs/COMPLIANCE-RUNBOOK.md with dates/contacts (no secrets)"
  artifacts:
    - path: "docs/COMPLIANCE-RUNBOOK.md"
      provides: "Filled-in Neon/Vercel BAA + HIPAA dates, env-vars-set confirmation, Anthropic process status"
      contains: "HIPAA mode enabled"
  key_links:
    - from: "Vercel project env"
      to: "the EXISTING Neon project"
      via: "DATABASE_URL / DATABASE_URL_UNPOOLED pointed at the verified existing project ID"
      pattern: "DATABASE_URL"
---

<objective>
Execute (and, for the long-pole, initiate) the vendor BAA + HIPAA actions that Claude cannot perform: Neon Scale + HIPAA + BAA on the EXISTING project, Vercel Pro HIPAA add-on + BAA + env-var setup, and kicking off the Anthropic HIPAA-Ready API BAA. This is Track B (user-action checklist, D-14). The deliverable is the executed action recorded in `docs/COMPLIANCE-RUNBOOK.md`, not code.

Purpose: Satisfies the compliance + env-setup substance of SC#1, SC#2, and SC#3, and unblocks the production deploy + pgAudit verification finalized in 02-04. The Anthropic sales path and the Neon Support pgAudit sample are the long poles — the user should START the vendor conversations IMMEDIATELY (in parallel with the Track A cutover in Wave 1), even though this plan's checkpoints execute in Wave 2 after the runbook scaffold (02-02) exists — vendor legal/support turnaround is the long pole and must not block the phase.

Output: Neon HIPAA enabled on the existing project; Vercel HIPAA add-on + env vars set; Anthropic BAA process initiated; all recorded in the runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-CONTEXT.md
@.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Neon — upgrade to Scale, enable HIPAA on the EXISTING project, accept the BAA</name>
  <read_first>
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md (## D — D1 exact steps + irreversibility/downtime warnings; Pitfall 1 wrong-project trap; Pitfall 4)
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-CONTEXT.md (D-09 KEEP existing Neon project; do NOT create a new empty DB)
  </read_first>
  <action>
    User-action (Claude cannot click the Neon dashboard or accept a legal BAA). Before any irreversible step, verify the EXISTING project that backs the live `DATABASE_URL` by matching the project ID in the connection hostname, then: upgrade to Scale, accept the org-level HIPAA BAA, and enable HIPAA on THAT existing project (never "Create New Neon Database"). Record the Scale plan, HIPAA-enable date, BAA-accept date, org name, and the verified project ID in `docs/COMPLIANCE-RUNBOOK.md` (Neon BAA section) — no secrets. Full step-by-step is in `<how-to-verify>`.
  </action>
  <what-built>
    Nothing automatable — this is a Neon Console account/billing action with an IRREVERSIBLE, data-sensitive step. Claude cannot click the Neon dashboard or accept a legal BAA on your behalf.
  </what-built>
  <how-to-verify>
    BEFORE any irreversible action — CRITICAL data-loss guard (per Pitfall 1 / the high-severity threat): in Neon Console, identify the EXISTING project that backs the live `DATABASE_URL` by matching the project ID in the connection hostname (`[project-id].[region].neon.tech`). You will enable HIPAA on THAT project only — never "Create New Neon Database".
    1. Neon Console -> Organization -> Billing: confirm/upgrade to the Scale plan (HIPAA requires Scale).
    2. Neon Console -> Organization Settings -> HIPAA support: Enable HIPAA compliance -> accept the click-to-accept BAA.
    3. Neon Console -> [the verified existing project] -> Settings -> HIPAA support -> Enable. NOTE: this is IRREVERSIBLE (the project cannot later disable HIPAA — only deletion) and restarts all computes (brief connection drop; safe — no data loss, app not yet serving client PHI). Do this in a quiet window.
    4. Confirm the HIPAA badge appears on the existing project and the project ID still matches the live `DATABASE_URL`.
    5. Record in `docs/COMPLIANCE-RUNBOOK.md` (Neon BAA section): plan = Scale, HIPAA-mode-enabled date, BAA-accepted date, org name, the verified project name + project ID. NO secrets.
  </how-to-verify>
  <verify>
    <human-check>Neon Console shows Scale + a HIPAA badge on the existing project whose ID matches the live DATABASE_URL; org BAA accepted; runbook Neon section filled in (no secrets).</human-check>
  </verify>
  <acceptance_criteria>
    Neon Console shows the Scale plan active and a HIPAA badge on the EXISTING project whose ID matches the live `DATABASE_URL`; the org-level BAA is accepted. The runbook Neon BAA section records the Scale plan, the HIPAA-enable date, the BAA-accept date, and the verified project ID — with NO secrets. (Data-loss guard satisfied: HIPAA was enabled on the existing project, not a new empty DB.)
  </acceptance_criteria>
  <resume-signal>Type "done — HIPAA enabled on existing project [project-id], recorded in runbook" or describe any blocker (e.g. plan-tier question).</resume-signal>
  <done>Neon Scale + HIPAA on the existing project + accepted BAA, recorded in the runbook.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Vercel — purchase HIPAA add-on, accept BAA, set project env vars</name>
  <read_first>
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md (## B Vercel Pro HIPAA add-on self-serve; ## E env vars to set; ## Code Examples — Vercel Environment Variables; D-04 domain; D-08 env)
    - docs/COMPLIANCE-RUNBOOK.md (Vercel BAA section to fill in)
  </read_first>
  <action>
    User-action (Claude cannot purchase the add-on, accept the BAA, or write secrets into Vercel env). Purchase the Vercel Pro HIPAA add-on and accept the BAA; then set `DATABASE_URL` (pooled), `DATABASE_URL_UNPOOLED` (direct), `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL` (`https://zoetrop.vercel.app`) in the Vercel project env for Production + Preview — both DB URLs pointing at the SAME existing Neon project verified in Task 1. Record the plan/tier + dates in `docs/COMPLIANCE-RUNBOOK.md` (Vercel BAA section) and confirm the four env vars are SET — never paste connection strings or secret values (D-11). Full step-by-step is in `<how-to-verify>`.
  </action>
  <what-built>
    Nothing automatable — this is a Vercel billing/legal action plus setting secrets in the Vercel dashboard. Claude cannot purchase the add-on, accept the BAA, or write secrets into Vercel env.
  </what-built>
  <how-to-verify>
    1. Vercel Dashboard -> Settings -> Billing: confirm the team is on the Pro plan, purchase the HIPAA add-on (self-serve since 2025-09-09; Enterprise NOT required), and accept the BAA. Note the add-on price shown (it is unpublished in docs — confirm in billing).
    2. Vercel Dashboard -> Project -> Settings -> Environment Variables (set for BOTH Production and Preview): `DATABASE_URL` = the Neon POOLED connection string (`.pooler.neon.tech`); `DATABASE_URL_UNPOOLED` = the Neon DIRECT/unpooled string (`[project-id].[region].neon.tech`); `BETTER_AUTH_SECRET` = 32+ random bytes (`openssl rand -base64 32`); `BETTER_AUTH_URL` = `https://zoetrop.vercel.app`. CRITICAL: both DB URLs must point to the SAME existing Neon project verified in Task 1 (compare the project ID in the hostname).
    3. Record in `docs/COMPLIANCE-RUNBOOK.md` (Vercel BAA section): plan = Pro + HIPAA add-on, add-on-enabled date, BAA-accepted date. Do NOT paste any connection strings or secret values into the runbook (D-11 NO SECRETS) — record only that the four env vars are SET.
  </how-to-verify>
  <verify>
    <human-check>Vercel Pro HIPAA add-on active + BAA accepted; 4 env vars set (Production + Preview) on the verified existing Neon project; runbook Vercel section filled in (no secret values).</human-check>
  </verify>
  <acceptance_criteria>
    The Vercel Pro HIPAA add-on is active and the BAA accepted; `DATABASE_URL`, `DATABASE_URL_UNPOOLED`, `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL` are set in the Vercel project env (Production + Preview), with the two DB URLs pointing at the verified existing Neon project. The runbook Vercel BAA section records the plan/tier + dates and confirms the env vars are set — with NO secret values written. (`DATABASE_URL_UNPOOLED` present so 02-04 migrations don't hit the pooler — Pitfall 2.)
  </acceptance_criteria>
  <resume-signal>Type "done — Vercel HIPAA add-on + BAA accepted, 4 env vars set on the existing Neon project, recorded in runbook" or describe a blocker (e.g. add-on cost question).</resume-signal>
  <done>Vercel HIPAA add-on + BAA + the four env vars set on the existing Neon project, recorded in the runbook.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Anthropic — initiate the HIPAA-Ready API BAA (long-pole)</name>
  <read_first>
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md (## C Anthropic HIPAA-Ready API path + D-03 compliance check; Pitfall 6 wrong-org trap; ## Open Questions #3 sales lead time)
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-CONTEXT.md (D-01 gate criterion, D-02 default-to-Claude, D-03 ZDR + no-training)
  </read_first>
  <action>
    User-action (sales-gated; Claude cannot initiate or sign the BAA). Start the Anthropic sales conversation NOW (long pole) for a HIPAA-Ready API BAA covering the Messages API (the `generateObject` path used in Phase 5), stating the D-03 hard requirements (zero-data-retention AND no-training-on-our-data). When signed, confirm a SEPARATE dedicated HIPAA org (Pitfall 6) and record provider/tier/dates/org/contact in `docs/COMPLIANCE-RUNBOOK.md` (LLM Provider BAA section) plus the explicit "Phase 5 blocked until signed" note; fall back to OpenAI per D-02 only if Anthropic's path is blocked. Full step-by-step is in `<how-to-verify>`.
  </action>
  <what-built>
    Nothing automatable — this is a sales-gated legal process. Anthropic provisions a dedicated HIPAA-enabled API org only after a BAA is signed; Claude cannot initiate or sign it.
  </what-built>
  <how-to-verify>
    1. Start the sales conversation NOW (long pole — allow 1-2 weeks): contact https://claude.com/contact-sales requesting a HIPAA-Ready API BAA for the Messages API (covers structured outputs / `generateObject` used in Phase 5). State the D-03 hard requirements: zero-data-retention AND a contractual no-training-on-our-data guarantee.
    2. When the BAA is signed, confirm Anthropic provisions a SEPARATE dedicated HIPAA-enabled API org (Pitfall 6 — do NOT reuse a non-HIPAA org key for PHI; keep HIPAA-org keys in a separate namespace, e.g. `ANTHROPIC_API_KEY_HIPAA`). This BAA is separate from the Enterprise chat product.
    3. Record in `docs/COMPLIANCE-RUNBOOK.md` (LLM Provider BAA section): provider = Anthropic, tier = HIPAA-Ready API org, process-initiated date, BAA-signed date (when complete), non-secret org ID, contact name. Note explicitly that Phase 5 is BLOCKED until this BAA is signed. If Anthropic's path becomes blocked/materially harder, fall back to OpenAI (`baa@openai.com`) per D-02 and record the switch.
  </how-to-verify>
  <verify>
    <human-check>Anthropic HIPAA-Ready API BAA process initiated (sales contacted) and recorded with the initiation date + D-03 requirements; signed-state + org recorded when complete; "Phase 5 blocked until signed" note present.</human-check>
  </verify>
  <acceptance_criteria>
    The Anthropic HIPAA-Ready API BAA process is initiated (sales contacted) and recorded in the runbook with the initiation date and the D-03 requirements stated; once signed, the runbook records the BAA-signed date, the dedicated HIPAA org (non-secret ID), the tier covering the extraction use case, and the explicit "Phase 5 blocked until signed" note. (If signing is still pending at phase close, the runbook shows the process initiated with the blocked-status note — initiation is the Phase 2 bar; the signed BAA is the Phase 5 prerequisite.)
  </acceptance_criteria>
  <resume-signal>Type "done — Anthropic HIPAA-Ready API BAA initiated (and signed if complete), recorded in runbook" or describe a blocker.</resume-signal>
  <done>Anthropic HIPAA-Ready API BAA initiated (signed if turnaround allows), recorded in the runbook with the Phase-5-blocked note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user <-> vendor dashboards (Neon/Vercel/Anthropic) | Legal + account actions that establish the PHI compliance envelope |
| Vercel env <-> Neon project | Connection strings cross here; must target the EXISTING project |
| (Phase 5) app <-> Anthropic API | PHI will cross here later — the BAA must be in place first |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | Enabling HIPAA on the WRONG Neon project (new/empty DB) -> data loss or unprotected PHI path | mitigate | HIGH-severity. Task 1 acceptance criterion REQUIRES verifying the project ID in the connection hostname matches the live `DATABASE_URL` BEFORE the irreversible enable; D-09 forbids creating a new DB; Pitfall 1 explicitly called out |
| T-02-08 | Information Disclosure | Secrets pasted into the runbook (git history) | mitigate | Tasks 1-3 acceptance criteria forbid writing connection strings/keys to the runbook (D-11); only metadata + "env vars SET" confirmation recorded |
| T-02-09 | Spoofing/Repudiation | Anthropic BAA signed on the wrong (non-HIPAA) org; PHI later sent uncovered | mitigate | Task 3 requires a SEPARATE dedicated HIPAA org and a distinct key namespace (Pitfall 6); runbook records the covered tier + "Phase 5 blocked until signed" |
| T-02-10 | Denial of Service | drizzle-kit migrations (02-04) fail against the pooler | mitigate | Task 2 sets `DATABASE_URL_UNPOOLED` (direct) so 02-04 migrations bypass PgBouncer (Pitfall 2) |
</threat_model>

<verification>
- Neon Console: Scale plan active + HIPAA badge on the EXISTING project whose ID matches the live `DATABASE_URL`; org BAA accepted.
- Vercel: Pro HIPAA add-on active + BAA accepted; `DATABASE_URL`, `DATABASE_URL_UNPOOLED`, `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL` set (Production + Preview).
- Anthropic: HIPAA-Ready API BAA process initiated (sales contacted); signed/org recorded when complete.
- `docs/COMPLIANCE-RUNBOOK.md` Neon + Vercel + Anthropic sections populated with dates/contacts/project-id, NO secrets.
</verification>

<success_criteria>
- SC#1 substance: Neon Scale + HIPAA on the existing project + accepted BAA, recorded.
- SC#2 substance: Vercel HIPAA add-on + BAA + env vars set on the existing Neon project, recorded.
- SC#3 substance: Anthropic HIPAA-Ready API BAA initiated (signed if turnaround allows), covering the extraction tier, recorded.
- All recorded in `docs/COMPLIANCE-RUNBOOK.md` with no secrets.
</success_criteria>

<output>
Create `.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-03-SUMMARY.md` when done.
</output>
