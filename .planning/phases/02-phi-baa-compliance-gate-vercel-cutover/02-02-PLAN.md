---
phase: 02-phi-baa-compliance-gate-vercel-cutover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/COMPLIANCE-RUNBOOK.md
  - CLAUDE.md
  - docs/PLATFORM.md
autonomous: true
requirements: [COMP-02, COMP-03]
track: A
user_setup: []

must_haves:
  truths:
    - "An auditable compliance runbook exists at docs/COMPLIANCE-RUNBOOK.md with per-vendor BAA registers and a pgAudit section, ready for the user to fill in dates"
    - "The runbook records the Phase 3 carry-forward (PHI read-access SELECT audit logging) so it is not lost"
    - "CLAUDE.md and docs/PLATFORM.md no longer present Netlify as the deploy target — they reference Vercel"
  artifacts:
    - path: "docs/COMPLIANCE-RUNBOOK.md"
      provides: "Per-vendor BAA register (Neon, Vercel, Anthropic) + pgAudit status + Phase 3 carry-forward; no secrets"
      contains: "Phase 3 Carry-Forward"
      min_lines: 30
    - path: "CLAUDE.md"
      provides: "Deployment section reflecting Vercel (no Netlify URLs / site id)"
      contains: "vercel.app"
    - path: "docs/PLATFORM.md"
      provides: "Deploy + 5.7 PHI/security references updated to Vercel"
      contains: "vercel.app"
  key_links:
    - from: "docs/COMPLIANCE-RUNBOOK.md"
      to: "Phase 3 carry-forward (PHI read-access logging, D-13)"
      via: "explicit '## Phase 3 Carry-Forward' section"
      pattern: "Phase 3 Carry-Forward"
---

<objective>
Create the auditable compliance runbook (`docs/COMPLIANCE-RUNBOOK.md`, D-10/D-11) that the user fills in as BAAs are signed, and update the project docs (`CLAUDE.md` Deployment section, `docs/PLATFORM.md` deploy + section 5.7) so they reflect Vercel instead of Netlify. This is the Claude-automatable docs/scaffold half of Track A — pure in-repo markdown edits, no vendor actions.

Purpose: The runbook is the auditable proof-of-gate referenced by Phases 3 and 5 (it records every BAA + the pgAudit verification). Scaffolding it now (in Wave 1) gives the user-action tasks in 02-03/02-04 a fixed place to record execution dates, the pgAudit log sample, and the Anthropic org as paperwork lands. The doc updates satisfy the documentation half of SC#5.

Output: New `docs/COMPLIANCE-RUNBOOK.md` (vendor registers + pgAudit section + Phase 3 carry-forward, NO secrets); `CLAUDE.md` and `docs/PLATFORM.md` edited to reference Vercel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-CONTEXT.md
@.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md

<interfaces>
<!-- Exact current text in the docs to edit. Edit in place; the runbook template is in RESEARCH.md. -->

CLAUDE.md "## Deployment" block (lines ~155-165) currently reads:
  Production      | https://zoetrop.netlify.app
  Dev preview     | https://dev--zoetrop.netlify.app
  Branch previews | https://{branch}--zoetrop.netlify.app
  CI/CD: "GitHub Actions: Type check + build on push to dev/main"; "Netlify: Auto-deploys all branches with unique preview URLs"
CLAUDE.md Naming line ~169 says the codename "matches zoetrop.netlify.app".
CLAUDE.md Database section mentions "Provider: Neon Postgres (via Netlify extension)" and "Site: zoetrop (Netlify ID: 0abb12f6-d11b-4f81-8a8d-86b44e99088f)".

docs/PLATFORM.md references to update:
  line 5:      "**Live:** zoetrop.netlify.app"
  line 30:     "...Recharts, Neon Postgres + Drizzle, Netlify CI/CD."
  lines 91-92: "### 5.7 PHI / security posture" — currently lists "BAA-readiness with vendors" generically.

RESEARCH.md "## Compliance Runbook Template" gives the exact section layout to use for docs/COMPLIANCE-RUNBOOK.md (Neon BAA / Vercel BAA / LLM Provider BAA / pgAudit Status / Phase 3 Carry-Forward).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold docs/COMPLIANCE-RUNBOOK.md (per-vendor registers, pgAudit, Phase 3 carry-forward)</name>
  <read_first>
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md (## Compliance Runbook Template — the exact section layout; ## D2/D3 — pgAudit settings + verification format)
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-CONTEXT.md (D-10, D-11, D-12, D-13 — runbook location/contents/pgAudit class/carry-forward)
  </read_first>
  <action>
    Create `docs/COMPLIANCE-RUNBOOK.md` using the template in RESEARCH.md "## Compliance Runbook Template". Include these sections with fields left as fillable placeholders (the user populates dates/contacts in 02-03/02-04): (1) Neon BAA — Plan: Scale, HIPAA mode enabled: [date], BAA accepted (click-to-accept): [date], Organization: [name], HIPAA projects: [project name], AND a line to record the verified project ID matching the live DATABASE_URL (per the wrong-project data-loss threat); (2) Vercel BAA — Plan: Pro + HIPAA add-on, HIPAA add-on enabled: [date], BAA accepted: [date]; (3) LLM Provider BAA (Anthropic) — Provider: Anthropic, Tier: HIPAA-Ready API org, BAA signed: [date], Org ID (non-secret): [id], Contact: [name] (per D-02 default to Claude; note OpenAI as documented fallback); (4) pgAudit Status — Enabled: auto-configured by Neon on HIPAA project enablement, log_parameter: off (verified [date] via Neon Support ticket #[number]), log class: all,-misc, Verification log sample: [paste AUDIT: SESSION line]; (5) Phase 3 Carry-Forward — PHI read-access (SELECT) audit logging deferred to Phase 3 (D-13), with rationale (no client PHI tables until Phase 3) and the Phase 3 action (configure object-level pgAudit for SELECT on PHI tables). Add a top-of-file note: NO SECRETS — no connection strings, keys, or credentials (D-11); those live only in Vercel env. Do NOT fabricate dates — leave placeholders for the user to fill.
  </action>
  <verify>
    <automated>test -f docs/COMPLIANCE-RUNBOOK.md && grep -q "Phase 3 Carry-Forward" docs/COMPLIANCE-RUNBOOK.md && grep -q "log_parameter" docs/COMPLIANCE-RUNBOOK.md && grep -qi "Anthropic" docs/COMPLIANCE-RUNBOOK.md && grep -qi "no secrets" docs/COMPLIANCE-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    `docs/COMPLIANCE-RUNBOOK.md` exists with all five sections (Neon BAA, Vercel BAA, LLM Provider BAA/Anthropic, pgAudit Status, Phase 3 Carry-Forward); contains the `log_parameter` off reference and the Phase 3 carry-forward; carries a "NO SECRETS" note; contains a line to record the verified Neon project ID against the live connection. No real credentials/connection strings present.
  </acceptance_criteria>
  <done>The runbook scaffold is committed and ready for the user-action tasks to record dates, the pgAudit sample, and the Anthropic org.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md + docs/PLATFORM.md from Netlify to Vercel</name>
  <read_first>
    - CLAUDE.md (## Deployment block ~155-165; Database section "via Netlify extension" + "Netlify ID"; Naming line ~169)
    - docs/PLATFORM.md (line 5 "Live:", line 30 stack line, lines 91-92 section 5.7 PHI/security posture)
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md (## Runtime State Inventory — exact CLAUDE.md/PLATFORM.md updates required for SC#5; D-04 domain = zoetrop.vercel.app)
  </read_first>
  <action>
    In `CLAUDE.md`: update the `## Deployment` section — replace production URL `https://zoetrop.netlify.app` with `https://zoetrop.vercel.app`; replace the Netlify dev/branch preview URL rows with Vercel's deploy/preview model (production = `zoetrop.vercel.app`; preview deployments per branch via Vercel); replace the CI/CD "Netlify: Auto-deploys..." line with the Vercel equivalent (Vercel auto-deploys pushes; per-branch preview URLs). In the Database section, change "Neon Postgres (via Netlify extension)" to reflect the Vercel env-var connection (`DATABASE_URL` / `DATABASE_URL_UNPOOLED` set in Vercel project env) and REMOVE the Netlify site id line `0abb12f6-d11b-4f81-8a8d-86b44e99088f`. Update the Naming line that says the codename "matches zoetrop.netlify.app" to `zoetrop.vercel.app`. In `docs/PLATFORM.md`: line 5 change `Live: zoetrop.netlify.app` to `zoetrop.vercel.app`; line 30 change `Netlify CI/CD` to `Vercel CI/CD`; in section 5.7 (lines 91-92) replace the generic "BAA-readiness with vendors" with an explicit reference that the Neon, Vercel, and Anthropic BAAs are executed and tracked in `docs/COMPLIANCE-RUNBOOK.md`. Preserve all surrounding prose; change only deploy/provider references. Do NOT introduce `any` or break markdown structure.
  </action>
  <verify>
    <automated>grep -q "vercel.app" CLAUDE.md && grep -q "vercel.app" docs/PLATFORM.md && [ "$(grep -c 'netlify.app' CLAUDE.md)" -eq 0 ] && [ "$(grep -ic 'netlify.app' docs/PLATFORM.md)" -eq 0 ] && [ "$(grep -c '0abb12f6-d11b-4f81-8a8d-86b44e99088f' CLAUDE.md)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    `CLAUDE.md` and `docs/PLATFORM.md` reference `zoetrop.vercel.app`; no `netlify.app` URLs remain in either; the Netlify site id `0abb12f6-d11b-4f81-8a8d-86b44e99088f` is removed from `CLAUDE.md`; section 5.7 of `docs/PLATFORM.md` references the runbook for the executed BAAs. Surrounding prose preserved; markdown structure intact.
  </acceptance_criteria>
  <done>Both docs present Vercel as the deploy target with no Netlify URLs or site id; section 5.7 points at the compliance runbook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → auditors / future phases | The runbook is the auditable proof-of-gate consumed by Phases 3 and 5 |
| runbook content → version control | Anything written here is committed to git history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | Secrets accidentally written into the runbook | mitigate | Top-of-file "NO SECRETS" note (D-11); runbook records only metadata (status, dates, plan/tier, non-secret org IDs) — never connection strings or keys (those live in Vercel env) |
| T-02-05 | Repudiation | Missing/undated BAA records | mitigate | Per-vendor register with explicit execution-date fields; pgAudit sample + support ticket ref recorded → auditable trail |
| T-02-06 | Tampering | Phase 3 carry-forward (read-access logging) lost | mitigate | Explicit `## Phase 3 Carry-Forward` section records the D-13 deferral and the required Phase 3 action so it cannot be silently dropped |
</threat_model>

<verification>
- `test -f docs/COMPLIANCE-RUNBOOK.md` and it contains all five sections + the Phase 3 carry-forward + a NO-SECRETS note.
- No secrets/connection strings present in the runbook (manual review + the NO-SECRETS note).
- `grep -q "vercel.app" CLAUDE.md docs/PLATFORM.md`; no `netlify.app` URL or Netlify site id remains.
- Surrounding prose in both docs preserved (only deploy/provider refs changed).
</verification>

<success_criteria>
- `docs/COMPLIANCE-RUNBOOK.md` scaffolded per D-10/D-11 (Neon, Vercel, Anthropic registers + pgAudit + Phase 3 carry-forward), no secrets.
- `CLAUDE.md` Deployment + Database + Naming references updated to Vercel; Netlify site id removed.
- `docs/PLATFORM.md` line 5 / line 30 / section 5.7 updated to Vercel + runbook reference.
- Documentation half of SC#5 satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-02-SUMMARY.md` when done.
</output>
