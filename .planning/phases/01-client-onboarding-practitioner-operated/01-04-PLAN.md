---
phase: 01-client-onboarding-practitioner-operated
plan: 04
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - remix-app/app/lib/invites.server.ts
  - remix-app/app/lib/auth.server.ts
autonomous: true
requirements: [ONB-02]
must_haves:
  truths:
    - "generateInvite accepts an optional subjectId and writes it to the invites row"
    - "resolveInviteByToken and consumeInviteByToken return the subjectId field (null for practitioner/owner invites)"
    - "The signup hooks thread the invite's subjectId through PendingInvite so redemption knows which subject the client account corresponds to"
    - "Redeeming a client invite injects role + tenantId onto the new user with no orphaned account (existing behavior preserved)"
  artifacts:
    - path: "remix-app/app/lib/invites.server.ts"
      provides: "subjectId threaded through GenerateInviteOpts/Result + generate/resolve/consume"
      contains: "subjectId"
    - path: "remix-app/app/lib/auth.server.ts"
      provides: "PendingInvite.subjectId + redemption-hook threading"
      contains: "subjectId"
  key_links:
    - from: "remix-app/app/lib/auth.server.ts (beforeSignUp)"
      to: "remix-app/app/lib/invites.server.ts (resolveInviteByToken)"
      via: "pendingInvite.set({ subjectId: invite.subjectId })"
      pattern: "subjectId"
---

<objective>
Extend the invite system to bind an invite to an existing subject (D-01) and thread that `subjectId` through the Better-Auth redemption hooks so a client account links to the right subject — closing the ONB-02 write path and giving the 03.1 residual UAT (Plan 07) real traffic.

Purpose: D-01 says create the subject first, then bind the invite to it. The invite machinery is already fully built (generate/resolve/consume, SHA-256 single-use tokens) — this plan only adds a nullable `subjectId` thread. For v1.1, no extra assignment-table write is needed at redemption (the practitioner operates on subjects directly; the client cannot act in v1.1, D-03); the subjectId is recorded on the invite + carried to audit logging.
Output: `invites.server.ts` + `auth.server.ts` with the subjectId thread, `tests/auth/invites-server.test.ts` extensions GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-client-onboarding-practitioner-operated/01-CONTEXT.md
@.planning/phases/01-client-onboarding-practitioner-operated/01-RESEARCH.md
@.planning/phases/01-client-onboarding-practitioner-operated/01-PATTERNS.md

<interfaces>
<!-- Live signatures the executor extends. Extracted from invites.server.ts + auth.server.ts. -->

invites.server.ts:
  export interface GenerateInviteOpts { inviter: {id, role?, tenantId?}; role: "practitioner"|"client" }  (L50-57)
  export interface GenerateInviteResult { token; role; tenantId; expiresAt }  (L59-65)
  export async function generateInvite(opts): db.insert(invites).values({...})  (L80-...)
  export async function resolveInviteByToken(raw): Promise<{ role; tenantId } | null>  (L183)
  export async function consumeInviteByToken(raw, consumedBy?): Promise<{ role; tenantId } | null>  (L239)

  After Plan 01, the invites table has a nullable subject_id column (FK → subjects.id).

auth.server.ts (Better-Auth hooks):
  interface PendingInvite { rawToken; role; tenantId; breakGlass }  (L40-48)
  beforeSignUp — resolves invite, calls pendingInvite.set({...})  (break-glass path + per-invite path)
  user.create.before — consumes invite, injects role+tenantId on the new user
  user.create.after — best-effort audit logging (try/catch)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread subjectId through invites.server.ts</name>
  <files>remix-app/app/lib/invites.server.ts</files>
  <behavior>
    - GenerateInviteOpts accepts an optional subjectId (string | null)
    - generateInvite writes subjectId to the invites row and returns it in GenerateInviteResult (null when not supplied)
    - resolveInviteByToken returns { role, tenantId, subjectId } with subjectId null when the column is null
    - consumeInviteByToken returns { role, tenantId, subjectId } likewise
    - (the RED contract added to tests/auth/invites-server.test.ts in Plan 01)
  </behavior>
  <read_first>
    - remix-app/app/lib/invites.server.ts (the file being modified — GenerateInviteOpts L50-57, GenerateInviteResult L59-65, generateInvite insert, resolveInviteByToken L183, consumeInviteByToken L239)
    - remix-app/tests/auth/invites-server.test.ts (the extended RED contract — from Plan 01)
    - remix-app/tests/auth/invites-consume.test.ts (existing consume coverage — confirm no regression to consumedBy/consumedAt behavior)
    - .planning/phases/01-client-onboarding-practitioner-operated/01-PATTERNS.md (section "invites.server.ts — MODIFY to thread subjectId" — exact interface + insert + return diffs)
  </read_first>
  <action>
    Add `subjectId?: string | null` to `GenerateInviteOpts` and `subjectId: string | null` to `GenerateInviteResult`. In `generateInvite`, add `subjectId: opts.subjectId ?? null` to the `db.insert(invites).values({...})` call and include `subjectId: opts.subjectId ?? null` in the returned result object. Extend the `resolveInviteByToken` return type to `{ role; tenantId; subjectId: string | null } | null` and return `subjectId: invite.subjectId ?? null`. Extend `consumeInviteByToken` return type the same way and return `subjectId: invite.subjectId ?? null`.

    Preserve ALL existing logic — token hashing, single-use consumption, expiry, role policy (owner→practitioner|client, practitioner→client), consumedBy/consumedAt writes. Only add the subjectId field. Do NOT validate subjectId here against the practitioner's own subjects — that server-resolution happens at the call site in the /clients action (Plan 06): the action passes a subjectId drawn from the practitioner's own subject list, never raw client form input (RESEARCH IDOR mitigation).
  </action>
  <verify>
    <automated>cd remix-app && npm test -- tests/auth/invites-server.test.ts 2>&1 | grep -v '^#' | grep -qE "(passed|✓)" && echo INVITES_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `GenerateInviteOpts` and `GenerateInviteResult` both reference `subjectId`: `grep -c "subjectId" remix-app/app/lib/invites.server.ts` ≥ 4
    - generateInvite's insert includes `subjectId`; both resolve + consume return shapes include `subjectId`
    - Existing token/expiry/role/consumedBy logic unchanged (no lines removed from those paths)
    - `cd remix-app && npm test -- tests/auth/invites-server.test.ts tests/auth/invites-consume.test.ts` GREEN (subjectId assertions pass; no consume regression)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>subjectId threaded through GenerateInviteOpts/Result + generate/resolve/consume; invites-server.test.ts subjectId cases GREEN; no consume regression.</done>
</task>

<task type="auto">
  <name>Task 2: Thread subjectId through the Better-Auth redemption hooks</name>
  <files>remix-app/app/lib/auth.server.ts</files>
  <read_first>
    - remix-app/app/lib/auth.server.ts (the file being modified — PendingInvite L40-48, beforeSignUp per-invite path + break-glass path, user.create.before, user.create.after audit at L221-238)
    - remix-app/app/lib/invites.server.ts (the resolveInviteByToken/consumeInviteByToken return shapes Task 1 just extended)
    - .planning/phases/01-client-onboarding-practitioner-operated/01-RESEARCH.md (section "Invite → Subject Link Seam" — "Correct v1.1 wiring": no extra assignment write needed; subjectId is recorded + audited)
    - .planning/phases/01-client-onboarding-practitioner-operated/01-PATTERNS.md (section "auth.server.ts — MODIFY PendingInvite + redemption hooks" — exact set() call diffs for both paths)
  </read_first>
  <action>
    Add `subjectId: string | null` to the `PendingInvite` interface. In `beforeSignUp`, extend the per-invite `pendingInvite.set({...})` call to include `subjectId: invite.subjectId ?? null` (threaded from the now-extended `resolveInviteByToken`). In the break-glass `pendingInvite.set({...})` path, add `subjectId: null` (break-glass has no subject binding). In `user.create.after`, make the burned invite's `subjectId` available as `pending?.subjectId` for best-effort audit logging using the existing try/catch audit pattern (lines 221-238) — log the subject association; do NOT add a new table write (v1.1 needs none — the practitioner operates on subjects directly, RESEARCH "Correct v1.1 wiring").

    Preserve the existing role + tenantId injection in `user.create.before` exactly — redemption must still produce a client account with `role: "client"` + the inviter's `tenantId` and no orphaned account (D-03, 03.1 UAT). Do NOT block redemption on subjectId being present (practitioner/owner invites legitimately have null subjectId).
  </action>
  <verify>
    <automated>cd remix-app && npx react-router typegen && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -ci "auth.server" | grep -qx 0 && grep -q "subjectId" app/lib/auth.server.ts && echo AUTH_THREAD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `PendingInvite` includes `subjectId`: `grep -c "subjectId" remix-app/app/lib/auth.server.ts` ≥ 3 (interface + per-invite set + break-glass set)
    - Both `pendingInvite.set({...})` calls (per-invite + break-glass) include a subjectId field
    - The existing role + tenantId injection in user.create.before is preserved (not removed)
    - No new DB table write added in the redemption hooks (audit-log thread only)
    - `cd remix-app && npm test && npx tsc --noEmit && npm run build` all pass (build gate); existing auth tests (`tests/auth/*`) still GREEN
  </acceptance_criteria>
  <done>PendingInvite carries subjectId; both signup-hook paths set it; user.create.after audits it; redemption still injects role+tenantId with no orphaned account; build gate + auth tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /clients action → generateInvite | subjectId is server-resolved from the practitioner's OWN subjects at the call site (Plan 06), never trusted from raw client form input |
| signup form → redemption hooks | The raw token (untrusted) crosses here; resolve/consume hash-validate it; role/tenant are injected server-side, not taken from the signup form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-invite-IDOR | Spoofing/Tampering | generateInvite subjectId | mitigate | subjectId is server-resolved from the practitioner's own subject list at the call site (Plan 06); generateInvite stores what it is given but the caller never forwards raw client-supplied subjectId (Task 1) |
| T-01-redemption-elevation | Elevation of Privilege | user.create.before role injection | mitigate | role + tenantId injected server-side from the burned invite, never from the signup form; existing behavior preserved (Task 2) |
| T-01-token-replay | Tampering | consumeInviteByToken | mitigate | Existing single-use SHA-256 consumption unchanged; subjectId thread does not touch the consume-once guard (Task 1) |
| T-01-orphan-account | Repudiation | redemption hooks | mitigate | role+tenantId injection preserved so a redeemed client is never orphaned (D-03); 03.1 UAT in Plan 07 verifies end-to-end (Task 2) |
| T-01-SC | Tampering | npm installs | accept | No new packages this plan (RESEARCH "Package Legitimacy Audit: No new packages") |
</threat_model>

<verification>
- `cd remix-app && npm test && npm run typecheck && npm run build` GREEN (wave-merge gate)
- `tests/auth/invites-server.test.ts` subjectId cases GREEN; `tests/auth/invites-consume.test.ts` + other auth tests no regression
- End-to-end redemption (private window) is the Plan 07 manual UAT — this plan delivers the wired write path it exercises
</verification>

<success_criteria>
- generateInvite binds an optional subjectId; resolve + consume return it; invites-server.test.ts GREEN
- Both signup-hook paths thread subjectId; redemption still injects role+tenantId with no orphaned account
- No extra table write at redemption (v1.1 correct wiring); build gate + auth suite pass
</success_criteria>

<output>
Create `.planning/phases/01-client-onboarding-practitioner-operated/01-04-SUMMARY.md` when done
</output>
