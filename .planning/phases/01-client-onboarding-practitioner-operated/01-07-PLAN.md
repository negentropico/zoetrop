---
phase: 01-client-onboarding-practitioner-operated
plan: 07
type: execute
wave: 3
depends_on: ["01-02", "01-04", "01-05", "01-06"]
files_modified: []
autonomous: false
requirements: [ONB-02, ONB-03]
must_haves:
  truths:
    - "Invite redemption works end-to-end in a private window: a real client account is created/linked with no orphaned account and invites.consumedAt is set"
    - "A redeemed client account hits a PHI route and receives 403 (assertSubjectAccess Gate 1)"
    - "Switching the active subject to a client scopes ingest, report, and protocol surfaces to that subject only; switching back resets to owner on a new session"
    - "The full Phase-1 verification suite (npm test + typecheck + build) is green"
  artifacts: []
  key_links:
    - from: "private-window signup with raw token"
      to: "auth.server.ts redemption hooks"
      via: "role+tenantId injection + subjectId thread"
      pattern: "consumedAt"
---

<objective>
Close SC-5 — the Phase 03.1 residual UAT — and confirm active-subject scoping end-to-end now that real client traffic exists. This is a verification-only plan: no production code changes. It exercises the wired write paths from Plans 02/04/06 with a real second user.

Purpose: SC-5 says invite redemption must work in a private window and the client-role 403 must fire with a real client account. These flows cross Better-Auth signup + a live browser session, so they are inherently manual. The 03.1 UAT was flagged "gets real traffic, not broken" — Phase 1 is the first time a real client subject + invite + redemption exist together, so it closes here.
Output: a recorded UAT result (pass/fail per check) plus the final green full-suite gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-client-onboarding-practitioner-operated/01-CONTEXT.md
@.planning/phases/01-client-onboarding-practitioner-operated/01-RESEARCH.md
@.planning/phases/01-client-onboarding-practitioner-operated/01-VALIDATION.md

<interfaces>
<!-- The wired paths this UAT exercises. -->

Plan 06: /clients → create client subject → "Generate invite link" → raw token revealed once.
Plan 04: redemption hooks inject role:"client" + tenantId + thread subjectId; invites.consumedAt + consumedBy set on consume.
Plan 02: requireSubjectCtx → assertSubjectAccess Gate 1 (client → 403) on every PHI loader; 7 Pattern-B ingest/report loaders scope to active subject.
Plan 05: SubjectChip switch elevates the chip + re-scopes surfaces.

RESEARCH "03.1 Residual UAT" — manual test protocol:
  1. As owner: create client subject → generate invite → copy raw token.
  2. Private window: GET /login?inviteToken=<raw_token>&role=client (the repo invite URL shape from settings/index.tsx L209) → sign up → verify the new user row has role:"client" + tenantId:<same> (not the default null tenant).
  3. As that client: attempt GET /dashboard → verify 403 (not a redirect, not 200).
  4. Verify the invite row has consumedAt set + consumedBy = the new user's id.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final full-suite green gate</name>
  <files></files>
  <read_first>
    - .planning/phases/01-client-onboarding-practitioner-operated/01-VALIDATION.md (Sampling Rate — full suite + build gate is mandatory before verify-work)
    - .planning/phases/01-client-onboarding-practitioner-operated/01-RESEARCH.md (Validation Architecture — full suite command + build-gate-catches-.server-leaks note)
  </read_first>
  <action>
    Run the full Phase-1 verification suite from `remix-app/`: `npm test && npm run typecheck && npm run build`. The build step is mandatory and non-negotiable — typecheck + vitest can both pass while `npm run build` fails on a `.server` module leaking into the client bundle (the known build-gate landmine). Confirm all Wave-0 tests that were RED in Plan 01 are now GREEN: `tests/lib/subjects.test.ts`, `tests/lib/active-subject.test.ts`, `tests/lib/checklist.test.ts`, `tests/lib/schema-integrity.test.ts`, and the `tests/auth/invites-server.test.ts` subjectId extensions. If anything is red, stop and report which plan's surface regressed (do not paper over a failure here).
  </action>
  <verify>
    <automated>cd remix-app && npm test && npm run typecheck && npm run build && echo FULL_SUITE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm test` exits 0 with all five Wave-0 test surfaces GREEN
    - `cd remix-app && npm run typecheck` exits 0
    - `cd remix-app && npm run build` exits 0 (no .server module leaks into the client bundle)
  </acceptance_criteria>
  <done>npm test + typecheck + build all exit 0; the Wave-0 RED stubs from Plan 01 are all GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [SC-5 / 03.1 UAT] Invite redemption end-to-end + client-role 403</name>
  <what-built>
    Plans 02/04/06 wired the full client-onboarding loop: create a client subject on /clients, generate a subject-bound invite (raw token shown once), redeem it via Better-Auth signup (role:"client" + tenantId injected, subjectId threaded, no orphaned account), and enforce a client-role 403 on every PHI loader via assertSubjectAccess Gate 1. This checkpoint verifies those paths with a REAL second user — the only way to close SC-5, since redemption + a live client session cross Better-Auth + browser boundaries that automated tests cannot exercise.
  </what-built>
  <how-to-verify>
    Run against the deployed app (or a local dev server with a live Neon connection). Follow the RESEARCH "03.1 Residual UAT" protocol exactly:

    1. As the owner: go to /clients → "Add client" → create a real client subject (fill the intake form) → return to /clients → "Generate invite link" for that client → COPY the raw token shown in the "SHOWN ONCE" panel.
    2. Open a PRIVATE / incognito window. Navigate to the invite URL: `/login?inviteToken=<raw_token>&role=client`. Complete the signup form for the new client account.
       - EXPECT: signup succeeds; the new user row has `role: "client"` and `tenantId` = the owner's tenant (NOT the default null tenant). No orphaned account is created.
    3. Verify in the DB (Drizzle Studio or a Neon query): the `invites` row for that subject has `consumedAt` set and `consumedBy` = the new client user's id. The `invites.subjectId` matches the client subject created in step 1.
    4. Still logged in as that client (private window): attempt to GET a PHI route — e.g. `/dashboard` or `/metrics`.
       - EXPECT: a 403 response (NOT a redirect to /login, NOT a 200). This is assertSubjectAccess Gate 1 firing.
    5. Back as the owner: open the SubjectChip → switch to the client subject → confirm the chip ELEVATES (accent wash + "VIEWING CLIENT") and that the ingest, report, and protocol surfaces now show only that client's data (empty for a fresh client). Confirm the dashboard shows the "No program started" placeholder (not a fake "Day 0 · Acute"). Switch back to owner → confirm owner data returns. Close + reopen the browser → confirm the session resets to owner (session-scoped cookie, D-06).

    Record each check as pass/fail in the SUMMARY. If any check fails, report which plan's surface is implicated (redemption → Plan 04; 403 → Plan 02; chip/scoping → Plans 02/05; create/invite UI → Plan 06) — do NOT mark SC-5 closed on a partial pass.
  </how-to-verify>
  <acceptance_criteria>
    - Private-window redemption creates a client account with role:"client" + the correct tenantId; no orphaned account
    - The invite row shows consumedAt set, consumedBy = the new user's id, and subjectId = the created client subject
    - The redeemed client account receives a 403 (not redirect, not 200) on a PHI route — assertSubjectAccess Gate 1
    - Switching the active subject to the client scopes ingest/report/protocol to that subject; dashboard shows "No program started"; switching back restores owner; new browser session resets to owner
    - All five checks recorded pass/fail in the SUMMARY
  </acceptance_criteria>
  <resume-signal>Type "approved" once all five UAT checks pass, or describe which check failed and the implicated plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private-window signup → redemption hooks | The raw invite token (untrusted) crosses here; the hooks hash-validate it and inject role/tenant server-side — this UAT confirms that boundary holds with real traffic |
| redeemed client session → PHI loaders | A real client session is the strongest test of assertSubjectAccess Gate 1; the UAT confirms the 403 fires, not just the unit test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-client-403-live | Elevation of Privilege | redeemed client → PHI route | mitigate | UAT check 4 confirms assertSubjectAccess Gate 1 returns 403 for a real client account, not just in unit tests (Task 2) |
| T-01-orphan-live | Repudiation | redemption | mitigate | UAT checks 2-3 confirm role+tenantId injection + consumedAt/consumedBy/subjectId, i.e. no orphaned account (Task 2, D-03) |
| T-01-session-reset | Session Management | zt-subject session cookie | mitigate | UAT check 5 confirms closing+reopening the browser resets to owner (session-scoped, D-06) (Task 2) |
| T-01-build-leak | Tampering | client bundle | mitigate | Task 1 runs npm run build as the mandatory gate — catches any .server module that leaked across Plans 02-06 |
</threat_model>

<verification>
- Task 1: `cd remix-app && npm test && npm run typecheck && npm run build` exit 0
- Task 2: all five SC-5 UAT checks recorded pass in the SUMMARY (manual, blocking human-verify)
</verification>

<success_criteria>
- SC-5 closed: invite redemption works end-to-end in a private window with no orphaned account; client-role 403 fires with a real client account
- Active-subject scoping confirmed end-to-end across ingest/report/protocol + dashboard placeholder; session resets to owner
- Full Phase-1 suite (test + typecheck + build) green; Wave-0 RED stubs all GREEN
</success_criteria>

<output>
Create `.planning/phases/01-client-onboarding-practitioner-operated/01-07-SUMMARY.md` when done
</output>
