---
phase: quick
plan: 260704-dh3
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
  - docs/PRINCIPLES.md
  - .planning/PROJECT.md
autonomous: true
requirements: [DOC-ACCURACY]

must_haves:
  truths:
    - "No core doc (README, CLAUDE.md, PRINCIPLES, PROJECT.md) claims Netlify deployment"
    - "Every doc reporting the schema size says 22 tables / 636 lines (not 8 tables / 201 lines)"
    - "README + CLAUDE.md route/structure tables match the real routes.ts + on-disk tree (auth, landing, _app/*, ingest, reports, clients, settings)"
    - "CLAUDE.md names Better-Auth and design-bridge, and reflects v1.0-shipped / v1.1-executing state"
    - "docs/HISTORY.md and docs/NAMING.md are untouched"
  artifacts:
    - path: "README.md"
      provides: "Vercel deployment, post-v1.0 status, explicit routing, full route surface"
      contains: "zoetrop.vercel.app"
    - path: "CLAUDE.md"
      provides: "accurate directory/route tables, Better-Auth + design-bridge, current planning state, footer date 2026-07-04"
      contains: "Better-Auth"
    - path: "docs/PRINCIPLES.md"
      provides: "corrected line-20 factual claim (Vercel, 22 tables, tenancy shipped)"
      contains: "vercel.app"
    - path: ".planning/PROJECT.md"
      provides: "Vercel replacing Netlify on lines 35 + 82"
      contains: "Vercel"
  key_links:
    - from: "README.md / CLAUDE.md route tables"
      to: "remix-app/app/routes.ts"
      via: "route paths transcribed verbatim"
      pattern: "routes/_app/"
---

<objective>
Refresh four stale core docs to post-v1.0 reality. A 2026-07-04 /orient audit found
verified-stale claims: Netlify deployment (now Vercel), pre-auth/pre-tenancy status
(v1.0 shipped 2026-06-14, v1.1 executing), "8 tables / 201 lines" schema (now 22
tables / 636 lines), "file-based routing" (now explicit `app/routes.ts`), and route
tables missing the entire authenticated app surface (auth, ingest, reports, clients,
settings). All claims were re-verified against the live codebase during planning.

Purpose: Core docs are the first thing a human or agent reads. Stale deployment URLs,
wrong table counts, and a missing auth/route surface actively mislead. Fix the facts,
preserve each doc's structure and voice.

Output: Corrected README.md, CLAUDE.md, docs/PRINCIPLES.md, .planning/PROJECT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@remix-app/app/routes.ts

<verified_facts>
<!-- All re-verified against the live codebase 2026-07-04. Use these directly. -->

SCHEMA (both README + CLAUDE.md say "201 lines" / "8 tables"):
- `grep -c pgTable remix-app/db/schema.ts` → 22
- `wc -l remix-app/db/schema.ts` → 636
- Correct phrasing: "636 lines, 22 tables".

ROUTING is explicit via `remix-app/app/routes.ts` (`RouteConfig` from
`@react-router/dev/routes` — index/route/layout), NOT file-name convention.

PUBLIC routes (no auth):
- `/`            → routes/landing.tsx   (landing page — NOT the dashboard)
- `/login`       → routes/auth/login.tsx
- `/logout`      → routes/auth/logout.tsx
- `/api/auth/*`  → routes/api.auth.$.ts  (Better-Auth catch-all handler)

AUTHENTICATED routes — all children of `routes/_app/layout.tsx`, whose loader throws
`redirect('/login?redirect=<pathname>')` (302 → /login) when there is no session:
- /dashboard                         → routes/_app/dashboard.tsx
- /metrics                           → routes/_app/metrics/index.tsx
- /metrics/:category                 → routes/_app/metrics/category.tsx
- /metrics/:category/:metricId       → routes/_app/metrics/detail.tsx
- /protocol                          → routes/_app/protocol/index.tsx
- /protocol/versions                 → routes/_app/protocol/versions.tsx
- /protocol/versions/:version        → routes/_app/protocol/version-detail.tsx
- /protocol/supplements              → routes/_app/protocol/supplements.tsx
- /protocol/cessation                → routes/_app/protocol/cessation.tsx
- /protocol/compare                  → routes/_app/protocol/compare.tsx
- /insights                          → routes/_app/insights/index.tsx
- /insights/correlations             → routes/_app/insights/correlations.tsx
- /insights/genetics                 → routes/_app/insights/genetics.tsx
- /import                            → routes/_app/import/index.tsx
- /import/whoop                      → routes/_app/import/whoop.tsx
- /import/vault                      → routes/_app/import/vault.tsx
- /ingest                            → routes/_app/ingest/index.tsx
- /ingest/upload                     → routes/_app/ingest/upload.tsx
- /ingest/review                     → routes/_app/ingest/review.tsx
- /ingest/consent                    → routes/_app/ingest/consent.tsx
- /ingest/documents/:id              → routes/_app/ingest/document.tsx
- /ingest/documents/:id/raw          → routes/_app/ingest/document-raw.tsx (raw PDF bytes)
- /reports                           → routes/_app/reports/index.tsx
- /reports/generate                  → routes/_app/reports/generate.tsx
- /reports/:reportId                 → routes/_app/reports/detail.tsx
- /clients                           → routes/_app/clients/index.tsx
- /clients/new                       → routes/_app/clients/new.tsx
- /settings                          → routes/_app/settings/index.tsx
- /settings/invites/:inviteId/revoke → routes/_app/settings/invites.ts (action)
- /settings/assignments              → routes/_app/settings/assignments.tsx
- /subject-switch                    → routes/_app/subject-switch.ts (action-only; sets zt-subject cookie)

AUTH: Better-Auth email/password. `remix-app/app/lib/auth.server.ts` calls
`betterAuth({ emailAndPassword: {...} })`; `routes/api.auth.$.ts` is the catch-all;
gated routes 302→/login via `_app/layout.tsx` loader.

DESIGN ROOT (since 2026-06-29): single top-level `design-bridge/` root —
LIVE `remix-app/app/` · DS PACKAGE `design-bridge/design-system/` ·
MACHINERY `design-bridge/` (harness + diagrams).

DEPLOYMENT: Vercel — https://zoetrop.vercel.app ; per-branch preview URLs
auto-generated by Vercel. (README, PROJECT.md ×2, PRINCIPLES line 20 still say Netlify.)

STATE (from .planning/STATE.md header):
- v1.0 "M1 Foundations" SHIPPED 2026-06-14: 9 phases / 50 plans / 116 tasks;
  27/29 requirements (COMP-02/03 deferred); tag v1.0.
- Current milestone: v1.1 "First Client (practitioner-operated)" — EXECUTING.
  5 phases, 12 requirements (ONB/ING/PRO/LIB/PROOF/POL).
  Phase 01 (client-onboarding-practitioner-operated) — plan 7 of 7.

STALE "two open decisions" (CLAUDE.md):
- Better-Auth↔Neon-JWK seam: RESOLVED by the v1.0 auth implementation.
- LLM-provider PHI/BAA: re-decided 2026-06-12 as pilot-first deferral —
  HIPAA/BAA/RLS hardening deferred to a pre-first-external-client gate; stay on Neon.
</verified_facts>
</context>

<constraints>
- Stage files explicitly by path (never `git add -A`) — parallel sessions share this tree.
- Do NOT touch docs/HISTORY.md or docs/NAMING.md — their Netlify mentions are intentional history.
- Targeted factual corrections only; preserve each doc's existing structure, headings, and voice. Do not rewrite whole docs.
- In CLAUDE.md keep the "GSD Workflow Enforcement" and "Naming & Direction" sections intact (still accurate).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Correct README.md to post-v1.0 reality</name>
  <files>README.md</files>
  <action>
Make these targeted edits, preserving section order and Markdown style:
1. Status blockquote (line 5): replace "n=1 personal instrument today (M0)" framing with the current state — v1.0 "M1 Foundations" shipped 2026-06-14; v1.1 "First Client (practitioner-operated)" now executing. Keep the PLATFORM.md / codename / deferred-brand pointers.
2. Tech Stack "Deployment: Netlify" → "Deployment: Vercel".
3. Project Structure block: change routes comment "File-based routing" → 'Explicit routing via `app/routes.ts` (RouteConfig)'; change `schema.ts` comment "Drizzle table definitions (201 lines)" → "Drizzle table definitions (636 lines, 22 tables)".
4. Routes table: replace with the real surface from <verified_facts>. Root `/` is the landing page (not the dashboard); add the authenticated app routes (dashboard, clients, ingest, reports, settings) and note the app is gated by Better-Auth (302→/login). Keep the table compact — representative routes are fine, but it must NOT still show `/` as "Dashboard with cessation tracker" or omit auth/clients/ingest/reports.
5. Deployment table: Production URL → https://zoetrop.vercel.app; replace the "Dev preview / dev--zoetrop.netlify.app" row with per-branch preview URLs auto-generated by Vercel.
Do not invent routes not in <verified_facts>.
  </action>
  <verify>
    <automated>grep -ic netlify README.md | grep -q '^0$' && grep -q 'zoetrop.vercel.app' README.md && grep -q '636 lines, 22 tables' README.md && grep -q 'app/routes.ts' README.md && ! grep -q '201 lines' README.md && echo PASS</automated>
  </verify>
  <done>README has zero Netlify mentions, states Vercel + vercel.app, cites 636 lines / 22 tables, describes explicit routing, and the route table includes the authenticated surface.</done>
</task>

<task type="auto">
  <name>Task 2: Correct CLAUDE.md directory/route tables, auth, design root, planning state, footer</name>
  <files>CLAUDE.md</files>
  <action>
Make these targeted edits, preserving the doc's heading structure and voice:
1. Directory Structure tree: replace the pre-v1.0 tree (top-level `routes/home.tsx`, `routes/metrics/` etc.) with the real tree from <verified_facts> — routes are explicit via `app/routes.ts`; the authenticated app lives under `routes/_app/` (dashboard, metrics, protocol, insights, import, ingest, reports, clients, settings, subject-switch), public routes are `landing.tsx`, `auth/{login,logout}.tsx`, and the `api.auth.$.ts` Better-Auth catch-all. Update the `db/schema.ts` annotation from "(201 lines, 8 tables)" → "(636 lines, 22 tables)". Keep the prose note that routing is explicit via `app/routes.ts` (already correct).
2. Route Structure table: replace with the real route → file map from <verified_facts>. `/` is `routes/landing.tsx` (landing, public); add `/login`, `/logout`, `/dashboard`, the `_app/*` file paths for existing sections, and the new `ingest`, `reports`, `clients`, `settings` routes. Note authenticated routes are gated (302→/login).
3. Add a short factual note (in Tech Stack or a brief Auth line, wherever it fits the existing structure) that auth is Better-Auth email/password via `api.auth.$.ts` catch-all, with gated routes redirecting 302→/login.
4. Add a design-root note: single top-level `design-bridge/` since 2026-06-29 — LIVE `remix-app/app/` · DS PACKAGE `design-bridge/design-system/` · MACHINERY `design-bridge/` (harness + diagrams). Place it near Directory Structure or Naming & Direction without disrupting those sections' intent.
5. Planning Workflow section: replace "the M1 roadmap (6 phases)" and the "Next step: /gsd:discuss-phase 1 … Phase 1 = schema baseline + engine tests + auth spike" line with the real state — v1.0 M1 Foundations shipped 2026-06-14 (9 phases / 50 plans, tag v1.0); current milestone v1.1 "First Client (practitioner-operated)", 5 phases, Phase 01 executing (plan 7 of 7). Update the next-step pointer accordingly (Phase 1 client onboarding is in progress; see .planning/STATE.md).
6. "Two open decisions" bullet: correct it — the Better-Auth↔Neon-JWK seam was RESOLVED by the v1.0 auth implementation; the LLM-provider PHI/BAA decision was re-decided 2026-06-12 as a pilot-first deferral (HIPAA/BAA/RLS hardening deferred to a pre-first-external-client gate; stay on Neon). If nothing remains genuinely open, reframe as "resolved decisions" rather than deleting silently.
7. Footer "Last Updated: June 7, 2026" → "Last Updated: July 4, 2026".
Leave the "GSD Workflow Enforcement" and "Naming & Direction" sections intact except where item 4 adds the design-root note.
  </action>
  <verify>
    <automated>grep -q '636 lines, 22 tables' CLAUDE.md && grep -q 'Better-Auth' CLAUDE.md && grep -q 'design-bridge' CLAUDE.md && grep -q 'v1.1' CLAUDE.md && grep -q 'routes/_app' CLAUDE.md && grep -q 'July 4, 2026' CLAUDE.md && ! grep -q '201 lines, 8 tables' CLAUDE.md && ! grep -q 'discuss-phase 1' CLAUDE.md && echo PASS</automated>
  </verify>
  <done>CLAUDE.md directory + route tables match routes.ts, names Better-Auth and design-bridge, reflects v1.0-shipped/v1.1-executing planning state with corrected decisions, and the footer reads July 4, 2026. No "201 lines, 8 tables" or "discuss-phase 1" remains.</done>
</task>

<task type="auto">
  <name>Task 3: Minimal Netlify→Vercel + table-count fixes in PRINCIPLES.md and PROJECT.md</name>
  <files>docs/PRINCIPLES.md, .planning/PROJECT.md</files>
  <action>
Minimal, surgical edits only — preserve each doc's voice and surrounding text:
1. docs/PRINCIPLES.md line 20: "Live at `zoetrop.netlify.app`. 8 Drizzle tables, all single-subject (no identity/tenancy yet — the central M1 gap)." → correct the three factual claims: live at `zoetrop.vercel.app`; 22 Drizzle tables; identity/tenancy shipped in v1.0 (so it is no longer the open M1 gap). Rephrase in the doc's existing narrative voice — this is a principles doc, not a status board; do not restructure the section.
2. .planning/PROJECT.md line 35: "+ Netlify CI/CD" → "+ Vercel CI/CD".
3. .planning/PROJECT.md line 82: "+ Netlify" → "+ Vercel".
Do not touch docs/HISTORY.md or docs/NAMING.md.
  </action>
  <verify>
    <automated>grep -ic netlify docs/PRINCIPLES.md | grep -q '^0$' && grep -q 'vercel.app' docs/PRINCIPLES.md && grep -ic netlify .planning/PROJECT.md | grep -q '^0$' && grep -qc Vercel .planning/PROJECT.md && echo PASS</automated>
  </verify>
  <done>PRINCIPLES line 20 reads Vercel / 22 tables / tenancy-shipped; PROJECT.md lines 35 and 82 say Vercel; neither file mentions Netlify; HISTORY.md and NAMING.md untouched.</done>
</task>

</tasks>

<verification>
- Repo-wide: the four target files contain zero "netlify" (case-insensitive) mentions; docs/HISTORY.md and docs/NAMING.md still do (intentional history, untouched).
- `grep -c pgTable remix-app/db/schema.ts` = 22 and `wc -l` = 636 match the numbers now written in README + CLAUDE.md.
- README + CLAUDE.md route surfaces contain no route absent from remix-app/app/routes.ts.
- `git status` shows only README.md, CLAUDE.md, docs/PRINCIPLES.md, .planning/PROJECT.md modified.
</verification>

<success_criteria>
- No target doc claims Netlify deployment; all say Vercel / zoetrop.vercel.app.
- Every schema-size claim reads 636 lines / 22 tables.
- README + CLAUDE.md describe explicit `app/routes.ts` routing and include the authenticated surface (auth, ingest, reports, clients, settings).
- CLAUDE.md names Better-Auth and design-bridge, reflects v1.0-shipped/v1.1-executing state with corrected decisions, footer dated July 4, 2026.
- docs/HISTORY.md and docs/NAMING.md unchanged; edits staged explicitly by path.
</success_criteria>

<output>
Create `.planning/quick/260704-dh3-refresh-stale-core-docs-to-post-v1-0-rea/260704-dh3-SUMMARY.md` when done.
</output>
