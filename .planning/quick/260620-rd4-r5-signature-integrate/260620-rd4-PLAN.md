---
id: 260620-rd4
slug: r5-signature-integrate
type: quick
status: ready
created: 2026-06-21
branch: design/zoetrop-dl-prime
worktrees: false   # background skill-tuner agent is editing the main tree; run sequential + stage explicitly
---

# Quick Task 260620-rd4 — Integrate round5 LINE-signature into the remix app

## Objective

Phase 5 of the round5 `LINE-signature` design line: bring the **gated** design return (the
spiral/phyllotaxis "signature" layer) into the real app as the repo's stack (React Router 7 + TS +
Tailwind v4) — **changing zero locked tokens.** Within-LOCK refinement off frozen ZOETROP-R1.

## Source of truth (on disk; do NOT paste prototype UMD JSX)

- `design-bridge/harness/rounds/round5/return/CHANGES.md` — the spec + per-screen log + (a)–(f) ledger.
- `design-bridge/harness/rounds/round5/return/new.css` — the `zt-sig-*` rules to fold into `app.css` **verbatim** (additions-only; every `var()` already resolves in `app.css`).
- `design-bridge/harness/rounds/round5/return/app/sig.jsx` — the prototype signature logic to **re-author** as real TS components (it uses `window` globals + UMD React — do not copy that shape).

## must_haves

- **truths:**
  - Zero new/changed CSS custom properties (tokens). Every `zt-sig-*` value resolves to an existing token. (verify: `git diff remix-app/app/app.css` shows no `--token:` definitions added/changed — only new `.zt-sig-*` / `@keyframes ztSig*` rules + the two logged shims).
  - Both themes work via the existing `html[data-theme="dark"]` remap — no per-component dark selectors beyond the two dark `zt-sig` rules already in `new.css`.
  - JS-off and reduced-motion both render the resting (final, visible) state — motion/grain gated under `.zt-sig-on` + `@media (prefers-reduced-motion: no-preference)`.
  - The dense **metrics catalog** rows carry the icon-signature only — **no watermark, no grain on rows**.
- **artifacts:**
  - `remix-app/app/components/ui/Signature.tsx` (new TS module).
  - `remix-app/app/app.css` (+ the `zt-sig-*` layer at the tail).
  - `remix-app/app/components/shell/AppShell.tsx`, `components/ui/TrendChart.tsx`, `routes/_app/dashboard.tsx`, `routes/_app/metrics/index.tsx`, `routes/_app/metrics/detail.tsx` (signature applied).
- **key_links:** `design-bridge/harness/rounds/round5/return/{CHANGES.md,new.css,app/sig.jsx}`.

## Resolved reconciliations (locked for this task — do not re-litigate)

1. **Spiral geometry:** keep the inline `ZT_SPIRAL_D` path constant in `Signature.tsx` (it is byte-identical to `docs/design-system/assets/mark-spiral.svg`). Add a comment citing that asset as the canonical source. (Avoids an asset fetch for a hairline watermark; keeps the module self-contained.)
2. **Grain:** adopt the new `feTurbulence` canvas grain from `new.css`. Leave the existing unused `.zt-grain` dotted utility in `docs/design-system/tokens/base.css` untouched (out of scope).
3. **Grain stacking:** keep `.zt-sig-on .zn-app { position: relative }` + `::before { z-index: -1 }`. Verify in the honesty render that grain sits behind content and does NOT disturb the collapsed-rail flyout / sticky topbar / mobile drawer stacking.

## Tasks

### Task 1 — Signature module + CSS layer (foundation)
- **files:** `remix-app/app/components/ui/Signature.tsx` (new); `remix-app/app/app.css`.
- **action:**
  - Re-author `sig.jsx` as `Signature.tsx`: typed React components/exports — `SigGhost`, `phyllotaxis`, `goldenRanks`, `sigDelay`, `SigSeed`, `SigEmpty`, `SigLoading`, `FrameDot`, `SigEyebrow`, `useSignature`. Use real `import { useEffect } from "react"` (not `window`/UMD). For the `--sig-i` inline custom property, type the style as `React.CSSProperties` (cast `{ "--sig-i": n } as React.CSSProperties`). Keep `ZT_SPIRAL_D` inline (reconciliation #1).
  - Append `new.css` (the whole `zt-sig-*` layer incl. the two shims + `@keyframes ztSigDraw/ztSigSettle/ztSigSeed`) verbatim to the end of `app.css`. **Add no tokens.**
- **verify:** `cd remix-app && npx react-router typegen && npx tsc --noEmit` clean; `grep -nE '^\s*--[a-z]' ` of the appended block shows no token definitions.
- **done:** module compiles; app.css carries the layer; no token delta.

### Task 2 — Root wiring + chart empty/loading delegation (shared plumbing)
- **files:** `remix-app/app/components/shell/AppShell.tsx`; `remix-app/app/components/ui/TrendChart.tsx`.
- **action:**
  - In `AppShell` (owns `.zn-app`): call `useSignature()` so `.zt-sig-on` is added on mount (enables grain + the settle under the honesty gate).
  - In `TrendChart`: render `SigEmpty` for the empty state and `SigLoading` for the loading state, replacing/extending the bare `zt-chart-empty` (graceful — the frame-card tooltip, band idiom, status-token rules stay unchanged; deepen finish only).
- **verify:** typecheck clean; empty/loading states render the spiral ghost / phyllotaxis seed.
- **done:** grain appears on the canvas; charts show branded empty/loading.

### Task 3 — Apply the signature to the three archetypes (per CHANGES per-screen)
- **files:** `routes/_app/dashboard.tsx`; `routes/_app/metrics/detail.tsx`; `routes/_app/metrics/index.tsx`.
- **action:**
  - **Dashboard:** corner spiral watermark (`SigGhost draw`) clipped on the cessation hero; `zt-sig-frame` + φ-stagger (`style={sigDelay(goldenRanks(n)[i])}`) on the category cards (9) and highlight cards (4); section eyebrows via `SigEyebrow` (frame-dot); hairline `zt-sig-icon` frames on category icon-tiles.
  - **Metric detail:** `zt-sig-frame` settle on the frame cards; the chart already delegates to `SigEmpty`/`SigLoading` (Task 2).
  - **Metrics catalog:** `zt-sig-frame` + φ-stagger + `zt-sig-icon` on category cards. **No watermark. No grain on rows.** (Grain is canvas-level only; do not add it per-row.)
- **verify:** typecheck clean; dense list reads calm (no watermark/row-grain).
- **done:** all three archetypes carry the signature per CHANGES; catalog stays calm.

### Task 4 — Gate + honesty render
- **files:** none (verification).
- **action:** `cd remix-app && npx react-router typegen && npx tsc --noEmit && npm run build` (build gate is mandatory — typecheck/vitest can pass while build fails on `.server` leaks). If tests exist, `npm run test`. Then honesty render: dev server, check the 3 surfaces in **light + dark**, **JS-off** (resting state visible), **reduced-motion** (instant), and confirm grain stacking (reconciliation #3) does not break flyout/sticky/drawer.
- **verify:** build passes; honesty render holds in both themes.
- **done:** within-LOCK signature lands; zero token delta; both themes + JS-off + reduced-motion pass.

## Out of scope
App-wide propagation to the other ~13 surfaces (carry-forward); wiring the settle as a live data-update gesture; any locked-token change (that would be ZOETROP-R2). The DECISIONS ledger close + manifest `returned`-flip happens in the design-line Phase 6 after this lands.
