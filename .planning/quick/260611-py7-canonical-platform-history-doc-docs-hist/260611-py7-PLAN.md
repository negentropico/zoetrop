---
phase: quick-260611-py7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/HISTORY.md, CLAUDE.md]
autonomous: true
requirements: [QUICK-py7]

must_haves:
  truths:
    - "docs/HISTORY.md exists and is byte-identical to the fact-checked draft"
    - "CLAUDE.md's Naming & Direction section points readers to docs/HISTORY.md"
    - "Both changes land in a single atomic commit with nothing else staged"
  artifacts:
    - path: "docs/HISTORY.md"
      provides: "Canonical platform history (Bwell vault -> Tracker -> Zoetrop)"
      contains: "# Zoetrop — Platform History"
    - path: "CLAUDE.md"
      provides: "One-line pointer to the history doc"
      contains: "**History doc**: `docs/HISTORY.md`"
  key_links:
    - from: "CLAUDE.md"
      to: "docs/HISTORY.md"
      via: "Naming & Direction bullet"
      pattern: "History doc.*docs/HISTORY\\.md"
---

<objective>
Install the canonical platform history document at `docs/HISTORY.md` (verbatim from the orchestrator-synthesized draft) and add a one-line pointer to it in CLAUDE.md's "Naming & Direction" section, committed atomically.

Purpose: Make the platform's full evolution (Bwell vault -> Tracker -> Zoetrop) discoverable as a first-class doc alongside `docs/PLATFORM.md`.
Output: `docs/HISTORY.md` + one added line in `CLAUDE.md`, one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260611-py7-canonical-platform-history-doc-docs-hist/260611-py7-DRAFT-HISTORY.md
@CLAUDE.md

Working directory: /Users/mac/Code/zoetrop/.claude/worktrees/left-nav (git worktree, branch left-nav-refactor). User runs concurrent sessions on this tree — stage files explicitly, never `git add -A`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install docs/HISTORY.md verbatim, add CLAUDE.md pointer, commit atomically</name>
  <files>docs/HISTORY.md, CLAUDE.md</files>
  <action>
    (a) Copy the draft verbatim to `docs/HISTORY.md`. Use a byte-preserving copy, NOT a Read+Write round-trip (which risks whitespace/encoding drift):
    `cp .planning/quick/260611-py7-canonical-platform-history-doc-docs-hist/260611-py7-DRAFT-HISTORY.md docs/HISTORY.md`
    Do NOT rewrite, reformat, or "improve" any draft content — it is the deliverable, already fact-checked against research.

    (b) In `CLAUDE.md`, section "## Naming & Direction": insert exactly one new bullet line immediately AFTER the "- **Direction doc**: `docs/PLATFORM.md` ..." line and BEFORE the "- **Flagship**: ..." line. The inserted line is exactly:
    `- **History doc**: \`docs/HISTORY.md\` — canonical record of the platform's full evolution (Bwell vault → Tracker → Zoetrop)`
    (em-dash and arrow characters as shown; no other edits anywhere in CLAUDE.md — do not touch the "Last Updated" footer.)

    (c) Stage ONLY these two files explicitly and commit both atomically:
    `git add docs/HISTORY.md CLAUDE.md`
    `git commit -m "docs: install canonical platform history at docs/HISTORY.md"`
    Before committing, confirm `git diff --cached --stat` lists exactly docs/HISTORY.md and CLAUDE.md and nothing else (concurrent sessions may have dirtied the tree — never `git add -A`).
  </action>
  <verify>
    <automated>diff .planning/quick/260611-py7-canonical-platform-history-doc-docs-hist/260611-py7-DRAFT-HISTORY.md docs/HISTORY.md && [ "$(git show HEAD --numstat -- CLAUDE.md | awk 'NR>1 && NF==3 {print $1"/"$2}')" = "1/0" ] && git show HEAD --stat | grep -c -E 'docs/HISTORY\.md|CLAUDE\.md' | grep -qx 2</automated>
  </verify>
  <done>
    - `diff` between the draft and `docs/HISTORY.md` produces no output (byte-identical)
    - The commit's CLAUDE.md change is exactly 1 line added, 0 removed (the History doc bullet, positioned between Direction doc and Flagship bullets)
    - The commit touches exactly two files: `docs/HISTORY.md` and `CLAUDE.md`
  </done>
</task>

</tasks>

<verification>
- `diff .planning/quick/260611-py7-canonical-platform-history-doc-docs-hist/260611-py7-DRAFT-HISTORY.md docs/HISTORY.md` → empty
- `grep -n "History doc" CLAUDE.md` → one hit, in the Naming & Direction section, on the line after the Direction doc bullet
- `git show HEAD --stat` → exactly docs/HISTORY.md (new, 175 lines) + CLAUDE.md (+1/-0)
- Working tree otherwise untouched by this task (no stray staged files)
</verification>

<success_criteria>
- Canonical history doc lives at `docs/HISTORY.md`, byte-identical to the fact-checked draft
- CLAUDE.md routes readers to it via exactly one added bullet
- Single atomic commit containing only these two files
</success_criteria>

<output>
Create `.planning/quick/260611-py7-canonical-platform-history-doc-docs-hist/260611-py7-SUMMARY.md` when done
</output>
