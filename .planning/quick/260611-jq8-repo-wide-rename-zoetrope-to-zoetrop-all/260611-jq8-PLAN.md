---
quick: 260611-jq8
type: execute
wave: 1
depends_on: []
files_modified:
  - "all tracked text files matching /zoetrope/i (~219 lines across docs/, .planning/, remix-app/, docs/design-system/)"
  - "docs/design-system/_rounds/round1/Zoetrope Redesign.html -> Zoetrop Redesign.html (git mv)"
  - remix-app/app/components/ui/Wordmark.tsx (pre-existing user edit, committed verbatim)
autonomous: true
requirements: [QUICK-260611-jq8]
must_haves:
  truths:
    - "git grep -I -i zoetrope returns zero matches repo-wide (or only documented base64-interior residue)"
    - "Harness acceptance test passes: unbundled second style block matches round2/extracted/zn-proto.css (whitespace-normalized)"
    - "typecheck, lint, test, build all pass in remix-app"
    - "Exactly one code commit containing sweep + filename rename + Wordmark.tsx edit"
  artifacts:
    - path: "docs/design-system/_rounds/round1/Zoetrop Redesign.html"
      provides: "renamed round1 archive file"
    - path: "remix-app/app/components/ui/Wordmark.tsx"
      provides: "user's rebrand edit (renders 'zoetrop', codename tag removed) — included verbatim"
  key_links:
    - from: "docs/design-system/_rounds/harness/unbundle.mjs"
      to: "docs/design-system/_rounds/round2/return/Left Nav Prototype (standalone).html"
      via: "acceptance test re-run after sweep"
      pattern: "zn-proto.css second-style-block match"
---

<objective>
Repo-wide mechanical rename Zoetrope→Zoetrop in all three case variants (ZOETROPE/Zoetrope/zoetrope → ZOETROP/Zoetrop/zoetrop), including archives and the round2 raw-drop bundle (explicit user instruction overrides the harness "never edit the raw drop" rule), plus git-mv of the one filename match. Verify with full remix-app gates and the harness acceptance test, then commit everything in one code commit — including the pre-existing unstaged Wordmark.tsx edit.

Purpose: Complete the codename rebrand (formalized 2026-05-20) at the text level.
Output: One commit; zero remaining /zoetrope/i matches; SUMMARY.md (uncommitted).
</objective>

<context>
Pre-verified facts (re-verify cheaply at execution start, do not re-survey):
- ~219 matching lines (145 "Zoetrope", 87 "zoetrope", 6 "ZOETROPE" occurrences). All text files — `git grep -lI` is safe. No plural/other variants.
- Worktree status: ONLY `M remix-app/app/components/ui/Wordmark.tsx` (user's intentional rebrand edit: wordmark renders "zoetrop", codename tag removed — include verbatim, do NOT modify further). Zero untracked files. This is a session-isolated worktree (branch left-nav-refactor) but other sessions may commit to .planning/ — re-check `git status` immediately before `git add`.
- One filename match: `docs/design-system/_rounds/round1/Zoetrope Redesign.html` (contains a space — quote it).
- Round2 raw drop (`docs/design-system/_rounds/round2/return/`): bundler HTML has 2 matching lines (readable template/JSX sections only; base64 manifest blobs contain no literal "zoetrope"); nav-app.jsx 2 lines, sidebar.jsx 3 lines. Sweep them — user instruction includes archives.
- Harness reference target `docs/design-system/_rounds/round2/extracted/zn-proto.css` contains ZERO matches — acceptance test must stay green after sweep.
- `docs/design-system/_ds_manifest.json` is JSON text — sweep it; note caveat in SUMMARY (if it embeds content hashes for the claude.ai skill bundle, bundle may need regeneration before next upload — known caveat, not a blocker).
- remix-app node_modules installed; scripts confirmed: typecheck (`react-router typegen && tsc`), lint, test (`vitest run`), build.
- Sidebar.tsx zn-wordmark: after sweep it reads "zoetrop" with its existing periwinkle period span — leave exactly as the sweep produces; do NOT harmonize with Wordmark.tsx's period removal (out of scope).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep, rename, gate, commit</name>
  <files>all tracked text files matching /zoetrope/i; docs/design-system/_rounds/round1/Zoetrope Redesign.html (git mv); remix-app/app/components/ui/Wordmark.tsx (pre-existing edit, committed as-is)</files>
  <action>
Execute the locked procedure exactly, in this order:

**1. Content sweep** (must run BEFORE the git mv so the grep file list sees the original path; longest-case-first ordering prevents double-application since "zoetrop" is a prefix of "zoetrope"):

```bash
git grep -lI -i zoetrope | while IFS= read -r f; do
  perl -pi -e 's/ZOETROPE/ZOETROP/g; s/Zoetrope/Zoetrop/g; s/zoetrope/zoetrop/g' "$f"
done
```

**2. Filename rename** (quoted — name contains a space):

```bash
git mv "docs/design-system/_rounds/round1/Zoetrope Redesign.html" "docs/design-system/_rounds/round1/Zoetrop Redesign.html"
```

**3. Gates — all four must pass:**

Gate 1 — zero residue: `git grep -I -i zoetrope` returns ZERO matches. If any hit remains in the round2 bundler HTML, inspect it: acceptable ONLY if inside a base64 data field; document any such residue in SUMMARY. Any other residue = fix and re-run.

Gate 2 — remix-app build pipeline: `cd remix-app && npm run typecheck && npm run lint && npm run test && npm run build`.

Gate 3 — harness acceptance test (from repo root):
```bash
node docs/design-system/_rounds/harness/unbundle.mjs "docs/design-system/_rounds/round2/return/Left Nav Prototype (standalone).html" /tmp/jq8-unbundle-test
```
then whitespace-normalized compare of the SECOND extracted style block (/tmp/jq8-unbundle-test/styles/, second NN.css) against `docs/design-system/_rounds/round2/extracted/zn-proto.css` — e.g. `diff <(tr -s '[:space:]' ' ' < /tmp/jq8-unbundle-test/styles/01.css) <(tr -s '[:space:]' ' ' < docs/design-system/_rounds/round2/extracted/zn-proto.css)` (confirm which NN is the second block from unbundle output; styles are zero- or one-indexed per the script — pick the second emitted file). Must match.

Gate 4 — spot-check: Sidebar.tsx zn-wordmark string now reads "zoetrop" (periwinkle period span structure untouched); Wordmark.tsx still contains the user's edit unmodified.

**4. Commit** — ONE code commit. First re-verify `git status` shows ONLY: sweep modifications + the round1 rename + Wordmark.tsx + this quick-task's .planning/quick/260611-jq8-* dir. Then `git add -A` (PERMITTED for this task only — user said "commit all") but EXCLUDE the planning artifacts: after `git add -A`, run `git reset .planning/quick/260611-jq8-repo-wide-rename-zoetrope-to-zoetrop-all/` so the orchestrator commits those separately. Commit message:

`chore: rename Zoetrope -> Zoetrop repo-wide (content + round1 filename; includes wordmark rebrand edit)`

If git status shows unexpected files from a concurrent session, stage the sweep files explicitly instead of add -A and note it in SUMMARY.
  </action>
  <verify>
    <automated>git grep -I -i zoetrope | wc -l  # must print 0 (or only documented base64-interior lines); plus gates 2-3 commands above all exit 0; git log -1 --stat shows the single commit with rename + Wordmark.tsx</automated>
  </verify>
  <done>Zero /zoetrope/i matches repo-wide; typecheck+lint+test+build green; harness second-style-block matches zn-proto.css whitespace-normalized; exactly one commit containing sweep + filename rename + Wordmark.tsx; .planning/quick/260611-jq8-* left uncommitted.</done>
</task>

</tasks>

<verification>
- `git grep -I -i zoetrope` → 0 matches (documented base64-interior residue is the only tolerated exception)
- `cd remix-app && npm run typecheck && npm run lint && npm run test && npm run build` → all exit 0
- Harness acceptance test → second extracted style block ≡ zn-proto.css (whitespace-normalized)
- `git status` after commit → only .planning/quick/260611-jq8-* remains uncommitted
- `git show --stat HEAD` → includes `Zoetrope Redesign.html => Zoetrop Redesign.html` rename and Wordmark.tsx
</verification>

<success_criteria>
- Repo-wide rename complete in all three case variants, archives included
- One filename renamed via git mv (history-preserving)
- All four gates green
- Single code commit with the locked message; Wordmark.tsx edit included verbatim
- SUMMARY notes the _ds_manifest.json hash-regeneration caveat and any base64 residue (expected: none)
</success_criteria>

<output>
Create `.planning/quick/260611-jq8-repo-wide-rename-zoetrope-to-zoetrop-all/260611-jq8-SUMMARY.md` when done (leave uncommitted — orchestrator commits planning artifacts).
</output>
