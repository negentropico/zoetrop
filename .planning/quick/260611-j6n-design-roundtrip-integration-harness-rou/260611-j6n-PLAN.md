---
phase: quick-260611-j6n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design-system/_rounds/harness/unbundle.mjs
  - docs/design-system/_rounds/harness/css-delta.mjs
  - docs/design-system/_rounds/harness/README.md
  - docs/design-system/_rounds/harness/RETURN-SPEC.md
  - docs/design-system/_rounds/round2/return/Left Nav Prototype (standalone).html
  - docs/design-system/_rounds/round2/return/nav-app.jsx
  - docs/design-system/_rounds/round2/return/sidebar.jsx
  - docs/design-system/_rounds/round2/return/*.png (5 screenshots)
  - docs/design-system/_rounds/round2/extracted/zn-proto.css
  - docs/design-system/_rounds/round2/README.md
  - docs/design-system/_rounds/round3/package/README.md
  - docs/design-system/_rounds/round3/package/BRIEF.md
  - docs/design-system/_rounds/round3/package/RETURN-SPEC.md
  - docs/design-system/_rounds/round3/package/current-state/app.css
  - docs/design-system/_rounds/round3/package/current-state/nav-tree.ts
  - docs/design-system/_rounds/round3/package/current-state/routes.md
  - docs/design-system/_rounds/round3/package/current-state/components.md
  - docs/DESIGN-SYSTEM-ADOPTION.md
autonomous: true
requirements: [quick-260611-j6n]

must_haves:
  truths:
    - "Running `node docs/design-system/_rounds/harness/unbundle.mjs <round2 html> <outdir>` reproduces the hand-extracted sidebar CSS: the second template style block matches round2/extracted/zn-proto.css modulo whitespace"
    - "Running `node docs/design-system/_rounds/harness/css-delta.mjs <prototype.css>` prints a markdown report with NEW/CHANGED/DUPLICATE custom-property sections and prototype-only / in-both selector sections against remix-app/app/app.css"
    - "Round2 artifacts (1.6MB bundler HTML, 2 jsx, 5 PNGs, extracted CSS, README) are archived under docs/design-system/_rounds/round2/"
    - "Round3 outbound package exists with README, BRIEF (Part A polish + Part B data-viz), RETURN-SPEC copy, and current-state inventory generated from the repo"
    - "Harness README documents the round protocol (package/return/extracted layout, workflow, history table)"
  artifacts:
    - path: "docs/design-system/_rounds/harness/unbundle.mjs"
      provides: "Bundler-HTML exploder (node >=18, zero deps)"
    - path: "docs/design-system/_rounds/harness/css-delta.mjs"
      provides: "Prototype-vs-app.css delta report (node, zero deps)"
    - path: "docs/design-system/_rounds/harness/README.md"
      provides: "Round protocol + history table"
    - path: "docs/design-system/_rounds/harness/RETURN-SPEC.md"
      provides: "Design-side return contract (master copy)"
    - path: "docs/design-system/_rounds/round2/README.md"
      provides: "Round2 record: what it was, how it landed, pain points"
    - path: "docs/design-system/_rounds/round2/extracted/zn-proto.css"
      provides: "Hand-extracted round2 sidebar CSS (acceptance-test reference)"
    - path: "docs/design-system/_rounds/round3/package/BRIEF.md"
      provides: "Round3 design brief (Part A + Part B + TODO(owner) markers)"
    - path: "docs/design-system/_rounds/round3/package/current-state/components.md"
      provides: "UI + shell component inventory with key props"
  key_links:
    - from: "docs/design-system/_rounds/round3/package/RETURN-SPEC.md"
      to: "docs/design-system/_rounds/harness/RETURN-SPEC.md"
      via: "byte-identical copy"
      pattern: "diff -q exits 0"
    - from: "docs/design-system/_rounds/round3/package/current-state/app.css"
      to: "remix-app/app/app.css"
      via: "verbatim copy"
      pattern: "diff -q exits 0"
    - from: "docs/design-system/_rounds/round3/package/current-state/nav-tree.ts"
      to: "remix-app/app/components/shell/nav-tree.ts"
      via: "verbatim copy"
      pattern: "diff -q exits 0"
---

<objective>
Build the design-roundtrip integration harness (unbundle.mjs + css-delta.mjs + round protocol docs + RETURN-SPEC contract), archive the round2 left-nav artifacts, and prep the round3 outbound package (whole-app polish + data-viz focus).

Purpose: Round 2 returned a 1.6MB self-decoding bundler HTML dumped in untracked `_notes/` — integration required hand-decoding a JSON-embedded template and sieving CSS by hand. The harness makes future returns mechanical; RETURN-SPEC prevents the bundler-only return from recurring; round3 package is the next outbound send.

Output: `docs/design-system/_rounds/{harness,round2,round3}` fully populated. Docs and scripts only — NO `remix-app/` source changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260611-j6n-design-roundtrip-integration-harness-rou/260611-j6n-CONTEXT.md  (locked spec — the authoritative content source for every doc written below)
@docs/design-system/uploads/screens-package/README.md  (round1 outbound package precedent — match its tone/structure)
@.planning/quick/260610-q56-refactor-app-chrome-to-consolidated-left/260610-q56-SUMMARY.md  (round2 integration reference run — source for pain points in round2 README)
@remix-app/app/routes.ts  (source for routes.md)
@remix-app/app/components/shell/nav-tree.ts  (copied verbatim into current-state/)

<interfaces>
<!-- Verified facts the executor needs. Do NOT re-derive these. -->

**Source artifact locations (CONTEXT.md says `_notes/` for everything, but the user has since moved the HTML/jsx — these are the verified current locations, all OUTSIDE the worktree, use absolute paths):**

| Artifact | Verified location |
|----------|-------------------|
| `Left Nav Prototype (standalone).html` (1.6MB) | `/Users/mac/Code/zoetrop/docs/design-system/_rounds/round2/` (main tree, untracked) |
| `nav-app.jsx` (2.9K), `sidebar.jsx` (12K) | `/Users/mac/Code/zoetrop/docs/design-system/_rounds/round2/` (main tree, untracked) |
| 5 PNGs (`Screenshot 2026-05-19 at 9.21.50 PM.png` … `9.22.10 PM.png`) | `/Users/mac/Code/zoetrop/_notes/` (main tree, untracked) |
| `zn-proto.css` (253 lines) | `.planning/quick/260610-q56-refactor-app-chrome-to-consolidated-left/260610-q56-zn-proto.css` (in worktree) |

Leave `/Users/mac/Code/zoetrop/_notes/` and the main-tree `_rounds/round2/` files alone (user's working copies) — copy, never move.

**Verified bundler HTML format (inspected in the actual round2 file):**
- ONE `<script type="__bundler/manifest">` — JSON object `{uuid: {data: <base64>, compressed: <bool>, mime}}`, 22 entries, all `compressed: true` (gzip).
- ONE `<script type="__bundler/template">` — a JSON **string** (must be `JSON.parse`d) containing the real 40KB HTML document.
- The decoded template contains exactly **2** `<style>` blocks: block 1 = design-system tokens (~27.5K chars, starts `/* ===…  Zoetrop Desi…`), block 2 = the left-nav prototype CSS (~11.4K chars, starts `:root { --excess: #c98910; …`). Block 2 is what q56 hand-extracted into zn-proto.css.
- The outer wrapper HTML also has `<style>` blocks (`#__bundler_loading` etc.) — those belong to the loader shell, NOT the template; do not extract them.

**TrendChart location (CONTEXT said "[verify]"):** `remix-app/app/components/ui/TrendChart.tsx` (NOT `components/TrendChart.tsx` as old CLAUDE.md says). All viz components live in `app/components/ui/`: TrendChart (also exports TrendSparkline), Sparkline, MetricRing, RangeBar, PhaseBar, ProgressBar, StatusDot, StatusBadge, DataTable.

**Component inventory scope:** `app/components/ui/` has 28 components (excluding `*.test.tsx`); `app/components/shell/` has AppShell, MobileTopBar, Sidebar, SidebarAccount + nav-tree.ts.

**routes.ts actual counts (override CONTEXT's rough "ingest×4"):** dashboard, metrics×3, protocol×6, insights×3, import×3, **ingest×5** (index, upload, review, consent, documents/:id), settings×2 — but `settings/invites/:inviteId/revoke` (`invites.ts`), `logout`, and `api/auth/*` are resource/action routes with no UI; mark them as such in routes.md and exclude from the design brief enumeration. Public: landing (`/`), login.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Round2 archive + harness scripts + protocol docs (with acceptance gate)</name>
  <files>docs/design-system/_rounds/round2/** , docs/design-system/_rounds/harness/{unbundle.mjs, css-delta.mjs, README.md, RETURN-SPEC.md}</files>
  <action>
**1a. Archive round2** (do this first — the acceptance test needs the archived HTML):

Create `docs/design-system/_rounds/round2/` in the worktree with:
- `return/` — copy (cp, preserve names exactly): `Left Nav Prototype (standalone).html`, `nav-app.jsx`, `sidebar.jsx` from `/Users/mac/Code/zoetrop/docs/design-system/_rounds/round2/`; the 5 PNGs from `/Users/mac/Code/zoetrop/_notes/`. 8 files total.
- `extracted/zn-proto.css` — copy from `.planning/quick/260610-q56-refactor-app-chrome-to-consolidated-left/260610-q56-zn-proto.css`.
- `README.md` — per CONTEXT: what round2 was (left-nav prototype; interaction model now BAKED — 264px/64px rail, single-open accordion, flyout); how it landed (260610-q56 chrome refactor → 260610-rj2 crumb meta-row → 260610-rwg header unification, merged via `left-nav-refactor` branch); integration pain points that motivated the harness (hand-decoding the JSON template, sieving new CSS from token duplicates, re-mapping prototype idioms); note that `_notes/` in the main tree is now a redundant copy. Read the q56 SUMMARY for accurate pain-point detail.

**Commit 1 (large-artifact commit, kept separate per approved plan):** `gsd-sdk query commit "chore(design): archive round2 left-nav return artifacts" --files "docs/design-system/_rounds/round2"` — stage ONLY round2/** explicitly (concurrent-session rule: never `git add -A`).

**1b. `harness/unbundle.mjs`** (node >=18, **zero dependencies** — only `node:fs`, `node:path`, `node:zlib`; runnable as `node unbundle.mjs <standalone.html> <outdir>`; no eval, no network, writes only inside outdir):
- Parse the `__bundler/manifest` script tag JSON. For each entry: base64-decode `data`, `zlib.gunzipSync` when `compressed`, write to `<outdir>/assets/` with extension inferred from `mime` (map at minimum: text/javascript + application/javascript → .js, text/css → .css, image/png → .png, image/svg+xml → .svg, image/jpeg → .jpg, application/json → .json, font/woff2 → .woff2; fallback `.bin`). Default filename `<uuid>.<ext>`; when the template references the asset with a recoverable original name (path-like src/href attribute), prefer that name.
- Parse the `__bundler/template` script tag: its content is a JSON STRING — `JSON.parse` it to get the real HTML. Replace uuid references in it with relative `assets/...` paths; write as `<outdir>/index.html`.
- Extract every `<style>` block **from the decoded template** (not the wrapper shell) → `<outdir>/styles/NN.css`, NN = 2-digit order of appearance starting at `01`.
- Extract inline `text/babel`/`text/jsx` template scripts → `<outdir>/src/NN.jsx` (same numbering); manifest assets with js/jsx mime already land in `assets/` per above.
- Plain (non-bundler) HTML fallback: if no `__bundler/template` tag, just extract styles/ and src/ from the file itself and copy it as index.html.
- No-arg / `--help`: print usage.

**1c. `harness/css-delta.mjs`** (node, zero deps; `node css-delta.mjs <prototype.css> [app.css]`, app.css defaulting to `remix-app/app/app.css` resolved from cwd). Markdown report to stdout with sections: NEW custom properties (in prototype, not app), CHANGED (same name, different normalized value — show both values), DUPLICATE (identical — "safe to skip"); selectors only in prototype ("port these"); selectors in both ("review for conflict"). Implementation: crude-but-honest string parsing — strip `/* */` comments, extract `--name: value` pairs, split rules on `}` boundaries, collapse whitespace before comparing. `--help` must state the known limits (naive about @media nesting, no real CSS parser).

**1d. `harness/README.md`** — the round protocol, per CONTEXT:
- Per-round directory layout: `roundN/package/` (outbound), `roundN/return/` (inbound raw drop — bundler HTML, loose files, screenshots), `roundN/extracted/` (unbundle.mjs output + css-delta report).
- Workflow: prep package (copy RETURN-SPEC in) → send with the zoetrop-design skill → drop return into `roundN/return/` → `unbundle.mjs` → `css-delta.mjs` → integrate via `/gsd-quick` with an approved plan. Cite q56/rj2/rwg as the reference run that shipped the left nav in 3 quick tasks.
- History table: round1 (full redesign, clean source, `_rounds/round1/`), round2 (left nav, bundler HTML in `_notes/` — the pain that motivated this harness), round3 (prepped, outbound).
- Script usage blurbs for both tools.

**1e. `harness/RETURN-SPEC.md`** — the return contract (master copy), all clauses from CONTEXT lines 51–57 verbatim in intent:
- Deliver LOOSE SOURCE FILES, not only a standalone bundle: `app/*.jsx` per screen/component + ONE `new.css` containing ONLY rules/vars not already in the included app.css snapshot (no token redefinitions, no dark-remap duplication, no radius re-declarations).
- Include `CHANGES.md`: per-screen what/why, every new/changed token, new icon names, interaction-model decisions ("baked" items).
- Use existing token names from the provided app.css; new tokens follow existing naming families.
- Idiom mapping table (prototype → app): `NLink href="#/x"` → react-router `NavLink`/`Link to`; `Icon name="x"` → direct lucide-react import (PascalCase); `window.ZD.categories` → `CATEGORY_INFO` (9 ids: vitamins, minerals, inflammatory, metabolic, hormones, autonomic, bodyComposition, lipids, hematology); local theme hooks → existing `ThemeToggle`/`data-theme` plumbing (never reimplement); `zt-`/`zn-` class prefixes reserved as in app.css.
- Standalone HTML welcome as a VIEWING ARTIFACT only; never the sole source.
- Breakpoint 760px; dark theme via `html[data-theme="dark"]` var remap only.

**1f. Acceptance gate (REAL gate — do not proceed to Task 2 until green):** run unbundle against the archived HTML into a temp dir (do NOT commit this output):
`node docs/design-system/_rounds/harness/unbundle.mjs "docs/design-system/_rounds/round2/return/Left Nav Prototype (standalone).html" /tmp/j6n-unbundle-test`
Then compare the SECOND extracted style block to the hand-extracted reference, modulo whitespace:
`node -e 'const f=p=>require("fs").readFileSync(p,"utf8").replace(/\s+/g,"");process.exit(f(process.argv[1])===f(process.argv[2])?0:1)' /tmp/j6n-unbundle-test/styles/02.css docs/design-system/_rounds/round2/extracted/zn-proto.css && echo MATCH`
If it fails, fix unbundle.mjs (or, ONLY if a true content diff exists beyond whitespace, inspect both files and report — do not loosen the comparison).

**Commit 2:** `gsd-sdk query commit "feat(design): roundtrip harness — unbundle, css-delta, round protocol, RETURN-SPEC" --files "docs/design-system/_rounds/harness"`.
  </action>
  <verify>
    <automated>node --check docs/design-system/_rounds/harness/unbundle.mjs && node --check docs/design-system/_rounds/harness/css-delta.mjs && node docs/design-system/_rounds/harness/unbundle.mjs "docs/design-system/_rounds/round2/return/Left Nav Prototype (standalone).html" /tmp/j6n-unbundle-test && node -e 'const f=p=>require("fs").readFileSync(p,"utf8").replace(/\s+/g,"");process.exit(f(process.argv[1])===f(process.argv[2])?0:1)' /tmp/j6n-unbundle-test/styles/02.css docs/design-system/_rounds/round2/extracted/zn-proto.css && node docs/design-system/_rounds/harness/css-delta.mjs docs/design-system/_rounds/round2/extracted/zn-proto.css | grep -qi "custom propert" && [ "$(ls docs/design-system/_rounds/round2/return | wc -l)" -eq 8 ]</automated>
  </verify>
  <done>Both scripts pass node --check; acceptance test exits 0 (second style block == zn-proto.css modulo whitespace); css-delta emits a report with custom-property sections against the real app.css; round2/return holds exactly 8 files; round2 README + harness README + RETURN-SPEC exist; two commits landed (round2 archive separate from harness).</done>
</task>

<task type="auto">
  <name>Task 2: Round3 outbound package (README, BRIEF, current-state inventory, RETURN-SPEC copy)</name>
  <files>docs/design-system/_rounds/round3/package/** , docs/DESIGN-SYSTEM-ADOPTION.md</files>
  <action>
Create `docs/design-system/_rounds/round3/package/`. Match the tone/structure of the round1 precedent (`docs/design-system/uploads/screens-package/README.md`) — numbered "how to use" steps, tables, terse context callouts.

**2a. `README.md`:** what this package is (round3 outbound: whole-app polish + data-viz language); contents map; deployed preview note — https://zoetrop-git-003-remix-foundation-negentropico.vercel.app, behind pilot basic-auth, credentials supplied out-of-band, NO screenshots included by design (design side works from the live preview); pointer to the zoetrop-design skill bundle as the brand source of truth; return instructions = "follow RETURN-SPEC.md in this package".

**2b. `BRIEF.md`** — the round3 design brief, two parts plus TODO markers:
- **Part A — whole-app polish pass:** enumerate ALL current UI routes from `remix-app/app/routes.ts` grouped by section: dashboard, metrics×3, protocol×6, insights×3, import×3, ingest×5, settings×1, plus public landing + login (exclude resource-only routes: logout, api/auth/*, settings/invites revoke — note them as non-UI). All reviewed under the new left-nav chrome (264px expanded / 64px collapsed rail, single-open accordion, flyout on collapsed rail; unified PageHeader: meta row with eyebrow-left/crumb-right, then title row). Goals: density, hierarchy, spacing refinement; consistent application of frame cards, pills, mono micro-labels. Explicit "what NOT to change" list: nav interaction model (baked in round2), routing/IA, theme plumbing, brand foundations.
- **Part B — data-viz/charts focus:** inventory the viz components WITH verified file paths (all under `remix-app/app/components/ui/`): TrendChart.tsx (exports TrendChart + TrendSparkline), Sparkline.tsx, MetricRing.tsx, RangeBar.tsx, PhaseBar.tsx, ProgressBar.tsx, StatusDot.tsx, StatusBadge.tsx, DataTable.tsx; plus the correlations screen (`routes/_app/insights/correlations.tsx`). Ask for a coherent chart language: axis/grid/label treatment, status-color usage (optimal/borderline/deficient/excess), hover/tooltip language, empty/loading states, ring-sweep motion rules.
- Insert `> TODO(owner):` blockquote markers where the user's voice is needed: overall round goal statement, screens to prioritize, anything explicitly out of scope. Do NOT invent the user's design intent.

**2c. `current-state/`** (generated from the repo at prep time):
- `app.css` — verbatim copy of `remix-app/app/app.css`.
- `nav-tree.ts` — verbatim copy of `remix-app/app/components/shell/nav-tree.ts`.
- `routes.md` — route table generated from `app/routes.ts`: columns Route | File | Section | Notes (mark resource/action routes non-UI; mark public vs authenticated).
- `components.md` — one-row-each inventory of `app/components/ui/*` (28 components, skip `*.test.tsx`) + `app/components/shell/*` (4 components + nav-tree.ts): name, one-line purpose, key props. Read each file's props interface — use grep for `interface`/`type.*Props` and read only the interface block, not whole files; keep rows terse.

**2d. `RETURN-SPEC.md`** — byte-identical copy of `docs/design-system/_rounds/harness/RETURN-SPEC.md` (cp, don't rewrite).

**2e. `docs/DESIGN-SYSTEM-ADOPTION.md` §3 (optional, only if trivially safe):** append one line under the §3 sequence diagram noting the round protocol now lives at `docs/design-system/_rounds/harness/README.md` and round3 is prepped at `_rounds/round3/package/`. Do not restructure the diagram or touch any other section; if any ambiguity, skip entirely per CONTEXT.

**Commit 3:** `gsd-sdk query commit "docs(design): round3 outbound package — polish + data-viz brief, current-state inventory" --files "docs/design-system/_rounds/round3" "docs/DESIGN-SYSTEM-ADOPTION.md"` (stage explicitly; drop the ADOPTION file from --files if 2e was skipped).
  </action>
  <verify>
    <automated>diff -q docs/design-system/_rounds/round3/package/RETURN-SPEC.md docs/design-system/_rounds/harness/RETURN-SPEC.md && diff -q docs/design-system/_rounds/round3/package/current-state/app.css remix-app/app/app.css && diff -q docs/design-system/_rounds/round3/package/current-state/nav-tree.ts remix-app/app/components/shell/nav-tree.ts && grep -q "TODO(owner)" docs/design-system/_rounds/round3/package/BRIEF.md && grep -qi "ingest" docs/design-system/_rounds/round3/package/current-state/routes.md && grep -q "MetricRing" docs/design-system/_rounds/round3/package/current-state/components.md && ls docs/design-system/_rounds/harness docs/design-system/_rounds/round2/return docs/design-system/_rounds/round2/extracted docs/design-system/_rounds/round3/package/current-state</automated>
  </verify>
  <done>round3/package complete: README (preview-URL note, no screenshots), BRIEF with Part A (all UI routes enumerated, what-NOT-to-change list) + Part B (viz inventory with verified paths) + TODO(owner) markers, current-state copies byte-identical to repo sources, routes.md + components.md generated, RETURN-SPEC identical to harness master; final `ls -R docs/design-system/_rounds` matches the CONTEXT layout (harness/, round1/, round2/{return,extracted}, round3/package/{current-state}); commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-side return → unbundle.mjs | Untrusted HTML/base64 payloads from external design sessions are parsed locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-j6n-01 | Tampering/Elevation | unbundle.mjs | mitigate | Pure string/zlib parsing — no eval, no dynamic import of extracted code, no network; writes confined to the user-supplied outdir |
| T-j6n-02 | Information Disclosure | round3 package README | mitigate | Preview URL noted but basic-auth credentials explicitly out-of-band — never written into any committed doc |
| T-j6n-SC | Tampering | package installs | accept | No package installs in this task — scripts are zero-dependency by requirement |
</threat_model>

<verification>
- `node --check` passes on both harness scripts (zero-dep, node >=18).
- Acceptance gate: unbundle of archived round2 HTML reproduces zn-proto.css (second template style block, whitespace-normalized equality).
- css-delta against zn-proto.css + real app.css emits the sectioned markdown report.
- `ls -R docs/design-system/_rounds` shows: `harness/` (4 files), `round1/` (untouched), `round2/{return: 8 files, extracted: zn-proto.css, README.md}`, `round3/package/{README.md, BRIEF.md, RETURN-SPEC.md, current-state: 4 files}`.
- `git diff --stat` shows NO changes under `remix-app/` (docs + scripts only).
- Three commits, with the 1.6MB HTML + PNGs isolated in the round2-archive commit.
</verification>

<success_criteria>
- A future bundler-format return integrates via: drop in `roundN/return/` → `node unbundle.mjs` → `node css-delta.mjs` → gsd-quick plan — no hand-decoding.
- RETURN-SPEC makes the loose-source + new-css-only + CHANGES.md contract explicit so a bundler-only return doesn't recur.
- Round2 is durably archived in-repo with its landing history; `_notes/` documented as redundant.
- Round3 package is send-ready except for the `TODO(owner)` voice markers the user fills in.
</success_criteria>

<output>
Create `.planning/quick/260611-j6n-design-roundtrip-integration-harness-rou/260611-j6n-SUMMARY.md` when done.
</output>
