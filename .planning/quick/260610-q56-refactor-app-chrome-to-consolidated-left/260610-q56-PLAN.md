---
phase: quick-260610-q56
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - remix-app/app/components/shell/nav-tree.ts
  - remix-app/app/components/shell/Sidebar.tsx
  - remix-app/app/components/shell/SidebarAccount.tsx
  - remix-app/app/components/shell/MobileTopBar.tsx
  - remix-app/app/components/shell/AppShell.tsx
  - remix-app/app/app.css
  - remix-app/app/routes.ts
  - remix-app/app/routes/_app/layout.tsx
  - remix-app/app/routes/_app/metrics/layout.tsx (delete)
  - remix-app/app/routes/_app/protocol/layout.tsx (delete)
  - remix-app/app/routes/_app/insights/layout.tsx (delete)
  - remix-app/app/routes/_app/import/layout.tsx (delete)
  - remix-app/app/routes/_app/ingest/layout.tsx (delete)
  - remix-app/app/components/shell/TopNav.tsx (delete)
  - remix-app/app/components/shell/BottomTab.tsx (delete)
  - remix-app/app/components/shell/AccountMenu.tsx (delete)
  - remix-app/app/components/ui/DropdownMenu.tsx (delete)
  - remix-app/app/routes/_app/metrics/category.tsx
  - remix-app/app/routes/_app/metrics/detail.tsx
  - remix-app/app/routes/_app/protocol/version-detail.tsx
autonomous: true
requirements: [Q56]
must_haves:
  truths:
    - "Signed-in user sees one consolidated left sidebar (264px) on every app route; no TopNav, no BottomTab, no per-section sub-navs"
    - "Collapse toggle shrinks sidebar to a 64px icon rail with hover flyout submenus; collapse state survives a full page reload with no flash"
    - "On mobile (≤760px) a hamburger top bar opens the sidebar as an off-canvas drawer over a backdrop; it closes on backdrop/Escape/navigation"
    - "Breadcrumbs render shell-wide via crumbsForPath; the 3 pages with loader-derived Crumbs show exactly one crumb trail (no duplicates)"
    - "/ingest resolves (redirect-to-upload) instead of colliding with the public landing index"
    - "Sign out (no-JS Form POST to /logout) and ThemeToggle still work from the sidebar footer account popover"
    - "/ and /login render with no sidebar or topbar"
  artifacts:
    - path: "remix-app/app/components/shell/nav-tree.ts"
      provides: "NAV_TREE, groupOfPath, isChildActive, crumbsForPath — single source of truth for sidebar, flyout, breadcrumbs"
      exports: ["NAV_TREE", "groupOfPath", "isChildActive", "crumbsForPath"]
    - path: "remix-app/app/components/shell/Sidebar.tsx"
      provides: "Expanded accordion + collapsed rail + flyout + mobile drawer variant"
      min_lines: 150
    - path: "remix-app/app/components/shell/SidebarAccount.tsx"
      provides: "Footer account popover with role Badge, /settings link, ThemeToggle row, danger Sign out Form"
    - path: "remix-app/app/components/shell/MobileTopBar.tsx"
      provides: "≤760px sticky hamburger bar"
    - path: "remix-app/app/components/shell/AppShell.tsx"
      provides: "zn-app shell: MobileTopBar + Sidebar + backdrop + zn-main/zn-page + Crumb + footer"
      contains: "zn-app"
  key_links:
    - from: "remix-app/app/routes/_app/layout.tsx"
      to: "AppShell"
      via: "loader returns { user, navCollapsed } from zt-nav cookie"
      pattern: "navCollapsed"
    - from: "remix-app/app/components/shell/AppShell.tsx"
      to: "nav-tree.ts"
      via: "crumbsForPath(pathname) gating Crumb render"
      pattern: "crumbsForPath"
    - from: "remix-app/app/routes.ts"
      to: "routes/_app/ingest/index.tsx"
      via: "route(\"ingest\", ...) — not index(...)"
      pattern: "route\\(\"ingest\","
    - from: "remix-app/app/components/shell/SidebarAccount.tsx"
      to: "/logout"
      via: "Form method=post action=/logout"
      pattern: "action=\"/logout\""
---

<objective>
Replace the app chrome (sticky TopNav + mobile BottomTab + 5 per-section sub-navs) with the consolidated left sidebar from the prototype: 264px expanded / 64px icon rail, single-open accordion, hover flyout on the rail, footer account popover owning the theme control, off-canvas mobile drawer at 760px, cookie-persisted collapse (SSR-read, no flash), shell-wide breadcrumbs, flattened routes.ts (which also fixes the `/ingest` index collision with the public landing route).

Purpose: One nav system instead of six; fixes an unroutable `/ingest`; chrome-only — page content unchanged.
Output: 4 new shell modules, rewritten AppShell + `_app/layout.tsx` loader, flattened routes.ts, 9 files deleted, app.css zn-* section, 3 one-line Crumb prepends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>

**THE AUTHORITATIVE SPEC — read first, follow exactly (all design decisions locked):**
@.planning/quick/260610-q56-refactor-app-chrome-to-consolidated-left/260610-q56-APPROVED-PLAN.md

**Prototype sources to port:**
@.planning/quick/260610-q56-refactor-app-chrome-to-consolidated-left/260610-q56-zn-proto.css  (the plan's "/tmp/zn-style-1.css" — same line numbers; port lines 65–233 + 238–239)
@/Users/mac/Code/zoetrop/_notes/sidebar.jsx  (Sidebar/Flyout/AccountMenu logic to port)
@/Users/mac/Code/zoetrop/_notes/nav-app.jsx  (shell composition reference)

**Files being rewritten/edited:**
@remix-app/app/components/shell/AppShell.tsx
@remix-app/app/routes/_app/layout.tsx
@remix-app/app/routes.ts
@remix-app/app/app.css
@remix-app/app/routes/_app/metrics/layout.tsx  (dying — LUCIDE_MAP moves to nav-tree.ts)
@remix-app/app/components/shell/AccountMenu.tsx  (dying — ROLE_TONE/ROLE_VARIANT maps + logout Form copy to SidebarAccount)

All work in `remix-app/` (node_modules installed). Branch `left-nav-refactor` in this worktree.

<interfaces>
<!-- Verified from the codebase 2026-06-10. Use directly — no exploration needed. -->

From app/components/ui/Crumb.tsx:
```typescript
export interface CrumbItem { label: string; to?: string }
export interface CrumbProps { items: CrumbItem[] }
export function Crumb({ items }: CrumbProps)
```

From app/components/shell/AccountMenu.tsx (copy these maps verbatim into SidebarAccount before deleting the file):
```typescript
const ROLE_TONE: Record<string, BadgeProps["tone"]> = { owner: "neutral", practitioner: "focus", client: "neutral" };
const ROLE_VARIANT: Record<string, BadgeProps["variant"]> = { owner: "solid", practitioner: "soft", client: "soft" };
// user prop shape everywhere: { name: string; email: string; role: string }
```

From app/routes/_app/metrics/layout.tsx (move LUCIDE_MAP into nav-tree.ts before deleting):
```typescript
const LUCIDE_MAP: Record<string, LucideIcon> = {
  pill: Pill, gem: Gem, flame: Flame, zap: Zap, "flask-conical": FlaskConical,
  "heart-pulse": HeartPulse, dumbbell: Dumbbell, droplet: Droplet, dna: Dna,
};
// CATEGORY_INFO from ~/types/metrics: Record<MetricCategory, { label, icon, family }>
```

Reusable ui components (do NOT rebuild):
```typescript
export function Wordmark()                                  // ui/Wordmark.tsx
export function ThemeToggle()                               // ui/ThemeToggle.tsx — embed in popover, do NOT reimplement useThemeLocal
export function Avatar({ ... }: AvatarProps)                // ui/Avatar.tsx
export function Badge({ tone, variant, children, style }: BadgeProps)  // ui/Badge.tsx
// SpiralMark({ size, color }) — ui/SpiralMark.tsx
```

Verified anchors:
- `routes.ts:42` — `index("routes/_app/ingest/index.tsx")` (the `/` collision to fix)
- `app.css:383` — `html, body, .zt-topnav, .zt-bottomtab, .zt-theme-toggle {` (theme-transition selector to update)
- `app.css:204` — layer-2 `:root` block ends near `--radius-2xl: 18px` (insert `--zn-*` geometry vars here)
- Crumb call sites: `metrics/category.tsx:216`, `metrics/detail.tsx:117`, `protocol/version-detail.tsx:140`
- 760px breakpoint already used at `app.css:502` — reuse it for the drawer media queries
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Foundations + new shell components (approved-plan Steps 1–3; commits 1–2)</name>
  <files>remix-app/app/components/shell/nav-tree.ts, remix-app/app/app.css, remix-app/app/components/shell/SidebarAccount.tsx, remix-app/app/components/shell/Sidebar.tsx, remix-app/app/components/shell/MobileTopBar.tsx</files>
  <action>
Implement approved-plan Steps 1–3 exactly as specified.

**Commit 1 — `nav-tree.ts` + app.css zn-* section (inert, nothing imports them yet):**
- `nav-tree.ts` per Step 1: `NavChild`/`NavGroup` interfaces, `NAV_TREE` (Dashboard exact `/dashboard` · Metrics with "All categories" + 9 from `CATEGORY_INFO` using the moved `LUCIDE_MAP` · Protocol ×5 · Insights ×3 · Import ×3 · Ingest with Overview `/ingest`/Upload/Review + hidden Consent child), `groupOfPath` (`path === base || path.startsWith(base + "/")`), `isChildActive` (exact for `end`, else base-or-deeper prefix so `/metrics/vitamins/:id` highlights Vitamins), `crumbsForPath` returning `CrumbItem[] | null` — null for `/dashboard` AND for anything deeper than an exact child (param routes keep their page-owned Crumbs), `/settings` → `[zoetrop, Account settings]`.
- app.css per Step 2: add `--zn-side-w/--zn-rail-w/--zn-row-h/--zn-child-h` to the layer-2 `:root` (~line 204); append the `/* === Sidebar shell (zn-*) === */` section porting proto-css **lines 65–233 + 238–239 only** (skip dark remap/radii/`a`/`::selection` — already present — and `.zn-ghost`/`.zn-grid-4` placeholders); add the NEW mobile-drawer rules at ≤760px (`.zn-topbar` sticky 56px z-40, `.zn-side` translateX(-100%) / `.is-mobile-open`, `.zn-collapse` hidden, `.zn-main` margin-left 0 !important, `.zn-mobile-backdrop` z-55 ink 35% color-mix) and the class-driven desktop offset (`.zn-app .zn-main { margin-left: var(--zn-side-w) }` / `.zn-app.is-collapsed .zn-main { margin-left: var(--zn-rail-w) }`). Preserve the z-index map: topbar 40 < backdrop 55 < sidebar 60 < fly-backdrop 70 < flyout 80 < account menu 90. Do NOT touch the line-383 selector yet (Task 3).

**Commit 2 — `SidebarAccount` + `Sidebar` + `MobileTopBar` per Step 3:**
- `SidebarAccount.tsx` `{ user, collapsed }`: `.zn-account` trigger (existing `Avatar` + name/role + `ChevronsUpDown`), `.zn-account-menu` popover (+`is-rail` when collapsed) with name/email + role `Badge` (ROLE_TONE/ROLE_VARIANT copied from AccountMenu.tsx — see interfaces), `Link` to `/settings`, a Theme row embedding `<ThemeToggle/>` (NOT a reimplementation of the prototype's useThemeLocal), and the `<Form method="post" action="/logout">` danger Sign out copied verbatim from AccountMenu (no-JS signout preserved). Closes on backdrop/Escape/navigation.
- `Sidebar.tsx` `{ user, collapsed, onToggleCollapsed, mobileOpen, onMobileClose }`: port `_notes/sidebar.jsx` — hash links → react-router `NavLink`/`Link`, prototype `Icon` → direct lucide components, `useLocation()` for path; single-open accordion keyed by group id with effect re-opening `groupOfPath(pathname)` on nav; collapsed rail + local `Flyout` component (fixed-position viewport-clamped `useLayoutEffect`, 180ms close timer, Escape, `zn-fly-backdrop`); `useIsMobile()` hook (matchMedia 760px, `false` during SSR) — on mobile always render the expanded variant; header `SpiralMark` + "zoetrop." wordmark linking `/dashboard` + collapse toggle (`PanelLeftOpen`/`PanelLeftClose`); footer `SidebarAccount`. Filter `hidden` children out of accordion/flyout rendering.
- `MobileTopBar.tsx` `{ onMenu }`: `.zn-topbar` with `Menu` hamburger (≥44px target, aria-label) + existing `Wordmark`.

Stage files explicitly per commit — never `git add -A` (concurrent sessions commit to `.planning/` on this tree).
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck</automated>
  </verify>
  <done>nav-tree.ts exports NAV_TREE/groupOfPath/isChildActive/crumbsForPath; app.css has zn-* section with geometry vars, drawer rules, class-driven margin; 3 new components compile; 2 atomic commits; app still renders the OLD chrome (nothing wired yet).</done>
</task>

<task type="auto">
  <name>Task 2: Wire the shell — AppShell rewrite, cookie loader, routes flatten + ingest fix, delete section layouts (Steps 4–6; commits 3–4)</name>
  <files>remix-app/app/components/shell/AppShell.tsx, remix-app/app/routes/_app/layout.tsx, remix-app/app/routes.ts, remix-app/app/routes/_app/metrics/layout.tsx, remix-app/app/routes/_app/protocol/layout.tsx, remix-app/app/routes/_app/insights/layout.tsx, remix-app/app/routes/_app/import/layout.tsx, remix-app/app/routes/_app/ingest/layout.tsx</files>
  <action>
Implement approved-plan Steps 4–6 exactly.

**Commit 3 — AppShell rewrite + cookie loader (sidebar live; old sub-navs render harmlessly until commit 4):**
- `_app/layout.tsx` loader: regex the `Cookie` header for `zt-nav=1` ⇒ `navCollapsed: true`; return `{ user, navCollapsed }`. Keep the existing auth gate and user normalization untouched.
- `AppShell.tsx` rewrite per Step 5: props `{ children, user, navCollapsed }`; state `collapsed` (init from `navCollapsed`; toggle writes `document.cookie = "zt-nav=" + (next?"1":"0") + "; Path=/; Max-Age=31536000; SameSite=Lax"` client-side — no fetcher/action) and `mobileOpen`; effects close drawer on `pathname` AND `search` change (ingest review uses `?doc=`), Escape closes, body scroll-lock while open. Render per the Step-5 JSX sketch: `.zn-app` (+` is-collapsed`) wrapping MobileTopBar, Sidebar, conditional `.zn-mobile-backdrop`, `<main className="zn-main"><div className="zn-page">` with `crumbsForPath(pathname)`-gated `<Crumb/>`, children, and the SpiralMark+zoetrop footer eyebrow. Delete: `wide` prop (dead — never passed), `BottomTab` render, the 80px mobile spacer, all inline `marginLeft`/maxWidth shell styles (class-driven now).
- Update `_app/layout.tsx` default export to pass `navCollapsed` to AppShell.

**Commit 4 — routes.ts flatten + ingest fix + delete 5 section layouts + typecheck:**
- `routes.ts` per Step 6: remove the five section `layout(...)` wrappers, registering all section routes flat under `layout("routes/_app/layout.tsx", [...])`. Change `index("routes/_app/ingest/index.tsx")` → `route("ingest", "routes/_app/ingest/index.tsx")` (fixes the `/` collision; `/ingest` becomes reachable). Keep route order/paths otherwise identical.
- Delete `routes/_app/{metrics,protocol,insights,import,ingest}/layout.tsx` (all verified pure-JSX, no loaders/meta).
- Run `npm run typecheck` (regenerates `+types` after the route table change) and fix any fallout before committing.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && grep -n 'route("ingest", "routes/_app/ingest/index.tsx")' app/routes.ts && test ! -f app/routes/_app/metrics/layout.tsx</automated>
  </verify>
  <done>Sidebar is the live chrome on all authed routes; collapse persists via zt-nav cookie with SSR-consistent initial render; routes.ts has zero section layouts and `/ingest` registered as a path route; 5 layout files deleted; 2 atomic commits.</done>
</task>

<task type="auto">
  <name>Task 3: Cleanup deletions + crumb prepends + full verification (Step 7 + Deletions; commits 5–6)</name>
  <files>remix-app/app/components/shell/TopNav.tsx, remix-app/app/components/shell/BottomTab.tsx, remix-app/app/components/shell/AccountMenu.tsx, remix-app/app/components/ui/DropdownMenu.tsx, remix-app/app/app.css, remix-app/app/routes/_app/metrics/category.tsx, remix-app/app/routes/_app/metrics/detail.tsx, remix-app/app/routes/_app/protocol/version-detail.tsx</files>
  <action>
**Commit 5 — dead chrome removal:**
- Delete `components/shell/TopNav.tsx`, `BottomTab.tsx`, `AccountMenu.tsx`, and `components/ui/DropdownMenu.tsx` (orphaned — AccountMenu was its only consumer). Verify no remaining imports first: `grep -rn "TopNav\|BottomTab\|AccountMenu\|DropdownMenu" remix-app/app --include="*.tsx" --include="*.ts"` must return only the files being deleted (and any test files for them — delete those too if they exist).
- app.css ~line 383: update the scoped theme-transition selector — swap `.zt-topnav, .zt-bottomtab` for `.zn-side, .zn-topbar` (keep `html, body, .zt-theme-toggle`).
- Keep: `Wordmark`, `ThemeToggle` (+its test), `Crumb`, `SpiralMark`, `Avatar`, `Badge`, all root.tsx theme plumbing (`zt-theme` localStorage + no-flash script + ThemeRestorer) untouched.

**Commit 6 — crumb prepends (Step 7):**
- Prepend `{ label: "zoetrop", to: "/dashboard" }` to the three page-owned Crumb item arrays: `metrics/category.tsx:~216`, `metrics/detail.tsx:~117`, `protocol/version-detail.tsx:~140`. No other page edits.

**Verification (after commit 6):** run the full gate from `remix-app/`: `npm run typecheck && npm run lint && npm run test && npm run build`. Fix any failures before finishing. Sanity-grep that nothing references deleted modules and that `crumbsForPath` returns null for the three param routes (so no double crumbs).
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && npm run lint && npm run test && npm run build && bash -c '! grep -rqn "shell/TopNav\|shell/BottomTab\|shell/AccountMenu\|ui/DropdownMenu" app'</automated>
  </verify>
  <done>All four checks green; no references to deleted chrome remain; theme-transition selector targets .zn-side/.zn-topbar; the 3 detail pages' crumbs start with "zoetrop → /dashboard"; 2 atomic commits (6 total for the refactor).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→loader (Cookie header) | `zt-nav` cookie read in `_app/layout.tsx` loader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q56-01 | Tampering | zt-nav cookie parse in layout loader | accept | Non-sensitive UI preference; parsed by strict regex to a boolean only — never interpolated, stored, or echoed |
| T-q56-02 | Elevation | routes.ts flatten | mitigate | All section routes remain nested under the authed `_app/layout.tsx` loader gate; verify in commit 4 that no route moved outside the `layout("routes/_app/layout.tsx", ...)` block |
| T-q56-03 | Repudiation | Sign-out path moved AccountMenu→SidebarAccount | mitigate | `<Form method="post" action="/logout">` copied verbatim (no-JS POST preserved); covered by build + manual sign-out check |

No package installs in this plan — supply-chain gate not applicable.
</threat_model>

<verification>
Automated (the phase gate, from `remix-app/`):
```bash
npm run typecheck && npm run lint && npm run test && npm run build
```

Manual smoke (per approved plan §Verification — desktop 1440 + mobile 390, light + dark; deferred to owner/post-merge preview, not a blocking checkpoint):
- `/` and `/login`: no sidebar/topbar. `/dashboard`: no crumb, Dashboard active.
- Metrics deep routes highlight the right child; no duplicate crumbs on the 3 page-owned-Crumb pages.
- `/ingest` redirect works (was 404); `/ingest/review?doc=…` selection doesn't close anything.
- Collapse → rail + clamped hover flyout; reload preserves collapse with no flash (`document.cookie` has `zt-nav=1`).
- Mobile drawer: hamburger opens, backdrop/Escape/navigation closes, scroll locked, no dead 80px at bottom; account popover opens upward.
- Sign-out Form POST works; ThemeToggle in popover behaves (no double-toggle).
</verification>

<success_criteria>
- All 6 atomic commits land in the approved ordering (foundations → components → wiring → flatten → deletions → crumbs)
- `npm run typecheck && npm run lint && npm run test && npm run build` all pass from `remix-app/`
- Zero references to TopNav/BottomTab/AccountMenu/DropdownMenu or the 5 section layouts remain
- All locked decisions preserved: cookie `zt-nav`, 760px breakpoint, class-driven `.zn-app`/`.is-collapsed` margin, z-index map 40<55<60<70<80<90, ThemeToggle/Avatar/Badge/Wordmark/Crumb reuse, crumbsForPath null on param routes, hidden Consent child, `route("ingest", …)` fix
</success_criteria>

<output>
Create `.planning/quick/260610-q56-refactor-app-chrome-to-consolidated-left/260610-q56-SUMMARY.md` when done.
</output>
