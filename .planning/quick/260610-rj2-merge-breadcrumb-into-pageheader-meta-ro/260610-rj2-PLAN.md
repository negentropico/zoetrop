---
phase: quick
plan: 260610-rj2
type: execute
wave: 1
depends_on: []
files_modified:
  - remix-app/app/components/shell/nav-tree.ts
  - remix-app/app/components/ui/Crumb.tsx
  - remix-app/app/components/ui/PageHeader.tsx
  - remix-app/app/components/shell/AppShell.tsx
  - remix-app/app/routes/_app/metrics/category.tsx
  - remix-app/app/routes/_app/metrics/detail.tsx
  - remix-app/app/routes/_app/protocol/version-detail.tsx
autonomous: true
requirements: []   # quick task — UI polish on left-nav-refactor worktree, no roadmap REQ IDs

must_haves:
  truths:
    - "On /protocol/versions the crumb 'PROTOCOL / VERSIONS' renders right-aligned on the same line as the eyebrow 'VERSION HISTORY' — no stacked crumb block above the header"
    - "No crumb anywhere contains the ZOETROP segment"
    - "Single-segment crumbs are suppressed: /protocol, /metrics, /insights, /import, /ingest, /settings show no crumb at all"
    - "/metrics/:category, /metrics/:category/:metricId, /protocol/versions/:version each show exactly ONE crumb (no shell/PageHeader double-render), right-aligned on the meta line"
    - "Pages with a `right` slot (e.g. /protocol/versions/:version prev/next buttons) keep that slot in the title row — crumb sits in the meta row above it"
  artifacts:
    - path: "remix-app/app/components/ui/PageHeader.tsx"
      provides: "crumbs?: CrumbItem[] | null prop + auto-derive via crumbsForPath(useLocation().pathname) + meta-row layout"
      contains: "crumbsForPath"
    - path: "remix-app/app/components/shell/nav-tree.ts"
      provides: "crumbsForPath without zoetrop prefix, null for <2 items"
    - path: "remix-app/app/components/shell/AppShell.tsx"
      provides: "shell with NO crumb render (PageHeader owns crumbs)"
  key_links:
    - from: "remix-app/app/components/ui/PageHeader.tsx"
      to: "remix-app/app/components/shell/nav-tree.ts"
      via: "crumbsForPath import for auto-derive"
      pattern: "crumbsForPath\\(.*pathname"
    - from: "remix-app/app/routes/_app/protocol/version-detail.tsx"
      to: "PageHeader crumbs prop"
      via: "explicit crumbs= on existing PageHeader call"
      pattern: "crumbs=\\{"
---

<objective>
Merge the breadcrumb into the PageHeader meta row. Kill the stacked three-line page top
(shell crumb / eyebrow / title): the crumb renders right-aligned on the eyebrow line,
the redundant ZOETROP segment is dropped everywhere, and single-segment crumbs
(pure title duplication) are suppressed. Net: crumbs appear only at depth >= 2.

Purpose: reclaim vertical space and remove duplication flagged by the user (screenshot
of /protocol/versions showed ZOETROP / PROTOCOL / VERSIONS stacked above VERSION
HISTORY above "Protocol evolution").

Output: 7 files modified on the `left-nav-refactor` worktree branch; full gate green
(typecheck + lint + test + build).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Working directory: /Users/mac/Code/zoetrop/.claude/worktrees/left-nav (branch left-nav-refactor). App lives in remix-app/.

@remix-app/app/components/ui/PageHeader.tsx
@remix-app/app/components/ui/Crumb.tsx
@remix-app/app/components/shell/AppShell.tsx
@remix-app/app/components/shell/nav-tree.ts
@remix-app/app/routes/_app/metrics/category.tsx
@remix-app/app/routes/_app/metrics/detail.tsx
@remix-app/app/routes/_app/protocol/version-detail.tsx

<interfaces>
Current contracts (verified against worktree 2026-06-10):

From remix-app/app/components/ui/Crumb.tsx:
```typescript
export interface CrumbItem { label: string; to?: string; }
export interface CrumbProps { items: CrumbItem[]; }
export function Crumb({ items }: CrumbProps): JSX
// Root div currently carries marginBottom: "var(--gap-lg)" (line 31) — to be removed.
```

From remix-app/app/components/ui/PageHeader.tsx:
```typescript
export interface PageHeaderProps { eyebrow?: string; title: string; sub?: string; right?: ReactNode; }
// Current root: <div className="flex items-end justify-between gap-4 flex-wrap mb-8">
// eyebrow renders inside the left column as <div className="zt-eyebrow mb-2.5">
```

From remix-app/app/components/shell/nav-tree.ts:
```typescript
export function crumbsForPath(pathname: string): CrumbItem[] | null
// Currently prefixes every result with { label: "zoetrop", to: "/dashboard" }
// (lines 153, 158, 164). ownCrumb children and deeper-than-exact-child paths -> null.
// Note: nav-tree.ts already type-imports CrumbItem from ~/components/ui/Crumb —
// PageHeader importing crumbsForPath from nav-tree creates no cycle.
```

AppShell.tsx anchors: Crumb import line 11, crumbsForPath import line 13,
`const crumbs = crumbsForPath(pathname)` line 61, `{crumbs && <Crumb items={crumbs} />}` line 86.

Crumb consumers (exhaustive, via grep): AppShell.tsx + the three route files below. No other call sites.
PageHeader consumers: 17 routes (dashboard, metrics/index, all protocol/insights/import/ingest pages, settings) — all gain auto-derived crumbs; dashboard/group-index/settings resolve to null (suppressed).
</interfaces>

**DISCREPANCY vs locked design (resolved, not a redesign):** the locked design item 5
says to pass crumbs "via the new `crumbs` prop on their existing PageHeader calls" for
all three loader-crumb pages, but only `protocol/version-detail.tsx` actually uses
PageHeader. `metrics/category.tsx` (CatChip + h1 custom header, line 219) and
`metrics/detail.tsx` (custom flex header, line 127) do not. For those two, apply the
design's own crumbs-only meta-row treatment ("If only crumbs (no eyebrow), crumb still
sits at the right — use justify-end"): a right-aligned flex row containing `<Crumb/>`
directly above their existing custom headers, marginBottom 10px (matches PageHeader's
mb-2.5). Their `Crumb` imports therefore STAY (still used); only version-detail.tsx
drops its Crumb import.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure crumb plumbing — nav-tree, Crumb, PageHeader, AppShell</name>
  <files>remix-app/app/components/shell/nav-tree.ts, remix-app/app/components/ui/Crumb.tsx, remix-app/app/components/ui/PageHeader.tsx, remix-app/app/components/shell/AppShell.tsx</files>
  <action>
    All edits per locked design items 1-4.

    **nav-tree.ts — `crumbsForPath` (lines 147-168):** Drop the leading
    `{ label: "zoetrop", to: "/dashboard" }` item from every return. After the drop,
    return null whenever fewer than 2 items remain — concretely: the `/settings` branch
    (would be 1 item: "Account settings") and the `pathname === group.base` branch
    (would be 1 item: group label) now return null. The only non-null return is the
    exact-child case: `[{ label: group.label, to: group.base }, { label: child.label }]`.
    Keep the `/dashboard` early return, the `ownCrumb` suppression (line 162), and the
    deeper-than-exact-child null (line 161) exactly as they are. Update the doc comment
    (lines 147-149) to describe the new contract: crumbs only at depth >= 2 (group +
    child), no zoetrop segment, single-segment crumbs suppressed as title duplication.
    Defensive: keep the function's return type `CrumbItem[] | null` unchanged.

    **Crumb.tsx:** Delete `marginBottom: "var(--gap-lg)"` (line 31) from the root div
    style. Spacing is now the container's job. No other changes — keep CrumbItem/
    CrumbProps exports and the Fragment/Link rendering as-is.

    **PageHeader.tsx:** Add optional prop `crumbs?: CrumbItem[] | null` to
    PageHeaderProps. Imports: `useLocation` from "react-router", `Crumb` + type
    `CrumbItem` from "./Crumb", `crumbsForPath` from "~/components/shell/nav-tree".
    Call `useLocation()` unconditionally at the top of the component (hooks rule), then
    resolve: `const resolvedCrumbs = crumbs === undefined ? crumbsForPath(pathname) : crumbs;`
    — explicit `crumbs={null}` or `crumbs={[...]}` therefore overrides auto-derive.
    New layout, preserving the existing `right` slot in the title row:
    - Outer wrapper: `<div className="mb-8">` (mb-8 moves here from the old root).
    - Meta row — renders only if `eyebrow || (resolvedCrumbs && resolvedCrumbs.length > 0)`:
      `<div className="flex items-baseline justify-between gap-4 flex-wrap mb-2.5">`.
      Left: `{eyebrow ? <div className="zt-eyebrow">{eyebrow}</div> : <span />}` (the
      empty span keeps justify-between pushing a crumbs-only row to the right; the
      eyebrow's old mb-2.5 moves to the row). Right: `{resolvedCrumbs && resolvedCrumbs.length > 0 && <Crumb items={resolvedCrumbs} />}`.
    - Title row: `<div className="flex items-end justify-between gap-4 flex-wrap">`
      containing the EXISTING left column (h1 + sub, unchanged inline styles, eyebrow
      removed from it) and the existing `{right && <div>{right}</div>}` slot.

    **AppShell.tsx:** Delete the shell-level crumb render `{crumbs && <Crumb items={crumbs} />}`
    (line 86), the `const crumbs = crumbsForPath(pathname);` computation (line 61), the
    `Crumb` import (line 11), and the `crumbsForPath` import (line 13). `pathname` stays
    (still used by the drawer-close effect). Update the file-top comment block (line 4
    mentions "shell-wide breadcrumbs (crumbsForPath)") — PageHeader owns crumbs now.
  </action>
  <verify>
    <automated>cd /Users/mac/Code/zoetrop/.claude/worktrees/left-nav/remix-app && npx react-router typegen && npx tsc --noEmit</automated>
  </verify>
  <done>
    Typecheck passes. crumbsForPath has no zoetrop segment and returns null for /settings
    and group-base paths. Crumb has no built-in margin. PageHeader renders meta row
    (eyebrow left, crumb right) above an unchanged title row and auto-derives crumbs when
    the prop is undefined. AppShell renders no crumb and has no Crumb/crumbsForPath imports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate the three loader-crumb pages + full gate</name>
  <files>remix-app/app/routes/_app/metrics/category.tsx, remix-app/app/routes/_app/metrics/detail.tsx, remix-app/app/routes/_app/protocol/version-detail.tsx</files>
  <action>
    Per locked design item 5, dropping the `{label:"zoetrop", to:"/dashboard"}` prefix
    (added in commit 28fc91c) from all three item arrays.

    **protocol/version-detail.tsx** (has PageHeader): Delete the standalone `<Crumb items={[...]} />`
    block (lines 140-147) and its `{/* Breadcrumb + header */}` comment's crumb half. Add
    to the existing PageHeader call (line 149):
    `crumbs={[{ label: "Protocol", to: "/protocol" }, { label: "Versions", to: "/protocol/versions" }, { label: version.version }]}`.
    The existing `right=` slot (prev/next Buttons + Current badge) is untouched — crumb
    lands in the meta row next to eyebrow "PROTOCOL VERSION", no conflict. Remove the
    now-unused `Crumb` import (line 15); no CrumbItem type import needed (array literal
    is inferred against the prop type).

    **metrics/category.tsx** (NO PageHeader — custom CatChip header; see context
    discrepancy note): Replace the standalone `<Crumb items={[...]} />` at line 216 with
    a right-aligned meta row matching PageHeader's crumbs-only treatment:
    `<div style={{ display: "flex", justifyContent: "flex-end", marginBottom: 10 }}>`
    wrapping `<Crumb items={[{ label: "Metrics", to: "/metrics" }, { label: categoryInfo.label }]} />`.
    Keep the `Crumb` import (line 33) — still used. Custom header div (line 219) unchanged.

    **metrics/detail.tsx** (NO PageHeader — custom flex header): Same treatment — replace
    the standalone `<Crumb items={[...]} />` at lines 117-124 with the same justify-end
    wrapper (marginBottom: 10) containing
    `<Crumb items={[{ label: "Metrics", to: "/metrics" }, { label: categoryInfo.label, to: \`/metrics/${category}\` }, { label: metric.name }]} />`.
    Keep the `Crumb` import (line 20).

    **Double-crumb verification (locked design item 6):** confirm by reading the final
    code paths — crumbsForPath returns null for /metrics/:category (ownCrumb child),
    /metrics/:category/:metricId and /protocol/versions/:version (deeper than exact
    child), so PageHeader auto-derive yields nothing on these routes; the only crumbs are
    the explicit ones above. On /protocol/versions/:version the explicit `crumbs` prop
    overrides auto-derive regardless. Spot-check the rendered HTML for one param route
    and one child route via the dev server or build output is NOT required — the gate
    plus code-path reasoning suffices for this quick task.

    Then run the full verification gate.
  </action>
  <verify>
    <automated>cd /Users/mac/Code/zoetrop/.claude/worktrees/left-nav/remix-app && npm run typecheck && npm run lint && npm run test && npm run build</automated>
  </verify>
  <done>
    All three pages pass zoetrop-free crumb items; version-detail uses the PageHeader
    crumbs prop with its right slot intact; category/detail render right-aligned meta-row
    crumbs above their custom headers; no `"zoetrop"` crumb label remains anywhere
    (`grep -rn '"zoetrop", to: "/dashboard"' app/` returns nothing); typecheck + lint +
    test + build all green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Pure client-side presentational restructure of existing
authenticated routes — no new inputs, no data access changes, no package installs.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q-01 | Information Disclosure | crumb labels (PageHeader meta row) | accept | Labels come from the static NAV_TREE and existing loader data already rendered on-page; no new data exposure |
</threat_model>

<verification>
From remix-app/ (node_modules installed):
1. `npm run typecheck` — react-router typegen + tsc clean
2. `npm run lint` — clean (no unused-import errors from removed Crumb/crumbsForPath)
3. `npm run test` — existing suite green
4. `npm run build` — production build succeeds
5. `grep -rn 'label: "zoetrop"' app/` — zero matches outside the AppShell footer eyebrow text (the footer's plain "zoetrop" string at AppShell.tsx ~line 105 is brand text, not a crumb — it stays)
</verification>

<success_criteria>
- Stacked crumb block is gone shell-wide; crumbs render right-aligned on the PageHeader meta row (or the equivalent justify-end row on the two custom-header metric pages)
- ZOETROP segment removed from every crumb
- Crumbs appear only at depth >= 2: child routes (e.g. /protocol/versions -> "PROTOCOL / VERSIONS") and param routes with explicit items; /protocol, /metrics, /insights, /import, /ingest, /settings, /dashboard show none
- No double-crumb on /metrics/:category, /metrics/:category/:metricId, /protocol/versions/:version
- version-detail's `right` slot (prev/next buttons) unaffected
- Full gate green: typecheck, lint, test, build
</success_criteria>

<output>
Create `.planning/quick/260610-rj2-merge-breadcrumb-into-pageheader-meta-ro/260610-rj2-SUMMARY.md` when done.
Commits on `left-nav-refactor`: 1-2 commits (suggested: one for the component plumbing, one for the three route migrations — or a single atomic commit since the change is only coherent as a whole).
</output>
