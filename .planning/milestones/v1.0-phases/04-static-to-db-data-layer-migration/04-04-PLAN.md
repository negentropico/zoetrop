---
phase: 04-static-to-db-data-layer-migration
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - remix-app/app/routes/_app/dashboard.tsx
  - remix-app/app/routes/_app/metrics/index.tsx
  - remix-app/app/routes/_app/metrics/category.tsx
  - remix-app/app/routes/_app/metrics/detail.tsx
  - remix-app/app/routes/_app/protocol/index.tsx
  - remix-app/app/routes/_app/protocol/versions.tsx
  - remix-app/app/routes/_app/protocol/version-detail.tsx
  - remix-app/app/routes/_app/protocol/supplements.tsx
  - remix-app/app/routes/_app/protocol/cessation.tsx
  - remix-app/app/routes/_app/protocol/compare.tsx
  - remix-app/app/routes/_app/insights/index.tsx
  - remix-app/app/routes/_app/insights/correlations.tsx
  - remix-app/app/routes/_app/insights/genetics.tsx
  - remix-app/tests/parity/loader-parity.test.ts
  - remix-app/eslint.config.mjs
  - remix-app/package.json
autonomous: false
requirements: [DATA-01]
must_haves:
  truths:
    - "Every one of the 13 data-returning _app loaders reads from Neon via data.server.ts, scoped to the owner's tenantId/subjectId"
    - "No _app route imports from real-data, protocol-data, or seed-data at runtime"
    - "The DB-backed loader output deep-matches the captured static fixtures (parity green)"
    - "A CI lint rule (eslint no-restricted-imports) blocks importing *-data.ts from routes/components"
    - "The genetics loader joins subject_genotypes (DB PHI plane) with GENETIC_KNOWLEDGE (server-only knowledge plane) by gene"
  artifacts:
    - path: "remix-app/tests/parity/loader-parity.test.ts"
      provides: "DATA-01 parity assertions for all 13 loaders vs gitignored fixtures"
      contains: "toMatchObject"
    - path: "remix-app/eslint.config.mjs"
      provides: "no-restricted-imports gate blocking *-data.ts from routes/components"
      contains: "no-restricted-imports"
    - path: "remix-app/package.json"
      provides: "lint script"
      contains: "\"lint\":"
  key_links:
    - from: "remix-app/app/routes/_app/**"
      to: "remix-app/app/lib/data.server.ts"
      via: "requireUser → getOwnerSubject → getX(tenantId, subjectId)"
      pattern: "getOwnerSubject"
    - from: "remix-app/app/routes/_app/insights/genetics.tsx"
      to: "remix-app/app/lib/genetics-knowledge.server.ts"
      via: "join subject_genotypes rows with GENETIC_KNOWLEDGE by gene"
      pattern: "GENETIC_KNOWLEDGE"
    - from: "remix-app/eslint.config.mjs"
      to: "remix-app/app/routes + components"
      via: "no-restricted-imports patterns"
      pattern: "real-data"
---

<objective>
Rewire all 13 data-returning `_app` loaders from static modules to live Neon via `data.server.ts` (DATA-01), prove the cut-over with the parity test against the captured fixtures, and install the CI lint gate that blocks `*-data.ts` imports from route/component contexts. This is the heart of the migration — after it, every authenticated page renders DB-backed, tenant-scoped data.

Purpose: DATA-01 requires loaders read live Neon (no static runtime source) AND a CI lint rule blocks direct `*-data.ts` imports from non-seed contexts. The parity test (D-09) is the safety gate: the DB-backed loaders must produce the same shape the static modules did, asserted against the fixtures captured in Plan 03. This plan does NOT delete the static files — deletion is gated on the owner's visual spot-check (Plan 05, D-09 order). This plan ends with a human-verify checkpoint confirming the live app renders correctly.

Output: 13 rewired loaders; `tests/parity/loader-parity.test.ts` filled and green; `eslint.config.mjs` + `lint` script; human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-static-to-db-data-layer-migration/04-CONTEXT.md
@.planning/phases/04-static-to-db-data-layer-migration/04-RESEARCH.md
@.planning/phases/04-static-to-db-data-layer-migration/04-PATTERNS.md

# Prior plans — the read layer (02) + seeded data + fixtures (03) this plan consumes
@.planning/phases/04-static-to-db-data-layer-migration/04-02-SUMMARY.md
@.planning/phases/04-static-to-db-data-layer-migration/04-03-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase + Plan 02. -->

From remix-app/app/lib/data.server.ts (Plan 02 — read functions to call):
- `getOwnerSubject(tenantId: string): Promise<Subject>` (throws 404 Response if none)
- `getMetrics(tenantId, subjectId, category?): Promise<MetricRow[]>`
- `getProtocolVersions(tenantId, subjectId)`, `getProtocolChanges(tenantId, subjectId)`, `getMilestones(tenantId, subjectId)`, `getSupplements(tenantId, subjectId)`, `getCorrelations(tenantId, subjectId)`, `getCessationLog(tenantId, subjectId)`, `getSubjectGenotypes(tenantId, subjectId)`

From remix-app/app/lib/db-mappers.server.ts (Plan 02): `dbRowToMetric(row): Metric` — map metrics rows before shaping.

From remix-app/app/lib/genetics-knowledge.server.ts (Plan 02): `GENETIC_KNOWLEDGE: Record<gene, GeneticKnowledgeEntry>` — join key is `gene`.

From remix-app/app/lib/authz.server.ts: `requireUser(request): Promise<{ user, session }>` — `user.tenantId` is non-null for owner/practitioner.

From remix-app/app/lib/protocol-data.ts (SURVIVOR engine fns — keep importing these; they are NOT deleted):
- `getCessationDay(now?: Date): number`, `getCurrentCessationPhase(day): CessationPhaseInfo`, `CESSATION_START_DATE`, `CESSATION_PHASES`, `dailySchedule`, `avoidList`. (The `real*` PHI ARRAYS in this file are deleted in Plan 05; the engine fns + non-PHI constants survive.)

Current loader rewire — 3 shapes (04-PATTERNS.md Shapes A/B/C):
- Shape A (list query): `protocol/supplements`, `protocol/versions`, `protocol/index`, `insights/index`, `insights/correlations`. Replace static array read with `requireUser` → `getOwnerSubject` → `getX(...)`; keep the existing shaping (byTier/stats/etc) verbatim.
- Shape B (date-math, injectable now): `protocol/cessation`, `dashboard`. Use `getCessationLog` for the row + survivor `getCessationDay(now)` / `getCurrentCessationPhase`. `now` defaults to `new Date()` in prod; parity test injects FIXED_NOW.
- Shape C (DB + knowledge join): `insights/genetics`. `getSubjectGenotypes` rows joined with `GENETIC_KNOWLEDGE[row.gene]` to reconstruct the GeneticVariant shape the page renders.

Loader async upgrade: change `export function loader()` (or `loader({ params })`) to `export async function loader({ request, params })`; add `import type { LoaderFunctionArgs } from "react-router"` where a typed args object is needed (or keep `Route.LoaderArgs` for routes already using `+types`). Route components consume `Route.ComponentProps`/`useLoaderData<typeof loader>` — typegen absorbs the async change, NO component-body edits.

Routes & params (from app/routes.ts):
- metrics/:category → `params.category` (MetricCategory); metrics/:category/:metricId → `params.metricId`; protocol/versions/:version → `params.version`.

Current data imports to REMOVE per route (grep-confirmed):
- dashboard.tsx: from seed-data (seedCorrelations, seedGeneticVariants, getCorrelationColor), protocol-data (realCessationLog, realProtocolVersions, realSupplements, + survivors getCessationDay/getCurrentCessationPhase), real-data (getLatestRealMetrics). Keep survivors; remove the real*/seed* PHI arrays.
- metrics/index, metrics/category, metrics/detail: real-data (getRealMetrics, getLatestRealMetrics, getMetricTargets, getProjections, MILESTONES).
- protocol/index, versions, version-detail, supplements, cessation, compare: protocol-data (real* arrays). Keep getCessationDay/getCurrentCessationPhase survivors.
- insights/index, correlations, genetics: seed-data (seedCorrelations, seedGeneticVariants, getCorrelationColor, getCorrelationSignificance).

Note on METRIC_TARGETS/MILESTONES/getProjections (RESEARCH Open Questions 1–2): targets are goals (non-PHI survivors — relocate or keep), but `MILESTONES` M1–M4 dates are appointment dates (PHI → from DB milestones). Where a loader uses `getMetricTargets`/`getProjections`, keep those as survivor helpers but source any measured/appointment values from the DB. Resolve the exact survivor homes in this plan's edits and record in the SUMMARY (Plan 05 relocates them physically); nothing with the owner's measured values may remain imported into a route at runtime.

From remix-app/tests/parity/loader-parity.test.ts (Plan 01 scaffold): `FIXED_NOW = new Date("2026-06-10T00:00:00.000Z")`; `describe.skipIf(!connectionString)`; reads `tests/fixtures/*.json`.

ESLint state: NO eslint config exists; `package.json` `eslintConfig` is `{}`; the eslint binary is NOT currently installed in node_modules (must be installed — see Task 3 + package legitimacy gate). ESLint 10.x flat config = `eslint.config.mjs`. The `.server.ts`/seed-script paths must be EXCLUDED from the rule (only `app/routes/**` + `app/components/**` are blocked).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire metrics + protocol loaders (7 routes) to data.server.ts</name>
  <files>remix-app/app/routes/_app/metrics/index.tsx, remix-app/app/routes/_app/metrics/category.tsx, remix-app/app/routes/_app/metrics/detail.tsx, remix-app/app/routes/_app/protocol/index.tsx, remix-app/app/routes/_app/protocol/versions.tsx, remix-app/app/routes/_app/protocol/version-detail.tsx, remix-app/app/routes/_app/protocol/supplements.tsx, remix-app/app/routes/_app/protocol/cessation.tsx, remix-app/app/routes/_app/protocol/compare.tsx</files>
  <read_first>
    - All 9 listed route files (full — current loader + the exact shaping logic to preserve verbatim)
    - remix-app/app/lib/data.server.ts (Plan 02 — function signatures)
    - remix-app/app/lib/db-mappers.server.ts (Plan 02 — dbRowToMetric)
    - remix-app/app/lib/authz.server.ts (requireUser)
    - remix-app/app/lib/protocol-data.ts (survivor engine fns to keep importing)
    - .planning/phases/04-static-to-db-data-layer-migration/04-PATTERNS.md (Shapes A/B/C; Loader Async Upgrade)
  </read_first>
  <action>
    For each of the 9 metrics + protocol loaders, convert `loader` to `async loader({ request, params })`: call `requireUser(request)` → `getOwnerSubject(user.tenantId)` → the matching `data.server.ts` read function(s), then run dbRowToMetric over metrics rows where the loader returns Metric objects, then keep the EXISTING shaping logic (grouping, stats, byTier, byCategory, projections) byte-for-byte so the return shape is unchanged. Remove every import of `real*`/`seed*` PHI arrays; KEEP survivor imports (`getCessationDay`, `getCurrentCessationPhase`, `CESSATION_PHASES`, `dailySchedule`, `avoidList`, and any non-PHI target/constant helpers). For param routes, read `params.category`/`params.metricId`/`params.version` and pass through (e.g. `getMetrics(tenantId, subjectId, params.category)`). For `protocol/cessation` use `getCessationLog` + survivor `getCessationDay(new Date())`. Add `import type { LoaderFunctionArgs } from "react-router"` only where the route does not already use `Route.LoaderArgs`. Do NOT touch component bodies (typegen absorbs the async change). Where a loader currently reads `MILESTONES`/measured values from real-data, source those from the DB (`getMilestones`/`getMetrics`); keep `getMetricTargets`/`getProjections` only if they hold non-PHI goals (they stay imported from real-data for now — Plan 05 relocates them to app/lib/metric-targets.ts; record the home in the SUMMARY).
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 9 files contain `requireUser` and `getOwnerSubject` and import from `~/lib/data.server`.
    - `grep -rnE "seedCorrelations|seedGeneticVariants|realSupplements|realProtocolVersions|realCessationLog|realMilestones|realProtocolChanges|getRealMetrics|getLatestRealMetrics" remix-app/app/routes/_app/metrics remix-app/app/routes/_app/protocol` returns NO matches (PHI-array imports gone).
    - Survivor imports preserved where used: `getCessationDay`/`getCurrentCessationPhase` still imported in cessation.tsx.
    - Every rewired loader is `async`.
    - `cd remix-app && npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>All 9 metrics + protocol loaders read DB-backed tenant-scoped data via data.server.ts; PHI-array imports removed; survivor engine fns kept; shaping unchanged; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire dashboard + insights loaders (4 routes) + fill parity test green</name>
  <files>remix-app/app/routes/_app/dashboard.tsx, remix-app/app/routes/_app/insights/index.tsx, remix-app/app/routes/_app/insights/correlations.tsx, remix-app/app/routes/_app/insights/genetics.tsx, remix-app/tests/parity/loader-parity.test.ts</files>
  <read_first>
    - dashboard.tsx, insights/index.tsx, insights/correlations.tsx, insights/genetics.tsx (full — composite shaping + the seed-data lib usage)
    - remix-app/app/lib/data.server.ts + db-mappers.server.ts + genetics-knowledge.server.ts (Plan 02)
    - remix-app/tests/parity/loader-parity.test.ts (Plan 01 scaffold — FIXED_NOW, skip-guard, fixture path)
    - remix-app/tests/db/constraints.test.ts (skip-guard + Pool lifecycle reference)
    - .planning/phases/04-static-to-db-data-layer-migration/04-03-SUMMARY.md (representative params used for fixtures; actual seeded counts)
    - .planning/phases/04-static-to-db-data-layer-migration/04-PATTERNS.md (Shape C genetics join; Pattern 6 parity harness)
  </read_first>
  <action>
    Rewire dashboard.tsx (Shape B composite): replace seedCorrelations/seedGeneticVariants/realCessationLog/realProtocolVersions/realSupplements/getLatestRealMetrics reads with `requireUser` → `getOwnerSubject` → `getCorrelations`/`getSubjectGenotypes` (joined with GENETIC_KNOWLEDGE for impact/confidence)/`getCessationLog`/`getProtocolVersions`/`getSupplements`/`getMetrics`+dbRowToMetric; keep survivor `getCessationDay`/`getCurrentCessationPhase` and ALL existing stat/grouping logic. Rewire insights/index + insights/correlations: `getCorrelations` (derive the significance label from `|correlation|` at render time per D-04 — keep `getCorrelationSignificance`/`getCorrelationColor` as survivor presentation helpers, imported from their non-PHI home or inlined; Plan 05 relocates them out of seed-data). Rewire insights/genetics (Shape C): `getSubjectGenotypes` rows `.map` joined with `GENETIC_KNOWLEDGE[row.gene]` to reconstruct each variant `{ id, gene, rsid, genotype, ...GENETIC_KNOWLEDGE[gene] }`; keep the existing byCategory/stats grouping. Remove all seed-data PHI-array imports from these 4 routes. Then FILL `tests/parity/loader-parity.test.ts`: for each of the 13 captured fixtures, invoke the rewired loader with a mock authenticated `request` and `params` matching the fixture's representative values and the pinned FIXED_NOW, and assert `expect(result).toMatchObject(fixture)` (toMatchObject tolerates extra DB-only fields like createdAt/id). Read fixtures from `tests/fixtures/<key>.json`; skip-guard the whole suite on connectionString. Mock `requireUser` (e.g. vi.mock the authz module) to return the owner's tenantId so the loader runs server-side against live Neon; or stub the session header. Record the mock approach in the SUMMARY.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && npm test -- tests/parity/loader-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.tsx + the 3 insights routes contain `getOwnerSubject` and import from `~/lib/data.server`; genetics.tsx imports `GENETIC_KNOWLEDGE` from `~/lib/genetics-knowledge.server`.
    - `grep -rnE "seedCorrelations|seedGeneticVariants|getLatestRealMetrics|realSupplements|realProtocolVersions|realCessationLog" remix-app/app/routes/_app/dashboard.tsx remix-app/app/routes/_app/insights` returns NO matches.
    - `tests/parity/loader-parity.test.ts` contains `toMatchObject` and references all 13 fixture keys; the suite runs (not `.skip`) and exits 0 against live Neon.
    - `cd remix-app && npm test -- tests/parity/loader-parity.test.ts` exits 0 (DATA-01 parity green).
    - `cd remix-app && npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>dashboard + 3 insights loaders DB-backed (genetics joins DB + knowledge plane); parity test filled and green for all 13 loaders against live Neon.</done>
</task>

<task type="auto">
  <name>Task 3: ESLint no-restricted-imports CI lint gate (DATA-01)</name>
  <files>remix-app/eslint.config.mjs, remix-app/package.json</files>
  <read_first>
    - remix-app/package.json (scripts + current empty eslintConfig + devDependencies)
    - .planning/phases/04-static-to-db-data-layer-migration/04-RESEARCH.md (ESLint no-restricted-imports rule; Standard Stack; assumption A3 eslint availability)
    - .planning/phases/04-static-to-db-data-layer-migration/04-PATTERNS.md (ESLint No-Restricted-Imports section — flat-config shape, files scope, lint script)
  </read_first>
  <action>
    The eslint binary is not currently installed (A3). Install the flat-config eslint toolchain as devDependencies WITHOUT --force/--legacy-peer-deps: `eslint` (^10) plus `typescript-eslint` if TS-aware parsing is needed for `.tsx` import resolution — prefer the minimal set that makes `no-restricted-imports` work on `.tsx`. The install MUST pass the package legitimacy checkpoint first (see threat model T-04-SC). Create `remix-app/eslint.config.mjs` (flat config) with a block scoped to `files: ["app/routes/**/*", "app/components/**/*"]` enforcing `"no-restricted-imports": ["error", { patterns: [ { group: ["*/lib/real-data", "*/lib/real-data*", "**/real-data"], message: "real-data.ts is PHI — read from DB via data.server.ts" }, { group: ["*/lib/protocol-data", "**/protocol-data"], message: "protocol-data PHI arrays removed — use data.server.ts (non-PHI survivor engine fns are exempt only outside routes)" }, { group: ["*/lib/seed-data", "**/seed-data"], message: "seed-data.ts is seed-only — use data.server.ts in routes" } ] }]`. Do NOT scope the rule to `scripts/**` or `*.server.ts`. NOTE: protocol/cessation + dashboard import the SURVIVOR engine fns from protocol-data — if the rule blocks the whole `protocol-data` module it will flag those legitimate survivor imports; resolve by either (a) relocating the survivor engine fns to a non-blocked module (e.g. `app/lib/cessation.ts`) as part of this task and repointing the two routes, OR (b) narrowing the rule to only block the deleted PHI symbols. Pick one, apply consistently, record in the SUMMARY. Add `"lint": "eslint app/routes app/components"` to package.json scripts. Run `npm run lint` — it MUST pass. Prove the gate works with a temporary scratch `real-data` import in a route (confirm lint errors), then revert; document the negative test in the SUMMARY.
  </action>
  <verify>
    <automated>cd remix-app && npm run lint && grep -c "no-restricted-imports" eslint.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `eslint` is installed (node_modules/eslint exists) without `--force`/`--legacy-peer-deps`.
    - `remix-app/eslint.config.mjs` contains `no-restricted-imports` and the three groups (`real-data`, `protocol-data`, `seed-data`) and the `files: ["app/routes/**` scope.
    - The rule does NOT apply to `scripts/**` or `*.server.ts`.
    - The survivor engine-fn imports (getCessationDay/getCurrentCessationPhase) do NOT trip the lint rule (resolved via relocation or narrowed pattern — documented in SUMMARY).
    - `package.json` contains `"lint":`.
    - `cd remix-app && npm run lint` exits 0.
    - SUMMARY documents the negative-test result (a deliberate `real-data` import in a route triggers an eslint error).
  </acceptance_criteria>
  <done>eslint flat config with no-restricted-imports blocking *-data.ts from routes/components; survivor imports unaffected; lint script registered; lint passes; negative test confirms the gate fires.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Owner visual verification of DB-backed app</name>
  <files>(none — verification-only checkpoint; all automation completed in Tasks 1–3)</files>
  <what-built>All 13 authenticated route loaders now read live, tenant-scoped data from Neon (no static runtime source); parity test confirms shape-equality with the pre-migration output; the CI lint gate blocks future *-data.ts imports from routes. The static PHI arrays are NOT yet deleted — deletion is gated on this verification (D-09).</what-built>
  <how-to-verify>
    1. Run `cd remix-app && npm run dev` and sign in as the owner.
    2. Visit each section and confirm data renders identically to before the migration:
       - `/dashboard` — cessation day/phase, top correlations, high-impact variants, metric status counts, active supplement count.
       - `/metrics`, `/metrics/metabolic`, then a metric detail page — values, ranges, status colors.
       - `/protocol`, `/protocol/versions`, a version detail, `/protocol/supplements`, `/protocol/cessation`, `/protocol/compare` — versions P0–P6, supplement tiers/active flags, cessation timeline.
       - `/insights`, `/insights/correlations`, `/insights/genetics` — correlation strengths, genetic variants with their K-grade/impact (DB genotype joined with knowledge plane).
    3. Confirm no blank pages, no 404/500, and the genetics page shows genotypes (from DB) WITH clinical implication/protocol action (from the server knowledge module).
  </how-to-verify>
  <action>CHECKPOINT — pause for the owner. All automation (loader rewiring, parity green, lint gate) is complete in Tasks 1–3. Present the steps in how-to-verify and wait for the owner's verdict. Do NOT authorize Plan 05 (deletion) until the owner types "approved". Record the verdict in the SUMMARY — Plan 05 Task 2 is gated on it (D-09).</action>
  <verify><human-check>Owner confirms all sections render DB-backed data identical to the pre-migration app; genetics page shows DB genotypes joined with the server knowledge plane.</human-check></verify>
  <resume-signal>Type "approved" to authorize Plan 05 (static-file deletion + build PHI grep), or describe any rendering discrepancy vs the pre-migration app.</resume-signal>
  <done>Owner has typed "approved" (or all reported discrepancies resolved and re-approved); the verdict is recorded so Plan 05's deletion is authorized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client request → loader → data.server.ts | untrusted session; tenant/subject scope enforced in data.server.ts |
| server module → client bundle | genetics-knowledge.server.ts (`.server.ts`) must not reach the client |
| npm registry → node_modules | eslint install must be a legitimate package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-XTEN | Information Disclosure | rewired loaders | mitigate | All reads go through data.server.ts with `tenantId`+`subjectId` from `requireUser`+`getOwnerSubject`; no raw Drizzle in routes; parity test confirms correct scoping (V4) |
| T-04-GEN-LEAK | Information Disclosure | insights/genetics loader | mitigate | Genotypes from DB (server loader); knowledge plane from `.server.ts` module — neither reaches the client bundle except via the loader's shaped return |
| T-04-LINT | Tampering | future PHI re-imports | mitigate | eslint no-restricted-imports gate blocks `*-data.ts` from routes/components — the DATA-01 CI control |
| T-04-SC | Tampering | eslint (+ typescript-eslint) install | mitigate | `eslint`/`typescript-eslint` are verified npm packages (RESEARCH Package Legitimacy Audit: eslint Approved). A blocking package-legitimacy human checkpoint precedes the install (verify on npmjs.com/package/eslint and npmjs.com/package/typescript-eslint); no `--force`/`--legacy-peer-deps`. T-{phase}-SC retained; not auto-approvable. |
</threat_model>

<verification>
- `cd remix-app && npm run typecheck` exits 0.
- `cd remix-app && npm test -- tests/parity/loader-parity.test.ts` exits 0 (DATA-01 parity green, all 13 loaders).
- `cd remix-app && npm run lint` exits 0 (no PHI-array imports in routes).
- `grep -rnE "real-data|seed-data" remix-app/app/routes/` returns no matches (protocol-data appears only for survivor engine-fn imports until Plan 05 relocation).
- Owner visual verification: approved.
</verification>

<success_criteria>
- DATA-01 satisfied: every loader reads live Neon scoped to the owner's tenant/subject; CI lint rule blocks `*-data.ts` route imports.
- Parity green: DB-backed loaders deep-match the static fixtures (D-09 gate cleared).
- D-01/D-03 realized end-to-end: genetics page renders DB genotypes joined with the server-only knowledge plane.
- Static files still present (deletion deferred to Plan 05, gated on the owner approval recorded here).
</success_criteria>

<output>
Create `.planning/phases/04-static-to-db-data-layer-migration/04-04-SUMMARY.md` when done. Record: the survivor-helper homes resolved (METRIC_TARGETS/getProjections/getCorrelationSignificance + the cessation engine-fn relocation decision), the eslint packages installed + their legitimacy-check outcome, the parity result per loader, and the owner's verification verdict.
</output>
