---
phase: 04-static-to-db-data-layer-migration
plan: 07
type: execute
wave: 2
depends_on:
  - 04-06
files_modified:
  - remix-app/app/routes/_app/insights/index.tsx
  - remix-app/app/routes/_app/protocol/cessation.tsx
autonomous: true
gap_closure: true
requirements:
  - DATA-04
user_setup: []

must_haves:
  truths:
    - "No subject-specific health fact is hardcoded as a JSX string literal in any client-bundled route component"
    - "The 'Key insights' card on insights/index.tsx derives its correlation insight from loader data (the strongest correlation in allCorrelations), not from a hardcoded r=−0.71"
    - "The genetic-narrative insight is gated on actual DB-sourced variant data, not a hardcoded FAAH/CYP1A2/K3/SelfDecode string"
    - "The 'Why 150 days?' card on cessation.tsx contains no PHI: no '76-day attempt', no 'K3 inferred from SelfDecode' — its text is either DB-sourced (cessation_log.notes) or a non-PHI generic protocol description"
    - "After `npm run build`, grep for the PHI markers in build/client/ returns zero matches (or only explicitly-justified non-PHI occurrences)"
  artifacts:
    - path: "remix-app/app/routes/_app/insights/index.tsx"
      provides: "Key insights card derived from loader data"
    - path: "remix-app/app/routes/_app/protocol/cessation.tsx"
      provides: "Why 150 days card with no hardcoded PHI"
  key_links:
    - from: "remix-app/app/routes/_app/insights/index.tsx"
      to: "loaderData (allCorrelations / highImpactVariants)"
      via: "derive strongest correlation + gate genetic narrative on DB variants"
      pattern: "loaderData|topCorrelations|highImpactVariants"
---

<objective>
Close VERIFICATION.md BLOCKER 1 (CR-03, DATA-04 / SC-3): subject-specific health facts are hardcoded as JSX string literals inside two client-bundled route components, so PHI compiles into the unauthenticated static client bundle. Confirmed present in `build/client/assets/cessation-*.js` (`"76-day attempt"`, `"SelfDecode"`) and `build/client/assets/index-*.js` (`r=−0.71`).

Two sites:
1. `insights/index.tsx` lines ~333-345 — the "Key insights" card: hardcoded `r=−0.71` correlation value, the subject's MTHFR protocol response, and the FAAH/CYP1A2/K3/SelfDecode genetic interpretation.
2. `protocol/cessation.tsx` lines ~414-419 — the "Why 150 days?" card: `Lower FAAH activity (K3 inferred from SelfDecode) ... The previous 76-day attempt was insufficient.` — the subject's genetic interpretation and prior cessation-attempt history.

Purpose: Route component bodies compile into static client JS served without authentication. These strings are individually identifiable health information. Per ROADMAP SC-3 (D-10 wording: "Vercel build output"), the client bundle must contain no PHI. These cards must be either derived from loader data or de-PHI'd.

Output: insights "Key insights" card derives its correlation insight from `allCorrelations` loader data and gates any genetic narrative on actual DB variant rows; the cessation "Why 150 days?" card carries only DB-sourced or non-PHI generic text. The PHI markers vanish from the client bundle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-static-to-db-data-layer-migration/04-VERIFICATION.md
@.planning/phases/04-static-to-db-data-layer-migration/04-CONTEXT.md

<interfaces>
<!-- Extracted from codebase. Executor uses these directly — no exploration needed. -->

From remix-app/app/routes/_app/insights/index.tsx loader (data already available to the component):
  topCorrelations: Array<{ id; supplementName; metricName; correlation: number; lagDays; significance: "strong"|"moderate"|"weak"|"none"; direction: "positive"|"negative" }>   // sorted by |correlation| desc, top 5
  highImpactVariants: Array<{ id; gene; rsid; genotype; confidence; category; impact; clinicalImplication; protocolAction }>   // variants where impact === "high"
  variantsByCategory: Record<string, number>
  stats: { totalCorrelations; strongCorrelations; significantCorrelations; totalVariants; confirmedVariants }
  // NOTE: the loader does NOT currently return allCorrelations or all variants; it returns topCorrelations (top 5) and highImpactVariants. Use topCorrelations[0] as the strongest correlation, and highImpactVariants for the variant gate. If a broader set is needed, add a minimal projected field to the loader return (e.g. strongestNegative) — do NOT serialize raw DB rows with tenantId/subjectId.
  // The component currently destructures: const { topCorrelations, highImpactVariants, variantsByCategory, stats } = loaderData;

From remix-app/app/routes/_app/protocol/cessation.tsx component (loaderData available):
  cessation: { ...; startDate: string; notes: string | null } | null   // cessation.notes is ALREADY rendered in the timeline card at lines 400-404: `{cessation?.notes && (<div ...>{cessation.notes}</div>)}`
  currentPhase: typeof CESSATION_PHASES[0]   // has .label, .focus, .description, .dayRange
  targetDay: number   // 150
  CESSATION_PHASES (imported): each phase has { phase, label, focus, description, dayRange:{start,end} } — these are NON-PHI generic protocol descriptions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the hardcoded "Key insights" card in insights/index.tsx with loader-derived, DB-gated content</name>
  <files>remix-app/app/routes/_app/insights/index.tsx</files>
  <read_first>
    - remix-app/app/routes/_app/insights/index.tsx (loader lines 33-109 producing topCorrelations/highImpactVariants/stats; the "Key insights" card with hardcoded literals at lines 323-363; component destructure at lines 111-113)
    - .planning/phases/04-static-to-db-data-layer-migration/04-VERIFICATION.md (BLOCKER 1 `missing` items: "derive strongest negative correlation from allCorrelations loader data; gate FAAH/K3 narrative on variant DB data")
  </read_first>
  <action>
    Remove the hardcoded three-item array at lines 333-345 (the literals `head: "Methylfolate → Homocysteine"` / `body: "...r=−0.71, supporting the MTHFR protocol action."`, `head: "Magnesium" / body: "...autonomic benefits."`, and `head: "FAAH and CYP1A2" / body: "...K3 (inferred) — consider SelfDecode verification."`). No subject-specific health fact may remain as a JSX string literal in this component body.

    Replace it with insights derived at render time from loader data:

    1. Strongest correlation insight: take the top entry of `topCorrelations` (it is already sorted by |correlation| desc). Render the head as `${corr.supplementName} → ${corr.metricName}` and the body using the live value: `shows a ${corr.significance} ${corr.direction} correlation (r=${corr.correlation >= 0 ? "+" : ""}${corr.correlation.toFixed(2)}).` Do not hardcode any number — read it from the loader. Guard for the empty case (`topCorrelations.length > 0`).

    2. Genetic-narrative insight: gate it on actual DB variant rows. Only render a genetics insight when `highImpactVariants.length > 0`. Build the text from the loader-provided variants generically — e.g. head `${variant.gene}` and body from the variant's own `protocolAction` (already DB-knowledge-joined, non-PHI) or a generic count like `${highImpactVariants.length} protocol-defining variants identified`. Do NOT hardcode gene names (FAAH, CYP1A2, MTHFR), confidence grades (K3), or third-party source names (SelfDecode) as literals — every such fact must come from the variant data.

    3. Keep the card shell (the `<Card tone="focus">`, "Key insights" eyebrow, and the `.map(({head, body}) => ...)` rendering structure at lines 346-361) — only the SOURCE of the head/body items changes from a hardcoded array to a loader-derived array built in the component body or the loader.

    Prefer building the derived insight items in the loader and returning them as a small `keyInsights: Array<{head: string; body: string}>` projection (so the component stays a pure renderer), OR build them inline in the component from the already-destructured `topCorrelations`/`highImpactVariants`. Either is acceptable; if you add a loader field, return ONLY the head/body strings — never raw DB rows, tenantId, or subjectId. If you destructure new fields in the component, update the destructure at lines 111-113.

    Do NOT introduce the words "SelfDecode", "K3" (as a literal), "MTHFR", "FAAH", "CYP1A2", or "76-day" as hardcoded strings anywhere in this file. If a gene name legitimately appears it must be interpolated from `variant.gene`, not typed as a literal.
  </action>
  <verify>
    <automated>cd remix-app && npx react-router typegen && npx tsc --noEmit && grep -cE "r=.0\.71|SelfDecode|MTHFR protocol action" app/routes/_app/insights/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && grep -cE "r=.0\.71|SelfDecode" app/routes/_app/insights/index.tsx` returns 0
    - `cd remix-app && grep -c "consider SelfDecode verification" app/routes/_app/insights/index.tsx` returns 0
    - `cd remix-app && grep -c "MTHFR protocol action" app/routes/_app/insights/index.tsx` returns 0
    - The "Key insights" card head/body items are derived from `topCorrelations` and/or `highImpactVariants` (verify by reading: at least one item interpolates `corr.correlation.toFixed(2)` or a variant field; no hardcoded correlation number or gene/confidence literal remains)
    - `cd remix-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The "Key insights" card renders from live loader data; the strongest correlation value is interpolated (not hardcoded); any genetic insight is gated on DB variant rows; no PHI literal (r=−0.71, MTHFR action, K3, SelfDecode, gene names) survives in source.</done>
</task>

<task type="auto">
  <name>Task 2: De-PHI the "Why 150 days?" card in protocol/cessation.tsx (DB-source or non-PHI generic text)</name>
  <files>remix-app/app/routes/_app/protocol/cessation.tsx</files>
  <read_first>
    - remix-app/app/routes/_app/protocol/cessation.tsx (the "Why 150 days?" card at lines 407-420 with the hardcoded PHI paragraph at lines 414-419; the timeline card already renders `cessation?.notes` at lines 400-404; loaderData fields available — cessation, currentPhase, targetDay, CESSATION_PHASES)
    - .planning/phases/04-static-to-db-data-layer-migration/04-VERIFICATION.md (BLOCKER 1 `missing`: "derive from cessation_log.notes or a non-PHI protocol description field; remove the '76-day attempt' sentence or source it from the DB cessation record")
  </read_first>
  <action>
    Rewrite the paragraph at lines 414-419 so it contains no subject-specific health information. The current text encodes two PHI facts that MUST be removed or DB-sourced:
    - genetic interpretation: "Lower FAAH activity (K3 inferred from SelfDecode)"
    - cessation history: "The previous 76-day attempt was insufficient."

    Choose ONE of these approaches (both close the gap):

    (A) Generic non-PHI protocol rationale: replace the paragraph with text that explains the 150-day target WITHOUT any individual genetic call, third-party source, or personal-history claim. Acceptable generic framing: the protocol uses a 150-day window because FAAH-informed metabolic clearing extends beyond the typical 30–60 day range; a minimum of 120 days is used with 150 recommended for full normalization. Drop "K3 inferred from SelfDecode" and drop the entire "previous 76-day attempt" sentence. The generic protocol-duration rationale (120/150 days, the typical-window comparison) is non-PHI and may stay as a literal. Optionally interpolate `targetDay` (150) and `currentPhase.label` from loaderData instead of hardcoding the number.

    (B) DB-source the subject-specific narrative: if any per-subject explanation is desired, render it from `cessation.notes` (already available on loaderData and already rendered in the timeline card at lines 400-404). Do NOT duplicate the notes text as a literal — read it from the loader. Gate with `cessation?.notes`.

    Recommended: approach (A) for the static "Why 150 days?" explanatory card (it is general protocol education, not subject data), and rely on the existing `cessation?.notes` block (lines 400-404) for any subject-specific commentary. Do NOT remove the existing notes block.

    Remove these exact PHI fragments from the file: "K3 inferred from SelfDecode", "76-day attempt", "SelfDecode". Keep the card shell (`<Card tone="focus">`, the Info icon, the "Why 150 days?" heading) — only the paragraph content changes.

    Do NOT touch the loader (04-06 already modified this file's loader; this task changes only the component JSX paragraph). Do NOT reintroduce any cessation-day-source change — that is 04-06's concern.
  </action>
  <verify>
    <automated>cd remix-app && npx react-router typegen && npx tsc --noEmit && grep -cE "76-day|SelfDecode|K3 inferred" app/routes/_app/protocol/cessation.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && grep -c "76-day" app/routes/_app/protocol/cessation.tsx` returns 0
    - `cd remix-app && grep -c "SelfDecode" app/routes/_app/protocol/cessation.tsx` returns 0
    - `cd remix-app && grep -c "K3 inferred" app/routes/_app/protocol/cessation.tsx` returns 0
    - The existing `cessation?.notes` render block (lines ~400-404) is still present: `grep -c "cessation?.notes" app/routes/_app/protocol/cessation.tsx` returns >= 1
    - The "Why 150 days?" heading is still present (card shell retained): `grep -c "Why 150 days" app/routes/_app/protocol/cessation.tsx` returns 1
    - `cd remix-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The "Why 150 days?" card contains only non-PHI generic protocol rationale (and/or DB-sourced cessation.notes); the genetic interpretation and 76-day history literals are gone; card shell and the existing notes block remain.</done>
</task>

<task type="auto">
  <name>Task 3: Build and verify the client bundle is PHI-free</name>
  <files>remix-app/app/routes/_app/insights/index.tsx, remix-app/app/routes/_app/protocol/cessation.tsx</files>
  <read_first>
    - .planning/phases/04-static-to-db-data-layer-migration/04-VERIFICATION.md (Behavioral Spot-Checks table — the failing grep commands to re-run as the acceptance evidence)
    - CLAUDE.md (build commands run from remix-app/; `npm run build` = react-router build)
  </read_first>
  <action>
    Run the production build from remix-app/ (`npm run build`) so the route components recompile into client assets, then re-run the exact verification greps that failed in VERIFICATION.md against the fresh `build/client/` output.

    This task makes no source edits of its own — it is the build-and-prove gate for Tasks 1 and 2. If any grep still returns a match, return to the offending file (insights/index.tsx for r=−0.71/SelfDecode markers, cessation.tsx for 76-day/SelfDecode markers) and remove the remaining literal, then rebuild.

    The build/client/ asset filenames are content-hashed (e.g. cessation-<hash>.js, index-<hash>.js) and change between builds — grep recursively across the whole build/client/ tree, do not rely on the old hashed names from VERIFICATION.md.

    If a grep matches a NON-PHI occurrence (e.g. the digits "76" appearing inside an unrelated minified token, or "150" in the generic protocol rationale), justify it explicitly in the SUMMARY — only individually-identifiable health strings are forbidden. The target markers (`76-day`, `SelfDecode`, `r=−0.71`/`r=-0.71`) must not appear as PHI.
  </action>
  <verify>
    <automated>cd remix-app && npm run build && echo "=== 76-day ===" && (grep -rc "76-day" build/client/ || echo 0) && echo "=== r=0.71/SelfDecode/K3 ===" && (grep -rEc "r=.0\.71|SelfDecode" build/client/ || echo 0)</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run build` completes successfully (exit 0)
    - `cd remix-app && grep -r "76-day" build/client/` returns 0 matches (no output)
    - `cd remix-app && grep -rE "r=.0\.71|r=-0\.71" build/client/` returns 0 matches
    - `cd remix-app && grep -r "SelfDecode" build/client/` returns 0 matches
    - `cd remix-app && grep -r "MTHFR protocol action" build/client/` returns 0 matches
    - Any residual match (if any) is a justified non-PHI occurrence documented in the SUMMARY; no individually-identifiable health string remains
  </acceptance_criteria>
  <done>A fresh production build's client bundle contains none of the PHI markers that VERIFICATION.md flagged; SC-3 / DATA-04 client-bundle criterion is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route component → client bundle | JSX string literals in route component bodies compile into unauthenticated static client JS assets — the central PHI-disclosure vector for this gap |
| DB → loader → component | Correlation values and variant facts must flow from Neon through the loader; they must NOT be duplicated as literals in the component body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Information Disclosure | insights/index.tsx "Key insights" card JSX literals (r=−0.71, MTHFR action, FAAH/CYP1A2/K3/SelfDecode) | mitigate | Remove hardcoded literals; derive the correlation insight from topCorrelations loader data; gate the genetic narrative on highImpactVariants DB rows; verify absent from build/client/ |
| T-04-07-02 | Information Disclosure | cessation.tsx "Why 150 days?" card JSX literals (K3 inferred from SelfDecode, 76-day attempt) | mitigate | Replace with non-PHI generic protocol rationale and/or DB-sourced cessation.notes; verify absent from build/client/ |
| T-04-07-03 | Information Disclosure | regression — a future component re-hardcodes a health fact | accept | Out of scope for this gap; a build-time PHI-grep CI gate is a follow-up (noted, not built here). This plan re-runs the manual grep as evidence. |
| T-04-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no new dependencies. |
</threat_model>

<verification>
After all three tasks:
- `cd remix-app && grep -rE "r=.0\.71|SelfDecode|76-day|MTHFR protocol action" app/routes/_app/insights/index.tsx app/routes/_app/protocol/cessation.tsx` returns 0
- `cd remix-app && npx react-router typegen && npx tsc --noEmit` exits 0
- `cd remix-app && npm run build` succeeds; `grep -r "76-day" build/client/` and `grep -rE "r=.0\.71|SelfDecode" build/client/` return 0 matches
- The loader-parity test (already green from 04-06) is not regressed — insights/index stats assertions still pass
</verification>

<success_criteria>
- No subject-specific health fact is a hardcoded JSX literal in either route component
- The "Key insights" correlation insight is interpolated from live loader data
- The genetic narrative is gated on DB variant rows (not hardcoded gene/confidence/source literals)
- The "Why 150 days?" card carries only non-PHI generic rationale and/or DB-sourced notes
- A fresh client bundle contains no PHI markers — SC-3 / DATA-04 client-bundle criterion satisfied
</success_criteria>

<output>
Create `.planning/phases/04-static-to-db-data-layer-migration/04-07-SUMMARY.md` when done.
Stage only the two route files (and the SUMMARY) explicitly — do NOT use `git add -A` (parallel sessions may have unrelated changes in the tree). Do NOT stage the build/client/ artifacts.
</output>
