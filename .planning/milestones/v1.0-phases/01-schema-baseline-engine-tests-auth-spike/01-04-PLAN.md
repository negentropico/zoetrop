---
phase: 01-schema-baseline-engine-tests-auth-spike
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - remix-app/app/lib/metrics.ts
  - remix-app/app/lib/metrics.test.ts
  - remix-app/app/routes/home.tsx
  - remix-app/app/routes/metrics/index.tsx
  - remix-app/app/routes/metrics/category.tsx
  - remix-app/app/routes/metrics/detail.tsx
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "getMetricStatus is defined once in app/lib/metrics.ts and imported by home.tsx, metrics/index.tsx, metrics/category.tsx, AND metrics/detail.tsx (all FOUR inline copies removed — no inline `function getMetricStatus` remains in any route)"
    - "metrics.test.ts covers all 4 status outcomes (optimal/borderline/deficient/excess) plus the exact-boundary and no-reference-range fallback cases — green BEFORE and AFTER the extraction"
    - "The extracted util is behaviorally identical to the home.tsx inline version (contract-lock test passes unchanged across the swap); all four inline copies were confirmed identical before deletion"
    - "tsc --noEmit passes after the import swap"
  artifacts:
    - path: "remix-app/app/lib/metrics.ts"
      provides: "Canonical getMetricStatus(metric: Metric): MetricStatus extracted from the 4 inline copies (home.tsx form)"
      exports: ["getMetricStatus"]
      min_lines: 10
    - path: "remix-app/app/lib/metrics.test.ts"
      provides: "Status classification boundary tests (>=9 cases per RESEARCH §2a matrix)"
      contains: "getMetricStatus"
  key_links:
    - from: "remix-app/app/routes/home.tsx"
      to: "remix-app/app/lib/metrics.ts"
      via: "import { getMetricStatus } from \"~/lib/metrics\""
      pattern: "from \"~/lib/metrics\""
    - from: "remix-app/app/routes/metrics/index.tsx"
      to: "remix-app/app/lib/metrics.ts"
      via: "import { getMetricStatus } from \"~/lib/metrics\""
      pattern: "from \"~/lib/metrics\""
    - from: "remix-app/app/routes/metrics/category.tsx"
      to: "remix-app/app/lib/metrics.ts"
      via: "import { getMetricStatus } from \"~/lib/metrics\""
      pattern: "from \"~/lib/metrics\""
    - from: "remix-app/app/routes/metrics/detail.tsx"
      to: "remix-app/app/lib/metrics.ts"
      via: "import { getMetricStatus } from \"~/lib/metrics\""
      pattern: "from \"~/lib/metrics\""
---

<objective>
Refactor #1 of the two test-enabling refactors (D-06): extract the FOUR identical inline `getMetricStatus` copies (home.tsx:28, metrics/index.tsx:52, metrics/category.tsx:131, metrics/detail.tsx:309) into a single canonical `app/lib/metrics.ts`, and lock the status-classification contract with Vitest boundary tests (COMP-01). The home.tsx form is canonical (most compact; all four are behaviorally identical). D-06's single-source-of-truth goal requires ALL FOUR copies to be removed.

Critical sequencing (Pitfall 3 / VALIDATION contract-lock rule): write the tests against the CURRENT inline behavior FIRST and confirm green, THEN extract to `metrics.ts` and swap the four route imports, THEN confirm the SAME tests still pass. A green→red transition across the swap means a behavioral divergence was introduced — investigate before proceeding.

Purpose: COMP-01 status-classification coverage + single source of truth for metric status.
Output: `app/lib/metrics.ts`, `app/lib/metrics.test.ts`, and four routes importing the shared util.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-CONTEXT.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-RESEARCH.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-PATTERNS.md
@CLAUDE.md
@remix-app/app/routes/home.tsx
@remix-app/app/types/metrics.ts

<interfaces>
<!-- Canonical inline form to extract (home.tsx lines 28-37) — behaviorally identical to the other three copies. -->
function getMetricStatus(metric: Metric): MetricStatus:
- if optimalRange && value >= optimalRange.min && value <= optimalRange.max → "optimal"
- else if referenceRange: value < referenceRange.min → "deficient"; value > referenceRange.max → "excess"; else → "borderline"
- else → "optimal"   (defensive fallback when no referenceRange, even if value is outside optimalRange)

Types (from ~/types/metrics): Metric has value:number, optimalRange?:{min,max}, referenceRange?:{min,max}; MetricStatus = "optimal"|"borderline"|"deficient"|"excess".

verbatimModuleSyntax: true → type-only imports MUST use `import type`. Add the new value import as `import { getMetricStatus } from "~/lib/metrics"` (the `~/*` → `./app/*` alias is configured in tsconfig.json and resolved by vite-tsconfig-paths; the existing type imports in these routes use relative paths — leave those as-is, only the new metrics import uses the `~/` alias).

Inline copy locations to replace (FOUR — confirmed by grep):
- home.tsx:28-37
- metrics/index.tsx:52-72
- metrics/category.tsx:131-148
- metrics/detail.tsx:309-329   ← the fourth copy (missed by earlier research/context)

IMPORTANT — type imports that MUST be kept (do not delete; they are still referenced after the inline function is removed):
- home.tsx ALSO uses `MetricStatus` directly (statusCounts at lines ~72-76, StatusDot at ~104-105) — KEEP its `MetricStatus` type import.
- detail.tsx ALSO uses `MetricStatus` (StatusBadge at ~68-69) AND `Metric` (RangeIndicator at ~83) — KEEP BOTH its `MetricStatus` and `Metric` type imports; only remove the inline `function getMetricStatus`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contract-lock test against the current inline getMetricStatus, then extract to metrics.ts</name>
  <files>remix-app/app/lib/metrics.test.ts, remix-app/app/lib/metrics.ts</files>
  <read_first>
    - remix-app/app/routes/home.tsx (inline getMetricStatus at lines 28-37 — canonical form)
    - remix-app/app/types/metrics.ts (Metric, MetricStatus types)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-RESEARCH.md (§2a boundary test matrix — 11 cases; §Code Examples metrics.test.ts; Pitfall 3)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-PATTERNS.md (§metrics.ts target shape, §metrics.test.ts fixture factory, contract-lock rule)
  </read_first>
  <behavior>
    Status classification boundary matrix (referenceRange [50,150], optimalRange [70,120] unless noted) — assert ALL:
    - value 70 (at optimal min) → "optimal"
    - value 120 (at optimal max) → "optimal"
    - value 90 (mid optimal) → "optimal"
    - value 60 (in ref, below optimal) → "borderline"
    - value 135 (in ref, above optimal) → "borderline"
    - value 50 (exactly at ref min, below optimal) → "borderline"
    - value 150 (exactly at ref max) → "borderline"
    - value 30 (below ref min) → "deficient"
    - value 200 (above ref max) → "excess"
    - value 42, no referenceRange, no optimalRange → "optimal" (defensive fallback)
    - value 50, no referenceRange, optimalRange [70,120] (out of optimal) → "optimal" (no ref → falls through — locks the known quirk)
  </behavior>
  <action>Step 1 (RED/contract-lock): Create `app/lib/metrics.test.ts` importing `{ describe, it, expect }` from "vitest" and `type { Metric }` from "~/types/metrics" (import type — verbatimModuleSyntax). Add the `makeMetric(value, referenceRange?, optimalRange?)` fixture factory from PATTERNS.md (using `as unknown as Metric` — acceptable in test fixtures only). Write the 11 assertions from the behavior block. INITIALLY import `getMetricStatus` from a tiny inline copy at the top of the test file (paste the home.tsx:28-37 body) so the suite goes green against the CURRENT behavior — this is the contract lock. Run `npx vitest run metrics` and confirm green.
  Step 2 (extract): Create `app/lib/metrics.ts` with the canonical `export function getMetricStatus(metric: Metric): MetricStatus` (home.tsx:28-37 body verbatim) and a JSDoc block, importing `type { Metric, MetricStatus }` from "~/types/metrics". Then change the test file's import from the inline copy to `import { getMetricStatus } from "~/lib/metrics"` and DELETE the inline copy from the test. Re-run `npx vitest run metrics` — it MUST stay green (same contract). If it goes red, the extraction diverged — fix `metrics.ts` to match the inline behavior exactly before continuing.</action>
  <verify>
    <automated>cd remix-app && test -f app/lib/metrics.ts && grep -q "export function getMetricStatus" app/lib/metrics.ts && grep -q 'import type' app/lib/metrics.ts && grep -q 'from "~/lib/metrics"' app/lib/metrics.test.ts && npx vitest run metrics</automated>
  </verify>
  <done>`metrics.test.ts` covers all 11 boundary cases and is green; `metrics.ts` exports the canonical `getMetricStatus`; the test imports from `~/lib/metrics` (no inline copy left in the test).</done>
</task>

<task type="auto">
  <name>Task 2: Swap all FOUR route imports to the shared util and confirm tests + types stay green</name>
  <files>remix-app/app/routes/home.tsx, remix-app/app/routes/metrics/index.tsx, remix-app/app/routes/metrics/category.tsx, remix-app/app/routes/metrics/detail.tsx</files>
  <read_first>
    - remix-app/app/routes/home.tsx (inline getMetricStatus at 28-37; MetricStatus also used at ~72-76 and ~104-105 — keep that type import)
    - remix-app/app/routes/metrics/index.tsx (inline getMetricStatus at 52-72)
    - remix-app/app/routes/metrics/category.tsx (inline getMetricStatus at 131-148)
    - remix-app/app/routes/metrics/detail.tsx (inline getMetricStatus at 309-329; MetricStatus used by StatusBadge at ~68-69 and Metric used by RangeIndicator at ~83 — keep BOTH type imports)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-PATTERNS.md (§import swap in consuming routes)
  </read_first>
  <action>In EACH of the FOUR route files, DELETE the inline `function getMetricStatus(metric: Metric): MetricStatus { ... }` definition and add `import { getMetricStatus } from "~/lib/metrics";` at the top with the other imports. Leave every CALL site (`getMetricStatus(m)`, `getMetricStatus(metric)`) unchanged — the imported function has the same signature. Keep the existing type imports that are still referenced after the inline function is removed: for `home.tsx` KEEP its `MetricStatus` type import (used in `statusCounts` ~72-76 and `StatusDot` ~104-105); for `detail.tsx` KEEP BOTH its `MetricStatus` (used in `StatusBadge` ~68-69) AND `Metric` (used in `RangeIndicator` ~83) type imports — only remove the inline function, not the type imports. For `metrics/index.tsx` and `metrics/category.tsx`: keep the `Metric`/`MetricStatus` type imports if they are still referenced elsewhere in the file; remove a now-unused type import only if grep confirms it has no other use (do not introduce an unused-import tsc error). Do NOT change any other logic in these routes. Then run `npx vitest run metrics` (still green — behavior unchanged) and `npx react-router typegen && npx tsc --noEmit` (zero errors).</action>
  <verify>
    <automated>cd remix-app && for f in app/routes/home.tsx app/routes/metrics/index.tsx app/routes/metrics/category.tsx app/routes/metrics/detail.tsx; do grep -q 'from "~/lib/metrics"' "$f" || { echo "MISSING import in $f"; exit 1; }; grep -q "^function getMetricStatus\|^  function getMetricStatus" "$f" && { echo "INLINE copy still present in $f"; exit 1; }; done && npx vitest run metrics && npx react-router typegen && npx tsc --noEmit && echo "OK all four routes swapped, tests + types green"</automated>
  </verify>
  <done>All FOUR routes import `getMetricStatus` from `~/lib/metrics`; no inline `function getMetricStatus` remains in any route (home, metrics/index, metrics/category, metrics/detail); `npx vitest run metrics` is green; `tsc --noEmit` passes with zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a pure internal refactor + tests on already-trusted, in-bundle code; introduces no new trust boundary, network surface, or untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | behavioral divergence introduced during the getMetricStatus extraction | mitigate | Contract-lock sequencing (Task 1): tests written against the inline form go green BEFORE extraction and must stay green AFTER the import swap; any green→red flags divergence (Pitfall 3) |
</threat_model>

<verification>
- `npx vitest run metrics` is green before and after the extraction/swap.
- `npx tsc --noEmit` passes with zero errors after the swap.
- `grep` confirms all FOUR routes (home, metrics/index, metrics/category, metrics/detail) import from `~/lib/metrics` and no inline `getMetricStatus` remains.
</verification>

<success_criteria>
- COMP-01 status-classification coverage: all 4 outcomes + boundary + fallback cases tested and green.
- `getMetricStatus` has a single source of truth in `app/lib/metrics.ts`, consumed by all four routes.
- No behavioral change to the routes; types clean.
</success_criteria>

<output>
Create `.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-04-SUMMARY.md` when done.
</output>
