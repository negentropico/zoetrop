---
phase: 01-schema-baseline-engine-tests-auth-spike
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - remix-app/app/lib/protocol-data.ts
  - remix-app/app/lib/protocol-data.test.ts
  - remix-app/app/lib/seed-data.test.ts
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "getCessationDay accepts an injectable now: Date = new Date() so day-boundary tests are deterministic (D-06)"
    - "protocol-data.test.ts asserts getCessationDay at days 1/21/22/60/61/120/121/150/151 via injected now, and getCurrentCessationPhase at every phase boundary using .phase"
    - "seed-data.test.ts covers calculatePearsonCorrelation: perfect positive/negative, zero-denominator, empty, mismatched-length, single-element degenerate cases"
    - "Existing getCessationDay() call sites (e.g. home.tsx line 55, no args) still work via the default parameter"
    - "tsc --noEmit passes after the signature change"
  artifacts:
    - path: "remix-app/app/lib/protocol-data.ts"
      provides: "getCessationDay(now: Date = new Date()): number with injectable now (D-06 refactor #2)"
      contains: "now: Date = new Date()"
    - path: "remix-app/app/lib/protocol-data.test.ts"
      provides: "Cessation day + phase boundary tests (>=17 cases) using injected now and .phase"
      contains: "getCurrentCessationPhase"
    - path: "remix-app/app/lib/seed-data.test.ts"
      provides: "Pearson correlation tests (>=7 cases incl. degenerate/zero-denominator)"
      contains: "calculatePearsonCorrelation"
  key_links:
    - from: "remix-app/app/lib/protocol-data.test.ts"
      to: "remix-app/app/lib/protocol-data.ts"
      via: "import { getCessationDay, getCurrentCessationPhase } from \"~/lib/protocol-data\""
      pattern: "from \"~/lib/protocol-data\""
    - from: "remix-app/app/lib/seed-data.test.ts"
      to: "remix-app/app/lib/seed-data.ts"
      via: "import { calculatePearsonCorrelation } from \"~/lib/seed-data\""
      pattern: "calculatePearsonCorrelation"
---

<objective>
Refactor #2 of the two test-enabling refactors (D-06) plus the remaining two engine test suites (COMP-01): inject `now: Date = new Date()` into `getCessationDay` in `protocol-data.ts` so cessation day-boundary math is deterministic, then cover (a) cessation phase math at every day boundary and (b) the already-pure `calculatePearsonCorrelation` (no refactor needed — direct import).

Key correction (PATTERNS.md, resolving RESEARCH Open Question 1 / Assumption A3): `CESSATION_PHASES` entries expose phase identity via `.phase` (type `CessationPhase`), NOT `.name`. Every cessation phase assertion MUST use `.phase`. Boundaries come from the schema enum + CESSATION_PHASES: acute 1–21, stabilization 22–60, clearing 61–120, optimization 121–150; cessation start `2025-12-23T00:00:00.000Z`.

Critical (Pitfall 5): tests MUST pass an explicit `now` to `getCessationDay` — never call it with no args in a test, or the assertion becomes date-coupled and breaks on a future run.

Purpose: COMP-01 cessation + Pearson coverage; deterministic cessation math.
Output: modified `protocol-data.ts` (one-parameter signature change), `protocol-data.test.ts`, `seed-data.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-CONTEXT.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-RESEARCH.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-PATTERNS.md
@CLAUDE.md
@remix-app/app/lib/protocol-data.ts
@remix-app/app/types/protocol.ts

<interfaces>
<!-- Current source (protocol-data.ts) — confirmed by reading the file. -->
CESSATION_START_DATE = "2025-12-23T00:00:00.000Z"
getCessationDay(): number  → differenceInDays(new Date(), parseISO(CESSATION_START_DATE))   [CHANGE: add now param]
getCurrentCessationPhase(day: number): typeof CESSATION_PHASES[0]  → already pure, NO change
  returns CESSATION_PHASES.find(p => day >= p.dayRange.start && day <= p.dayRange.end) || last phase

CESSATION_PHASES (types/protocol.ts:89-118) — each entry: { phase: CessationPhase, label, dayRange:{start,end}, focus, description }
  phase identity field is `.phase` (NOT `.name`). CessationPhase = 'acute'|'stabilization'|'clearing'|'optimization'.
  acute {1,21}, stabilization {22,60}, clearing {61,120}, optimization {121,150}.

calculatePearsonCorrelation(x: number[], y: number[]): number (seed-data.ts:605) — already pure:
  returns 0 if x.length !== y.length || x.length === 0; returns 0 if denominator === 0; else Pearson r.

date-fns (^4.1.0, already a dependency): parseISO, addDays, differenceInDays.
home.tsx line 55 calls getCessationDay() with no args — the default param must preserve this.
verbatimModuleSyntax: true → use `import type` for type-only imports; use the `~/` alias in test files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject now into getCessationDay and write cessation boundary tests (using .phase)</name>
  <files>remix-app/app/lib/protocol-data.ts, remix-app/app/lib/protocol-data.test.ts</files>
  <read_first>
    - remix-app/app/lib/protocol-data.ts (getCessationDay at 29-31, getCurrentCessationPhase at 36-41)
    - remix-app/app/types/protocol.ts (CESSATION_PHASES at 89-118 — `.phase` field)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-RESEARCH.md (§2b cessation matrix, Pitfall 5, Assumption A1)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-PATTERNS.md (§protocol-data.ts modified signature; §protocol-data.test.ts — `.phase` not `.name`, date-injection pattern)
  </read_first>
  <action>Step 1 (refactor): In `protocol-data.ts`, change the `getCessationDay` signature from `getCessationDay(): number` to `getCessationDay(now: Date = new Date()): number` and change its body to `return differenceInDays(now, parseISO(CESSATION_START_DATE));` (use the injected `now` instead of the inline `new Date()`). Keep the existing JSDoc block. Do NOT touch `getCurrentCessationPhase` — it is already pure (takes `day` as a parameter). Do NOT change `CESSATION_START_DATE`. No other edits to this 741-line file.
  Step 2 (tests): Create `app/lib/protocol-data.test.ts` importing `{ describe, it, expect }` from "vitest", `{ parseISO, addDays }` from "date-fns", and `{ getCessationDay, getCurrentCessationPhase }` from "~/lib/protocol-data". Define `const START = parseISO("2025-12-23T00:00:00.000Z")` and a `day = (n) => addDays(START, n)` helper. Assert `getCessationDay(day(n))` equals `n` for n in {1, 21, 22, 60, 61, 120, 121, 150, 151}. Assert `getCurrentCessationPhase(d).phase` (USE `.phase`, NOT `.name`) equals: 1→"acute", 21→"acute", 22→"stabilization", 60→"stabilization", 61→"clearing", 120→"clearing", 121→"optimization", 150→"optimization", 151→"optimization" (post-endpoint fallback to last phase). NEVER call `getCessationDay()` with no args in a test (Pitfall 5). Run `npx vitest run protocol-data`.</action>
  <verify>
    <automated>cd remix-app && grep -q "now: Date = new Date()" app/lib/protocol-data.ts && grep -q "differenceInDays(now" app/lib/protocol-data.ts && grep -q "\.phase" app/lib/protocol-data.test.ts && ! grep -q "\.name).toBe" app/lib/protocol-data.test.ts && npx vitest run protocol-data</automated>
  </verify>
  <done>`getCessationDay` takes an injectable `now`; `protocol-data.test.ts` asserts day boundaries via injected `now` and phase boundaries via `.phase`; `npx vitest run protocol-data` is green.</done>
</task>

<task type="auto">
  <name>Task 2: Write Pearson correlation tests and confirm full suite + types green</name>
  <files>remix-app/app/lib/seed-data.test.ts, remix-app/app/lib/protocol-data.ts</files>
  <!-- protocol-data.ts is listed because this task's `tsc --noEmit` verify covers the Task 1 getCessationDay signature change across all call sites; Task 2 does not edit protocol-data.ts, only validates it. -->
  <read_first>
    - remix-app/app/lib/seed-data.ts (calculatePearsonCorrelation at line 605 — already pure)
    - remix-app/app/lib/protocol-data.ts (Task 1's modified getCessationDay signature — verified, not edited, by this task's tsc run)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-RESEARCH.md (§2c Pearson test matrix, §Code Examples seed-data.test.ts)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-PATTERNS.md (§seed-data.test.ts — floating-point assertion pattern)
  </read_first>
  <action>Create `app/lib/seed-data.test.ts` importing `{ describe, it, expect }` from "vitest" and `{ calculatePearsonCorrelation }` from "~/lib/seed-data" (no refactor of seed-data.ts — the function is already pure). Assert: perfect positive `[1,2,3,4,5] vs [1,2,3,4,5]` → `toBeCloseTo(1.0, 10)`; perfect negative `[1,2,3,4,5] vs [5,4,3,2,1]` → `toBeCloseTo(-1.0, 10)`; constant y `[1,2,3,4,5] vs [3,3,3,3,3]` → `toBe(0)` (zero denominator); empty arrays `[] vs []` → `toBe(0)`; mismatched lengths `[1,2] vs [1,2,3]` → `toBe(0)`; single element `[5] vs [5]` → `toBe(0)` (zero variance); two-element `[1,3] vs [2,4]` → `toBeCloseTo(1.0, 10)`. Use exact `toBe(0)` for the guard/degenerate cases and `toBeCloseTo(_, 10)` for the precise-correlation cases. Then run the FULL suite `npx vitest run` (all three suites green) and `npx react-router typegen && npx tsc --noEmit` (zero errors — this also confirms the Task 1 `getCessationDay` signature change does not break any call site, e.g. home.tsx:55).</action>
  <verify>
    <automated>cd remix-app && test -f app/lib/seed-data.test.ts && grep -q "calculatePearsonCorrelation" app/lib/seed-data.test.ts && npx vitest run && npx react-router typegen && npx tsc --noEmit && echo "OK full suite + types green"</automated>
  </verify>
  <done>`seed-data.test.ts` covers the 7 Pearson cases incl. degenerate/zero-denominator; `npx vitest run` (full suite) is green; `tsc --noEmit` passes with zero errors (confirming the Task 1 signature change is call-site-safe).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure-function signature change + unit tests on already-trusted in-bundle code; no new trust boundary, network surface, or untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Tampering | getCessationDay default-parameter change silently altering existing call-site behavior | mitigate | The `now: Date = new Date()` default preserves all no-arg call sites (e.g. home.tsx:55); Task 2 runs `tsc --noEmit` to confirm no call site breaks |
| T-01-12 | Tampering | date-coupled test (calling getCessationDay() with no args) producing a flaky/self-invalidating gate | mitigate | Pitfall 5: every test passes an explicit injected `now`; Task 1 verify greps that assertions use `.phase` and the suite is run deterministically |
</threat_model>

<verification>
- `npx vitest run` (full suite — metrics from Plan 04 + protocol-data + seed-data) is green.
- `npx tsc --noEmit` passes with zero errors after the `getCessationDay` signature change.
- `getCessationDay` has the injectable `now` default param; cessation tests use `.phase` and injected dates only.
</verification>

<success_criteria>
- COMP-01 cessation + Pearson coverage: every day boundary and every degenerate Pearson case tested and green.
- Cessation math is deterministic via injectable `now`; existing no-arg call sites unaffected.
- Types clean.
</success_criteria>

<output>
Create `.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-05-SUMMARY.md` when done.
</output>
