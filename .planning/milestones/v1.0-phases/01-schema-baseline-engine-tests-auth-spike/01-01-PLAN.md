---
phase: 01-schema-baseline-engine-tests-auth-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - remix-app/vite.config.ts
  - remix-app/package.json
autonomous: false
requirements: [COMP-01]

must_haves:
  truths:
    - "Running `npx vitest run` from remix-app/ exits 0 on an empty test suite (harness proven before any test exists)"
    - "vitest and @vitest/coverage-v8 are present in remix-app devDependencies at ^4.x"
    - "better-auth ^1.6 is present in remix-app devDependencies (spike-only; Phase 3 promotes to dependencies)"
    - "A `test` script (`vitest run`) and `test:watch` script (`vitest`) exist in package.json"
    - "The `~/` path alias resolves inside test files (tsconfigPaths plugin is inherited by Vitest)"
  artifacts:
    - path: "remix-app/vite.config.ts"
      provides: "Vitest test block (node env, app/**/*.test.{ts,tsx} include) without changing the defineConfig import source"
      contains: "test:"
    - path: "remix-app/package.json"
      provides: "test + test:watch scripts and vitest/coverage-v8/better-auth devDependencies"
      contains: "\"test\": \"vitest run\""
  key_links:
    - from: "remix-app/vite.config.ts"
      to: "vitest test runner"
      via: "test block read by vitest from vite.config.ts"
      pattern: "test:\\s*\\{"
    - from: "remix-app/vite.config.ts"
      to: "~/ alias resolution in tests"
      via: "tsconfigPaths() plugin already in plugins array"
      pattern: "tsconfigPaths\\(\\)"
---

<objective>
Install and configure the Vitest harness for the engine test suites that Plans 04 and 05 will write. This plan delivers ONLY the harness — no engine tests yet — proven by an empty `vitest run` that exits 0. It also vets the new packages (`vitest`, `@vitest/coverage-v8`, `better-auth`) through the package-legitimacy gate before install, since RESEARCH.md tagged all three `[ASSUMED]` (slopcheck was unavailable at research time).

Purpose: Wave 0 gap closure — the codebase has zero tests and no test runner. Plans 04 (status classification) and 05 (cessation + Pearson) both depend on this harness. Keeping it as a small standalone Wave 1 plan lets both downstream test plans run in parallel in Wave 2.

Output: Modified `vite.config.ts` (adds `test:` block), modified `package.json` (adds scripts + devDependencies), `node_modules` populated, and a verified empty `vitest run` exit-0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-CONTEXT.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-RESEARCH.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-PATTERNS.md
@.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Current vite.config.ts (full file) — modify, do NOT replace the defineConfig import. -->
From remix-app/vite.config.ts:
- imports: reactRouter (@react-router/dev/vite), tailwindcss (@tailwindcss/vite), defineConfig (from "vite"), tsconfigPaths (vite-tsconfig-paths)
- export: defineConfig({ plugins: [tailwindcss(), reactRouter(), tsconfigPaths()] })

Current package.json scripts (do NOT remove existing): build, dev, start, typecheck (`react-router typegen && tsc`), db:generate, db:migrate, db:studio.
Existing devDependency: drizzle-kit ^0.31.8. date-fns ^4.1.0 is already a dependency.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for vitest, @vitest/coverage-v8, better-auth</name>
  <read_first>
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-RESEARCH.md (§Package Legitimacy Audit — all three tagged [ASSUMED]; §Standard Stack — pinned versions)
  </read_first>
  <what-built>
    Nothing installed yet. RESEARCH.md §Package Legitimacy Audit tagged all three packages `[ASSUMED]` because slopcheck was unavailable at research time. Per the package legitimacy gate, an `[ASSUMED]` package cannot be installed without a human verification of registry legitimacy. This checkpoint is NOT auto-approvable (workflow.auto_advance is ignored for legitimacy gates).
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vitest — confirm: official vitest-dev org, weekly downloads in the millions, source repo github.com/vitest-dev/vitest. Target version `4.1.8` (install range `^4.1.8`).
    2. Open https://www.npmjs.com/package/@vitest/coverage-v8 — confirm same vitest-dev org, version `4.1.8`.
    3. Open https://www.npmjs.com/package/better-auth — confirm org `better-auth`, source github.com/better-auth/better-auth, version `1.6.14` (install range `^1.6.14`), weekly downloads >100K.
    4. Confirm none are typosquats (exact names: `vitest`, `@vitest/coverage-v8`, `better-auth`).
  </how-to-verify>
  <verify>
    <human-check>All three packages confirmed legitimate on npmjs.com with matching org/repo/version, no typosquat.</human-check>
  </verify>
  <resume-signal>Type "approved" to proceed with install, or name any package that fails verification.</resume-signal>
  <done>Human has confirmed all three packages are legitimate; install may proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Install Vitest deps and add test scripts to package.json</name>
  <files>remix-app/package.json</files>
  <read_first>
    - remix-app/package.json (current scripts + devDependencies block)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-PATTERNS.md (§package.json — exact target scripts + devDependencies)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-RESEARCH.md (§Standard Stack — install command)
  </read_first>
  <action>From `remix-app/`, run `npm install -D vitest@^4.1.8 @vitest/coverage-v8@^4.1.8 better-auth@^1.6.14`. All three go in `devDependencies` (better-auth is spike-only for Phase 1; Phase 3 will move it to `dependencies` — do not add it to `dependencies` now). Then add exactly two scripts to the `scripts` block in `package.json`: `"test": "vitest run"` and `"test:watch": "vitest"`. Place them after the existing `"typecheck"` script and before `"db:generate"`. Do NOT modify, reorder, or remove any existing script (build, dev, start, typecheck, db:generate, db:migrate, db:studio). Do NOT change the existing drizzle-kit ^0.31.8 devDependency.</action>
  <verify>
    <automated>cd remix-app && node -e "const p=require('./package.json'); const d=p.devDependencies; if(!d.vitest||!d['@vitest/coverage-v8']||!d['better-auth']) throw new Error('missing devDep'); if(p.scripts.test!=='vitest run'||p.scripts['test:watch']!=='vitest') throw new Error('missing script'); if(p.dependencies&&p.dependencies['better-auth']) throw new Error('better-auth must be devDependency only'); console.log('OK')"</automated>
  </verify>
  <done>`vitest`, `@vitest/coverage-v8`, `better-auth` are in devDependencies; `test` and `test:watch` scripts exist; no existing script changed; better-auth is NOT in dependencies.</done>
</task>

<task type="auto">
  <name>Task 3: Add Vitest test block to vite.config.ts and prove empty run exits 0</name>
  <files>remix-app/vite.config.ts</files>
  <read_first>
    - remix-app/vite.config.ts (full current file)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-PATTERNS.md (§vite.config.ts modified — exact target shape + the "keep defineConfig from vite" constraint)
    - .planning/phases/01-schema-baseline-engine-tests-auth-spike/01-RESEARCH.md (§Stream 1 Vitest Setup, Pitfall 2, Open Question 2 fallback)
  </read_first>
  <action>Add a `/// <reference types="vitest/config" />` triple-slash directive as the FIRST line of `vite.config.ts` (above all imports). Keep `import { defineConfig } from "vite"` exactly as-is — do NOT change it to `vitest/config` (changing it breaks the `reactRouter()` plugin per RESEARCH.md Pitfall 2). Add a `test` key to the `defineConfig({...})` object with `environment: "node"` and `include: ["app/**/*.test.ts", "app/**/*.test.tsx"]`. Leave the `plugins` array untouched (tailwindcss, reactRouter, tsconfigPaths must remain — tsconfigPaths is what makes the `~/` alias resolve in test files). Then run `npx vitest run` from `remix-app/` to prove the harness boots and exits 0 on an empty suite (no `*.test.ts` files exist yet, so Vitest reports "no test files found" and exits 0). FALLBACK (RESEARCH.md Open Question 2): if `npx vitest run` errors due to the `reactRouter()` plugin crashing in the node context, add `exclude: ["app/routes/**", "app/root.tsx"]` to the `test` block as the first remedy before considering a separate `vitest.config.ts`; if that also fails, create `remix-app/vitest.config.ts` importing only `tsconfigPaths()` and document the deviation in the SUMMARY.</action>
  <verify>
    <automated>cd remix-app && head -1 vite.config.ts | grep -q 'reference types="vitest/config"' && grep -q 'from "vite"' vite.config.ts && grep -q 'environment: "node"' vite.config.ts && npx vitest run 2>&1 | grep -Eq 'No test files found|0 passed|no tests' ; echo "exit:$?"</automated>
  </verify>
  <done>`vite.config.ts` has the triple-slash reference, keeps `defineConfig` from "vite", has a `test` block with node env and the `app/**/*.test.{ts,tsx}` include; `npx vitest run` exits 0 on the empty suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local devDependencies | Untrusted package code is pulled into the dev/build toolchain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (vitest, @vitest/coverage-v8, better-auth) | mitigate | Task 1 blocking-human legitimacy gate — verify org/repo/version on npmjs.com before install; all three were `[ASSUMED]` in RESEARCH.md |
| T-01-01 | Elevation of Privilege | better-auth pulled into runtime bundle | mitigate | Install as devDependency only; Task 2 verify asserts better-auth is absent from `dependencies`; spike code (Plan 03) lives outside `app/` so it is never bundled |
</threat_model>

<verification>
- `npx vitest run` from `remix-app/` exits 0 (empty suite — no test files yet).
- `package.json` devDependencies include vitest, @vitest/coverage-v8, better-auth; scripts include test + test:watch.
- `vite.config.ts` keeps `defineConfig` from "vite", adds the triple-slash reference and a node-env `test` block.
</verification>

<success_criteria>
- The Vitest harness is installed and boots; an empty `vitest run` exits 0.
- Downstream Plans 04 and 05 can write `app/lib/*.test.ts` files that are auto-discovered and can import via the `~/` alias.
- No existing package.json script or plugin was altered.
</success_criteria>

<output>
Create `.planning/phases/01-schema-baseline-engine-tests-auth-spike/01-01-SUMMARY.md` when done.
</output>
