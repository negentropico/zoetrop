---
phase: 04.1-design-system-adoption
plan: 07
type: execute
wave: 4
depends_on: ["04.1-03", "04.1-04"]
files_modified:
  - remix-app/app/routes/insights/layout.tsx
  - remix-app/app/routes/insights/index.tsx
  - remix-app/app/routes/insights/correlations.tsx
  - remix-app/app/routes/insights/genetics.tsx
  - remix-app/app/routes/import/layout.tsx
  - remix-app/app/routes/import/index.tsx
  - remix-app/app/routes/import/whoop.tsx
  - remix-app/app/routes/import/vault.tsx
autonomous: true

requirements: [UI-01]
user_setup: []

must_haves:
  truths:
    - "The insights section renders in-brand: underline tab strip, diverging CorrBar, DataTable (card-per-row on mobile), Badge significance, Ink-active filter pills"
    - "The import section renders in-brand: underline tab strip, Dropzone (periwinkle drag-active), brand success/CTA states with 'Save to tracker' / 'Parse WHOOP data' copy"
    - "Every loader, route, and data value is preserved — only markup/classes/components change"
    - "The correlations DataTable reflows to card-per-row at <=760px (no horizontal scroll on the densest screen)"
    - "Decisions covered: D-10 — all insights + import section routes retrofit in-brand (part of all-16-routes, no half-branded app)"
  artifacts:
    - path: "remix-app/app/routes/insights/correlations.tsx"
      provides: "In-brand correlations (archetype 07) with diverging CorrBar + DataTable"
      contains: "DataTable"
    - path: "remix-app/app/routes/import/whoop.tsx"
      provides: "In-brand WHOOP import (archetype 08) with Dropzone + brand CTAs"
      contains: "Dropzone"
  key_links:
    - from: "remix-app/app/routes/import/whoop.tsx"
      to: "remix-app/app/components/ui/Dropzone.tsx"
      via: "file upload area rendered as client-only Dropzone"
      pattern: "Dropzone"
    - from: "remix-app/app/routes/insights/correlations.tsx"
      to: "remix-app/app/components/ui/DataTable.tsx"
      via: "dense correlations table → DataTable desktop + card-per-row mobile"
      pattern: "DataTable"
---

<objective>
Retrofit the insights and import sections (8 routes: insights/layout, index, correlations, genetics; import/layout, index, whoop, vault) to the Zoetrop brand using the ported components. Underline tab strips for both section layouts; diverging CorrBar + DataTable (card-per-row mobile) for correlations; Dropzone + brand CTA/success states for import. Apply the canonical copy ("Save to tracker", "Parse WHOOP data", sentence case). Preserve every loader, route, and data value.

Purpose: Delivers ROADMAP SC3 for archetype 07 (Insights/correlations) and 08 (WHOOP import), plus the other 6 non-archetype routes (D-10: all 16 routes in-brand). Import/whoop seeds the Dropzone pattern Phase 5 builds on (client-only here).
Output: 8 retrofit route files; typecheck + build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md

<interfaces>
Components available (Plans 03/04): Card, Button, Badge, StatusBadge, StatusDot, DataTable, Dropzone, PageHeader, Crumb, Stat, SegmentedControl, Sparkline.

Retrofit map (UI-SPEC § Per-Screen Retrofit Map rows 07-08 + D-10):
- insights/layout.tsx + import/layout.tsx: section sub-nav → underline tab strip (PATTERNS § Underline Tab Strip — NavLink, Ink-active 2px border-bottom weight 600; preserve route table + useLocation). Insights/Import pages use the WIDE shell (max-width 1280 — AppShell wide prop; if AppShell width is controlled at root, ensure these sections still center at 1280 per UI-SPEC § Responsive content max-widths).
- insights/correlations.tsx (CorrelationsScreen): correlation bar → CorrBar (diverging: vital positive / danger negative, center line via absolute positioning); significance → <Badge tone="vital|focus|energy">; dense table → <DataTable> desktop + card-per-row at ≤760px (UI-SPEC § Dense table reflow); filter pills → Ink-active pills (PATTERNS § Ink-Active Filter Pills); selects → brand-styled <select> (pill border, Ink chevron). Eyebrow "TOP CORRELATIONS".
- insights/index.tsx: Card overview pattern, eyebrow labels, sentence case.
- insights/genetics.tsx: Badge + DataTable (GENETIC INSIGHTS eyebrow); confirmed-genetics badge tone vital.
- import/whoop.tsx (ImportScreen): file input zone → <Dropzone accept=".json" onFile={setFile} /> (client-only); file-loaded card → Card with file icon + name + remove; "Parse WHOOP data" → Button(primary) when ready; "Parsing…" → Button disabled; success → Card(accent:"vital") + check + "[N] metrics read from your frame"; "Save to tracker" → Button(primary); "Discard import" → Button(ghost). Tabs → underline strip.
- import/index.tsx: Card overview pattern.
- import/vault.tsx: follows Dropzone pattern (client-only), Card surfaces.

Copy (UI-SPEC § Copywriting): "Import Data"→"Import data"; "Save to Database"→"Save to tracker"; "Parse WHOOP Data"→"Parse WHOOP data"; "Discard import"; "[N] metrics read from your frame"; empty/error states per the contract; "Open correlations" CTA. Sentence case headings; section labels as mono eyebrows.
Universal class swaps: PATTERNS § Route retrofit pattern (gray→brand, gradient→component, h2/h3→.zt-eyebrow, blue→accent, no emoji).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit insights layout + correlations (insights/layout.tsx, insights/correlations.tsx)</name>
  <read_first>
    - remix-app/app/routes/insights/layout.tsx, correlations.tsx (current — preserve loaders, routes, data)
    - docs/design-system/_rounds/round1/app/screen-correlations.jsx (CorrelationsScreen, CorrBar, InsightsTabs, sigPill)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ insights/correlations.tsx analog, § Underline Tab Strip, § Ink-Active Filter Pills)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Per-Screen Retrofit Map row 07, § Dense table reflow, § Responsive content max-widths)
  </read_first>
  <files>remix-app/app/routes/insights/layout.tsx, remix-app/app/routes/insights/correlations.tsx</files>
  <action>
    Retrofit `insights/layout.tsx`: sub-nav → underline tab strip (NavLink Overview→/insights end, Correlations→/insights/correlations, Genetics→/insights/genetics; Ink-active 2px border-bottom); ensure the section uses the wide (1280) content width. Retrofit `insights/correlations.tsx`: correlation bar → CorrBar (diverging vital positive / danger negative with absolute center line); significance → <Badge tone={...}>; dense correlations table → <DataTable> on desktop, card-per-row at ≤760px (each row a Card with supp+metric, CorrBar, significance Badge, lag/p/n in mono); filter pills → Ink-active pills; selects → brand-styled <select> (pill border + Ink chevron); eyebrow "TOP CORRELATIONS"; sentence case. Preserve loaders + correlation data/r-values.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && grep -q "DataTable" app/routes/insights/correlations.tsx && grep -q "NavLink" app/routes/insights/layout.tsx && test -z "$(grep -nE 'text-gray-|bg-gray-|bg-gradient' app/routes/insights/layout.tsx app/routes/insights/correlations.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0.
    - correlations.tsx renders DataTable + Badge + a diverging CorrBar and includes a card-per-row layout for ≤760px; layout.tsx renders an underline-tab NavLink strip.
    - Both files contain no `text-gray-`/`bg-gray-`/`bg-gradient` classes.
    - Loaders and correlation data/r-values unchanged.
  </acceptance_criteria>
  <done>Insights correlations in-brand (diverging CorrBar, DataTable card-per-row mobile, Ink filter pills); data intact.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit insights overview + genetics (insights/index.tsx, insights/genetics.tsx)</name>
  <read_first>
    - remix-app/app/routes/insights/index.tsx, genetics.tsx (current — preserve loaders, routes, data)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ Route retrofit pattern, § Eyebrow Section Labels — insights/index + genetics follow Card overview + Badge + DataTable)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Copywriting — GENETIC INSIGHTS eyebrow, confirmed-genetics badge vital)
  </read_first>
  <files>remix-app/app/routes/insights/index.tsx, remix-app/app/routes/insights/genetics.tsx</files>
  <action>
    Retrofit `insights/index.tsx`: Card overview pattern (links to correlations/genetics with "Open correlations" accent CTA), eyebrow labels, sentence case, gray→brand swaps. Retrofit `insights/genetics.tsx`: genetic variant list → <DataTable> + <Badge> (confirmed variants tone vital), eyebrow "GENETIC INSIGHTS", Card surfaces. Preserve loaders + data.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && test -z "$(grep -nE 'text-gray-|bg-gray-|bg-gradient|🧬|🩸' app/routes/insights/index.tsx app/routes/insights/genetics.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0.
    - genetics.tsx renders DataTable + Badge; index.tsx renders Card overview with an accent CTA link.
    - Both files contain no `text-gray-`/`bg-gray-`/`bg-gradient` classes and no emoji.
    - Loaders and data values unchanged.
  </acceptance_criteria>
  <done>Insights overview + genetics in-brand via Card/DataTable/Badge; data intact.</done>
</task>

<task type="auto">
  <name>Task 3: Retrofit import section (import/layout.tsx, import/index.tsx, import/whoop.tsx, import/vault.tsx)</name>
  <read_first>
    - remix-app/app/routes/import/layout.tsx, index.tsx, whoop.tsx, vault.tsx (current — preserve loaders, routes, data, and any client state logic)
    - docs/design-system/_rounds/round1/app/screen-import.jsx (ImportScreen, ImportTabs, Dropzone)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ import/whoop.tsx analog, § Underline Tab Strip)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Per-Screen Retrofit Map row 08, § Copywriting Contract)
    - .planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md (§ Security Domain — Dropzone client-only in 04.1)
  </read_first>
  <files>remix-app/app/routes/import/layout.tsx, remix-app/app/routes/import/index.tsx, remix-app/app/routes/import/whoop.tsx, remix-app/app/routes/import/vault.tsx</files>
  <action>
    Retrofit `import/layout.tsx`: sub-nav → underline tab strip (NavLink Overview→/import end, WHOOP→/import/whoop, Vault→/import/vault; Ink-active); wide (1280) content width. Retrofit `import/index.tsx`: Card overview pattern, "Import data" (sentence case), eyebrow labels. Retrofit `import/whoop.tsx`: file input zone → <Dropzone accept=".json" onFile={setFile} /> (client-only — do not add server upload); file-loaded card → Card + file icon + name + remove (Button ghost); "Parse WHOOP data" → Button(variant="primary") when file ready; "Parsing…" → Button disabled; success → Card(accent="vital") + check + "[N] metrics read from your frame"; "Save to tracker" → Button(primary); "Discard import" → Button(ghost). Preserve the existing client parse/state logic and any loader; only restyle and swap copy ("Save to Database"→"Save to tracker", "Parse WHOOP Data"→"Parse WHOOP data"). Retrofit `import/vault.tsx`: <Dropzone> (client-only) + Card surfaces, brand copy. Keep Dropzone usage UI-only — no new fetch/upload introduced in any of these files (Phase 5 scope).
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && npm run build && grep -q "Dropzone" app/routes/import/whoop.tsx && grep -q "Save to tracker" app/routes/import/whoop.tsx && test -z "$(grep -nE 'text-gray-|bg-gray-|bg-gradient' app/routes/import/layout.tsx app/routes/import/index.tsx app/routes/import/whoop.tsx app/routes/import/vault.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0 and `npm run build` exits 0.
    - whoop.tsx renders Dropzone, a primary "Parse WHOOP data" Button, and contains the copy "Save to tracker"; vault.tsx renders Dropzone; both layouts render the underline tab strip.
    - The four files contain no `text-gray-`/`bg-gray-`/`bg-gradient` classes; no NEW `fetch(`/upload introduced for the Dropzone (client-only — T-04.1-12).
    - Loaders and existing client state logic preserved.
  </acceptance_criteria>
  <done>Import section in-brand (Dropzone client-only, brand CTAs/success/copy); whole import section branded (D-10); data/logic intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client file picker → Dropzone (whoop/vault) | A user-selected File enters the page but is handed to client parse logic only — no server upload/processing in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-12 | Tampering / premature file handling | import/whoop.tsx + import/vault.tsx Dropzone usage | mitigate | Dropzone remains UI-only in 04.1; the retrofit introduces no new server upload, fetch, or storage path — server-side ingest is Phase 5 (LAB-01..03) with its own threat model. Existing client parse logic is preserved as-is |
| T-04.1-13 | Tampering | route loader integrity during retrofit | mitigate | Retrofit changes markup/classes/components/copy only; loaders + data values preserved (typecheck/build) |
| T-04.1-14 | n/a | static-data rendering | accept | Same M0 static loader data; no new untrusted input or external call |
</threat_model>

<verification>
- `cd remix-app && npm run typecheck` exits 0.
- `cd remix-app && npm run build` exits 0.
- `cd remix-app && bash scripts/ds-audit.sh` exits 0.
- Manual (phase gate): correlations + WHOOP import match their round1 screens (diverging CorrBar, Dropzone periwinkle drag-active, brand CTAs); all 8 insights/import routes in-brand (D-10); correlations table reflows to cards at 375px.
</verification>

<success_criteria>
- SC3 (archetypes 07-08): insights/correlations + WHOOP import render in-brand.
- D-10: all 8 insights + import routes branded (no half-branded section).
- Dropzone stays client-only (no server processing this phase).
- Loaders, routes, and data values preserved across all 8 files.
</success_criteria>

<output>
Create `.planning/phases/04.1-design-system-adoption/04.1-07-SUMMARY.md` when done.
</output>
