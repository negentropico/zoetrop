---
phase: 04.1-design-system-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - remix-app/package.json
  - remix-app/vite.config.ts
autonomous: false
requirements: [UI-01]
user_setup: []

must_haves:
  truths:
    - "Component tests can run in a jsdom DOM environment via vitest"
    - "lucide-react is installed and importable in the app"
    - "A repeatable shell audit proves no .jsx files ship under app/components and no banned semantic hexes appear in components"
    - "Decisions covered: D-11 — lucide-react installed and the three brand fonts wired in place of Inter (flagged as licensed-grotesque stand-ins)"
  artifacts:
    - path: "remix-app/package.json"
      provides: "@testing-library/react, @testing-library/user-event, lucide-react in deps"
      contains: "lucide-react"
    - path: "remix-app/vite.config.ts"
      provides: "jsdom-capable test config (per-file pragma supported)"
      contains: "environment"
    - path: "remix-app/scripts/ds-audit.sh"
      provides: "no-jsx-shipped + semantic-hex audit gate (UI-01-b, UI-01-i)"
      contains: "components"
  key_links:
    - from: "remix-app/vite.config.ts"
      to: "@testing-library/react"
      via: "jsdom environment enables component render tests"
      pattern: "jsdom|environmentMatchGlobs|environment"
---

<objective>
Stand up the Wave 0 test and build infrastructure that every later wave depends on: install the component-testing toolchain (`@testing-library/react`, `@testing-library/user-event`) and the icon library (`lucide-react`), make vitest able to run component tests in a `jsdom` DOM environment (existing engine tests stay in `node`), and add the repeatable audit gate that enforces "no `.jsx` shipped into the app" and "no banned semantic hexes in components".

Purpose: Component ports (Wave 2) and screen retrofits (Wave 3) cannot be verified without a DOM test environment and the audit gate. lucide-react must be installed and human-verified before any component imports it (slopcheck was unavailable at research time — security_config gate).
Output: Updated `package.json` deps, vitest jsdom support, `remix-app/scripts/ds-audit.sh` audit script, and a confirmed-clean `npm test` baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md
@.planning/phases/04.1-design-system-adoption/04.1-VALIDATION.md

<interfaces>
Existing vitest config (remix-app/vite.config.ts) — preserve and extend, do not replace:
- plugins: [tailwindcss(), reactRouter(), tsconfigPaths()]
- test: { environment: "node", include: ["app/**/*.test.ts", "app/**/*.test.tsx"], passWithNoTests: true }

Package legitimacy (RESEARCH § Package Legitimacy Audit):
- lucide-react@1.17.0 — official Lucide React package, repo github.com/lucide-icons/lucide, created 2020, latest 1.17.0. Disposition: Approved [CITED]. slopcheck NOT run → blocking human-verify required before install (D-11, security_config threat 2).
- @testing-library/react@16.x, @testing-library/user-event@14.x, jsdom — long-established (2019+), not flagged.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify lucide-react package legitimacy before install</name>
  <read_first>
    - .planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md (§ Package Legitimacy Audit, Assumption A8)
  </read_first>
  <action>Do NOT install anything in this task. Surface the package-legitimacy evidence below to the user and PAUSE for explicit approval before Task 2 runs any `npm install`. This is a blocking, non-auto-approvable gate (security_config T-04.1-01; workflow.auto_advance is ignored).</action>
  <what-built>
    Nothing installed yet. This checkpoint gates the install of `lucide-react` (and the testing-library devDeps) because slopcheck was unavailable at research time. The researcher's manual verdict: lucide-react is the official Lucide React package (repo github.com/lucide-icons/lucide, created 2020-10-19, current 1.17.0), and @testing-library/react / @testing-library/user-event are long-established Testing Library packages.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/lucide-react and confirm: repository is github.com/lucide-icons/lucide, weekly downloads are high (millions), and the latest version is in the 1.x line.
    2. Open https://www.npmjs.com/package/@testing-library/react and https://www.npmjs.com/package/@testing-library/user-event and confirm they are the official Testing Library packages (org `@testing-library`).
    3. Confirm none look slop-squatted (no typo names, no zero-download clones).
  </how-to-verify>
  <verify>
    <automated>MISSING — human-verify gate; legitimacy confirmed manually on npmjs.com per security_config (not auto-approvable)</automated>
  </verify>
  <acceptance_criteria>
    - User confirms lucide-react and the two testing-library packages are legitimate official packages.
    - This checkpoint is NOT auto-approvable (workflow.auto_advance ignored — security_config). Execution pauses until the user responds.
  </acceptance_criteria>
  <done>User has approved the install of lucide-react + testing-library packages after verifying legitimacy on npmjs.com.</done>
  <resume-signal>Type "approved" to proceed with install, or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install icon + component-testing dependencies</name>
  <read_first>
    - remix-app/package.json (current deps/devDeps)
    - .planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md (§ Standard Stack — versions)
  </read_first>
  <files>remix-app/package.json</files>
  <action>From `remix-app/`, run `npm install lucide-react` (runtime dep, per D-11) and `npm install -D @testing-library/react @testing-library/user-event jsdom`. Do not pin exact versions beyond what npm resolves (lucide-react 1.x, @testing-library/react 16.x, @testing-library/user-event 14.x). Do NOT add `@lucide/lab`. Confirm `react`/`react-dom` 19.x already satisfy @testing-library/react 16 peer deps (they do — see package.json). Only run installs AFTER Task 1 is approved.</action>
  <verify>
    <automated>cd remix-app && node -e "const p=require('./package.json'); const ok = p.dependencies['lucide-react'] && p.devDependencies['@testing-library/react'] && p.devDependencies['@testing-library/user-event']; process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `remix-app/package.json` lists `lucide-react` under `dependencies`.
    - `remix-app/package.json` lists `@testing-library/react` and `@testing-library/user-event` under `devDependencies`.
    - `cd remix-app && npm run build` exits 0 (no install corruption; lucide-react importable in SSR — RESEARCH Pitfall 6).
  </acceptance_criteria>
  <done>lucide-react in dependencies; testing-library packages + jsdom in devDependencies; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Enable jsdom component tests + add the DS audit gate</name>
  <read_first>
    - remix-app/vite.config.ts (current test block — preserve node default + passWithNoTests)
    - .planning/phases/04.1-design-system-adoption/04.1-VALIDATION.md (§ Wave 0 Requirements, § Per-Requirement Verification Map UI-01-b / UI-01-i)
    - .planning/phases/04.1-design-system-adoption/04.1-RESEARCH.md (§ Validation Architecture — environment note)
  </read_first>
  <files>remix-app/vite.config.ts, remix-app/scripts/ds-audit.sh</files>
  <action>
    Edit `remix-app/vite.config.ts` so component tests can run in jsdom while engine tests stay in `node`: keep `environment: "node"` as the default and keep `passWithNoTests: true` and the existing `include` glob; the per-file `// @vitest-environment jsdom` pragma (used by the Wave 2 component tests) overrides the default per file — confirm this works with the existing config and add a clarifying comment. Do NOT switch the global default to jsdom (would break the node engine tests from Phase 1).
    Create `remix-app/scripts/ds-audit.sh` (make it executable) implementing two gates from VALIDATION: (1) UI-01-b — `find app/components -name '*.jsx'` must be empty; (2) UI-01-i — search component sources for banned semantic hexes. For the hex grep, exclude comment lines to avoid header-prose self-invalidation: pipe through `grep -v '^[[:space:]]*[/*]'` style filtering, then `grep -nE "#22c55e|#3b82f6|#a855f7|#eab308|#ef4444|#f97316" app/components`. The script exits non-zero if either gate fails, zero otherwise. Path scope is `app/components/` only (round1 docs/.jsx are out of scope). Add an `npm run ds:audit` script in package.json that runs `bash scripts/ds-audit.sh`.
  </action>
  <verify>
    <automated>cd remix-app && npm test && bash scripts/ds-audit.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm test` exits 0 (engine tests still pass in node; passWithNoTests preserved).
    - `remix-app/scripts/ds-audit.sh` exists, is executable, and `bash scripts/ds-audit.sh` exits 0 against the current tree (no .jsx in app/components, no banned hexes in app/components today).
    - `package.json` contains a `ds:audit` script.
    - A component test file containing `// @vitest-environment jsdom` runs under jsdom (verified in Wave 2; config supports the pragma now).
  </acceptance_criteria>
  <done>vitest supports per-file jsdom; ds-audit.sh enforces UI-01-b + UI-01-i and currently passes; npm run ds:audit wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | Third-party packages enter the build (lucide-react, testing-library) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering | npm install of lucide-react / testing-library | mitigate | Task 1 blocking-human-verify checkpoint (not auto-approvable) confirms package legitimacy on npmjs.com before install; researcher manual verdict recorded (slopcheck unavailable) |
| T-04.1-SC | Tampering | npm package supply chain | mitigate | Only [CITED]/approved packages installed; no [ASSUMED]/[SUS]/[SLOP] packages; install gated behind blocking checkpoint |
</threat_model>

<verification>
- `cd remix-app && npm test` exits 0 (node engine tests intact).
- `cd remix-app && npm run build` exits 0 (lucide-react SSR-safe, deps install clean).
- `cd remix-app && bash scripts/ds-audit.sh` exits 0.
</verification>

<success_criteria>
- lucide-react installed (human-verified) as a runtime dependency.
- @testing-library/react + @testing-library/user-event + jsdom installed as devDependencies.
- Component tests can run in jsdom via per-file pragma; engine tests still run in node.
- DS audit gate (no-.jsx-shipped + semantic-hex) exists, is wired to `npm run ds:audit`, and passes on the current tree.
</success_criteria>

<output>
Create `.planning/phases/04.1-design-system-adoption/04.1-01-SUMMARY.md` when done.
</output>
