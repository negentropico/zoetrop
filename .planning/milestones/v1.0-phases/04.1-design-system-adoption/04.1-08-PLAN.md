---
phase: 04.1-design-system-adoption
plan: 08
type: execute
wave: 5
depends_on: ["04.1-02", "04.1-05", "04.1-06", "04.1-07"]
files_modified:
  - docs/DESIGN-SYSTEM-ADOPTION.md
autonomous: true

requirements: [UI-01]
user_setup: []

must_haves:
  truths:
    - "DESIGN-SYSTEM-ADOPTION.md records that the original D3 'defer dark mode' decision is superseded by the dark-now decision (D-09) shipped in this phase"
    - "The adoption doc reflects the as-built integration architecture (token bridge into @theme, typed TSX ports, no shadcn, all 16 routes, dark mode)"
  artifacts:
    - path: "docs/DESIGN-SYSTEM-ADOPTION.md"
      provides: "Updated adoption strategy doc with D3 superseded note"
      contains: "superseded"
  key_links:
    - from: "docs/DESIGN-SYSTEM-ADOPTION.md"
      to: ".planning/phases/04.1-design-system-adoption/04.1-CONTEXT.md"
      via: "references the D-09 dark-now decision"
      pattern: "D-09|dark"
---

<objective>
Update `docs/DESIGN-SYSTEM-ADOPTION.md` so its recorded decisions match what was actually shipped: note that the original "D3 — defer dark mode" decision is explicitly superseded by the in-phase dark-mode decision (CONTEXT D-09), and reconcile the integration-architecture section with the as-built outcome (token bridge into Tailwind `@theme inline`, signature components ported to typed TSX under `app/components/ui/` + `app/components/shell/`, no shadcn, all 16 routes retrofit). Keep it a focused doc edit — no scope creep.

Purpose: Closes the documentation half of ROADMAP SC6 (the binding `UI-SPEC.md` already exists and is the contract; this keeps the companion strategy doc honest so future phases do not relitigate "defer dark"). Runs last so the note reflects what actually shipped.
Output: An updated `docs/DESIGN-SYSTEM-ADOPTION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-design-system-adoption/04.1-CONTEXT.md
@.planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md

<interfaces>
- docs/DESIGN-SYSTEM-ADOPTION.md — existing strategy doc; contains D1–D3 + integration architecture §6. D3 = "defer dark". Edit in place; do not rewrite the whole file.
- CONTEXT D-09: ship dark mode in this phase (supersedes adoption-doc D3). CONTEXT D-01/D-02/D-03: token bridge into @theme, typed TSX ports under app/components/ui/, no shadcn. CONTEXT D-10: all 16 routes retrofit.
- UI-SPEC § Dark Theme is the binding dark-mode contract that was implemented.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark D3 (defer dark) as superseded + reconcile integration architecture with as-built</name>
  <read_first>
    - docs/DESIGN-SYSTEM-ADOPTION.md (full — D1-D3 + §6 integration architecture)
    - .planning/phases/04.1-design-system-adoption/04.1-CONTEXT.md (§ decisions D-01, D-02, D-03, D-09, D-10)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Dark Theme)
  </read_first>
  <files>docs/DESIGN-SYSTEM-ADOPTION.md</files>
  <action>
    Edit `docs/DESIGN-SYSTEM-ADOPTION.md` in place: (1) On the D3 "defer dark" decision, add a clearly marked SUPERSEDED note pointing to CONTEXT D-09 / Phase 04.1 — dark mode WAS shipped (data-theme attribute, no-flash inline script, warm-dark token remap, ThemeToggle) per UI-SPEC § Dark Theme; instruct future phases not to relitigate. Do NOT delete the original D3 text — strike/annotate it so the decision history stays visible. (2) Reconcile the integration-architecture section (§6) with the as-built outcome: tokens bridged into Tailwind 4 `@theme inline` (not shadcn, D-03), signature components ported to typed TSX under `app/components/ui/` + `app/components/shell/` (D-02), all 16 routes retrofit (D-10). Keep edits surgical and accurate to what shipped; do not invent new decisions. Add a short "Last updated 2026-06-08 (Phase 04.1)" stamp if the doc has a dated footer pattern.
  </action>
  <verify>
    <automated>grep -iq "supersed" docs/DESIGN-SYSTEM-ADOPTION.md && grep -q "D-09\|dark" docs/DESIGN-SYSTEM-ADOPTION.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `docs/DESIGN-SYSTEM-ADOPTION.md` contains a "superseded" note tied to the D3 defer-dark decision and references the D-09 / dark-mode-now decision.
    - The original D3 text is still present (annotated, not deleted) — decision history preserved.
    - The integration-architecture section reflects `@theme` bridge + typed TSX ports + no shadcn + all 16 routes (matches what shipped).
    - No claims in the doc contradict the UI-SPEC or what was built (e.g., no remaining "dark is deferred" statement without the superseded annotation).
  </acceptance_criteria>
  <done>DESIGN-SYSTEM-ADOPTION.md records D3-superseded-by-D-09 and matches the as-built architecture; decision history intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Documentation-only edit; no code, no runtime surface, no data path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-15 | n/a | docs/DESIGN-SYSTEM-ADOPTION.md | accept | Documentation edit only — no trust boundary, no executable surface, no PII |
</threat_model>

<verification>
- `grep -i superseded docs/DESIGN-SYSTEM-ADOPTION.md` returns a match.
- The doc's integration-architecture section matches the as-built (`@theme`, typed TSX, no shadcn, 16 routes).
</verification>

<success_criteria>
- SC6 (companion doc): the strategy doc no longer asserts "defer dark" without the superseded annotation; it matches the binding UI-SPEC and what shipped.
</success_criteria>

<output>
Create `.planning/phases/04.1-design-system-adoption/04.1-08-SUMMARY.md` when done.
</output>
