---
phase: 04.1-design-system-adoption
plan: 05
type: execute
wave: 4
depends_on: ["04.1-03", "04.1-04"]
files_modified:
  - remix-app/app/routes/home.tsx
  - remix-app/app/routes/metrics/layout.tsx
  - remix-app/app/routes/metrics/index.tsx
  - remix-app/app/routes/metrics/category.tsx
  - remix-app/app/routes/metrics/detail.tsx
autonomous: true

requirements: [UI-01]
user_setup: []

must_haves:
  truths:
    - "The dashboard renders in-brand: no emoji, no gradient bars, periwinkle actions, warm Paper surfaces, segmented PhaseBar and MetricRing"
    - "Metrics overview, category, and detail screens render in-brand using CatChip (Lucide), StatusBadge, RangeBar, DataTable, and the brand TrendChart"
    - "Every loader, route, data value, and chart semantic is preserved — only markup/classes/components change"
    - "The metrics section nav is a sticky sidebar on desktop and a horizontal pill scroll on mobile"
    - "Decisions covered: D-10 — the dashboard + all metrics-section routes retrofit in-brand (part of all-16-routes, no half-branded app)"
  artifacts:
    - path: "remix-app/app/routes/home.tsx"
      provides: "In-brand dashboard (archetype 01)"
      contains: "PhaseBar"
    - path: "remix-app/app/routes/metrics/detail.tsx"
      provides: "In-brand metric detail (archetype 04) with brand TrendChart + RangeBar + DataTable"
      contains: "TrendChart"
  key_links:
    - from: "remix-app/app/routes/metrics/detail.tsx"
      to: "remix-app/app/components/ui/TrendChart.tsx"
      via: "imports the relocated brand TrendChart"
      pattern: "components/ui/TrendChart|TrendChart"
    - from: "remix-app/app/routes/home.tsx"
      to: "remix-app/app/components/ui/PhaseBar.tsx"
      via: "cessation card renders segmented PhaseBar (not a gradient bar)"
      pattern: "PhaseBar"
    - from: "remix-app/app/routes/metrics/layout.tsx"
      to: "remix-app/app/components/ui/CatChip.tsx"
      via: "category nav renders CatChip with Lucide icons"
      pattern: "CatChip"
---

<objective>
Retrofit the dashboard and the entire metrics section (5 routes: home, metrics/layout, metrics/index, metrics/category, metrics/detail) to the Zoetrop brand using the ported components — class swaps + component substitution per the UI-SPEC Per-Screen Retrofit Map. Preserve every loader, route, data value, and chart semantic. Apply the canonical copy rewrites (sentence case, eyebrow section labels, "Your signals, one frame at a time.", "Need a look").

Purpose: Delivers ROADMAP SC3 for archetypes 01 (Dashboard), 02 (Metrics overview), 03 (Metric category), 04 (Metric detail) — in-brand, no emoji, no gradient bars, periwinkle actions, warm Paper. Detail screen exercises the brand TrendChart (Phase 6 chart language) and RangeBar/DataTable.
Output: 5 retrofit route files; typecheck + build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md

<interfaces>
Retrofit map (UI-SPEC § Per-Screen Retrofit Map + PATTERNS § Route Retrofit Patterns):
- Universal class swaps (PATTERNS § Route retrofit pattern): bg-white/gray → <Card>; border-gray-* → border-border; rounded-lg → rounded-xl; text-gray-600 → text-secondary; text-gray-500 → text-muted; text-gray-900 → text-ink; status bg pills → <StatusBadge>; status dots → <StatusDot>; section <h2>/<h3> → <div className="zt-eyebrow">; text-blue-* → text-accent; gradient bars → <PhaseBar>; emoji <span> → <CatChip icon={LUCIDE_MAP[cat]} family={CATEGORY_INFO[cat].family} />; page title block → <PageHeader>.
- Components available (from Plans 03/04): Card, Button, StatusBadge, StatusDot, CatChip, MetricRing, RangeBar, PhaseBar, Sparkline, TrendChart (app/components/ui/TrendChart.tsx — TrendChart + TrendSparkline), DataTable, PageHeader, Crumb, Stat, SegmentedControl, Badge.
- CATEGORY_INFO now exposes Lucide icon names + family (Plan 03). Build a LUCIDE_MAP from icon name → lucide-react component for CatChip rendering.

Source screens (faithful composition target):
- screen-dashboard.jsx DashboardScreen (68-138): StatTile row (Card+eyebrow+.zt-readout), CessationProgress (Card + Day count .zt-readout + PhaseBar compact), CategoryCard grid (CatChip + CountDots), MetricStatus (MetricRing tone vital size 132 + StatusDot list); grid classes zt-grid-4/zt-grid-2/zt-grid-3.
- screen-metrics.jsx MetricsScreen + CategoryScreen: CatChip neutral Ink headers, RangeBar bars, Sparkline (Ink), Ink-active FilterPill, Card(padding:md); metrics/layout sidebar 232px top:92px desktop, horizontal pill scroll mobile (≤760px) per UI-SPEC § Metrics section nav.
- screen-detail.jsx DetailScreen: <StatusBadge>, .zt-readout text-3xl big value, <RangeBar height:14 showEndpoints>, measurement log → <DataTable>, brand <TrendChart>, breadcrumb → <Crumb items=[Metrics→/metrics, category→/metrics/:cat, metric.name]>.

Copy (UI-SPEC § Copywriting Contract): hero sub "Your signals, one frame at a time. 9 categories, tracked."; dashboard eyebrow "Today's frame · [date]"; "Need Attention"→"Need a look"; section labels ALL-CAPS mono eyebrow (METRIC CATEGORIES, METRIC STATUS, TREND OVER TIME, RANGE STATUS, MEASUREMENTS, DETAILS); sentence case headings.

Existing TrendChart importer: if any current route imports `~/components/TrendChart` (old path), repoint to `~/components/ui/TrendChart` (Plan 03 relocated it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit the dashboard (home.tsx)</name>
  <read_first>
    - remix-app/app/routes/home.tsx (current — preserve loader + all data values)
    - docs/design-system/_rounds/round1/app/screen-dashboard.jsx (DashboardScreen 68-138 — composition target)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ home.tsx analog, § Route retrofit pattern, § Eyebrow Section Labels, § Shell + Layout)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Per-Screen Retrofit Map row 01, § Copywriting Contract)
  </read_first>
  <files>remix-app/app/routes/home.tsx</files>
  <action>
    Retrofit `home.tsx` to the DashboardScreen composition WITHOUT touching the loader or any data value. Do NOT render a header/nav (AppShell owns it). Replace: stat row → StatTile cards (Card padding md + .zt-eyebrow + .zt-readout + optional StatusBadge hint); cessation tracker gradient bar → Card with Day count (.zt-readout text-3xl) + "of [target]" mono + <PhaseBar compact phases={cessationPhases} />; the metric-status block → <MetricRing tone="vital" size={132} thickness={13} label={statusCounts.optimal} sublabel="optimal" /> + a StatusDot list; category grid → Card(interactive) per category with <CatChip icon={LUCIDE_MAP[cat]} family={CATEGORY_INFO[cat].family} /> + CountDots. Use zt-grid-4 / zt-grid-2 / zt-grid-3 wrappers. Apply copy: PageHeader/eyebrow "Today's frame · [date]", hero sub "Your signals, one frame at a time. 9 categories, tracked.", "Need a look" (not "Need Attention"), sentence case. No emoji, no gradient bars, periwinkle only for accent (action links / target).
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && grep -q "PhaseBar" app/routes/home.tsx && grep -q "CatChip" app/routes/home.tsx && test -z "$(grep -nE 'bg-gradient|text-gray-|bg-gray-|💊|🔥' app/routes/home.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0.
    - home.tsx imports + renders PhaseBar, CatChip, MetricRing, Card; contains no `bg-gradient`, no `text-gray-`/`bg-gray-` class, and no emoji characters.
    - home.tsx contains the hero sub copy "Your signals, one frame at a time." and "Need a look".
    - The loader and all rendered data values are unchanged from the prior version (no data sourcing edits).
  </acceptance_criteria>
  <done>Dashboard renders in-brand (segmented PhaseBar, MetricRing, CatChips, warm Paper, sentence-case copy); loader/data intact.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit metrics overview + layout (metrics/index.tsx, metrics/layout.tsx)</name>
  <read_first>
    - remix-app/app/routes/metrics/index.tsx, remix-app/app/routes/metrics/layout.tsx (current — preserve loaders + NavLink/useLocation pattern)
    - docs/design-system/_rounds/round1/app/screen-metrics.jsx (MetricsScreen + CategoryScreen sidebar)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ metrics/layout.tsx analog, § Ink-Active Filter Pills, § Eyebrow Section Labels)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Metrics section nav, § Per-Screen Retrofit Map rows 02-03)
  </read_first>
  <files>remix-app/app/routes/metrics/index.tsx, remix-app/app/routes/metrics/layout.tsx</files>
  <action>
    Retrofit `metrics/layout.tsx`: replace emoji NavLink sidebar items with CatChip (Lucide icon) + text NavLink; desktop = sticky left column 232px top:92px (active item: bg-surface border-border shadow-xs weight 600; inactive: transparent, text-secondary); mobile (≤760px) = horizontal scroll pill bar (Ink-active pills: bg-ink text-n-50 border-ink; inactive: bg-surface text-secondary border-border). First item "All categories" with LayoutGrid icon. Keep the existing useLocation/NavLink isActive logic. Retrofit `metrics/index.tsx`: CatChip neutral Ink category headers, RangeBar value bars, Sparkline (Ink) trend indicators, Ink-active FilterPill, Card(padding md) section cards, eyebrow section labels ("METRIC CATEGORIES"). Preserve loaders + data.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && grep -q "CatChip" app/routes/metrics/layout.tsx && test -z "$(grep -nE 'text-gray-|bg-gray-|💊|🔥|💎' app/routes/metrics/index.tsx app/routes/metrics/layout.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0.
    - metrics/layout.tsx renders CatChip in the section nav and retains a `useLocation`/`NavLink` active pattern; the mobile pill bar uses bg-ink active state.
    - metrics/index.tsx + metrics/layout.tsx contain no `text-gray-`/`bg-gray-` classes and no emoji.
    - Loaders and data values unchanged.
  </acceptance_criteria>
  <done>Metrics overview + section nav in-brand (CatChip Lucide nav, sticky sidebar ⇄ mobile pill scroll); data intact.</done>
</task>

<task type="auto">
  <name>Task 3: Retrofit metric category + detail (metrics/category.tsx, metrics/detail.tsx)</name>
  <read_first>
    - remix-app/app/routes/metrics/category.tsx, remix-app/app/routes/metrics/detail.tsx (current — preserve loaders, data, chart props)
    - docs/design-system/_rounds/round1/app/screen-detail.jsx (DetailScreen), screen-metrics.jsx (CategoryScreen)
    - .planning/phases/04.1-design-system-adoption/04.1-PATTERNS.md (§ metrics/detail.tsx analog, § Brand-Tokened Recharts Props)
    - .planning/phases/04.1-design-system-adoption/04.1-UI-SPEC.md (§ Per-Screen Retrofit Map rows 03-04)
  </read_first>
  <files>remix-app/app/routes/metrics/category.tsx, remix-app/app/routes/metrics/detail.tsx</files>
  <action>
    Retrofit `metrics/category.tsx` (category list of metrics): metric rows with StatusDot + name + value (.zt-readout / mono) + RangeBar + Sparkline, Card(padding md) grouping, eyebrow labels; preserve loader/data. Retrofit `metrics/detail.tsx`: breadcrumb → <Crumb items=[{label:"Metrics",to:"/metrics"},{label:category,to:`/metrics/${category}`},{label:metric.name}]>; status badge → <StatusBadge status={status} />; big value readout → .zt-readout text-3xl; chart → import the brand <TrendChart> from `~/components/ui/TrendChart` (repoint if the old `~/components/TrendChart` import exists) preserving ALL existing chart props/data; range section → <RangeBar height={14} showEndpoints />; measurement log table → <DataTable columns={...} rows={history} /> with mono numeric columns; eyebrow section labels ("TREND OVER TIME", "RANGE STATUS", "MEASUREMENTS", "DETAILS"). Preserve every numeric value and the actual/target/optimal/reference chart semantics — only restyle.
  </action>
  <verify>
    <automated>cd remix-app && npm run typecheck && npm run build && grep -q "StatusBadge" app/routes/metrics/detail.tsx && grep -q "TrendChart" app/routes/metrics/detail.tsx && grep -q "DataTable" app/routes/metrics/detail.tsx && test -z "$(grep -nE 'text-gray-|bg-gray-|bg-gradient' app/routes/metrics/category.tsx app/routes/metrics/detail.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd remix-app && npm run typecheck` exits 0 and `npm run build` exits 0.
    - metrics/detail.tsx renders StatusBadge, the brand TrendChart (imported from components/ui), RangeBar, DataTable, and Crumb.
    - No remaining import of the old `~/components/TrendChart` path (repointed to `components/ui/TrendChart`).
    - category.tsx + detail.tsx contain no `text-gray-`/`bg-gray-`/`bg-gradient` classes.
    - Chart data props and reference/optimal/target semantics are unchanged (only colors restyled in the component).
  </acceptance_criteria>
  <done>Metric category + detail in-brand; brand TrendChart wired with semantics preserved; loaders/data intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | UI reskin only — loaders, routes, and data sources unchanged (RESEARCH § Security Domain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-08 | Tampering | route loader integrity during retrofit | mitigate | Retrofit changes markup/classes/components only; loaders and data values are preserved (verified by unchanged loader exports + typecheck/build); no new data path introduced |
| T-04.1-09 | n/a | static-data rendering | accept | Routes render the same M0 static loader data; no new untrusted input or external call |
</threat_model>

<verification>
- `cd remix-app && npm run typecheck` exits 0.
- `cd remix-app && npm run build` exits 0.
- `cd remix-app && bash scripts/ds-audit.sh` exits 0.
- Manual (phase gate): each of the 4 archetype screens matches its round1 realized screen — no emoji, no gradient bars, periwinkle actions, warm Paper (UI-01 SC3 spot-check); responsive nav at 375px/1280px (UI-01-k); dark toggle flips (UI-01-m).
</verification>

<success_criteria>
- SC3 (archetypes 01-04): dashboard, metrics overview, metric category, metric detail render in-brand.
- Loaders, routes, data values, and chart semantics preserved across all 5 files.
</success_criteria>

<output>
Create `.planning/phases/04.1-design-system-adoption/04.1-05-SUMMARY.md` when done.
</output>
