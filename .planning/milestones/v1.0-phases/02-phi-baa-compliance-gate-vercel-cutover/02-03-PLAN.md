---
phase: 02-phi-baa-compliance-gate-vercel-cutover
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - docs/COMPLIANCE-RUNBOOK.md
autonomous: false
requirements: []
track: B
re_scoped: "2026-06-08 — pilot-first. Original Track B (Neon HIPAA + Vercel HIPAA add-on + Anthropic BAA) DEFERRED to Phase 7. This plan now sets only the STANDARD env vars needed for a pilot deploy."
user_setup:
  - service: vercel
    why: "Standard env vars required for any deploy (no HIPAA add-on, standard Pro plan)"
    dashboard_config:
      - task: "Set DATABASE_URL (pooled), DATABASE_URL_UNPOOLED (direct), BETTER_AUTH_SECRET, BETTER_AUTH_URL in project env (Production + Preview)"
        location: "Vercel Dashboard -> Project -> Settings -> Environment Variables"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> Project -> Connect -> pooled connection string (.pooler.neon.tech) — EXISTING standard-tier project"
      - name: DATABASE_URL_UNPOOLED
        source: "Neon Console -> Project -> Connect -> direct connection string ([project-id].[region].neon.tech)"
      - name: BETTER_AUTH_SECRET
        source: "Generate 32+ random bytes: openssl rand -base64 32"
      - name: BETTER_AUTH_URL
        source: "https://zoetrop.vercel.app"

must_haves:
  truths:
    - "The 4 standard env vars are SET in the Vercel project (Production + Preview) on the standard Pro plan — NO HIPAA add-on"
    - "Both DB URLs point at the SAME existing standard-tier Neon project (no new DB, no HIPAA mode)"
    - "The env-vars-set confirmation is recorded in docs/COMPLIANCE-RUNBOOK.md (Pilot Deploy Baseline section) — NO secret values written"
    - "DEFERRED to Phase 7 (recorded, not done here): Neon Scale+HIPAA+BAA, Vercel HIPAA add-on+BAA, Anthropic HIPAA-Ready API BAA"
  artifacts:
    - path: "docs/COMPLIANCE-RUNBOOK.md"
      provides: "Pilot Deploy Baseline section: env-vars-SET confirmation + standard-tier plan record"
      contains: "Env vars SET"
---

<objective>
Set the standard Vercel environment variables required for the single-user pilot deploy — on the STANDARD Vercel Pro plan and the EXISTING standard-tier Neon project. No HIPAA add-on, no BAAs, no HIPAA mode. The PHI/BAA/HIPAA hardening that used to live here is deferred to Phase 7 (PHI Compliance Hardening — Pre-Client Gate) per the 2026-06-08 re-scope.

Purpose: unblocks the production deploy in 02-04. The deliverable is the four env vars set in Vercel + the confirmation recorded in the runbook (no secrets).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-CONTEXT.md
@docs/COMPLIANCE-RUNBOOK.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Set the 4 standard Vercel env vars (standard Pro plan, existing Neon project)</name>
  <read_first>
    - docs/COMPLIANCE-RUNBOOK.md (Pilot Deploy Baseline section to fill in)
  </read_first>
  <action>
    User-action (Claude cannot write secrets into the Vercel dashboard). In Vercel Dashboard -> Project -> Settings -> Environment Variables, set for BOTH Production and Preview: `DATABASE_URL` = the Neon POOLED connection string (`.pooler.neon.tech`); `DATABASE_URL_UNPOOLED` = the Neon DIRECT string (`[project-id].[region].neon.tech`); `BETTER_AUTH_SECRET` = `openssl rand -base64 32`; `BETTER_AUTH_URL` = `https://zoetrop.vercel.app`. Both DB URLs must point at the SAME existing standard-tier Neon project (compare the project ID in the hostname). Do NOT purchase the HIPAA add-on and do NOT enable Neon HIPAA mode — those are Phase 7. Record in docs/COMPLIANCE-RUNBOOK.md (Pilot Deploy Baseline section) that the four env vars are SET with the date — NO secret values.
  </action>
  <how-to-verify>
    1. Vercel Dashboard -> Project -> Settings -> Environment Variables shows all four vars present for Production and Preview.
    2. The two DB URLs share the same Neon project ID in their hostnames (the existing project that holds the M0 schema).
    3. docs/COMPLIANCE-RUNBOOK.md "Pilot Deploy Baseline" records "Env vars SET" with the date and the standard-tier plan note (no secret values).
  </how-to-verify>
  <verify>
    <human-check>Four env vars set (Production + Preview) on standard Pro plan, both DB URLs on the existing Neon project; runbook Pilot Deploy Baseline filled in (no secrets).</human-check>
  </verify>
  <acceptance_criteria>
    `DATABASE_URL`, `DATABASE_URL_UNPOOLED`, `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL` are set in the Vercel project env (Production + Preview) on the standard Pro plan, both DB URLs pointing at the existing standard-tier Neon project; the runbook Pilot Deploy Baseline section records "Env vars SET" + date with NO secret values. HIPAA add-on / Neon HIPAA mode are NOT enabled (deferred to Phase 7).
  </acceptance_criteria>
  <resume-signal>Type "done — 4 env vars set on standard Pro + existing Neon project, recorded in runbook" or describe a blocker.</resume-signal>
  <done>The standard env vars are set and recorded; 02-04 can trigger the production deploy.</done>
</task>

</tasks>

<verification>
- Vercel project env shows the 4 standard vars (Production + Preview) on the standard Pro plan.
- Both DB URLs target the existing standard-tier Neon project (same project ID).
- docs/COMPLIANCE-RUNBOOK.md Pilot Deploy Baseline section records env-vars-SET + date (no secrets).
</verification>

<success_criteria>
- The 4 standard env vars are set in Vercel (Production + Preview) on the standard Pro plan.
- Both DB URLs point at the existing Neon project; no HIPAA add-on / HIPAA mode (deferred to Phase 7).
- Recorded in the runbook (no secrets).
</success_criteria>

<output>
Create `.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-03-SUMMARY.md` when done.
</output>
