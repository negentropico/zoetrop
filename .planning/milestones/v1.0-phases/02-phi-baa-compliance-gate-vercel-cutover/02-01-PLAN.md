---
phase: 02-phi-baa-compliance-gate-vercel-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - remix-app/react-router.config.ts
  - remix-app/drizzle.config.ts
  - remix-app/package.json
  - remix-app/package-lock.json
  - netlify.toml
autonomous: false
requirements: [COMP-02]
track: A
user_setup: []

must_haves:
  truths:
    - "The app builds with the React Router 7 Vercel preset (`npm run build` succeeds)"
    - "`netlify.toml` no longer exists at the repo root"
    - "`drizzle-kit` migrations target the unpooled/direct Neon URL"
    - "The Phase 1 Vitest suite still passes after the config changes"
    - "Locked decisions honored: D-07 (add the RR7 Vercel preset), D-05 (remove Netlify cleanly up front), D-08 (drizzle-kit uses the unpooled/direct Neon URL), D-14 (Track A — the Claude-automatable cutover code/config)"
  artifacts:
    - path: "remix-app/react-router.config.ts"
      provides: "Vercel preset wired into RR7 config"
      contains: "vercelPreset"
    - path: "remix-app/package.json"
      provides: "@vercel/react-router dependency"
      contains: "@vercel/react-router"
    - path: "remix-app/drizzle.config.ts"
      provides: "drizzle-kit credential order preferring the unpooled URL"
      contains: "DATABASE_URL_UNPOOLED"
  key_links:
    - from: "remix-app/react-router.config.ts"
      to: "@vercel/react-router/vite"
      via: "import { vercelPreset } and presets: [vercelPreset()]"
      pattern: "vercelPreset\\(\\)"
    - from: "remix-app/drizzle.config.ts"
      to: "process.env.DATABASE_URL_UNPOOLED"
      via: "dbCredentials.url fallback order"
      pattern: "DATABASE_URL_UNPOOLED"
---

<objective>
Cut the build/deploy toolchain over from Netlify to Vercel: install the official React Router 7 Vercel preset, wire it into `react-router.config.ts`, remove the Netlify deploy config, and point `drizzle-kit` at the unpooled Neon URL for migrations. This is the Claude-automatable code/config half of the Vercel cutover (Track A).

Purpose: Satisfies the code/config portion of SC#5 (RR7 Vercel preset configured, Netlify adapter/`netlify.toml` removed, `drizzle.config.ts` ready for the direct URL). Verifying the actual production deploy + DB connectivity is the user-action finalization step in plan 02-04 — this plan makes the repo deploy-ready and proves the preset builds locally.

Output: Modified `react-router.config.ts`, `drizzle.config.ts`, `package.json` (+ lockfile); deleted `netlify.toml`; a green `npm run build` and a still-green `npm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-CONTEXT.md
@.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md

<interfaces>
<!-- Current ground-truth of the files this plan edits. Executor edits these in place — no exploration needed. -->

remix-app/react-router.config.ts (current — bare ssr):
```typescript
import type { Config } from "@react-router/dev/config";

export default {
  ssr: true,
} satisfies Config;
```

remix-app/drizzle.config.ts (current — Netlify-first credential order):
```typescript
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  dialect: 'postgresql',
  schema: './db/schema.ts',
  out: './migrations',
  dbCredentials: {
    url: process.env.NETLIFY_DATABASE_URL || process.env.DATABASE_URL!,
  },
});
```

netlify.toml (repo root — to delete):
```toml
[build]
  base = "remix-app"
  command = "npm run build"
  publish = "build/client"

[build.environment]
  NODE_VERSION = "20"
```

package.json deps relevant facts: `@react-router/dev: 7.12.0` (preset peer dep `@react-router/dev: 7` is satisfied); NO `@netlify/*` package present (nothing to uninstall). `@react-router/serve` + the `start` script may stay (Vercel ignores them).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — verify @vercel/react-router before install</name>
  <files>remix-app/package.json</files>
  <read_first>
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md (## Package Legitimacy Audit — package marked [ASSUMED]; slopcheck was unavailable in the research environment)
  </read_first>
  <action>
    Pause for human verification BEFORE installing any package. RESEARCH.md marks `@vercel/react-router@1.3.1` as [ASSUMED] (slopcheck unavailable). The human verifies the package on npmjs.com is the official Vercel RR7 adapter before Task 2 installs it. This checkpoint is never auto-approvable (legitimacy gate; `workflow.auto_advance` is ignored). No code is written in this task.
  </action>
  <what-built>
    Nothing built yet — this is the mandatory legitimacy gate before any package install. RESEARCH.md marks `@vercel/react-router@1.3.1` as [ASSUMED] (slopcheck unavailable). The manual check in RESEARCH.md found it published 2026-05-27 by `vercel-release-bot` from `github.com/vercel/vercel` with peer dep `@react-router/dev: 7`, no postinstall script.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@vercel/react-router in a browser.
    2. Confirm: publisher/maintainers include Vercel (`vercel-release-bot`, Vercel employees); repository is `github.com/vercel/vercel`; latest version is `1.3.1` (published ~2026-05-27); weekly downloads are non-trivial; there is NO `postinstall` script in the package.
    3. Confirm this is `@vercel/react-router` (the RR7 adapter) and NOT `@vercel/remix` (the deprecated Remix-era adapter — do not approve that one).
  </how-to-verify>
  <verify>
    <human-check>User confirms on npmjs.com that the package is the official Vercel RR7 adapter (publisher Vercel, repo vercel/vercel, no postinstall) and types "approved".</human-check>
  </verify>
  <acceptance_criteria>
    User types "approved" after confirming the package is the official Vercel RR7 adapter on npmjs.com. If anything looks off (typosquat name, unknown publisher, postinstall script), user describes the issue and the install does NOT proceed. This checkpoint is never auto-approvable (legitimacy gate; `workflow.auto_advance` is ignored).
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issue.</resume-signal>
  <done>The package is human-verified as the official Vercel RR7 adapter; Task 2 may install it.</done>
</task>

<task type="auto">
  <name>Task 2: Install @vercel/react-router and wire the Vercel preset</name>
  <files>remix-app/package.json, remix-app/package-lock.json, remix-app/react-router.config.ts</files>
  <read_first>
    - remix-app/package.json (confirm `@react-router/dev: 7.12.0`; no `@netlify/*` present)
    - remix-app/react-router.config.ts (the bare `ssr: true` config to modify)
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md (## Standard Stack A — exact package, version, import path)
  </read_first>
  <action>
    From `remix-app/`, install `@vercel/react-router` at `^1.3.1` (the exact version verified in RESEARCH.md is `1.3.1`, published 2026-05-27). Run `npm install @vercel/react-router@^1.3.1` so `package.json` + `package-lock.json` both update. Then edit `remix-app/react-router.config.ts`: add `import { vercelPreset } from '@vercel/react-router/vite';` (note the `/vite` subpath) and add `presets: [vercelPreset()]` to the default-exported config object, keeping `ssr: true`. Keep the `satisfies Config` type assertion. Do NOT add a `vercel.json`, do NOT change `vite.config.ts`, and do NOT use `@vercel/remix` (deprecated Remix-era adapter). Do NOT remove `@react-router/serve` or the `start` script (Vercel ignores them; their removal is optional cleanup out of scope here).
  </action>
  <verify>
    <automated>cd remix-app && grep -q "vercelPreset" react-router.config.ts && grep -q "@vercel/react-router" package.json && npm run build</automated>
  </verify>
  <acceptance_criteria>
    `react-router.config.ts` imports `vercelPreset` from `@vercel/react-router/vite` and lists `presets: [vercelPreset()]`; `package.json` lists `@vercel/react-router` under dependencies; `npm run build` completes successfully with the preset installed (build smoke — Wave 0 requirement). Typecheck path remains clean (no `any`).
  </acceptance_criteria>
  <done>The Vercel preset is wired and the production build succeeds locally with it.</done>
</task>

<task type="auto">
  <name>Task 3: Remove netlify.toml and point drizzle-kit at the unpooled URL</name>
  <files>netlify.toml, remix-app/drizzle.config.ts</files>
  <read_first>
    - netlify.toml (repo root — the file to delete)
    - remix-app/drizzle.config.ts (the credential order to update)
    - .planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-RESEARCH.md (## E — drizzle.config unpooled URL guidance; Pitfall 2 — pooled URL fails DDL)
  </read_first>
  <action>
    Delete the repo-root `netlify.toml` (Netlify deploy config — base/command/publish/NODE_VERSION; per D-05 remove Netlify cleanly up front, no parallel-fallback). There are no `@netlify/*` packages to uninstall. Then update `remix-app/drizzle.config.ts` `dbCredentials.url` fallback order to prefer the unpooled URL: `process.env.DATABASE_URL_UNPOOLED || process.env.NETLIFY_DATABASE_URL || process.env.DATABASE_URL!`. Rationale: `drizzle-kit` migrations must use the direct/unpooled Neon endpoint — the pooled (PgBouncer) URL fails DDL with `prepared statement already exists` (Pitfall 2). Leave the `NETLIFY_DATABASE_URL` link in the chain intact (its cleanup is deferred to Phase 4 / DATA-05 per CONTEXT.md deferred list). Do NOT touch `db.server.ts` (its runtime fallback `NETLIFY_DATABASE_URL || DATABASE_URL` already works once `DATABASE_URL` is set in Vercel; refactor is Phase 4).
  </action>
  <verify>
    <automated>test ! -f netlify.toml && cd remix-app && grep -q "DATABASE_URL_UNPOOLED" drizzle.config.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    `test ! -f netlify.toml` passes (Wave 0 shell assertion — Netlify config removed); `drizzle.config.ts` `dbCredentials.url` reads `DATABASE_URL_UNPOOLED` first in the fallback chain; `npm test` (Phase 1 engine suite) still passes with zero regressions from the config changes.
  </acceptance_criteria>
  <done>Netlify config is gone, drizzle-kit prefers the unpooled URL, and the Phase 1 test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Third-party package code enters the build (supply-chain surface) |
| local repo → Vercel build | Config drives how SSR routes are deployed as Vercel Functions |
| drizzle-kit → Neon | Migration tool connects to the production DB control plane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `@vercel/react-router` install (npm) | mitigate | Blocking package-legitimacy human-verify checkpoint (Task 1) before install — verify publisher/repo/no-postinstall on npmjs.com; [ASSUMED] per RESEARCH.md |
| T-02-02 | Denial of Service | drizzle-kit against pooled URL | mitigate | `drizzle.config.ts` prefers `DATABASE_URL_UNPOOLED` (direct); pooled PgBouncer URL fails DDL (Pitfall 2) |
| T-02-03 | Information Disclosure | Connection strings | accept | No connection strings are written to the repo in this plan — only env var NAMES are referenced; actual secrets live in Vercel env (set in 02-03/02-04). No code change to `db.server.ts`. |
| T-02-SC | Tampering | npm install of `@vercel/react-router` | mitigate | Covered by T-02-01 blocking-human checkpoint; package is [ASSUMED] (slopcheck unavailable) and must be human-verified |
</threat_model>

<verification>
- `cd remix-app && npm run build` succeeds with `@vercel/react-router` preset (build smoke — Wave 0).
- `test ! -f netlify.toml` passes from repo root (Netlify removal — Wave 0 shell assertion).
- `cd remix-app && npm test` passes (Phase 1 engine suite stays green — no regression).
- `grep -q "vercelPreset" remix-app/react-router.config.ts` and `grep -q "DATABASE_URL_UNPOOLED" remix-app/drizzle.config.ts` both return matches.
</verification>

<success_criteria>
- React Router 7 Vercel preset (`@vercel/react-router@^1.3.1`) installed and wired via `presets: [vercelPreset()]`.
- `netlify.toml` removed from repo root; no `@netlify/*` packages remain (none existed).
- `drizzle.config.ts` prefers `DATABASE_URL_UNPOOLED` for migrations.
- `npm run build` and `npm test` both green locally.
- (Production deploy + DB connectivity verification is plan 02-04 — not this plan.)
</success_criteria>

<output>
Create `.planning/phases/02-phi-baa-compliance-gate-vercel-cutover/02-01-SUMMARY.md` when done.
</output>
