---
status: partial
phase: 03.1-account-roles-ux-authorization
source: [03.1-VERIFICATION.md]
started: 2026-06-10
updated: 2026-06-10
---

## Current Test

[all items verified in-session at the 03.1-03 / 03.1-04 human-verify checkpoints; two residual items recommended for a final private-window pass]

## Tests

### 1. Account dropdown UX (DropdownMenu / AccountMenu)
expected: Avatar pill opens a dropdown with name/email/role badge; outside-click closes; Escape closes and returns focus to the trigger; "Sign out" POSTs to /logout and redirects to /login.
result: passed — verified at 03.1-03 checkpoint, owner-approved 2026-06-10

### 2. Mobile BottomTab Account entry (≤760px)
expected: At mobile width the bottom tab shows an Account tab (User icon) where Import was; tapping it navigates to /settings.
result: passed — verified at 03.1-03 checkpoint, owner-approved 2026-06-10

### 3. Profile name save + persist
expected: Editing the display name and saving shows "Changes saved." and persists across reload.
result: passed — verified at 03.1-04 checkpoint, owner-approved 2026-06-10

### 4. Password change inline validation + success
expected: <8 chars → "Password must be at least 8 characters."; mismatch → "Passwords do not match."; wrong current → "Current password is incorrect."; valid → "Password updated."
result: passed — verified at 03.1-04 checkpoint, owner-approved 2026-06-10

### 5. Invite generate / copy / list / revoke
expected: Owner generates a role-scoped invite (Practitioner/Client); link card appears; Copy flips to a check; invite shows ACTIVE; inline revoke confirmation → row persists with REVOKED badge.
result: passed — verified at 03.1-04 checkpoint, owner-approved 2026-06-10

### 6. Full invite redemption end-to-end (private window)
expected: Opening the invite link in a logged-out/private window shows "Create your account" (name/email/password); submitting creates an account with the invite's encoded role + tenant (NOT the URL's role param), auto-signs-in to /dashboard, and the invite flips ACTIVE → USED in the owner's list. A revoked/expired link is refused with an error.
result: pending — redemption surface built (commit 9664108) and dev-server render/route-resolution verified; recommend one private-window end-to-end pass to confirm the new account receives the correct role+tenant and the invite flips to USED.

### 7. Client-role gating (403)
expected: A signed-in client sees no INVITES section at /settings; a hand-crafted POST to the generate-invite / revoke surfaces returns 403.
result: pending — server-side gates verified in code (requireRole + can() at the action boundary, revoke route 403/404 guards) and unit-tested; recommend confirming with a real client account once one exists.

## Summary

total: 7
passed: 5
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps
