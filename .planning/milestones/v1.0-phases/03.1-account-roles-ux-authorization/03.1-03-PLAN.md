---
phase: 03.1-account-roles-ux-authorization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - remix-app/app/components/ui/DropdownMenu.tsx
  - remix-app/app/components/shell/AccountMenu.tsx
  - remix-app/app/components/shell/TopNav.tsx
  - remix-app/app/components/shell/BottomTab.tsx
  - remix-app/app/components/shell/AppShell.tsx
  - remix-app/app/routes/_app/layout.tsx
autonomous: false
requirements: ["AUTH-01"]
user_setup: []

must_haves:
  truths:
    - "A signed-in user sees an avatar/initials dropdown trigger top-right in TopNav (where ThemeToggle sat), opening a panel with Account settings, the theme toggle, and a Sign-out affordance (D-02)"
    - "Sign out is a <Form method=\"post\" action=\"/logout\"> that POSTs to the existing logout action — no JS required (D-02)"
    - "The dropdown closes on outside-click and Escape, returns focus to the trigger, and is keyboard-navigable (UI-SPEC interaction contract)"
    - "On mobile (≤760px) the BottomTab exposes an Account entry navigating to /settings, replacing the Import tab (D-03 + UI-SPEC)"
    - "The shell threads the session user (name, email, role) from the layout loader through AppShell → TopNav → AccountMenu; the role badge renders the correct chip (D-02)"
  artifacts:
    - path: "remix-app/app/components/ui/DropdownMenu.tsx"
      provides: "Accessible dropdown primitive (open/close, outside-click, Escape, keyboard nav)"
      exports: ["DropdownMenu"]
      min_lines: 50
    - path: "remix-app/app/components/shell/AccountMenu.tsx"
      provides: "Avatar trigger + dropdown panel with Settings / Theme / Sign out"
      exports: ["AccountMenu"]
    - path: "remix-app/app/components/shell/TopNav.tsx"
      provides: "Right cluster = [ThemeToggle] [AccountMenu], accepts user prop"
      contains: "AccountMenu"
    - path: "remix-app/app/components/shell/BottomTab.tsx"
      provides: "Account tab (User icon → /settings)"
      contains: "/settings"
  key_links:
    - from: "remix-app/app/routes/_app/layout.tsx"
      to: "remix-app/app/components/shell/AppShell.tsx"
      via: "useLoaderData user passed to <AppShell user=...>"
      pattern: "AppShell user="
    - from: "remix-app/app/components/shell/AccountMenu.tsx"
      to: "/logout"
      via: "Form method post action /logout"
      pattern: "action=\"/logout\""
---

<objective>
Build the account navigation surface: a reusable accessible `DropdownMenu` primitive, the `AccountMenu`
shell component (avatar trigger + Settings/Theme/Sign-out panel), and the shell wiring that threads the
session user from the authenticated layout loader down to it — desktop (TopNav) and mobile (BottomTab).

Purpose: D-02/D-03 — close the day-to-day usability gap (there is currently NO logout UI; the only way
out is clearing cookies). This plan is UI-only and depends on nothing server-side: the layout loader
already returns `{ user: session.user }` (role included). It runs in parallel with Plan 01.

Output: `DropdownMenu.tsx`, `AccountMenu.tsx`, modified `TopNav/BottomTab/AppShell/_app/layout.tsx`,
and a human-verify checkpoint confirming the dropdown UX + mobile Account entry match the UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03.1-account-roles-ux-authorization/03.1-CONTEXT.md
@.planning/phases/03.1-account-roles-ux-authorization/03.1-PATTERNS.md
@.planning/phases/03.1-account-roles-ux-authorization/03.1-UI-SPEC.md

<interfaces>
<!-- Existing components + the loader user shape. Use directly. -->

remix-app/app/components/ui/Avatar.tsx — `<Avatar name size={32} />` (initials fallback, no ring/status needed).
remix-app/app/components/ui/Badge.tsx — `<Badge tone variant>...</Badge>`; tones: energy|vital|focus|neutral|success|danger; variants: soft|solid|outline.
remix-app/app/components/ui/ThemeToggle.tsx — the existing theme toggle (reuse inside the menu; same `zt-theme` localStorage key).
remix-app/app/components/shell/TopNav.tsx — right cluster (lines 79-118) currently ends with `<ThemeToggle />`.
remix-app/app/components/shell/BottomTab.tsx — NAV_ITEMS array (lines 8-39); the Import item is `{ to: "/import/whoop", ... Download }`.
remix-app/app/components/shell/AppShell.tsx — `AppShellProps { children, wide? }` (lines 9-12); renders `<TopNav />`.
remix-app/app/routes/_app/layout.tsx — loader returns `{ user: session.user }` (role included); default export renders `<AppShell><Outlet/></AppShell>`.

Session user shape passed through the shell:
```typescript
user: { name: string; email: string; role: string }   // role: "owner" | "practitioner" | "client"
```
Logout: POST to `/logout` (action already exists at routes/auth/logout.tsx — clears the cookie correctly).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the accessible DropdownMenu primitive</name>
  <files>remix-app/app/components/ui/DropdownMenu.tsx</files>
  <read_first>
    - remix-app/app/components/ui/ThemeToggle.tsx (the closest analog — interactive button, useState/useLayoutEffect, CSS-var focus-ring/border/transition styles, lines 39-68)
    - .planning/phases/03.1-account-roles-ux-authorization/03.1-PATTERNS.md (the DropdownMenu.tsx section — exact props interface, open/close + outside-click + Escape effect patterns, panel positioning styles)
    - .planning/phases/03.1-account-roles-ux-authorization/03.1-UI-SPEC.md (Interaction Contracts § Dropdown open/close: aria-expanded, Escape returns focus to trigger, ArrowDown/Up moves focus, opacity+translateY animation 120ms; panel spec: 200px, --surface, --shadow-lg, --radius-xl, z-50)
  </read_first>
  <action>
    Create `app/components/ui/DropdownMenu.tsx` (client component — uses refs + DOM listeners). Export
    `interface DropdownMenuProps { trigger: ReactNode; children: ReactNode; align?: "left" | "right";
    width?: number; }` and `function DropdownMenu({ trigger, children, align = "right", width = 200 })`.
    Manage `const [open, setOpen] = useState(false)` and a `containerRef`. Wire:
    - outside-click: `useEffect` adding a `mousedown` listener that closes when the click target is outside
      `containerRef.current` (only attach when open).
    - Escape: `useEffect` adding a `keydown` listener that closes on `e.key === "Escape"` AND returns focus to
      the trigger element (per UI-SPEC).
    - trigger wrapper: a button-like element with `aria-haspopup="menu"`, `aria-expanded={open}`, that toggles
      `open` on click; render the passed `trigger` node inside it.
    - panel: absolutely positioned `top: calc(100% + 8px)`, right-aligned when align==="right" (`right: 0`),
      `width`, `background: var(--surface)`, `border: 1px solid var(--border)`,
      `borderRadius: var(--radius-xl)`, `boxShadow: var(--shadow-lg)`, `padding: 4`, `zIndex: 50`,
      `role="menu"`. Only render the panel when `open`. Apply the opacity 0→1 + translateY(-4px)→0 transition
      at --dur-fast --ease-out. ALL styles via CSS vars — NO hardcoded hex.
    ArrowDown/ArrowUp focus movement between focusable menu items is part of the UI-SPEC keyboard contract —
    implement a simple roving focus over the panel's focusable children. No `any`.
  </action>
  <verify>
    <automated>cd remix-app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/ui/DropdownMenu.tsx` exports `DropdownMenu` and `DropdownMenuProps`
    - Source: the trigger sets `aria-expanded` and the panel has `role="menu"`
    - Source: an Escape keydown handler closes the menu AND focuses the trigger
    - Source: an outside `mousedown` handler closes the menu
    - Source: no hardcoded hex — all colors are `var(--...)` tokens (grep finds no `#[0-9a-fA-F]{3,6}` in style values)
    - `cd remix-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A reusable, accessible dropdown primitive exists (open/close, outside-click, Escape-returns-focus, keyboard nav, brand tokens).</done>
</task>

<task type="auto">
  <name>Task 2: Build AccountMenu and wire the shell (TopNav, BottomTab, AppShell, layout)</name>
  <files>remix-app/app/components/shell/AccountMenu.tsx, remix-app/app/components/shell/TopNav.tsx, remix-app/app/components/shell/BottomTab.tsx, remix-app/app/components/shell/AppShell.tsx, remix-app/app/routes/_app/layout.tsx</files>
  <read_first>
    - remix-app/app/components/shell/TopNav.tsx (FULL — the right cluster lines 79-118, the `<ThemeToggle />` placement; TopNav currently takes no props)
    - remix-app/app/components/shell/BottomTab.tsx (FULL — NAV_ITEMS lines 8-39, the per-item aria-label + active-color pattern lines 56-83)
    - remix-app/app/components/shell/AppShell.tsx (FULL — AppShellProps lines 9-12, the `<TopNav />` render)
    - remix-app/app/routes/_app/layout.tsx (FULL — loader returns { user }; default export renders AppShell)
    - remix-app/app/components/ui/ThemeToggle.tsx (reused inside the menu; same component)
    - remix-app/app/components/ui/Avatar.tsx, Badge.tsx (the trigger avatar + role badge)
    - remix-app/app/routes/auth/logout.tsx (the POST /logout action the menu targets)
    - .planning/phases/03.1-account-roles-ux-authorization/03.1-PATTERNS.md (the AccountMenu.tsx + TopNav/BottomTab/AppShell/layout modify sections — exact props, trigger style, Form logout, role badge tone/variant maps, ROLE_TONE/ROLE_VARIANT)
    - .planning/phases/03.1-account-roles-ux-authorization/03.1-UI-SPEC.md (Screen-Level Layout Contracts: Account Menu desktop + mobile; Copywriting Contract: "Open account menu", "Account settings", "Sign out"; mobile decision = replace Import tab with Account; Authorization UI Rules: account menu is full for all roles)
  </read_first>
  <action>
    Create `app/components/shell/AccountMenu.tsx`. Export `interface AccountMenuProps { user: { name: string;
    email: string; role: string } }` and `function AccountMenu({ user })`. Compose `DropdownMenu` (align
    "right", width 200) with:
    - trigger: a pill (`<Avatar name={user.name} size={32} />` + `<ChevronDown size={16} />`), min-height 44px,
      `borderRadius: var(--radius-pill)`, `border: 1.5px solid var(--border)`, transparent bg (hover
      `var(--surface-sunken)`), `aria-label="Open account menu"`.
    - panel header (non-interactive): name (--text-base, weight 500, --ink), email (--text-xs, --text-muted,
      truncate), and a role `<Badge>` using the ROLE_TONE/ROLE_VARIANT maps from PATTERNS.md
      (owner→neutral/solid, practitioner→focus/soft, client→neutral/soft), label `{user.role.toUpperCase()}`.
      border-bottom 1px var(--border).
    - menu items: "Account settings" (User icon → a Link to `/settings`), the theme toggle row (reuse
      `<ThemeToggle />` or a labeled row using the same `zt-theme` path), and Sign out wrapped in
      `<Form method="post" action="/logout"><button type="submit">...</button></Form>` with `color: var(--danger)`
      and a LogOut icon. Menu items: 40px (min 44px touch), --radius-md, gap 8, hover bg --surface-sunken.
    Copy strings EXACTLY per UI-SPEC ("Open account menu", "Account settings", "Sign out"). All CSS-var tokens.

    Modify the shell to thread `user`:
    - `_app/layout.tsx`: add `useLoaderData` import; in the default export read `const { user } =
      useLoaderData<typeof loader>();` and render `<AppShell user={user}>`.
    - `AppShell.tsx`: add `user: { name: string; email: string; role: string }` to AppShellProps; pass it to
      `<TopNav user={user} />`.
    - `TopNav.tsx`: accept a `user` prop (same type); import AccountMenu; render `<AccountMenu user={user} />`
      after `<ThemeToggle />` in the desktop right cluster.
    - `BottomTab.tsx`: import the `User` icon from lucide-react; in NAV_ITEMS REPLACE the Import item with
      `{ to: "/settings", label: "Account", icon: User, match: (p) => p.startsWith("/settings") }` (per UI-SPEC
      mobile decision). Keep the existing per-item `aria-label={label}` and active-color pattern. (BottomTab
      navigates to /settings unconditionally — no dropdown on mobile, no user prop needed.)
    NO `any`. The route file `routes/_app/settings/index.tsx` does not exist yet (Plan 04 builds it) — the
    `/settings` Link/tab will 404 until then; that is expected and the checkpoint accounts for it.
  </action>
  <verify>
    <automated>cd remix-app && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/shell/AccountMenu.tsx` exports `AccountMenu`; trigger has `aria-label="Open account menu"`
    - Source: AccountMenu contains `<Form method="post" action="/logout">` wrapping a submit button (no JS logout)
    - Source: `TopNav.tsx` renders `<AccountMenu user={user} />` and accepts a `user` prop
    - Source: `AppShell.tsx` AppShellProps includes `user` and passes it to `<TopNav user=...>`
    - Source: `_app/layout.tsx` uses `useLoaderData` and renders `<AppShell user={user}>`
    - Source: `BottomTab.tsx` NAV_ITEMS contains an Account item with `to: "/settings"` and the User icon; the Import item is gone
    - `cd remix-app && npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>The account dropdown renders in TopNav with Settings/Theme/Sign-out, the mobile BottomTab exposes Account→/settings, and the session user threads through the shell.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify account dropdown UX + mobile Account entry</name>
  <what-built>
    A new account dropdown in the TopNav (avatar/initials trigger top-right where ThemeToggle was), opening
    a panel with the user's name/email/role badge, "Account settings", a theme toggle, and a red "Sign out"
    that POSTs to /logout. On mobile (≤760px) the BottomTab's Import tab is replaced by an Account tab that
    navigates to /settings. (The /settings page itself is built in Plan 04 — clicking Account settings / the
    mobile tab will 404 until then; that is expected.)
  </what-built>
  <action>Pause for the developer to run the dev server and manually verify the account dropdown + mobile Account entry against the steps below. No code changes in this task — this is a verification gate.</action>
  <how-to-verify>
    1. Run `cd remix-app && npm run dev` and sign in as the owner at http://localhost:3000/login.
    2. On the dashboard, confirm the top-right shows an avatar pill with a chevron (ThemeToggle still beside it).
    3. Click the pill → a dropdown opens showing your name, email, an OWNER role badge, "Account settings",
       a theme toggle, and a red "Sign out".
    4. Click outside the panel → it closes. Open it again, press Escape → it closes and focus returns to the trigger.
    5. Click "Sign out" → you are logged out and redirected to /login (cookie cleared).
    6. Sign back in. Resize the browser to ≤760px (or use device emulation) → confirm the BottomTab shows an
       "Account" tab (User icon) on the right where "Import" was; tapping it navigates to /settings (expect a
       404/empty route for now — Plan 04 builds the page).
    7. Toggle light/dark from the menu → the whole app re-themes; reload → theme persists.
  </how-to-verify>
  <verify>
    <human-check>Account dropdown opens/closes (outside-click + Escape-returns-focus), name/email/role badge render, Sign out logs out and redirects to /login, the mobile BottomTab shows Account→/settings (Import replaced), and theme toggle persists.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe any visual/interaction issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → /logout action | The only state-changing affordance here; it is a POST Form to an existing, tested action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-031-UI-1 | Tampering | logout affordance | mitigate | Sign-out is a `<Form method="post" action="/logout">` (CSRF-protected POST via Better-Auth's existing action), not a GET link — no drive-by logout via image/link. |
| T-031-UI-2 | Information Disclosure | role/email in client bundle | accept | name/email/role already returned by the layout loader to the authenticated client; this is the user's own session data, no other tenant's data is exposed. |
| T-031-UI-3 | Spoofing | role badge cosmetic only | accept | The role chip is display-only; the authoritative gate is the server loader/action (Plan 01/04). A spoofed client-side role changes no server behavior. |
</threat_model>

<verification>
- `cd remix-app && npx tsc --noEmit` exits 0; `npm run build` succeeds
- Human-verify checkpoint approved (dropdown UX, outside-click/Escape, logout, mobile Account tab, theme persistence)
- Source: AccountMenu logout is a POST Form; BottomTab Account → /settings
</verification>

<success_criteria>
- Avatar dropdown in TopNav: Settings / Theme / Sign-out, accessible (outside-click, Escape, keyboard)
- Mobile BottomTab exposes Account → /settings (Import tab replaced)
- Session user threaded loader → AppShell → TopNav → AccountMenu; role badge correct
- Logout is a no-JS POST Form to the existing /logout action
- Human-verify checkpoint passed
</success_criteria>

<output>
Create `.planning/phases/03.1-account-roles-ux-authorization/03.1-03-SUMMARY.md` when done
</output>
