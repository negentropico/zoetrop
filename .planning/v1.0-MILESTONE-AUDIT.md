---
milestone: v1.0
milestone_name: M1 — Engine-First Platform
audited: 2026-06-14
status: gaps_found   # only "gaps" are the intentional Phase-8 pre-client deferral (COMP-02/03) + 4 carried-forward non-blocking warnings; M1 build (Phases 1–7) is complete + verified
verdict_context: build_complete_gate_deferred   # Phases 1–7 done & verified; Phase 8 (compliance gate) intentionally deferred to before the first external client's PHI
supersedes: 2026-06-08 audit (which read "5 of 7 phases not executed" — long stale)
scores:
  requirements: 27/29   # 2 unsatisfied = COMP-02, COMP-03, both deferred to Phase 8 by design
  phases: 9/10          # Phase 8 not started (deferred pre-client gate)
  integration: 5/5      # all core E2E flows wired end-to-end, 0 broken
  flows: 5/5
gaps:
  requirements:
    - id: "COMP-02"
      status: "deferred"
      phase: "Phase 8 — Compliance Envelope & Host Gate"
      verification_status: "n/a — not in n=1 pilot scope"
      evidence: "PHI BAA coverage (DB host + Vercel + LLM provider) is the pre-first-external-client gate. Pilot is owner-only (n=1) on standard-tier infra + subscription API. Documented deferral: ROADMAP §pilot-first re-scope, REQUIREMENTS traceability, PROJECT key decisions."
    - id: "COMP-03"
      status: "deferred"
      phase: "Phase 8 — Compliance Envelope & Host Gate"
      verification_status: "n/a — not in n=1 pilot scope"
      evidence: "pgAudit + PHI SELECT-logging verification, on whichever host wins the Phase-8 cost/BAA comparison. Same pre-client gate as COMP-02."
  integration: []   # 0 broken flows
  flows: []
tech_debt:
  - phase: 07-phi-compliance-hardening
    items:
      - "WR-01: insertAuthAuditLog fallback `?? 'owner'` records highest-privilege role for NULL-role users in the immutable audit log; INSERT-only RLS makes it uncorrectable. Fix: fallback to 'client' (least-privilege) or make audit_log.role nullable. Partially fixed (real role now threaded from hooks)."
      - "WR-02 (security): open redirect — consent.tsx passes unvalidated `next` query param to redirect(next). Fix: only allow next.startsWith('/') && !startsWith('//')."
      - "WR-03 (security): document.tsx reads full labDocuments row incl. base64 pdfBytes via getDb() (BYPASSRLS) before assertSubjectAccess; RLS backstop bypassed for that read. Fix: two-step read (admin select of {tenantId,subjectId} for the gate, then withTenantDb read of pdfBytes)."
      - "CR-01: assignSubject 23505 idempotency guard is dead code on drizzle-orm 0.45.2 (pg code lives on err.cause.code, not err.code). Double-assign throws → caught → generic flash; no duplicate row, no access-control impact (UAT-verified graceful). Fix: check err.cause?.code."
  - phase: cross-cutting
    items:
      - "Stale tracking corrected this session: REQUIREMENTS.md checklist still shows AUTH-03/04, TEN-02/03 as `[ ]` and LAB-04 as Pending though all are verified satisfied (Phase 7 VERIFICATION + Phase 5 E2E UAT). Update the checkboxes on next REQUIREMENTS.md edit."
nyquist:
  compliant_phases: ["01", "04", "06"]
  partial_phases: ["02", "03", "04.1", "05"]
  missing_phases: ["03.1", "07"]
  overall: partial
  note: "Discovery only. Partial/missing phases still carry passing VERIFICATION.md + green test suites; Nyquist is a stricter validation-coverage standard, not a build blocker. Optional retro-fill via /gsd:validate-phase per flagged phase."
---

# Milestone v1.0 (M1 — Engine-First Platform) — Audit

**Audited:** 2026-06-14 · **Status:** gaps_found (build-complete, compliance gate deferred)
**Supersedes** the stale 2026-06-08 audit ("5 of 7 phases not executed") — all build phases are now done.

## Verdict in one line

**The M1 build is complete and verified.** Phases 1–7 (+ inserted 3.1/4.1) deliver
the engine-first platform end-to-end: auth + tenancy, live DB data layer, lab
ingest → review → approve, the pure confidence-graded engine + report generator,
and the RLS/isolation retrofit. The only outstanding requirements — **COMP-02 and
COMP-03** — are the **intentionally-deferred Phase-8 pre-client compliance gate**
(BAAs + pgAudit), which fires before the first *external* client's PHI, not for the
n=1 owner pilot. Plus **4 non-blocking warnings** carried forward from the Phase-7
review (tracked as tech debt).

`gaps_found` is the strict label (2 unsatisfied requirements force it), but the
"gaps" are a planned future gate, not build defects.

## Phase Status (9/10)

| Phase | State | Verification | Notes |
|-------|-------|--------------|-------|
| 01 Schema/Engine tests/Auth spike | complete | passed | Nyquist compliant |
| 02 Vercel cutover + pilot deploy | complete | passed | infra/ops; live at zoetrop.vercel.app |
| 03 Identity + tenancy scoping | complete | passed (HUMAN-UAT 2/2 — closed 2026-06-13) | AUTH-01/02, TEN-01/04 |
| 03.1 Account & roles UX + authz | complete | passed (HUMAN-UAT 5/7; 2 residual deferred-with-approval) | invite-redemption private-window pass + client-403 await a real client |
| 04 Static→DB data layer | complete | passed (4/4) | DATA-01/02/04/05 |
| 04.1 Design system adoption | complete | passed | UI-01 |
| 05 Lab ingest pipeline | complete | passed + owner E2E UAT passed (2026-06-11) | LAB-01..06 |
| 06 Engine promotion + reports | complete | passed (5/5) | ENG-01..03, RPT-01..03 |
| 07 PHI hardening — RLS + isolation | complete | passed (4/5; 1 non-blocking idempotency defect) | AUTH-03/04, TEN-02/03 |
| 08 Compliance envelope & host gate | **not started** | — | **deferred** pre-external-client gate (COMP-02/03) |

## Requirements Coverage (27/29 satisfied)

All 29 v1 requirements are mapped. **27 satisfied and wired end-to-end** (verified
by 3-source cross-reference: phase VERIFICATION.md + SUMMARY frontmatter +
traceability, plus the integration check). **2 deferred** to Phase 8:

- **COMP-02** — PHI BAA coverage (DB host + Vercel + LLM provider) — Phase 8 gate.
- **COMP-03** — pgAudit + PHI SELECT-logging verification — Phase 8 gate.

> Note: AUTH-03/04 and TEN-02/03 were delivered + verified in Phase 7; LAB-04 was
> delivered + E2E-UAT-verified in Phase 5. The `[ ]`/`Pending`/`Active` marks still
> in REQUIREMENTS.md for these are stale tracking, not real gaps (corrected note in
> tech_debt above).

## Cross-Phase Integration (5/5 flows wired, 0 broken)

Integration checker (gsd-integration-checker) verdict: **0 blockers**, all five
core flows complete end-to-end at the code level, 0 orphaned exports.

1. **Auth** — `/login` → Better-Auth 30-day cookie → `_app/layout.tsx` session gate → unauthed redirect to `/login`; invite sign-up injects role+tenant via hooks; `/settings` invites RBAC server-gated. ✓
2. **Data layer** — all 13 PHI loaders → `requireSubjectCtx` → `data.server.ts` → `withTenantDb` (SET LOCAL + GUCs) → Neon; static `*-data.ts` deleted + ESLint-gated. ✓
3. **Lab ingest → metrics** — upload → consent → `waitUntil` extraction → per-field review (no bulk) → approved rows `INSERT metrics(source='lab')` + auditLog in one tx → surface on dashboard via shared `getMetrics`. ✓ (E2E UAT confirmed)
4. **Engine → report** — pure `engine.ts` (0 Drizzle/Remix imports, ESLint+test enforced) + corpus (30 variant + 22 metric rules, non-null K) → `report-generator.server.ts` → frozen `ReportSnapshot` with inline K + K4 disclaimer. ✓
5. **RLS/tenancy** — `withTenantDb` consumed by all PHI read/write paths; `rls-isolation.test.ts` (cross-tenant zero-read + WITH CHECK reject + pool non-leak) green on live Neon; 16 PHI tables ENABLE+FORCE RLS, `app_user` NOBYPASSRLS. ✓

### Warnings (4, non-blocking — tracked as tech debt)
1. **WR-01** audit-log role fallback `?? 'owner'` (privilege over-record for NULL-role).
2. **WR-02** (security) open redirect via unvalidated `next` in `consent.tsx`.
3. **WR-03** (security) `document.tsx` reads `pdfBytes` via BYPASSRLS before the authz gate.
4. **CR-01** `assignSubject` 23505 idempotency guard is dead code (wrong error layer) — UX-only, UAT-verified graceful, no access-control impact.

None block any flow; all were surfaced + accepted in Phase 7's review. Recommend
clearing via `/gsd:code-review 7 --fix` (or folding the two security items into Phase 8).

## Build Gates (this audit, clean `main`)
- `npm run typecheck` → exit 0, no errors
- `npm test` → 296 passed | 80 skipped
- `npm run build` → ✓ (no `.server` bundle leaks)
- Prod healthy: `/` 200, `/login` 200, `/dashboard` 302→/login

## Recommendation

**Complete the milestone, accepting the Phase-8 deferral** (COMP-02/03) as the
documented pre-external-client gate. The 4 warnings are tech debt to clear when
Phase 8 (the compliance/security gate) runs, since two are security items that
naturally belong with the BAA/pgAudit work. No closure phase is needed to call
M1's *build* complete.
