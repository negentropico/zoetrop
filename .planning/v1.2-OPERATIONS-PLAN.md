# v1.2 (parked) — M1 Operations: multi-client at scale

**Status:** Parked 2026-06-14 — deferred from v1.1 so the instrument is sharpened on a single real client first. Formalize via `/gsd:new-milestone` when v1.1 completes. Full prior detail: `git show 5b31ac6:.planning/ROADMAP.md` and `git show 5b31ac6:.planning/REQUIREMENTS.md`.

## Why parked
v1.1 was recut to **First Client (practitioner-operated, n=1 real client)** — prove the practice loop with the owner acting as practitioner onboarding one real client, using actual tools (PureInsight genetics, WHOOP, labs), with library curation. v1.1 pulls a *thin* slice of operations forward (create one client + subject + invite + minimal active-subject context) but stays practitioner-operated with no scale.

## v1.2 residual scope (the heavy operations work)
- **Multi-client at scale** — subject-switcher UX beyond 2 subjects, client list/management, archive/deactivate.
- **Client self-service** — clients upload their own labs/WHOOP, view their own report (the minimal client surface); in v1.1 the practitioner does all data entry.
- **Cadence + monitoring** — review-due surfacing, "new version per 4-week cycle" flow, practitioner monitoring view (which clients need attention).
- **More wearables** — Apple Watch (Apple Health) and Oura Ring ingest (v1.1 leads with WHOOP only).
- **Compliance Envelope & Host Gate (COMP-02/03)** — Vercel HIPAA add-on + BAAs, LLM-provider BAA, host cost/BAA comparison (+ possible migration), pgAudit + PHI SELECT-logging — the hard release gate before the first *external* client's PHI. (If v1.1's pilot client uses real PHI under a posture that needs it, parts may pull forward.)

Original requirement family: **OPS-01..09** (subject creation, invite→subject linkage, subject switching/scoped surfaces, genotype entry, manual metrics, per-client protocol authoring, cadence surfacing, WHOOP persist, E2E proof slice) — note several (genotype, manual metrics, WHOOP, single-subject protocol authoring, thin invite→subject) get pulled into v1.1 single-subject; v1.2 re-scopes the residual at new-milestone time.

## Deferred integrations (explicitly skipped in v1.1, revisit in v1.2+)
- **Google Drive** doc storage/import (PHI → needs Workspace BAA; ties to the compliance gate).
- **PureInsight API** (programmatic invite via stored provider account) — v1.1 uses manual portal + report import instead.
