# dependencies
node_modules/

# build output
dist/
build/

# generated types
.astro/

# environment variables
.env
.env.production
.env.local

# macOS
.DS_Store

# IDEs
.idea/

# Netlify
.netlify

# database (anchored to repo root — an unanchored `data/` also matched
# design-bridge/design-system/components/data/, hiding the data component gallery)
/data/
*.db
*.db-wal
*.db-shm

# logs
npm-debug.log*

# archive (browsable but excluded from searches)
.archive/

# coverage
coverage/

# Local Claude Code tooling + design scratch (not repo content)
.claude/
_notes/
.vercel

# design-bridge harness: synced CORE + return staging are never committed
design-bridge/harness/.staging-core/
design-bridge/harness/.staging/
