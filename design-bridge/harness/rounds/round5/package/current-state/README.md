# current-state

Snapshot of the shipped app at prep time.

| File | What |
|------|------|
| `app.css` | The shipped token stylesheet — returned new rules must contain only what is **not** here |

> Repo-authored slots to add manually (not auto-extracted): `routes.md`, `components.md`, `nav-tree.*`.
