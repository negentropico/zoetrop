Linear goal/completion bar — pairs with MetricRing for secondary metrics.

```jsx
<ProgressBar value={6418} max={8000} tone="energy" label="Steps" showValue />
```

`tone`: energy / vital / focus. Set `showValue` for a % readout.
