Pill toggle for settings and reminders — on-color maps to a metric family.

```jsx
<Switch checked={remind} onChange={setRemind} tone="focus" label="Wind-down reminder" />
```

Sizes `sm`/`md`. Controlled via `checked` + `onChange(next)`.
