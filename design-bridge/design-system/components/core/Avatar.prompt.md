Round avatar with auto-initials fallback, optional metric ring and presence dot.

```jsx
<Avatar name="Maya Okafor" ring="vital" status="online" size={48} />
<Avatar src="/me.jpg" name="Me" />
```

`ring` tints a colored halo (energy/vital/focus); `status` shows a corner dot.
