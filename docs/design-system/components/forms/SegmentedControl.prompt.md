Pill segmented switch for time ranges and view toggles — the active segment lifts onto a white surface.

```jsx
<SegmentedControl options={['Day','Week','Month']} value={range} onChange={setRange} />
```

Pass plain strings or `{value,label}` objects. Sizes `sm`/`md`.
